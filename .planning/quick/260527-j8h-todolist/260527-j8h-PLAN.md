---
phase: quick-todolist-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/stores/sessionStore.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "User sends a message → old todos cleared → TodoList panel hides immediately"
    - "Agent creates new todos via write_todos → TodoList panel shows with new todos"
    - "No stale todo data from previous rounds appears in new round"
  artifacts:
    - path: "src/renderer/src/stores/sessionStore.ts"
      provides: "sendMessage clears todos at start"
      contains: "todos: []"
  key_links:
    - from: "src/renderer/src/stores/sessionStore.ts::sendMessage"
      to: "streamingSessionsCache"
      via: "initialState"
      pattern: "todos:\\s*\\[\\]"
---

<objective>
Fix TodoList cross-round persistence: when user sends a new message, clear old todos so stale data from previous rounds does not flash in the UI.

Purpose: `fetchAgentActivity` reconstructs todos from DB `write_todos` history. When user sends a new message, `isStreaming=true` and `shouldShowTodos = hasTodos && (!allTodosCompleted || isStreaming || showCompletedTodos)` causes old todos to briefly appear before new todos_update overwrites them.

Output: Single-line fix in `sendMessage` ensuring `todos: []` in both cache and store at stream start.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/renderer/src/stores/sessionStore.ts
@src/renderer/src/components/ChatArea/ChatArea.tsx

<interfaces>
<!-- sendMessage signature (sessionStore.ts:339) -->
sendMessage: (projectId: string, content: string, overrides?: ChatRuntimeOverrides) => Promise<void>

<!-- StreamingSessionState (sessionStore.ts:70-80) -->
interface StreamingSessionState {
  messages: Message[];
  todos: TodoItem[];
  delegatedTasks: DelegatedTask[];
  agentRuns: AgentRun[];
  agentToolCalls: AgentToolCall[];
  activeRunId: string | null;
  pendingApproval: AgentApprovalRequest | null;
  isStreaming: boolean;
  streamingMessageId: string | null;
}

<!-- shouldShowTodos logic (ChatArea.tsx:59-61) -->
const hasTodos = todos.length > 0;
const allTodosCompleted = hasTodos && todos.every((todo) => todo.status === 'completed');
const shouldShowTodos = hasTodos && (!allTodosCompleted || isStreaming || showCompletedTodos);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clear todos at sendMessage start</name>
  <files>src/renderer/src/stores/sessionStore.ts</files>
  <behavior>
    - sendMessage called → store.todos becomes [] immediately
    - sendMessage called → streamingSessionsCache entry has todos: []
    - Agent sends todos_update event → todos repopulated with fresh data
  </behavior>
  <action>
In `sendMessage` (sessionStore.ts), the `initialState` object at line 370 already sets `todos: []`. Verify this is working correctly. The real issue may be that `fetchAgentActivity` runs during `selectSession` and loads old todos from DB.

Two-pronged fix:

1. **Verify `initialState.todos = []`** at line 375 — confirm it clears the store when `set(initialState)` runs at line 384. This should already work.

2. **Add explicit `set({ todos: [] })` BEFORE the cache setup** — add a defensive `set({ todos: [] })` call at the very beginning of the `sendMessage` function body, right after the `isStreaming` guard check (after line 341). This ensures store.todos is cleared before any cache operations, and before any potential race with `fetchAgentActivity`.

The change should be minimal: insert one line `set({ todos: [] });` after line 341 (after `if (!activeSessionId || isStreaming) return;`).

Do NOT touch `fetchAgentActivity`, `selectSession`, or `ChatArea.tsx`. The fix is purely in `sendMessage` entry point.
  </action>
  <verify>
    <automated>cd /Users/suntc/project/CDF && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    - `sendMessage` clears `todos` at function entry via `set({ todos: [] })`
    - `initialState` still has `todos: []` for cache consistency
    - TypeScript compiles without errors
    - Manual verification: send message → old todos do not flash → agent creates new todos → panel shows
  </done>
</task>

</tasks>

<verification>
- [ ] `sendMessage` has `set({ todos: [] })` at entry
- [ ] `initialState.todos` is still `[]`
- [ ] TypeScript compiles clean
- [ ] TodoList panel hides on new message send
- [ ] TodoList panel shows when agent creates fresh todos
</verification>

<success_criteria>
sendMessage clears todos at start. No stale todos appear when user begins a new round. Agent-created todos still display correctly.
</success_criteria>

<output>
Create `.planning/quick/260527-j8h-todolist/260527-j8h-SUMMARY.md` when done
</output>
