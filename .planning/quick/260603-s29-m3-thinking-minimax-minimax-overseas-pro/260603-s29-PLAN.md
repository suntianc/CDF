---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/deepagent/llm-adapter.ts
  - src/main/deepagent/llm-adapter.test.ts
autonomous: true
user_setup: []
---

<objective>
为 minimax / minimax-overseas provider 创建的 ChatAnthropic 实例透传 `thinking: { type: "adaptive" }`,让上游 MiniMax M3 API 在 SSE 流里发 `content_block.type === "thinking"` / `delta.type === "thinking_delta"`,触发 LangChain v3 reasoning 流(`msg.reasoning` 异步迭代器),从而在 chat 路径上消费 `<think>` 折叠区。

Purpose: 当前 M3 chat 完全无思考区,根因是 `llm-adapter.ts:525-554` 共享 modelConfig 未声明 thinking 开关,客户端从未请上游发思考事件(详见 `.planning/debug/minimax-m3-thinking-missing.md`)。本次只补 minimax/minimax-overseas 两个 provider,deepseek/zhipu/glm-overseas 上游对 `type: "adaptive"` 的行为未知,绝不在本次范围内动。

Output:
- `src/main/deepagent/llm-adapter.ts` 在多 case 块中为 minimax/minimax-overseas 单独注入 `thinking: { type: "adaptive" }`
- `src/main/deepagent/llm-adapter.test.ts` 新增正负向断言:minimax + minimax-overseas 看到 `{ type: "adaptive" }`,openai / anthropic / ollama 必须 `undefined`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/minimax-m3-thinking-missing.md
@src/main/deepagent/llm-adapter.ts
@src/main/deepagent/llm-adapter.test.ts

# 关键决策:窄范围(per planner constraint Option A)
# - 共享 case 块包含 deepseek / minimax / minimax-overseas / zhipu / glm-overseas 五个 provider
# - 同一 case 块也只设一组 modelConfig 字段
# - 实施方式:在 `model = new ChatAnthropic(modelConfig)` 之前插入
#     if (config.providerType === 'minimax' || config.providerType === 'minimax-overseas') {
#       modelConfig.thinking = { type: "adaptive" };
#     }
#   这样只影响 minimax/minimax-overseas,不影响同 case 块的 deepseek/zhipu/glm-overseas
# - 不要把 thinking 加进共享的 modelConfig 字面量(那是 Option B,会污染 3 个未知上游行为 provider)

# 关键事实(从 debug doc 抽取,executor 直接信任,无需再 grep)
# - LangChain 1.4.0 类型层面已支持简化协议:thinking: { type: "adaptive" }(chat_models.d.ts:164/181)
# - LangChain 内部对 thinking 字段只做透传(_thinkingInParams / fields?.thinking ?? this.thinking),不做 enabled/budget 强校验
# - modelConfig 类型为 Record<string, unknown>,直接加 thinking 字段类型安全
# - ChatAnthropic 实例化后,thinking 字段读作 model.thinking(实例字段,不是 raw config)
# - 现有 5 个 M3 相关测试在 llm-adapter.test.ts:18-67(已存在 minimax + minimax-overseas 正向断言,可补 thinking 断言)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注入 thinking 字段(仅 minimax/minimax-overseas)]<]minimax[>[</name>
  <files>src/main/deepagent/llm-adapter.ts]<]minimax[>[</files>
  <action>
在 `src/main/deepagent/llm-adapter.ts` 的 `case 'deepseek': case 'minimax': case 'minimax-overseas': case 'zhipu': case 'glm-overseas':` 块(line 525-554)中,在 `model = new ChatAnthropic(modelConfig);`(line 550)之前,新增一段条件赋值:

```ts
if (config.providerType === 'minimax' || config.providerType === 'minimax-overseas') {
  modelConfig.thinking = { type: 'adaptive' };
}
```

注意:
- 必须放在 `model = new ChatAnthropic(modelConfig)` 之前,因为 `modelConfig` 之后会被读
- 不要放进共享 modelConfig 字面量(line 530-535),那样会污染 deepseek/zhipu/glm-overseas 三个 provider(它们上游对 `type: "adaptive"` 行为未验证,本次必须严格不动)
- modelConfig 类型是 `Record<string, unknown>`,直接 `modelConfig.thinking = { type: 'adaptive' }` 类型安全
- 实际值用单引号 `'adaptive'` 保持与项目代码风格一致
- 缩进对齐 2 空格(项目标准),不引入空行分隔

不要修改:
- case 块中其他 3 个 provider 的任何逻辑
- `patchMiniMaxAssistantRole(model)` 调用(line 551-553,只是 role 推断补丁,与 thinking 无关)
- 共享 modelConfig 字段集合(model / temperature / streaming / maxTokens / apiKey / anthropicApiUrl / createClient)
- Anthropic 原生 case 块(line 556-579)和其他任何分支
  </action>
  <verify>
    <automated>cd /Users/suntc/project/CDF && npx vitest run src/main/deepagent/llm-adapter.test.ts -t "MiniMax" 2>&1 | tail -30</automated>
  </verify>
  <done>Task 1 完成后,现有 5 个 M3 相关测试(line 18-67)仍然全部通过(本任务不破坏任何已有断言)。代码层面 `modelConfig.thinking` 仅在 `config.providerType === 'minimax' || 'minimax-overseas'` 时被设置,deepseek/zhipu/glm-overseas 路径上 `modelConfig.thinking` 保持 undefined。</done>
</task>

<task type="auto">
  <name>Task 2: 补 thinking 字段正负向测试]<]minimax[>[</name>
  <files>src/main/deepagent/llm-adapter.test.ts]<]minimax[>[</files>
  <action>
在 `src/main/deepagent/llm-adapter.test.ts` 的 `describe('createLangChainModel', ...)` 块内,追加两个 `it` 块:

1. 正向断言(只覆盖 minimax + minimax-overseas 两个 provider):
   - 第一个 it:`should pass thinking: { type: 'adaptive' } to ChatAnthropic for MiniMax`(用 `providerType: 'minimax'` + `defaultModel: 'MiniMax-M3'`)
   - 第二个 it:`should pass thinking: { type: 'adaptive' } to ChatAnthropic for MiniMax-overseas`(用 `providerType: 'minimax-overseas'` + `defaultModel: 'MiniMax-M3'`)
   - 两个 it 都要断言 `model.thinking` 严格等于 `{ type: 'adaptive' }`(用 `expect(model.thinking).toEqual({ type: 'adaptive' })`,注意 toEqual 不是 toBe,因为对象比较)

2. 负向断言(确保未污染其他 provider):在一个新的 it 块里,依次为 openai / anthropic / ollama / deepseek / zhipu 5 个 provider 各创建一个 model,断言 `model.thinking === undefined`(用 `expect(model.thinking).toBeUndefined()`)。这一个 it 用 5 段顺序断言即可,不要拆 5 个 it。
   - openai:`apiUrl: 'https://api.example.com/v1/chat/completions'`,`defaultModel: 'gpt-4o-mini'`,`providerType: 'openai'`
   - anthropic:`apiUrl: 'https://api.anthropic.com'`,`defaultModel: 'claude-3-5-sonnet-20241022'`,`providerType: 'anthropic'`
   - ollama:`apiUrl: 'http://localhost:11434'`,`defaultModel: 'llama3'`,`providerType: 'ollama'`
   - deepseek:`apiUrl: 'https://api.deepseek.com/anthropic/v1'`,`defaultModel: 'deepseek-chat'`,`providerType: 'deepseek'`
   - zhipu:`apiUrl: 'https://open.bigmodel.cn/api/anthropic/v1'`,`defaultModel: 'glm-4.5'`,`providerType: 'zhipu'`

3. 用 `as any` 转型保持与文件里其他测试风格一致(line 12/24/36 等已用)。

4. 不要改动任何已有测试。openai/anthropic/ollama 的现有 case(line 6-16)继续作为基础断言,新负向断言只补 `model.thinking === undefined` 一行。

5. 三个新 it 块按正向 → 负向顺序排列,放在已有 5 个 it(line 18-67)之后、`getOllamaBaseUrl` describe 块之前。

6. 严格不要写 fence 代码块到 PLAN 或注释里,只写 directive 描述(本规则已遵守)。
  </action>
  <verify>
    <automated>cd /Users/suntc/project/CDF && npx vitest run src/main/deepagent/llm-adapter.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>所有测试通过(原 5 个 M3 相关 + 新 3 个 thinking 相关 = 共 8 个 it 在 createLangChainModel 块)。正向断言 2 个 it 确认 `model.thinking === { type: 'adaptive' }` 对 minimax / minimax-overseas 成立;负向断言 1 个 it 确认 openai/anthropic/ollama/deepseek/zhipu 的 `model.thinking` 都是 undefined,确保未把 thinking 字段泄漏到其他 provider。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → config | 用户在 LLM provider 配置界面选择 providerType / apiKey / apiUrl,本次不修改任何 UI / 配置 schema |
| adapter → upstream MiniMax API | 本次唯一变更:adapter 透传 `thinking: { type: "adaptive" }` 给上游 MiniMax M3 endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-s29-01 | T-Tampering | llm-adapter.ts:530-554 modelConfig 共享 case 块 | mitigate | 用 if 守卫精确作用域,只命中 minimax / minimax-overseas 两个 provider,共享 case 块内其他 3 个 provider(deepseek/zhipu/glm-overseas)modelConfig.thinking 保持 undefined |
| T-quick-s29-02 | T-Tampering | 负向测试覆盖 | mitigate | 5 provider(openai/anthropic/ollama/deepseek/zhipu)逐一断言 `model.thinking === undefined`,回归即挂 |
| T-quick-s29-03 | S-Spoofing | 上游 MiniMax M3 API 返回字段 | accept | `type: "adaptive"` 协议由 MiniMax 平台官方文档定义,签名回带是上游责任,LangChain 1.4.0 字段透传无注入风险 |
| T-quick-s29-04 | I-Info Disclosure | n/a | n/a | 本次不动日志、不打 patch,无新增信息暴露面 |
| T-quick-s29-05 | D-DoS | n/a | n/a | 单字段透传,无资源放大 |
| T-quick-s29-06 | E-Elevation | n/a | n/a | adapter 层无权限变更 |
| T-quick-s29-07 | R-Repudiation | n/a | n/a | 本次不改日志/审计 |
| T-quick-s29-SC | Tampering | n/a(本 quick 无 npm/pip install 任务) | n/a | 不适用 |
  </threat_model>

<verification>
整体验证步骤:
1. `npx vitest run src/main/deepagent/llm-adapter.test.ts` 全部通过
2. 手动 grep 确认 `modelConfig.thinking` 出现位置:应只在 `llm-adapter.ts` 共享 case 块内 if 守卫内,只设置 1 次,且只对 minimax/minimax-overseas 生效
3. 手动确认 `llm-adapter.ts:530-554` 共享 case 块中的 modelConfig 字面量(line 530-535)未添加 `thinking` 字段(避免 Option B 污染)
4. 已存在的 5 个 M3 测试(line 18-67)不修改,继续通过
</verification>

<success_criteria>
- `src/main/deepagent/llm-adapter.ts` 在 minimax/minimax-overseas 路径上透传 `thinking: { type: 'adaptive' }` 给 ChatAnthropic
- 共享 case 块中 deepseek/zhipu/glm-overseas 三个 provider 的 modelConfig.thinking 保持 undefined
- `src/main/deepagent/llm-adapter.test.ts` 新增 2 个正向 it + 1 个负向 it(覆盖 5 个 provider),全部通过
- 现有 5 个 M3 相关测试继续通过
- 整体 vitest run 0 失败
- executor 在 worktree 中以单一原子 commit 提交代码 + 测试变更
</success_criteria>

<output>
- 修改:src/main/deepagent/llm-adapter.ts(1 处 if 守卫,5 行新增)
- 修改:src/main/deepagent/llm-adapter.test.ts(2 个正向 it + 1 个负向 it)
- 不创建:本 plan 不要求新建 SUMMARY.md(simplified for quick)
- 不更新:ROADMAP.md(quick 任务约定不动)
- 不创建:任何 .md 报告文件
</output>
