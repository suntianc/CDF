---
quick_id: 260603-s29
slug: m3-thinking-minimax-minimax-overseas-pro
status: complete
date: 2026-06-03
commits:
  - b165698: feat(llm): enable M3 extended thinking (adaptive) for minimax/minimax-overseas
---

# Quick 260603-s29: M3 extended thinking for minimax/minimax-overseas

**One-liner:** Injects `thinking: { type: 'adaptive' }` into the shared ChatAnthropic `modelConfig` only for `minimax` / `minimax-overseas`, so MiniMax M3 upstream SSE emits `content_block.type="thinking"` / `thinking_delta` events and the existing `consumeReasoning → <think>` pipeline lights up.

## Changes

### Code (1 if-guard, 3 lines)

`src/main/deepagent/llm-adapter.ts` — shared case block (lines 525-554) now has a 3-line if-guard placed BEFORE `new ChatAnthropic(modelConfig)`:

```ts
if (config.providerType === 'minimax' || config.providerType === 'minimax-overseas') {
  modelConfig.thinking = { type: 'adaptive' };
}
```

The `new ChatAnthropic(modelConfig)` call still happens ONCE outside the if-guard, keeping the type unified. The existing `patchMiniMaxAssistantRole` call (lines 551-553) is unchanged.

### Tests (3 new `it` blocks, +65 lines)

`src/main/deepagent/llm-adapter.test.ts` — added inside `describe('createLangChainModel', ...)`:

1. **Positive — `minimax` + `MiniMax-M3`**: `expect(model.thinking).toEqual({ type: 'adaptive' })`
2. **Positive — `minimax-overseas` + `MiniMax-M3`**: `expect(model.thinking).toEqual({ type: 'adaptive' })`
3. **Negative sweep — openai / anthropic / ollama / deepseek / zhipu**: ensures no leak to other providers.

Test result: **PASS (10) FAIL (0)** — all 7 pre-existing tests + 3 new tests pass.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Negative test assertion mismatch with ChatAnthropic default**
- **Found during:** Task 2 (first test run failed)
- **Issue:** Plan specified `expect(model.thinking).toBeUndefined()` for all 5 negative-sweep providers, but `ChatAnthropic` declares a class field initializer `thinking = { type: "disabled" }` at `node_modules/@langchain/anthropic/dist/chat_models.js:633`. So `model.thinking` is `{ type: "disabled" }` (the SDK default), not `undefined`, for the 3 ChatAnthropic-based providers in the sweep (anthropic / deepseek / zhipu). Only the ChatOpenAI/ChatOllama-based providers (openai / ollama) actually have `model.thinking === undefined`.
- **Fix:** Split the assertion: openai / ollama keep `toBeUndefined()`; anthropic / deepseek / zhipu assert `toEqual({ type: 'disabled' })`. The intent ("we did NOT inject `{ type: 'adaptive' }`") is preserved — any leak of `'adaptive'` to those providers would still fail the assertion, which is the regression the negative test exists to catch.
- **Files modified:** `src/main/deepagent/llm-adapter.test.ts`
- **Commit:** b165698 (folded into the same atomic commit since code+tests are one logical change)

No plan structural changes, no scope expansion, no architectural decisions.

## Verification

- `npx vitest run src/main/deepagent/llm-adapter.test.ts` → **PASS (10) FAIL (0)**
- Code search confirms `modelConfig.thinking` only appears inside the new if-guard (1 occurrence in shared case block)
- Shared `modelConfig` literal (lines 530-535) untouched — deepseek / zhipu / glm-overseas paths are NOT polluted
- All 5 pre-existing M3 tests (lines 18-67) still pass unmodified

## Notes

- Full `npm run build` / full test suite NOT run per quick-task convention (per 260601-nzn summary, project has pre-existing unrelated failures in file-tools.test.ts / skill-manager.test.ts).
- `modelConfig.thinking` is typed as `Record<string, unknown>`, so the direct assignment is type-safe.
- LangChain 1.4.0 (`@langchain/anthropic`) field `thinking?: AnthropicThinkingConfigParam` already accepts the simplified `{ type: "adaptive" }` shape (no `budget_tokens` required), confirmed at `node_modules/@langchain/anthropic/dist/chat_models.d.ts:164,181` and pass-through at `chat_models.js:671`.
