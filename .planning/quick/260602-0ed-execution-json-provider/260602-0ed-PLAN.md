---
phase: 260602-0ed-execution-json-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/database.ts
  - src/main/workflow/workflow-runtime.ts
  - src/main/workflow/log-exporter.ts
  - src/preload/index.ts
  - src/shared/types.ts
  - src/renderer/src/components/WorkflowEditor/ExecutionHistoryDrawer.tsx
  - src/renderer/src/components/WorkflowEditor/WorkflowToolbar.tsx
  - src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx
  - src/renderer/src/stores/workflowStore.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - 用户能在工作流顶栏点击"历史"按钮,从右侧滑出"历史执行记录"抽屉
    - 抽屉按时间倒序展示该工作流所有 execution,每条显示状态图标/触发源/开始时间(相对+绝对)/耗时
    - 每条历史记录提供"下载"按钮 → 弹出系统保存对话框,导出单 JSON 文件
    - 每条历史记录提供"删除"按钮 → 二次确认后删除该 execution 及其 node_runs
    - 导出的 JSON 包含 workflow 配置(graph_data + 节点引用的 Agent/MCP/Skills 快照)+ 完整执行过程(input/output/logs/error + 事件时间线)
    - 导出的 JSON 中 agents 数组**不包含** provider_id / model / api_key / api_url 等敏感字段
    - 导出的 JSON 中 MCP env 内 key 匹配 /api_?key|token|secret|password|bearer/i 的值被脱敏为 "***"
  artifacts:
    - path: src/main/database.ts
      provides: "workflow_executions 表新增 config_snapshot + events_snapshot 列(idempotent ALTER)"
    - path: src/main/workflow/workflow-runtime.ts
      provides: "runWorkflow 启动时写入 config_snapshot,workflow_end 时写入 events_snapshot,node_end 时 logs 已落库"
    - path: src/main/workflow/log-exporter.ts
      provides: "buildExportPayload(executionId) — 拼装导出 JSON 并强制脱敏"
    - path: src/preload/index.ts
      provides: "electronAPI.workflow.listExecutions / deleteExecution / exportExecution"
    - path: src/shared/types.ts
      provides: "ElectronAPI.workflow 三个新方法的类型签名"
    - path: src/renderer/src/components/WorkflowEditor/ExecutionHistoryDrawer.tsx
      provides: "右侧抽屉 UI + 列表项 + 下载/删除按钮 + 二次确认"
    - path: src/renderer/src/components/WorkflowEditor/WorkflowToolbar.tsx
      provides: "顶栏 History 图标按钮 + onHistoryToggle 回调"
    - path: src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx
      provides: "historyDrawerOpen state + 抽屉挂载"
    - path: src/renderer/src/stores/workflowStore.ts
      provides: "historyExecutions 数组 + listExecutions / deleteExecution / exportExecution action"
  key_links:
    - from: src/renderer/src/components/WorkflowEditor/ExecutionHistoryDrawer.tsx
      to: src/main/workflow/workflow-runtime.ts
      via: "window.electronAPI.workflow.listExecutions / deleteExecution / exportExecution"
      pattern: "electronAPI\\.workflow\\.(list|delete|export)Execution"
    - from: src/main/workflow/log-exporter.ts
      to: src/main/database.ts
      via: "读取 workflow_executions.config_snapshot + workflow_node_runs.logs + workflow_executions.events_snapshot"
      pattern: "db\\.prepare\\(.workflow_executions.\\).get"
    - from: src/main/workflow/workflow-runtime.ts
      to: src/main/database.ts
      via: "INSERT workflow_executions 时一并写入 config_snapshot 列"
      pattern: "INSERT INTO workflow_executions.*config_snapshot"
    - from: src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx
      to: src/renderer/src/components/WorkflowEditor/ExecutionHistoryDrawer.tsx
      via: "state.historyDrawerOpen 切换抽屉显隐"
      pattern: "historyDrawerOpen.*ExecutionHistoryDrawer"
---

<objective>
为工作流系统新增"历史执行记录"功能:在顶栏增加"历史"入口,从右侧滑出抽屉展示该工作流所有 execution,支持每条 JSON 文件下载(脱敏)和单条删除。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/main/workflow/workflow-runtime.ts
@src/main/database.ts
@src/shared/types.ts
@src/renderer/src/components/WorkflowEditor/ExecutionPanel.tsx
@src/renderer/src/components/WorkflowEditor/WorkflowToolbar.tsx
@src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx
@src/renderer/src/stores/workflowStore.ts
@src/preload/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端持久化补全 + 配置快照固化</name>
  <files>
    - src/main/database.ts
    - src/main/workflow/workflow-runtime.ts
  </files>
  <action>
**1. `src/main/database.ts` — schema 迁移**

在 workflow_executions 表创建块之后(约 426 行后)、idx 索引块之前,新增两条 idempotent ALTER,沿用现有的 try/catch 模式(参考 450 行的 `try { ALTER TABLE workflow_node_runs ADD COLUMN logs }` 写法):

```ts
// 工作流配置快照（执行时固化 agents/mcp/skills 引用配置，导出用）
try { db.exec(`ALTER TABLE workflow_executions ADD COLUMN config_snapshot TEXT`); } catch {}
// 完整事件流时间线（导出用）
try { db.exec(`ALTER TABLE workflow_executions ADD COLUMN events_snapshot TEXT`); } catch {}
```

注意:`config_snapshot` 和 `events_snapshot` 留作新增列;`logs` 列在 node_runs 上已经存在(450-454 行),不用动。

**2. `src/main/workflow/workflow-runtime.ts` — runWorkflow 启动时拼装并写入 config_snapshot**

在 `runWorkflow` 函数体内,加载 `workflowRow` 之后(约 124 行,`const executionInput = enrichWorkflowInput(input, graphData);` 之后),在 INSERT 语句前(约 127 行)插入新逻辑:

- 收集 `graphData.nodes` 中所有 `data.agentId`(去重,过滤空值)
- 一次性 `db.prepare('SELECT * FROM agents WHERE id IN (...)')` 查所有被引用的 agents
- 一次性查 `agent_mcp_servers` 和 `agent_skills` 多对多关联表
- 一次性查 `mcp_servers`(用 IN 过滤出上述 agent 引用到的 mcp_server_ids)
- 一次性查 `skills`(用 IN 过滤 skill_names,需要 join `agent_skills` 表)

组装的 `configSnapshot` 结构(严格按用户锁定设计,不含 provider):

```ts
const configSnapshot = {
  graph_data: graphData,
  agents: agents.map(a => ({
    id: a.id,
    name: a.name,
    system_prompt: a.system_prompt,
    description: a.description,
    mcp_server_ids: mcpIdsByAgent[a.id] || [],
    skill_names: skillNamesByAgent[a.id] || [],
  })),
  mcp_servers: mcpServers.map(s => {
    // env 脱敏 — 在拼装阶段就脱敏,避免之后导出逻辑再处理
    const cfg = s.config || {};
    const sanitizedEnv: Record<string, string> = {};
    if (cfg.env && typeof cfg.env === 'object') {
      for (const [k, v] of Object.entries(cfg.env)) {
        if (/api_?key|token|secret|password|bearer/i.test(k)) {
          sanitizedEnv[k] = '***';
        } else {
          sanitizedEnv[k] = String(v);
        }
      }
    }
    return {
      id: s.id,
      name: s.name,
      command: cfg.command,
      args: cfg.args,
      env: sanitizedEnv,
    };
  }),
  skills: skills.map(s => ({ name: s.name, description: s.description })),
};
```

修改现有的 INSERT 语句,加 `config_snapshot` 列:

```ts
db.prepare(`
  INSERT INTO workflow_executions (id, workflow_id, project_id, trigger_source, status, input, started_at, config_snapshot)
  VALUES (?, ?, ?, ?, 'running', ?, ?, ?)
`).run(executionId, workflowId, projectId, triggerSource, JSON.stringify(executionInput), now, JSON.stringify(configSnapshot));
```

**3. `node_end` 处 — logs 已经在 246-251 行 INSERT 时写入了 `JSON.stringify(accumulatedLogs)`,无需修改**

**4. `workflow_end` 处 — 持久化 events_snapshot**

在现有 `db.prepare('UPDATE workflow_executions SET status = ?, output = ?, ended_at = ? WHERE id = ?').run(finalStatus, ...)` (约 270 行)后,**以及** catch 块里的 `UPDATE ... SET status = 'failed'`(约 288 行)后,新增:

```ts
const eventsForSnapshot = eventBuffers.get(executionId) || [];
db.prepare('UPDATE workflow_executions SET events_snapshot = ? WHERE id = ?')
  .run(JSON.stringify(eventsForSnapshot), executionId);
```

放在两个 UPDATE 之后(成功路径和失败路径都要写)。注意不要在 finally 中写,finally 之前 buffers 可能已被清理——其实正好相反,finally 里的 setTimeout 30s 才删,所以即使在 finally 写也安全;但为了语义清晰,放在成功/失败分支里更直接。

**verify 步骤:**
- 运行 `npx tsc --noEmit` 确认类型正确
- 启动 dev 模式,跑一个工作流,完成后用 sqlite 工具查 `workflow_executions` 表,确认 `config_snapshot` 和 `events_snapshot` 都有 JSON 字符串
- 启动后再跑一次,确认幂等(多次启动不报错,新执行的快照只包含本次引用的 agents)
  </action>
  <verify>
    <automated>cd /Users/suntc/project/CDF && npx tsc --noEmit -p tsconfig.main.json 2>&1 | head -30 || npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - workflow_executions 表多出 config_snapshot + events_snapshot 两列(已在数据库里存在则 ALTER 静默跳过)
    - runWorkflow 启动时拼装 configSnapshot 并 INSERT 进去
    - workflow_end(成功/失败两种)UPDATE 时把 eventBuffers 的内容写进 events_snapshot
    - `npm run typecheck` 0 错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 主进程导出/列表/删除 IPC + log-exporter 模块</name>
  <files>
    - src/main/workflow/log-exporter.ts (新文件)
    - src/main/workflow/workflow-runtime.ts
    - src/preload/index.ts
    - src/shared/types.ts
  </files>
  <action>
**1. 新增 `src/main/workflow/log-exporter.ts`(单独模块,职责单一)**

```ts
import fs from 'fs';
import { dialog, BrowserWindow } from 'electron';
import db from '../database';

const SECRET_KEY_REGEX = /api_?key|token|secret|password|bearer/i;
const SENSITIVE_AGENT_KEYS = ['provider_id', 'config']; // config 中可能含 model override
const SECRET_VALUE_PLACEHOLDER = '***';
const SCHEMA_VERSION = '1.0';

export interface ExportExecutionResult {
  saved: boolean;
  path?: string;
  canceled?: boolean;
  error?: string;
}

/**
 * 读取单次执行的完整数据,过滤 provider 信息并脱敏 MCP env,组装导出 JSON。
 * 优先使用 config_snapshot（runWorkflow 启动时已固化）;为 null 时 fallback 到即时拼装。
 */
export function buildExportPayload(executionId: string): Record<string, unknown> {
  // 1. 读 workflow_executions
  const row = db.prepare('SELECT * FROM workflow_executions WHERE id = ?').get(executionId) as any;
  if (!row) throw new Error(`Execution not found: ${executionId}`);

  // 2. 读 workflow (graph_data + name)
  const workflowRow = db.prepare('SELECT * FROM workflows WHERE id = ?').get(row.workflow_id) as any;
  if (!workflowRow) throw new Error(`Workflow not found: ${row.workflow_id}`);

  // 3. 读 node_runs（含 logs）
  const nodeRunRows = db.prepare('SELECT * FROM workflow_node_runs WHERE execution_id = ? ORDER BY started_at').all(executionId) as any[];

  // 4. 解析 workflow 部分
  const graphData = JSON.parse(workflowRow.graph_data);
  const configSnapshot = row.config_snapshot ? JSON.parse(row.config_snapshot) : null;

  // 5. 强制过滤：agents 数组中剔除 provider_id / config
  const safeAgents = (configSnapshot?.agents || []).map((a: any) => {
    const { provider_id, config, ...rest } = a;
    return rest;
  });

  // 6. 解析 events
  const events = row.events_snapshot ? JSON.parse(row.events_snapshot) : [];

  return {
    schema_version: SCHEMA_VERSION,
    exported_at: Date.now(),
    workflow: {
      id: workflowRow.id,
      name: workflowRow.name,
      graph_data: configSnapshot?.graph_data || graphData,
      agents: safeAgents,
      mcp_servers: configSnapshot?.mcp_servers || [],
      skills: configSnapshot?.skills || [],
    },
    execution: {
      id: row.id,
      status: row.status,
      started_at: row.started_at,
      ended_at: row.ended_at,
      duration_ms: row.ended_at && row.started_at ? row.ended_at - row.started_at : null,
      input: row.input ? JSON.parse(row.input) : {},
      output: row.output ? JSON.parse(row.output) : undefined,
      error: row.error || undefined,
      node_runs: nodeRunRows.map(r => ({
        node_id: r.node_id,
        node_name: r.node_name,
        status: r.status,
        input: r.input ? JSON.parse(r.input) : undefined,
        output: r.output ? JSON.parse(r.output) : undefined,
        error: r.error || undefined,
        retry_count: r.retry_count,
        started_at: r.started_at,
        ended_at: r.ended_at,
        logs: r.logs ? JSON.parse(r.logs) : [],
      })),
      events,
    },
  };
}

/**
 * 弹保存对话框,导出 JSON 文件
 */
export async function exportExecutionToFile(executionId: string): Promise<ExportExecutionResult> {
  try {
    const payload = buildExportPayload(executionId);
    const win = BrowserWindow.getFocusedWindow() || BrowserWindow.getAllWindows()[0];
    const result = await dialog.showSaveDialog(win!, {
      title: '导出工作流执行记录',
      defaultPath: `workflow-execution-${executionId.slice(0, 8)}.json`,
      filters: [{ name: 'JSON', extensions: ['json'] }],
    });
    if (result.canceled || !result.filePath) {
      return { saved: false, canceled: true };
    }
    fs.writeFileSync(result.filePath, JSON.stringify(payload, null, 2), 'utf-8');
    return { saved: true, path: result.filePath };
  } catch (err) {
    return { saved: false, error: err instanceof Error ? err.message : String(err) };
  }
}

/**
 * 列出某工作流的执行记录,按 started_at 倒序,limit 50
 */
export function listExecutionsByWorkflow(workflowId: string): any[] {
  const rows = db.prepare(`
    SELECT * FROM workflow_executions
    WHERE workflow_id = ?
    ORDER BY started_at DESC
    LIMIT 50
  `).all(workflowId) as any[];
  return rows.map(r => ({
    ...r,
    input: r.input ? JSON.parse(r.input) : {},
    output: r.output ? JSON.parse(r.output) : undefined,
  }));
}

/**
 * 删除单次执行(级联删除 node_runs),单事务
 */
export function deleteExecution(executionId: string): void {
  const txn = db.transaction((id: string) => {
    db.prepare('DELETE FROM workflow_node_runs WHERE execution_id = ?').run(id);
    db.prepare('DELETE FROM workflow_executions WHERE id = ?').run(id);
  });
  txn(executionId);
}
```

**2. `src/main/workflow/workflow-runtime.ts` — 注册三个新 IPC handler**

在 `registerWorkflowIpcHandlers` 函数体内(约 333 行),追加:

```ts
import { listExecutionsByWorkflow, deleteExecution, exportExecutionToFile } from './log-exporter';
// ... 在 ipcMain.handle('workflow:getEvents', ...) 之后追加:
ipcMain.handle('workflow:listExecutions', (_, workflowId: string) => {
  return listExecutionsByWorkflow(workflowId);
});
ipcMain.handle('workflow:deleteExecution', (_, executionId: string) => {
  deleteExecution(executionId);
});
ipcMain.handle('workflow:exportExecution', async (_, executionId: string) => {
  return exportExecutionToFile(executionId);
});
```

**3. `src/preload/index.ts` — 暴露 API**

在 `workflow: { ... }` 块(约 81-96 行)尾部追加:

```ts
listExecutions: (workflowId: string) =>
  ipcRenderer.invoke('workflow:listExecutions', workflowId),
deleteExecution: (executionId: string) =>
  ipcRenderer.invoke('workflow:deleteExecution', executionId),
exportExecution: (executionId: string) =>
  ipcRenderer.invoke('workflow:exportExecution', executionId),
```

**4. `src/shared/types.ts` — 类型补全**

在 `ElectronAPI.workflow` 块(约 398-403 行)新增:

```ts
listExecutions: (workflowId: string) => Promise<WorkflowExecution[]>;
deleteExecution: (executionId: string) => Promise<void>;
exportExecution: (executionId: string) => Promise<{ saved: boolean; path?: string; canceled?: boolean; error?: string }>;
```

**verify 步骤:**
- 跑 `npx tsc --noEmit` 0 错误
- 在 dev 模式下用 DevTools / 临时 console 调用 `window.electronAPI.workflow.listExecutions(workflowId)` 验证返回数组
- 临时直接 require log-exporter 调用 `buildExportPayload` 验证过滤逻辑(可选)
  </action>
  <verify>
    <automated>cd /Users/suntc/project/CDF && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - log-exporter.ts 存在,导出 buildExportPayload + exportExecutionToFile + listExecutionsByWorkflow + deleteExecution 四个导出
    - workflow-runtime.ts 注册了 workflow:listExecutions / deleteExecution / exportExecution 三个 IPC
    - preload/index.ts 的 electronAPI.workflow 多了三个方法
    - shared/types.ts 类型签名匹配
    - `npm run typecheck` 0 错误
    - agents 数组中不含 provider_id / config;mcp env 中 key 匹配正则的值是 "***"
  </done>
</task>

<task type="auto">
  <name>Task 3: 前端 ExecutionHistoryDrawer 抽屉 + Toolbar 入口 + Store 接入</name>
  <files>
    - src/renderer/src/components/WorkflowEditor/ExecutionHistoryDrawer.tsx (新文件)
    - src/renderer/src/components/WorkflowEditor/WorkflowToolbar.tsx
    - src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx
    - src/renderer/src/stores/workflowStore.ts
  </files>
  <action>
**1. `src/renderer/src/stores/workflowStore.ts` — 接入 IPC**

在 `WorkflowState` interface 内(约 4-27 行)追加:

```ts
historyExecutions: WorkflowExecution[];
fetchHistoryExecutions: (workflowId: string) => Promise<void>;
deleteHistoryExecution: (executionId: string, workflowId: string) => Promise<void>;
exportHistoryExecution: (executionId: string) => Promise<{ saved: boolean; path?: string; canceled?: boolean; error?: string }>;
```

在 create() 内的初始 state 块(约 30-39 行)追加 `historyExecutions: []`。

在 action 区域(约 167 行前)追加:

```ts
fetchHistoryExecutions: async (workflowId: string) => {
  set({ isLoading: true, error: null });
  try {
    const list = await window.electronAPI.workflow.listExecutions(workflowId);
    set({ historyExecutions: list, isLoading: false });
  } catch (err) {
    set({ error: err instanceof Error ? err.message : 'Failed to fetch history', isLoading: false });
  }
},

deleteHistoryExecution: async (executionId: string, workflowId: string) => {
  try {
    await window.electronAPI.workflow.deleteExecution(executionId);
    set((state) => ({
      historyExecutions: state.historyExecutions.filter((e) => e.id !== executionId),
    }));
  } catch (err) {
    set({ error: err instanceof Error ? err.message : 'Failed to delete execution' });
    throw err;
  }
},

exportHistoryExecution: async (executionId: string) => {
  return window.electronAPI.workflow.exportExecution(executionId);
},
```

**2. 新增 `src/renderer/src/components/WorkflowEditor/ExecutionHistoryDrawer.tsx`**

参考 `ExecutionPanel.tsx` 的设计 token(`bg-[var(--color-bg-sidebar)]`、`border-[var(--color-border)]/50` 等),宽度 380px,从右侧滑出。结构:

```tsx
import { useEffect, useState, useRef } from 'react';
import { X, History, Download, Trash2, Clock, CheckCircle2, XCircle, Square, Loader2, AlertTriangle, FileJson } from 'lucide-react';
import { useWorkflowStore } from '../../stores/workflowStore';
import { WorkflowExecution } from '../../../../shared/types';

// 复用 ExecutionPanel 里的 statusConfig(导出常量,这里复制定义以避免循环依赖;若想统一可抽到 utils.ts)
const statusConfig: Record<string, { icon: any; color: string; label: string }> = {
  pending: { icon: Clock, color: 'text-[var(--color-text-muted)]', label: '等待中' },
  running: { icon: Loader2, color: 'text-[var(--color-info)]', label: '运行中' },
  completed: { icon: CheckCircle2, color: 'text-[var(--color-success)]', label: '已完成' },
  failed: { icon: XCircle, color: 'text-[var(--color-danger)]', label: '失败' },
  stopped: { icon: Square, color: 'text-[var(--color-text-muted)]', label: '已停止' },
};

function formatRelativeTime(ts: number): string {
  const diff = Date.now() - ts;
  if (diff < 60000) return '刚刚';
  if (diff < 3600000) return `${Math.floor(diff / 60000)} 分钟前`;
  if (diff < 86400000) return `${Math.floor(diff / 3600000)} 小时前`;
  return `${Math.floor(diff / 86400000)} 天前`;
}

function formatDuration(start: number, end?: number): string {
  if (!end) return '-';
  const ms = end - start;
  if (ms < 1000) return `${ms}ms`;
  return `${(ms / 1000).toFixed(1)}s`;
}

const triggerLabel: Record<string, string> = { editor: '编辑器', chat: '对话', schedule: '定时' };

interface Props {
  workflowId: string;
  onClose: () => void;
}

export function ExecutionHistoryDrawer({ workflowId, onClose }: Props) {
  const { historyExecutions, fetchHistoryExecutions, deleteHistoryExecution, exportHistoryExecution } = useWorkflowStore();
  const [confirmingId, setConfirmingId] = useState<string | null>(null);

  useEffect(() => {
    fetchHistoryExecutions(workflowId);
  }, [workflowId, fetchHistoryExecutions]);

  const handleDelete = async (executionId: string) => {
    if (confirmingId !== executionId) {
      setConfirmingId(executionId);
      // 3 秒后自动取消二次确认
      setTimeout(() => setConfirmingId((cur) => (cur === executionId ? null : cur)), 3000);
      return;
    }
    setConfirmingId(null);
    try {
      await deleteHistoryExecution(executionId, workflowId);
    } catch (err) {
      // 错误已写入 store;UI 静默
    }
  };

  const handleExport = async (executionId: string) => {
    const result = await exportHistoryExecution(executionId);
    if (result.error) {
      alert(`导出失败: ${result.error}`);
    }
  };

  return (
    <div className="w-[380px] bg-[var(--color-bg-sidebar)] border-l border-[var(--color-border)]/50 flex flex-col shrink-0">
      <div className="flex items-center justify-between px-4 py-3 border-b border-[var(--color-border)]/50">
        <div className="flex items-center gap-2">
          <History className="w-4 h-4 text-[var(--color-accent)]" />
          <span className="text-sm font-semibold text-[var(--color-text-primary)]">历史执行记录</span>
        </div>
        <button onClick={onClose} className="p-1 rounded-md hover:bg-[var(--color-bg-hover)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-all cursor-pointer">
          <X className="w-4 h-4" />
        </button>
      </div>

      <div className="flex-1 overflow-y-auto px-4 py-3 space-y-2">
        {historyExecutions.length === 0 ? (
          <div className="flex flex-col items-center justify-center py-12 text-[var(--color-text-muted)] text-xs gap-2">
            <FileJson className="w-8 h-8 opacity-40" />
            <span>暂无历史执行记录</span>
            <span className="text-[10px]">运行工作流后将在此显示</span>
          </div>
        ) : (
          historyExecutions.map((exec) => {
            const config = statusConfig[exec.status] || statusConfig.pending;
            const StatusIcon = config.icon;
            const isRunning = exec.status === 'running';
            const isConfirming = confirmingId === exec.id;
            return (
              <div key={exec.id} className="rounded-lg border border-[var(--color-border)]/50 bg-[var(--color-bg-surface)] p-3">
                <div className="flex items-center justify-between mb-1.5">
                  <div className="flex items-center gap-2 min-w-0 flex-1">
                    <StatusIcon className={`w-3.5 h-3.5 ${config.color} ${isRunning ? 'animate-spin' : ''} shrink-0`} />
                    <span className={`text-xs font-medium ${config.color}`}>{config.label}</span>
                    <span className="text-[10px] text-[var(--color-text-muted)]">·</span>
                    <span className="text-[10px] text-[var(--color-text-muted)]">{triggerLabel[exec.trigger_source] || exec.trigger_source}</span>
                  </div>
                  <div className="flex items-center gap-1 shrink-0">
                    <button
                      onClick={() => handleExport(exec.id)}
                      className="p-1 rounded hover:bg-[var(--color-bg-hover)] text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)] transition-all cursor-pointer"
                      title="下载 JSON"
                    >
                      <Download className="w-3.5 h-3.5" />
                    </button>
                    <button
                      onClick={() => handleDelete(exec.id)}
                      className={`p-1 rounded transition-all cursor-pointer ${isConfirming ? 'bg-[var(--color-danger)]/20 text-[var(--color-danger)]' : 'hover:bg-[var(--color-bg-hover)] text-[var(--color-text-secondary)] hover:text-[var(--color-danger)]'}`}
                      title={isConfirming ? '再次点击确认删除' : '删除'}
                    >
                      <Trash2 className="w-3.5 h-3.5" />
                    </button>
                  </div>
                </div>
                <div className="flex items-center justify-between text-[10px] text-[var(--color-text-muted)]">
                  <span title={new Date(exec.started_at).toLocaleString()}>{formatRelativeTime(exec.started_at)}</span>
                  <span>{formatDuration(exec.started_at, exec.ended_at)}</span>
                </div>
              </div>
            );
          })
        )}
      </div>
    </div>
  );
}
```

二次确认采用"再点一次确认"的内联模式(避免引入 AlertDialog 依赖),3 秒自动取消;若想用 Radix Dialog 也可,这里采用最简实现。

**3. `src/renderer/src/components/WorkflowEditor/WorkflowToolbar.tsx` — 加按钮**

在 import 块追加 `History` 图标。在 props interface 追加 `onHistoryToggle: () => void;`。在 destructure 追加 `onHistoryToggle`。在 `<button onClick={onRun}>` 之前插入:

```tsx
<button
  className="topbar-btn cursor-pointer"
  onClick={onHistoryToggle}
  title="历史执行记录"
>
  <History className="w-3.5 h-3.5" />
  <span>历史</span>
</button>
```

**4. `src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx` — 挂载抽屉**

- import 块追加:`import { ExecutionHistoryDrawer } from './ExecutionHistoryDrawer';`
- state 块(约 81 行后)追加:`const [historyDrawerOpen, setHistoryDrawerOpen] = useState(false);`
- `<WorkflowToolbar ... />` 的 props 追加:`onHistoryToggle={() => setHistoryDrawerOpen((v) => !v)}`
- 在 `</div>` (主内容 flex 容器结束,约 700 行)在 `NodeConfigDrawer` 旁边或 `ExecutionPanel` 旁边加:
```tsx
{historyDrawerOpen && (
  <ExecutionHistoryDrawer
    workflowId={workflowId}
    onClose={() => setHistoryDrawerOpen(false)}
  />
)}
```
建议放在 `executionId && <ExecutionPanel ... />` 之后(同侧位置,二者不冲突——执行面板在执行时显示,历史抽屉按需打开,UI 上是相同区域的概念延伸)。

**verify 步骤:**
- `npx tsc --noEmit` 0 错误
- dev 模式启动,新建/打开一个工作流,点击"历史"按钮,抽屉从右滑出
- 抽屉为空时显示空状态
- 跑一次工作流后,刷新打开抽屉能看到刚才那条 execution
- 点下载 → 系统保存对话框出现,默认文件名 `workflow-execution-xxxxxxxx.json`
- 点垃圾桶 → 按钮变红/变提示文案"再次点击确认删除";3 秒内再点一次,记录从列表消失
- 导出的 JSON 用编辑器打开,确认 schema_version / workflow (含 graph_data + agents 但**无** provider_id / config / api_key) / execution (含 node_runs[].logs 和 events)
  </action>
  <verify>
    <automated>cd /Users/suntc/project/CDF && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - store 暴露 historyExecutions + 三个 action
    - ExecutionHistoryDrawer.tsx 存在并接入 store
    - WorkflowToolbar 多了一个"历史"按钮(onHistoryToggle prop)
    - WorkflowEditor 多了 historyDrawerOpen state 和抽屉挂载
    - 顶栏点击"历史"按钮可切换抽屉
    - 抽屉列表展示 + 下载按钮触发系统保存对话框 + 删除按钮二次确认生效
    - `npm run typecheck` 0 错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main (IPC) | listExecutions/deleteExecution/exportExecution 三个新 IPC |
| File system | exportExecution 写入用户选择的任意路径 |
| DB schema | workflow_executions 新增两列;旧数据库的 idempotent 迁移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260602-01 | Information Disclosure | log-exporter.buildExportPayload | mitigate | 强制剔除 agents 数组中的 `provider_id` / `config` 字段;MCP env 中匹配 `/api_?key|token|secret|password|bearer/i` 的 key 替换为 `***` |
| T-260602-02 | Tampering | exportExecution file path | mitigate | 使用 Electron `dialog.showSaveDialog` 让用户选择路径,不接受 renderer 传入路径;写入前提示 |
| T-260602-03 | Denial of Service | listExecutions 大量历史 | accept | LIMIT 50 限制单次返回行数(用户未要求分页,YAGNI) |
| T-260602-04 | Repudiation | deleteExecution 无审计 | accept | 单纯功能,无合规要求 |
| T-260602-05 | Elevation of Privilege | DB schema 迁移 | mitigate | 使用 try/catch 包裹 ALTER TABLE,重复迁移静默跳过(沿用 450 行既有 pattern) |
| T-260602-06 | Information Disclosure | node_runs.logs 包含 LLM 内部 prompt/response | accept | 用户已明确要求导出"详细执行过程",且 logs 在 DB 中始终可读;不在本任务范围 |

## Notes
- 严格按用户锁定的脱敏规则实现,不引入额外加密/哈希(YAGNI)
- 不引入新依赖(沿用 lucide-react / electron / zustand 已存在)
- 不新增用户设置/配置项/定时清理策略
</threat_model>

<verification>
1. `npx tsc --noEmit` 0 错误
2. 数据库启动后 `workflow_executions` 表多出 `config_snapshot` 和 `events_snapshot` 两列(用 sqlite 客户端验证)
3. 跑一次工作流,完成后:
   - `config_snapshot` 列写入 JSON,含 `graph_data + agents(无 provider_id/config) + mcp_servers(env 脱敏) + skills`
   - `events_snapshot` 列写入完整事件流
   - `workflow_node_runs.logs` 列写入日志数组
4. 顶栏点击"历史"按钮 → 抽屉从右侧滑出,显示该工作流的执行记录
5. 点击下载按钮 → 弹出系统保存对话框,选择路径后写入 JSON 文件
6. 导出的 JSON 用文本编辑器打开,确认:
   - 顶层含 `schema_version: "1.0"` 和 `exported_at`
   - `workflow.agents` 数组中**没有** `provider_id` / `config` 字段
   - `workflow.mcp_servers[].env` 中,key 匹配正则的 value 是 `"***"`
   - `execution.node_runs[].logs` 数组非空
   - `execution.events` 数组非空(包含 workflow_start / node_start / node_end / workflow_end)
7. 点击删除按钮 → 内联二次确认 → 3 秒内再点一次 → 记录从列表消失,DB 中 workflow_executions 和 workflow_node_runs 同步删除
</verification>

<success_criteria>
- 所有 3 个 task 完成,每个 task 的 `verify` 和 `done` 全部通过
- 顶栏"历史"按钮可正常切换抽屉
- 抽屉列表与 DB 数据一致
- 导出 JSON 文件包含完整执行过程且不含 provider / api_key 等敏感字段
- 删除操作有二次确认,事务原子
- 无新依赖引入,无 YAGNI 内容
</success_criteria>

<output>
Create `.planning/quick/260602-0ed-execution-json-provider/260602-0ed-SUMMARY.md` when done
</output>
