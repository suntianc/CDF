---
phase: quick-260603-w0y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .codegraph/daemon.pid (git rm --cached; file preserved on disk)
  - .codegraph/.gitignore (git rm --cached; file preserved on disk)
autonomous: true
requirements:
  - QUICK-260603-w0y (eliminate .codegraph/ runtime noise from git status)
user_setup: []

must_haves:
  truths:
    - "git status no longer shows .codegraph/daemon.pid as a modified tracked file"
    - "git ls-files .codegraph/ returns empty (all runtime state untracked)"
    - ".codegraph/daemon.pid and other runtime files remain on disk so the codegraph daemon keeps working"
    - "A single atomic commit captures both .gitignore change and the --cached removals"
  artifacts:
    - path: ".gitignore"
      provides: "Root ignore rule covering the entire .codegraph/ directory"
      contains: ".codegraph/"
  key_links:
    - from: ".gitignore"
      to: ".codegraph/daemon.pid"
      via: ".codegraph/ path rule"
      pattern: "^\\.codegraph/$"
    - from: "git index"
      to: ".codegraph/ runtime files"
      via: "git rm --cached"
      pattern: "git rm --cached \\.codegraph/"
---

<objective>
Stop `.codegraph/daemon.pid` (and the rest of the codegraph daemon's per-machine runtime state) from polluting `git status`.

Approach: add `.codegraph/` to the root `.gitignore` (so future runtime files are auto-ignored) and untrack the two files currently in the index (`.codegraph/daemon.pid` and `.codegraph/.gitignore`) using `git rm --cached`, which removes them from the index only — the actual files stay on disk so the codegraph daemon keeps running. Land everything in a single atomic commit.

Output: a single git commit on the current branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Reference (current state — read for verification only, do NOT modify)
- Current root .gitignore: 45 lines, has `node_modules/`, `dist/`, `*.log`, `*.tsbuildinfo`, etc. No `.codegraph/` entry.
- Currently tracked in `.codegraph/`: only `.codegraph/.gitignore` (daemon's internal self-config) and `.codegraph/daemon.pid` (runtime PID file).
- Currently dirty: `M .codegraph/daemon.pid` (PID changes every daemon restart).
- Runtime files on disk (NOT tracked, must remain on disk): `codegraph.db`, `codegraph.db-shm`, `codegraph.db-wal`, `daemon.log`, `daemon.pid`, plus any `cache/` and `daemon.sock`.
- DO NOT modify `.codegraph/.gitignore` (daemon's own self-config; root `.gitignore` supersedes it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit .gitignore and untrack currently-tracked .codegraph/ files</name>
  <files>.gitignore, .codegraph/daemon.pid, .codegraph/.gitignore</files>
  <action>
Apply two changes (working tree + index), do NOT commit yet — Task 2 owns the commit.

1. **Edit root `.gitignore`** to add the codegraph rule.
   - Append a new section at the end of the file (after the existing `# TypeScript cache` block, preserve the trailing newline).
   - Section content — use exactly:
     ```
     
     # CodeGraph MCP — per-machine daemon runtime state (db, pid, log, sock, cache)
     # These files are managed locally by the codegraph daemon and must never be committed.
     .codegraph/
     ```
   - Why whole-directory (not just `daemon.pid`): the daemon evolves and adds new runtime files (db, log, sock, cache, future PID formats). A blanket rule is future-proof and matches the daemon's intent (every file under `.codegraph/` is per-machine state).

2. **`git rm --cached` the two tracked files** to remove them from the index. The files stay on disk.
   - Run: `git rm --cached .codegraph/daemon.pid .codegraph/.gitignore`
   - DO NOT run `git rm` (no `--cached`) — that would delete the files and break the running daemon.
   - Why also untrack `.codegraph/.gitignore`: the daemon's internal gitignore becomes dead weight once the root `.gitignore` covers the whole directory. Removing it from the index keeps the working tree consistent (no "intent to track a config that no longer matters").

3. **Pre-commit sanity check** (must all pass before handing off to Task 2):
   - `git status --short` — must show: `M  .gitignore`, `D  .codegraph/daemon.pid`, `D  .codegraph/.gitignore`. The PID file's previous "M" (modified) line should be gone, replaced by a clean "D" (deleted from index).
   - `git ls-files .codegraph/` — must return empty (no tracked files under `.codegraph/`).
   - `ls -la .codegraph/` — must still show `daemon.pid` and `.gitignore` on disk (working tree preserved).
   - `test -f .codegraph/daemon.pid && echo "pid preserved"` — must print `pid preserved`.
  </action>
  <verify>
    <automated>git ls-files .codegraph/ | wc -l | grep -qx 0 && test -f .codegraph/daemon.pid && test -f .codegraph/.gitignore && grep -qx '.codegraph/' .gitignore</automated>
  </verify>
  <done>
  - `.gitignore` ends with the new `# CodeGraph MCP` section containing `.codegraph/`.
  - `git ls-files .codegraph/` is empty.
  - `.codegraph/daemon.pid` and `.codegraph/.gitignore` still exist on disk.
  - `git status --short` shows the expected `M` + `D` lines and no `M .codegraph/daemon.pid` noise.
  </done>
</task>

<task type="auto">
  <name>Task 2: Single atomic commit + post-commit verification</name>
  <files>.gitignore, .codegraph/daemon.pid, .codegraph/.gitignore</files>
  <action>
Land the changes from Task 1 as a single atomic commit. The staging area already has the right contents from Task 1 — do not run `git add` again or you'll risk staging unrelated modifications. Verify staging first, then commit, then verify the result.

1. **Confirm staging area is exactly the 3 expected files** (sanity gate before commit):
   - Run: `git diff --cached --name-only` — must list exactly: `.codegraph/.gitignore`, `.codegraph/daemon.pid`, `.gitignore` (order may vary).
   - Run: `git diff` (unstaged) — must be empty. If something unstaged appears, STOP and surface it — do not commit unrelated work.

2. **Create the atomic commit** with a message that explains bug + fix + preservation of local files. Use a HEREDOC to preserve formatting:
   ```bash
   git commit -m "$(cat <<'EOF'
   chore(git): gitignore .codegraph/ + untrack runtime artifacts
   
   .codegraph/ is the codegraph MCP daemon's per-machine runtime state
   (db, pid, log, sock, cache). Tracking .codegraph/daemon.pid made every
   daemon restart appear as a modification in `git status`, polluting the
   working tree with noise that was never user-authored content.
   
   Fix:
   - Add `.codegraph/` to the root .gitignore so the entire directory is
     ignored (future-proof: covers new runtime files the daemon may add).
   - `git rm --cached` the two previously-tracked files (.codegraph/daemon.pid
     and .codegraph/.gitignore) to untrack them. The actual files stay on
     disk — `git rm --cached` only removes them from the index — so the
     running codegraph daemon is unaffected.
   
   The daemon's internal .codegraph/.gitignore is preserved on disk but
   no longer tracked, since the root .gitignore now supersedes it.
   
   Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
   EOF
   )"
   ```
   - Use the project's existing commit style (see recent commits: `chore(deps): ...`, `fix(llm): ...`, `docs(...): ...`). This one is `chore(git): ...` because it's a config/meta change with no production code impact.

3. **Post-commit verification** (all must pass):
   - `git log -1 --format='%H %s'` — must show the new commit with the `chore(git): gitignore .codegraph/ + untrack runtime artifacts` subject.
   - `git status` — must be clean (no output beyond "nothing to commit, working tree clean"), EXCLUDING any new runtime churn the daemon produces after the commit (a fresh `daemon.pid` mtime change right after commit is acceptable as long as it appears as `??` untracked, not `M` modified).
   - `git ls-files .codegraph/` — still empty.
   - `ls -la .codegraph/daemon.pid` — file still present, daemon still has its PID file.
   - `cat .codegraph/daemon.pid | head -1` — should print a numeric PID (sanity: file content is real, not corrupted).

4. **If `git status` is NOT clean after commit**, do not auto-fix. Surface the unexpected output in the SUMMARY and let the human decide — something else may have changed concurrently.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -qx 'chore(git): gitignore .codegraph/ + untrack runtime artifacts' && git ls-files .codegraph/ | wc -l | grep -qx 0 && test -f .codegraph/daemon.pid && git diff --cached --name-only | wc -l | grep -qx 0</automated>
  </verify>
  <done>
  - One new commit on the current branch with subject `chore(git): gitignore .codegraph/ + untrack runtime artifacts`.
  - Commit body explains the bug (PID file noise), the fix (.codegraph/ gitignored + untracked), and that local files are preserved.
  - `git ls-files .codegraph/` is empty.
  - `.codegraph/daemon.pid` still on disk; daemon keeps running.
  - Working tree is clean (modulo any new runtime churn the daemon produces, which will appear as untracked, not as `M`).
  - Co-authored-by trailer present.
  </done>
</task>

</tasks>

<verification>
- `grep -qx '.codegraph/' .gitignore` — the new ignore rule is in the root .gitignore.
- `git ls-files .codegraph/ | wc -l` — equals 0 (nothing tracked under .codegraph/).
- `test -f .codegraph/daemon.pid` — PID file preserved on disk; daemon still functional.
- `git log -1 --format='%s'` — last commit is the chore(git) commit from this plan.
- `git status` — working tree clean (no `M .codegraph/daemon.pid` noise; daemon churn, if any, shows as untracked only).
</verification>

<success_criteria>
- `.codegraph/daemon.pid` no longer appears as a modified tracked file in `git status`.
- A single commit adds `.codegraph/` to the root `.gitignore` AND untracks the two previously-tracked `.codegraph/` files.
- The codegraph daemon's local files (PID, sock, db, log) remain on disk untouched.
- The fix is future-proof: any new runtime files the codegraph daemon creates under `.codegraph/` are auto-ignored.
</success_criteria>

<output>
Create `.planning/quick/260603-w0y-gitignore-daemon-pid-codegraph-gitignore/260603-w0y-SUMMARY.md` when done
</output>
