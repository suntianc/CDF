---
phase: 260602-dz3-json-schema-output-validator-node-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/workflow/output-validator.ts
  - src/shared/node-output-schemas.ts
  - src/main/workflow/node-executor.ts
  - src/main/workflow/node-executor.test.ts
autonomous: true
requirements:
  - REMOVE-OUTPUT-VALIDATOR-MECHANISM
user_setup: []
must_haves:
  truths:
    - "src/main/workflow/output-validator.ts 文件不再存在"
    - "src/shared/node-output-schemas.ts 文件不再存在"
    - "node-executor.ts 中没有 import 或调用 executeWithValidation 的代码"
    - "node-executor.ts 中三个执行路径(Loop/For-Each/Task)直接调用 invokeAgent,不再返回 structuredOutput/_degraded/_validationErrors 字段"
    - "extractWorkflowRouting 与 extractJsonCandidate 行为不变,routing 提取仍能从原始输出中工作"
    - "node-executor.test.ts 不再断言已删除的字段;tsc 编译通过;npm test 通过"
  artifacts:
    - path: "src/main/workflow/node-executor.ts"
      provides: "直接调用 invokeAgent 的节点执行器,无校验包装"
      contains: "const invokeAgent = async"
    - path: "src/main/workflow/node-executor.test.ts"
      provides: "节点执行的单元测试,移除校验相关用例"
  key_links:
    - from: "src/main/workflow/node-executor.ts"
      to: "deepagents (agent.invoke)"
      via: "invokeAgent 闭包"
      pattern: "agent\\.invoke"
    - from: "src/main/workflow/node-executor.ts"
      to: "extractWorkflowRouting"
      via: "从 resultText 提取路由"
      pattern: "extractWorkflowRouting\\(resultText\\)"
---

<objective>
完全移除工作流节点输出 JSON Schema 校验机制,还原 node-executor.ts 为直接调用 invokeAgent 的简洁实现,删除与校验相关的所有代码与测试。

Purpose: 上次 quick task 260531-umu 引入的校验机制存在 6 处根本性矛盾(无 schema 提示、5 次重试代价大、与 routing 提取冲突、被 tool call JSON 误伤、字段无下游消费者、TaskOutputSchema 过度抽象)。移除后可消除 1 个错误复杂度源,使执行器职责更聚焦(routing 提取 + 原始透传)。

Output:
- 删除 `src/main/workflow/output-validator.ts`
- 删除 `src/shared/node-output-schemas.ts`
- `node-executor.ts` 移除校验包装,直接 await invokeAgent
- `node-executor.test.ts` 清理校验相关测试,保留核心 routing/loop/foreach/执行测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
./CLAUDE.md

# 必读:三处 executeWithValidation 调用点(代码快照)
# node-executor.ts 第 416 行(Loop)、第 499 行(For-Each)、第 550 行(Task/Review)
# 替换模式统一为:
#   const { output: resultText, validated, degraded } = await executeWithValidation(invokeAgent, nodeKind, ctx, onLog);
#   onLog?.(`[X] 节点执行完毕`);
#   const routing = extractWorkflowRouting(resultText);
#   return { result: resultText, ...(validated ? { structuredOutput: validated } : {}), ...(degraded ? { _degraded: true, _validationErrors: degraded._validationErrors } : {}), nodeId, agentId, duration_ms, ...(routing ? { routing } : {}) };
# 替换后:
#   const resultText = await invokeAgent(ctx);
#   onLog?.(`[X] 节点执行完毕`);
#   const routing = extractWorkflowRouting(resultText);
#   return { result: resultText, nodeId, agentId, duration_ms, ...(routing ? { routing } : {}) };

# 保留函数(整个文件其它逻辑不能动)
# - extractJsonCandidate(node-executor.ts:141)
# - extractWorkflowRouting(node-executor.ts:164)
# - invokeAgent 闭包(node-executor.ts:332)
# - Loop / For-Each / Task 主流程结构

# 测试文件当前结构(共 567 行,行号基于现状)
# - 125-160: 既有 loop 路径测试,toMatchObject 包含 structuredOutput 断言需要清理
# - 192-352: 输出校验场景区块(整段删除 + 部分保留,详见 Task 2)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除校验文件 + 改造 node-executor.ts 三处调用]<]minimax[>[</name>
  <files>src/main/workflow/output-validator.ts, src/shared/node-output-schemas.ts, src/main/workflow/node-executor.ts]<]minimax[>[</files>
  <action>
**步骤 1:删除两个文件**
```
rm src/main/workflow/output-validator.ts
rm src/shared/node-output-schemas.ts
```
不要在 package.json 中删除 zod 依赖(留给后续 cleanup 任务,且 types.ts/state-schema.ts/tools.ts 仍使用 zod,本次改动不会让 zod 变孤儿)。

**步骤 2:node-executor.ts 移除 import**
- 删除第 20 行 `import { executeWithValidation } from './output-validator';`

**步骤 3:Loop 路径替换(第 416-431 行)**
- 删除:
  ```
  const { output: resultText, validated, degraded } = await executeWithValidation(
    invokeAgent,
    nodeKind,
    iterationContext,
    onLog,
  );
  ```
- 替换为:
  ```
  const resultText = await invokeAgent(iterationContext);
  ```
- 在 iterations.push 中删除 `...(validated ? { structuredOutput: validated } : {})` 和 `...(degraded ? { _degraded: true, _validationErrors: degraded._validationErrors } : {})` 两行 spread
- 保留 `...(routing ? { routing } : {})` 与其余 push 字段

**步骤 4:For-Each 路径替换(第 499-516 行)**
- 同上模式替换 executeWithValidation 调用为 `const resultText = await invokeAgent(itemContext);`
- 注释 `// Validate For-Each individual items using 'task' (TaskOutputSchema) instead of 'foreach'` 同步删除
- 删除 validated / degraded 两行 spread
- 保留 routing 提取与 `success: true` 等其它字段

**步骤 5:Task / Review 路径替换(第 550-570 行)**
- 替换 executeWithValidation 为 `const resultText = await invokeAgent(taskContext);`
- 注释 `// 4. 执行 DeepAgent(普通任务 / 审查节点为单次调用,带输出校验)` 中的"带输出校验"改为"单次调用"(或不保留——直接删除该行注释也可以,KISS)
- 删除 return 中 validated / degraded 两行 spread
- 保留 resultText、nodeId、agentId、duration_ms、routing 字段

**严格约束(KISS / Surgical Changes):**
- ❌ 不要新增任何文件、不要重构 invokeAgent、不要碰 extractJsonCandidate / extractWorkflowRouting
- ❌ 不要重命名 `resultText` 局部变量、不要改 onLog 文案、不要把 `invokeAgent` 提到外层
- ❌ 不要在 return 中额外加新字段(例如什么 rawValidated、validationDisabled 之类的开关)
-  唯一目标:让三个调用点从"被 executeWithValidation 包装"变成"直接 await invokeAgent",并把不再有意义的两个 spread 行删掉
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `output-validator.ts` 与 `node-output-schemas.ts` 已删除
    - `node-executor.ts` 中无 `executeWithValidation` import 与调用
    - 三个 return 对象中无 `structuredOutput` / `_degraded` / `_validationErrors` 字段
    - `npx tsc --noEmit -p tsconfig.node.json` 无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 清理 node-executor.test.ts 中校验相关测试]<]minimax[>[</name>
  <files>src/main/workflow/node-executor.test.ts]<]minimax[>[</files>
  <action>
**目标**:删除"输出校验场景"区块中所有断言已移除字段的测试用例,并清理既有测试中残留的 structuredOutput 断言。**保留所有 routing 提取、Loop 早停、For-Each 执行、节点执行核心测试**。

**步骤 1:删除 4 个完全失效的测试用例(行号基于当前文件)**
整段删除以下 it() 块(包含前后空行):
- `// ---- Task 3: 输出校验场景测试 ----` 注释(行 192)
- `it('should include structuredOutput when task node output passes validation', ...)` 行 194-212
- `it('should retry when output fails validation and succeed on second attempt', ...)` 行 214-235
- `it('should degrade after MAX_RETRIES (5) consecutive validation failures', ...)` 行 237-259
- `it('should validate review node output with verdict field', ...)` 行 261-289

**步骤 2:清理既有 loop 路径测试中的 structuredOutput 断言(行 125-160)**
- 在 `should execute loop nodes once per iteration and pass previous output forward` 测试的 `iterations: [...]` 数组中
- 删除每一项对象里的 `, structuredOutput: { ... }` 字段(行 155-157 三处)
- 保持 `iteration` 与 `result` 字段不变
- 说明:`toMatchObject` 允许实际对象缺少期望中的字段,但保留已删除的字段会误导未来读者,故显式删除

**步骤 3:删除两个仅描述"校验不影响 routing"的测试(行 291-352)**
- 删除 `it('should include structuredOutput in loop iteration records on validation success', ...)` 行 291-310
- 删除 `it('should preserve routing extraction from raw output text regardless of validation', ...)` 行 312-330
- 删除 `it('should still extract routing from raw text even when validation degrades', ...)` 行 332-352
- 说明:routing 行为已由第 118-123 行的 `should extract routing decisions from agent JSON output`(纯函数测试)+ 第 162-190 行的 `should stop loop nodes early when the agent emits a completion routing signal` 覆盖,不需要单独再测"校验是否影响 routing"

**步骤 4:验证测试文件结构**
- 跑一次测试,确认所有剩余测试通过
- 若有任何已删除字段被遗漏的 toMatchObject / expect 断言,定位到行并修正

**严格约束(KISS / Surgical Changes):**
- ❌ 不要新增任何测试用例(用户说"完全移除",不要借机补新功能)
- ❌ 不要改 `beforeEach` 中 createDeepAgentMock 默认返回(mocks 链路被其它测试依赖)
- ❌ 不要改 `extractWorkflowRouting` 单测(行 118-123)
- ❌ 不要为已删除的 `output-validator.test.ts` 写新文件(若不存在就不存在)
-  唯一目标:把依赖已删除字段的断言全部清掉,保留测试套件对核心行为(routing、loop 早停、foreach 执行、节点执行、tool 回调日志)的覆盖
  </action>
  <verify>
    <automated>npx vitest run src/main/workflow/node-executor.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `node-executor.test.ts` 中无 `structuredOutput` / `_degraded` / `_validationErrors` 字符串
    - 所有 `should include structuredOutput ...` / `should retry when output fails validation ...` / `should degrade after MAX_RETRIES ...` / `should validate review node output with verdict field` / `should include structuredOutput in loop iteration records ...` / `should preserve routing extraction ... regardless of validation` / `should still extract routing ... even when validation degrades` 7 个测试已删除
    - `npx vitest run src/main/workflow/node-executor.test.ts` 全部通过(无 skipped、无 failed)
    - `npx tsc --noEmit -p tsconfig.node.json` 无错误(与 Task 1 一起验证)
  </done>
</task>

</tasks>

<verification>
## 静态检查
```bash
npx tsc --noEmit -p tsconfig.node.json
```
无错误。

## 单元测试
```bash
npx vitest run src/main/workflow/node-executor.test.ts
```
全部通过,无 skipped。

## 残留检查
```bash
# 应返回空(代码层 + 测试层)
grep -rn "output-validator\|node-output-schemas\|executeWithValidation\|structuredOutput\|_degraded\|_validationErrors" src/
```
无匹配。

## 文件存在性
```bash
# 应两个都不存在
test ! -f src/main/workflow/output-validator.ts
test ! -f src/shared/node-output-schemas.ts
```
</verification>

<success_criteria>
- 校验机制的 4 个文件改动全部完成
- 整个 codebase 不再引用 output-validator / node-output-schemas / executeWithValidation / structuredOutput / _degraded / _validationErrors
- routing 提取功能不受影响(routing 路径仍从 resultText 提取)
- extractJsonCandidate / extractWorkflowRouting 行为完全不变
- zod 依赖保留(其它文件仍使用,不删除)
- tsc + vitest 双重通过
</success_criteria>

<output>
完成后写 SUMMARY 到:
.planning/quick/260602-dz3-json-schema-output-validator-node-output/260602-dz3-01-SUMMARY.md
</output>
