---
quick_task: 260607-i7c
title: Add i18n (zh-CN + en-US) for renderer UI
type: execute
autonomous: true
files_modified:
  - package.json
  - src/main/store.ts
  - src/renderer/src/main.tsx
  - src/renderer/src/i18n/index.ts
  - src/renderer/src/i18n/locales/zh-CN.json
  - src/renderer/src/i18n/locales/en-US.json
  - src/renderer/src/stores/i18nStore.ts
  - src/renderer/src/stores/i18nStore.test.ts
  - src/renderer/src/i18n/index.test.ts
  - src/renderer/src/components/Sidebar/Sidebar.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/hooks/useLanguage.ts
  - src/renderer/src/hooks/useLanguage.test.ts
---

<objective>
Add internationalization (i18n) support to the CDF renderer's UI: install `i18next` + `react-i18next`, provide zh-CN (default) and en-US locales, follow system language on first launch, allow manual override from the Sidebar's settings menu with instant switching (no restart), and persist the user's choice via `electron-store`.

Scope is renderer UI copy only. Main process menus, IPC error messages, LLM prompts, and logs are out of scope per user decision.

Purpose: Users running CDF in an English environment get a localized UI; existing zh-CN users see no behavior change. The architecture must scale to additional languages later without touching every component.

Output: Working i18n init on renderer boot, language switcher in Sidebar settings menu, ~14 hardcoded zh-CN strings in Sidebar converted to translation keys, persistence to electron-store, test coverage for i18n init + language switching + persistence sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/Users/suntc/project/CDF/CLAUDE.md
@/Users/suntc/project/CDF/src/renderer/src/main.tsx
@/Users/suntc/project/CDF/src/renderer/src/App.tsx
@/Users/suntc/project/CDF/src/renderer/src/stores/themeStore.ts
@/Users/suntc/project/CDF/src/renderer/src/components/Sidebar/Sidebar.tsx
@/Users/suntc/project/CDF/src/main/store.ts
@/Users/suntc/project/CDF/package.json
@/Users/suntc/project/CDF/vitest.config.ts
</context>

<interfaces>
<!-- Key existing types and contracts the executor needs. -->

From `src/main/store.ts`:
- `Store<StoreSchema>` (electron-store) with `defaults` and `schema`. Schema fields: `theme`, `currentProjectId`, `sidebarWidth`, `sidebarCollapsed`, `windowBounds`. Add `language: 'zh-CN' | 'en-US'` to interface and schema.

From `src/preload/index.ts`:
- `window.electronAPI.store.get(key)` returns `Promise<unknown>` — reads from electron-store via IPC.
- `window.electronAPI.store.set(key, value)` returns `Promise<unknown>` — writes to electron-store via IPC.
- IPC channels: `store:get` / `store:set` already registered in `src/main/ipc-handlers.ts:139-140`.

From `src/renderer/src/stores/themeStore.ts`:
- Pattern: `create<State>()(persist((set) => ({...}), { name: 'theme-storage' }))`. Local-storage persistence; i18n uses electron-store (different mechanism) — do NOT use `persist` middleware for i18n.

From `src/renderer/src/main.tsx`:
- Bootstrap: `createRoot(...).render(<StrictMode><ErrorBoundary><TooltipProvider><App /></TooltipProvider></ErrorBoundary></StrictMode>)`. i18n init runs before render.

From `src/renderer/src/App.tsx`:
- `useEffect` at line 31-44 reads `window.electronAPI.store.get('theme')` and calls `setTheme`. Use the same pattern for language boot.

From `src/renderer/src/components/Sidebar/Sidebar.tsx`:
- Settings menu block (lines 150-188) under `<div className={styles.settingsMenu}>` has section header "通用设置" + 5 nav items. The Language selector will be added as a new section at the top of this settings menu (before "通用设置" header). View nav items stay unchanged.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install i18n deps and add language to electron-store schema</name>
  <files>package.json, src/main/store.ts</files>
  <behavior>
    - Test 1: `package.json` dependencies include `i18next` and `react-i18next` (no `i18next-http-backend` — JSON imports only).
    - Test 2: `StoreSchema` interface in `src/main/store.ts` includes `language: 'zh-CN' | 'en-US'`.
    - Test 3: `defaults` for `language` is `'zh-CN'` (current project default per D-01).
    - Test 4: `schema.language` validates `enum: ['zh-CN', 'en-US']`.
  </behavior>
  <action>
    1. Run `npm install i18next react-i18next` (NOT `i18next-http-backend` — user said JSON imports are fine, and electron-vite handles static JSON natively). Use `npm install` not `npm i --save-dev` (these are runtime deps).
    2. In `src/main/store.ts`:
       - Add to `StoreSchema`: `language: 'zh-CN' | 'en-US'`
       - Add to `defaults`: `language: 'zh-CN'`
       - Add to `schema`: `language: { type: 'string', enum: ['zh-CN', 'en-US'] }`
    3. Do NOT touch other store fields. Surgical change.
  </action>
  <verify>
    <automated>grep -E '"(i18next|react-i18next)"' package.json && grep -A2 "language:" src/main/store.ts | head -10</automated>
  </verify>
  <done>Both packages in package.json deps; electron-store schema extended with `language` field; `npm install` succeeded; no other store fields modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create i18n init, locales, and Zustand i18nStore with persistence sync</name>
  <files>
    src/renderer/src/i18n/index.ts,
    src/renderer/src/i18n/locales/zh-CN.json,
    src/renderer/src/i18n/locales/en-US.json,
    src/renderer/src/stores/i18nStore.ts,
    src/renderer/src/stores/i18nStore.test.ts,
    src/renderer/src/i18n/index.test.ts
  </files>
  <behavior>
    - Test 1: `i18nStore.currentLanguage` is `'zh-CN' | 'en-US'`, never undefined.
    - Test 2: `i18nStore.setLanguage('en-US')` updates `currentLanguage` AND calls `window.electronAPI.store.set('language', 'en-US')` AND calls `i18n.changeLanguage('en-US')`.
    - Test 3: `i18nStore.initFromStore()` reads `window.electronAPI.store.get('language')`; if returns valid value, sets it; if null/invalid, falls back to `navigator.language` detection (zh-* → 'zh-CN', everything else → 'en-US'); if detection throws, default to 'zh-CN'.
    - Test 4: i18n init registers 'zh-CN' and 'en-US' resources; switching language updates `t()` output (in test: `i18n.t('sidebar.newChat')` returns '新对话' for 'zh-CN', 'New Chat' for 'en-US').
  </behavior>
  <action>
    1. Create `src/renderer/src/i18n/locales/zh-CN.json` and `en-US.json` with these keys (covers Sidebar's 14 hardcoded strings + the new Language label):
       ```json
       {
         "sidebar": {
           "newChat": "新对话",
           "agents": "Agents 管理",
           "plugins": "插件",
           "workflows": "工作流",
           "back": "返回",
           "collapse": "折叠侧边栏",
           "modelConfig": "模型配置",
           "settings": {
             "header": "通用设置",
             "llm": "LLM 管理",
             "agents": "Agents 管理",
             "skillsMcp": "Skills & MCP 管理",
             "tools": "工具配置",
             "workflows": "工作流"
           },
           "language": {
             "label": "界面语言",
             "zh-CN": "简体中文",
             "en-US": "English"
           }
         }
       }
       ```
       For en-US: translate each value. "新对话" → "New Chat", "插件" → "Plugins", "工作流" → "Workflows", "返回" → "Back", "折叠侧边栏" → "Collapse sidebar", "模型配置" → "Model config", "通用设置" → "General Settings", "LLM 管理" → "LLM Management", "Agents 管理" → "Agents", "Skills & MCP 管理" → "Skills & MCP", "工具配置" → "Tools", "界面语言" → "Language".

    2. Create `src/renderer/src/i18n/index.ts`:
       - Import i18next and `initReactI18next` (named import from `react-i18next`).
       - Import both JSON locales.
       - Call `i18n.use(initReactI18next).init({ resources: { 'zh-CN': { translation: zhCN }, 'en-US': { translation: enUS } }, lng: 'zh-CN', fallbackLng: 'zh-CN', interpolation: { escapeValue: false }, react: { useSuspense: false } })`.
       - Export `i18n` as default.

    3. Create `src/renderer/src/stores/i18nStore.ts`:
       - Zustand store with `currentLanguage: 'zh-CN' | 'en-US'` and `setLanguage`, `initFromStore` actions.
       - `setLanguage(lang)`: validate it's in the union, call `i18n.changeLanguage(lang)`, set state, fire-and-forget `window.electronAPI.store.set('language', lang)` (do not block UI; do not throw on IPC failure, just `console.error`).
       - `initFromStore()`: try `await window.electronAPI.store.get('language')`; if result is `'zh-CN'` or `'en-US'`, use it; else detect: `navigator.language.toLowerCase().startsWith('zh')` → `'zh-CN'`, else `'en-US'`. After deciding initial language, call `setLanguage(initialLang)` (which syncs i18n + state + store; the IPC `set` is idempotent if value is unchanged).
       - Do NOT use Zustand `persist` middleware — we have a manual sync layer with electron-store.
       - Add `import type { StoreApi } from 'zustand'` if needed; the actual export is `useI18nStore`.

    4. Create `src/renderer/src/stores/i18nStore.test.ts`:
       - Mock `window.electronAPI.store.get` and `store.set` and `i18n.changeLanguage`.
       - Test: initial state is 'zh-CN'.
       - Test: `setLanguage('en-US')` updates state, calls store.set with 'language' and 'en-US', calls i18n.changeLanguage('en-US').
       - Test: `initFromStore` with store returning 'en-US' uses 'en-US'.
       - Test: `initFromStore` with store returning null + `navigator.language = 'en-GB'` uses 'en-US'.
       - Test: `initFromStore` with store returning null + `navigator.language = 'zh-TW'` uses 'zh-CN'.
       - Test: `setLanguage('fr-FR')` (invalid) is rejected (state unchanged). Add runtime guard.

    5. Create `src/renderer/src/i18n/index.test.ts`:
       - Test: i18n has 'zh-CN' and 'en-US' resources registered.
       - Test: `i18n.t('sidebar.newChat')` returns '新对话' by default.
       - Test: after `i18n.changeLanguage('en-US')`, `i18n.t('sidebar.newChat')` returns 'New Chat'.
       - Test: missing key falls back to zh-CN (or returns the key — depends on i18next config; assert one is fine).

    6. In `src/renderer/src/main.tsx`:
       - Add `import './i18n'` (side-effect import, runs init) before the createRoot call.
       - No `I18nextProvider` wrapper needed (initReactI18next hooks work via the singleton i18n instance).

    7. In `src/renderer/src/App.tsx`:
       - Add a `useEffect` mirroring the theme init (around line 31-44): call `useI18nStore.getState().initFromStore()` once on mount.
       - Import `useI18nStore` from `./stores/i18nStore`.

    8. Run tests: `npm test -- --reporter=verbose src/renderer/src/stores/i18nStore.test.ts src/renderer/src/i18n/index.test.ts`.

  </action>
  <verify>
    <automated>npm test -- src/renderer/src/stores/i18nStore.test.ts src/renderer/src/i18n/index.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>i18n module exports initialized i18n instance with both locales; i18nStore tests pass (>= 6 cases); i18n init tests pass (>= 4 cases); App.tsx calls initFromStore on mount; main.tsx imports i18n before render.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Language selector in Sidebar and replace hardcoded strings with t() calls</name>
  <files>src/renderer/src/components/Sidebar/Sidebar.tsx</files>
  <action>
    1. In `src/renderer/src/components/Sidebar/Sidebar.tsx`:
       - Import `useTranslation` from `react-i18next`.
       - Import `useI18nStore` from `../../stores/i18nStore`.
       - Inside the component, call `const { t, i18n } = useTranslation()` and `const { currentLanguage, setLanguage } = useI18nStore()`.
       - Replace all 14 hardcoded zh-CN strings with `t('sidebar.*')` calls. Map exactly:
         * Line 88 `返回` → `t('sidebar.back')`
         * Line 94 `新对话` → `t('sidebar.newChat')`
         * Line 102 `Agents 管理` → `t('sidebar.agents')`
         * Line 110 `插件` → `t('sidebar.plugins')`
         * Line 118 `工作流` → `t('sidebar.workflows')`
         * Line 128 `折叠侧边栏` → `t('sidebar.collapse')`
         * Line 144 `模型配置` → `t('sidebar.modelConfig')`
         * Line 151 `通用设置` → `t('sidebar.settings.header')`
         * Line 157 `LLM 管理` → `t('sidebar.settings.llm')`
         * Line 164 `Agents 管理` → `t('sidebar.settings.agents')`
         * Line 171 `Skills & MCP 管理` → `t('sidebar.settings.skillsMcp')`
         * Line 178 `工具配置` → `t('sidebar.settings.tools')`
         * Line 185 `工作流` → `t('sidebar.settings.workflows')`
       - Add a new section at the top of the settings menu block (BEFORE the "通用设置" header div, inside the `<div className={styles.settingsMenu}>`):
         ```tsx
         <div className={styles.settingsMenuHeader}>{t('sidebar.language.label')}</div>
         <div className={styles.settingsMenuItem}>
           <select
             value={currentLanguage}
             onChange={(e) => setLanguage(e.target.value as 'zh-CN' | 'en-US')}
             className="bg-transparent text-[var(--color-text-primary)] text-sm outline-none w-full cursor-pointer"
           >
             <option value="zh-CN">{t('sidebar.language.zh-CN')}</option>
             <option value="en-US">{t('sidebar.language.en-US')}</option>
           </select>
         </div>
         ```
       - Do NOT touch other Sidebar logic (resize, sessions, projects, theme toggle). Surgical change.
       - The select is a native `<select>` for now — no shadcn component to keep scope tight. Per project CLAUDE.md (simplicity first), no need to build a custom dropdown.

    2. Verify no other Sidebar strings remain hardcoded:
       ```bash
       grep -P "[\x{4e00}-\x{9fff}]" src/renderer/src/components/Sidebar/Sidebar.tsx
       ```
       Should return zero matches.

    3. Run all tests: `npm test -- src/renderer/src/stores/i18nStore.test.ts src/renderer/src/i18n/index.test.ts src/renderer/src/App.test.tsx 2>&1 | tail -20` (App.test.tsx must still pass — i18n import is a side-effect that should not break render).

    4. Atomic commit: `feat(260607-i7c): wire language selector and replace hardcoded sidebar strings with t() calls`.
  </action>
  <verify>
    <automated>grep -P "[\x{4e00}-\x{9fff}]" src/renderer/src/components/Sidebar/Sidebar.tsx | wc -l | xargs -I {} test {} -eq 0 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Zero Chinese characters remain in Sidebar.tsx; language selector is rendered in settings menu; selecting an option updates state and re-renders Sidebar with new translations; all existing tests still pass; one atomic commit.</done>
</task>

</tasks>

<verification>
## Phase Checks
1. `npm test` — all tests pass (i18n init + i18nStore + existing App.test.tsx + Sidebar logic).
2. `npm run build` — electron-vite build succeeds; i18n JSON locales are bundled into renderer output.
3. Manual: launch dev, Sidebar shows zh-CN by default, language selector exists in settings menu, switching to en-US re-renders all sidebar labels to English, reload app remembers the choice, clearing electron-store reverts to navigator.language detection.

## Must-Have Truths (Goal-Backward)
- [ ] Renderer UI strings are sourced from i18n keys, not hardcoded zh-CN.
- [ ] zh-CN is the default (no behavior change for existing users).
- [ ] User can switch language from the Sidebar settings menu; switch is instant (no reload).
- [ ] Language preference persists across app restarts via electron-store.
- [ ] First launch with no saved preference follows `navigator.language` (zh-* → zh-CN, else en-US).
- [ ] All test suites pass; build succeeds.
</verification>

<success_criteria>
- Three tasks completed in this order: deps + schema → i18n init + store + tests → sidebar wiring.
- Files modified match the `files_modified` list (no more, no less).
- No main-process code touched (no IPC channel additions; existing `store:get`/`store:set` reused).
- One atomic commit per task (per project CLAUDE.md atomic-commits convention).
- ~30% context usage target met (light task set, no discovery phase).
</success_criteria>

<output>
Create `.planning/quick/260607-i7c-cdf-i18n-ui-react-i18next-zh-cn-en-us/260607-i7c-SUMMARY.md` when done.
</output>
