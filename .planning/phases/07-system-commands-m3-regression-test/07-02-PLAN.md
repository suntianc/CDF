---
phase: 07-system-commands-m3-regression-test
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - src/renderer/src/components/ChatArea/ChatArea.tsx
  - src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx
  - src/main/deepagent/llm-adapter.test.ts
  - src/main/deepagent/runtime.test.ts
  - src/main/llm.test.ts
autonomous: true
requirements:
  - SLASH-07
  - SLASH-REGRESSION
user_setup: []

must_haves:
  truths:
    - User types `/goal X` in textarea and presses Enter; handleSend's 5-line slash sniff detects the leading `/` + `selectionStart === 0` and routes through dispatcher.resolve + dispatch (D-14/D-15: 5-line sniff at top of handleSend body per C-06) — not through sendMessage
    - User types mid-text slash (selectionStart > 0) and presses Enter; the sniff does NOT trigger and the message goes through the regular sendMessage path (D-15 case 2: mid-text `/baz` is regular message)
    - User types `/  foo` and presses Enter; dispatcher.resolve returns null and the input falls through to the regular sendMessage path (D-15 case 3 clarified per A7 in RESEARCH: dispatcher.resolve null, NOT dispatcher error)
    - SLASH-REGRESSION it 7.1 (llm-adapter.test.ts, D-16): first `message_chunk` after `/plan` contains `<think>` (D-18) — load-bearing for 6-hunk patch-package on @langchain/anthropic@1.4.0 (D-19)
    - SLASH-REGRESSION it 7.2 (runtime.test.ts): plan mode strips bash/delete_file from `builtInTools` AND `interruptOn` is false (D-13 + A4 from RESEARCH open questions) so neither file-mutation nor shell tools are callable
    - SLASH-REGRESSION it 7.3 (llm.test.ts): slash `/plan` full roundtrip (dispatcher → IPC → runLLMChat → runtime) preserves M3 thinking chain end-to-end; `planOnly: true` propagates from renderer to deepagent without field loss (D-12 + D-17)
    - All 149 Phase 7-01 tests + 3 handleSend tests + 3 SLASH-REGRESSION it blocks pass green (155+ total)
  artifacts:
    - path: src/renderer/src/components/ChatArea/ChatArea.tsx
      provides: "textareaRef + 5-line slash sniff at top of handleSend (D-14)"
      contains: "textareaRef"
    - path: src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx
      provides: "3 test cases for 5-line sniff: recognized / not recognized / fall-through (D-15)"
      min_lines: 80
    - path: src/main/deepagent/llm-adapter.test.ts
      provides: "SLASH-REGRESSION it 7.1: first message_chunk in plan mode contains think-tag (D-17)"
      min_lines: 200
    - path: src/main/deepagent/runtime.test.ts
      provides: "SLASH-REGRESSION it 7.2: plan mode does not invoke write_file/edit_file/bash tools (D-17)"
      min_lines: 400
    - path: src/main/llm.test.ts
      provides: "SLASH-REGRESSION it 7.3 (a+b): slash-plan preserves M3 thinking chain + payload.overrides.planOnly propagation (D-17)"
      min_lines: 950
  key_links:
    - from: src/renderer/src/components/ChatArea/ChatArea.tsx
      to: src/renderer/src/lib/commands/dispatcher.ts
      via: "5-line sniff: dispatcherResolve(inputVal, registry.commands) + dispatcherDispatch(plan)"
      pattern: "dispatcherResolve"
    - from: src/renderer/src/components/ChatArea/ChatArea.tsx
      to: src/renderer/src/components/ChatArea/ChatArea.tsx
      via: "textareaRef.current?.selectionStart === 0 (D-14)"
      pattern: "textareaRef"
    - from: src/main/deepagent/llm-adapter.test.ts
      to: src/main/deepagent/anthropic-roundtrip.test.ts
      via: "v3Events() async generator pattern (Anthropic SDK mock)"
      pattern: "v3Events|message_chunk"
    - from: src/main/deepagent/runtime.test.ts
      to: src/main/deepagent/runtime.ts
      via: "createDeepAgentRuntime with overrides.planOnly=true → assert builtInTools.length === 1 (only fetch) and interruptOn === false"
      pattern: "planOnly"
    - from: src/main/llm.test.ts
      to: src/main/llm.ts
      via: "runLLMChat with payload.overrides.planOnly=true → assert first sender.send chunk is message_chunk with think-tag text"
      pattern: "planOnly"
---

<objective>
Wire the 5-line slash sniff in ChatArea.handleSend so that messages starting with slash (caret at position 0) route through dispatcher.resolve + dispatch instead of the regular sendMessage path. Add the 3 SLASH-REGRESSION it blocks that protect the 6-hunk patch-package on @langchain/anthropic@1.4.0 and verify that plan mode actually suppresses write tools at the deepagent runtime.

Purpose: Plan 07-01 built the 3 system command implementations and wired the planOnly type chain end-to-end. This plan adds the user-visible entry point (handleSend sniff) that triggers those commands AND the regression tests that prove the M3 thinking chain survives Phase 7 changes. The 3 it blocks are the load-bearing test for the 6-hunk patch-package — if npm install strips the patch in the future, these tests fail loudly.

Output: 5 files modified/created (1 modified + 1 new test file for handleSend + 3 extended test files for SLASH-REGRESSION) + 6 new tests (3 handleSend + 3 SLASH-REGRESSION) on top of the ~149-test Phase 7-01 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-system-commands-m3-regression-test/07-CONTEXT.md
@.planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md
@.planning/phases/07-system-commands-m3-regression-test/07-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-02-SUMMARY.md
@.planning/research/PITFALLS.md

# Source files the executor must read before editing
@src/renderer/src/components/ChatArea/ChatArea.tsx (1162 lines — focus on lines 1-15 imports, 141-156 state/refs, 148-154 useRef cluster, 611-628 handleSend body, 665-684 handleSlashSelect (uses dispatcherResolve), 686-711 handleKeyDown, 1040-1059 textarea JSX with no ref attribute)
@src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx (focus on TestHarness pattern around lines 1-80 to mirror it in handleSend test)
@src/renderer/src/lib/commands/dispatcher.ts (115 lines — exports resolve and dispatch; Plan 07-01 upgraded the 3 branches)
@src/renderer/src/lib/commands/dispatcher.test.ts (177 lines — exports resolve and dispatch; tests for resolve include slash-goal, slash-then-spaces returning null)
@src/main/deepagent/llm-adapter.test.ts (159 lines — existing 9 tests in createLangChainModel describe block; the new it 7.1 goes in a new describe block at the end)
@src/main/deepagent/anthropic-roundtrip.test.ts (focus on v3Events() async generator at lines 130-180 — the pattern to copy for it 7.1)
@src/main/deepagent/runtime.test.ts (16.5K / ~400 lines — existing 13 tests in createDeepAgentRuntime describe block; the new it 7.2 goes at the end of that describe)
@src/main/deepagent/runtime.ts (613 lines — focus on lines 460 overrides parameter, 488 builtInTools, 599 interruptOn — Plan 07-01 added isPlanMode gate here)
@src/main/llm.test.ts (30.8K / ~963 lines — existing 18+ tests in runLLMChat describe block; the new it 7.3 + payload.overrides.planOnly test go at the end)
@src/main/llm.ts (825 lines — focus on lines 376-378 sender.send(channel, message_chunk, think-tag) — the first chunk in a reasoning-emitting stream)
</context>

<interfaces>
From src/renderer/src/components/ChatArea/ChatArea.tsx (handleSend insertion point):
- `const handleSend = async (e?: React.FormEvent) => Promise<void>` at line 617-628 — must insert 5-line sniff at the top (after `if (e) e.preventDefault();`)
- `const messagesEndRef = useRef<HTMLDivElement>(null)` at line 148 — useRef cluster pattern (add textareaRef next to it)
- `<textarea value={inputVal} onChange={...} onCompositionStart={...} onCompositionEnd={...} onKeyDown={handleKeyDown} ...>` at lines 1044-1059 — must add `ref={textareaRef}` attribute
- The `resolve as dispatcherResolve` and `dispatch as dispatcherDispatch` imports are already at the top of the file (Phase 6 added them; verify by `grep`)

From src/renderer/src/lib/commands/dispatcher.ts:
- `export function resolve(inputVal, commands): CommandDispatchAction | null` — returns null for slash-then-spaces (A7 + D-15 case 3 fall-through)
- `export async function dispatch(plan: CommandDispatchAction): Promise<void>` — Plan 07-01 wired the 3 real implementations (SystemSilent → sessionGoals + toast, SystemLocal → IPC + toast, PlanMode → sendMessage + { planOnly: true })

From src/main/deepagent/runtime.ts (Plan 07-01 output):
- `createDeepAgentRuntime(projectId, sessionId, message, agentId?, overrides?: RuntimeModelOverrides, subagentIds?)` at line 453
- `const isPlanMode = Boolean(overrides?.planOnly)` added at function body start
- `const builtInTools: any[] = [createFetchTool()]` with `if (!isPlanMode) { ... push bash + delete_file }` at line 488
- `interruptOn: isPlanMode ? false : DEFAULT_INTERRUPT_ON` at line 599

From src/main/llm.ts (Plan 07-01 output):
- `ChatPayload.overrides?: ChatRuntimeOverrides` at line 32-44 (Gap 1 fixed)
- `sender.send(channel, { type: 'message_chunk', text: '<think>' })` at line 376-378 — the first reasoning chunk in a thinking stream

From src/main/deepagent/anthropic-roundtrip.test.ts (v3 stream fixture):
- `v3Events()` async generator at lines 130-180 — emits message-start + content-block-start (reasoning) + content-block-delta (reasoning-delta) + content-block-finish (reasoning) + text blocks + message-finish events
- Use this pattern as the template for it 7.1 mock
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ChatArea — add textareaRef + 5-line slash sniff in handleSend (D-14)</name>
  <files>src/renderer/src/components/ChatArea/ChatArea.tsx</files>
  <read_first>
    - src/renderer/src/components/ChatArea/ChatArea.tsx:1-15 (imports — already imports useRef from React; check for existing dispatcherResolve / dispatcherDispatch imports)
    - src/renderer/src/components/ChatArea/ChatArea.tsx:148-156 (useRef cluster — pattern for adding textareaRef)
    - src/renderer/src/components/ChatArea/ChatArea.tsx:617-628 (handleSend function body — exact insertion point)
    - src/renderer/src/components/ChatArea/ChatArea.tsx:665-684 (handleSlashSelect — Phase 6 pattern showing how dispatcherResolve + dispatcherDispatch are wired)
    - src/renderer/src/components/ChatArea/ChatArea.tsx:1040-1059 (textarea JSX — must add ref={textareaRef} attribute)
    - .planning/phases/07-system-commands-m3-regression-test/07-CONTEXT.md:60-67 (D-14 sniff rule + D-15 3 cases)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:777-870 (sniff implementation + A7 dispatch fall-through analysis)
  </read_first>
  <action>
    Per D-14, add a 5-line slash sniff at the top of handleSend that routes slash-prefixed messages through dispatcher.resolve + dispatch instead of sendMessage. The sniff fires when `inputVal.startsWith('/')` AND `textareaRef.current?.selectionStart === 0` (caret at the very start of the textarea).

    1. **Add `textareaRef`** in `src/renderer/src/components/ChatArea/ChatArea.tsx`. After line 148 (`const messagesEndRef = useRef<HTMLDivElement>(null);`) or near the existing useRef cluster (lines 148-155), add:
       ```
       const textareaRef = useRef<HTMLTextAreaElement>(null);
       ```
       Place it next to the other useRef declarations (e.g., after line 153 `const slashRef = useRef<SlashCommandPopupHandle>(null);`).

    2. **Bind `ref={textareaRef}` to the `<textarea>` JSX element** at lines 1044-1059. Add the attribute to the existing textarea element:
       ```
       <textarea
         ref={textareaRef}
         value={inputVal}
         onChange={...}
         ...
       />
       ```
       This is the load-bearing wiring — without `ref={textareaRef}`, `textareaRef.current` is always null and the selectionStart check is always false (Pitfall P7-4).

    3. **Insert the 5-line sniff at the top of handleSend** at lines 617-628. The current function is:
       ```
       const handleSend = async (e?: React.FormEvent) => {
         if (e) e.preventDefault();
         if (!inputVal.trim() || !currentProjectId || isStreaming) return;
         ...
       };
       ```
       REPLACE with the same lines plus an inserted sniff block between `e.preventDefault()` and the trim check:
       ```
       const handleSend = async (e?: React.FormEvent) => {
         if (e) e.preventDefault();
         // Phase 7 D-14: 5-line sniff — only treat as slash command when caret is at start
         if (
           inputVal.startsWith('/') &&
           textareaRef.current?.selectionStart === 0
         ) {
           const plan = dispatcherResolve(inputVal, registry.commands);
           if (plan) {
             setInputVal('');
             dispatcherDispatch(plan).catch((err) => console.error('[handleSend/slash] error:', err));
             return;
           }
         }
         if (!inputVal.trim() || !currentProjectId || isStreaming) return;
         ...
       };
       ```
       The existing `import { resolve as dispatcherResolve, dispatch as dispatcherDispatch } from '@/lib/commands/dispatcher'` should already be at the top of the file (verify by grep); if not, add it. Also verify `registry` is in scope — it comes from the useCommandRegistry hook call at line 665.

    4. **Behavior contract**:
       - When `plan` is non-null: clear the textarea (setInputVal('')), dispatch the command asynchronously (the .catch prevents unhandled promise rejection; the dispatch itself is fire-and-forget), and return early — sendMessage is NOT called.
       - When `plan` is null (e.g., slash-then-spaces does not match any registered command — A7): the if(plan) block does NOT execute, control falls through to the existing sendMessage path. This is D-15 case 3: slash-then-spaces is treated as a regular message.
       - When `inputVal` does NOT start with slash: the startsWith check short-circuits to false, sniff is skipped entirely.
       - When selectionStart > 0 (e.g., mid-text slash with caret at position 9): the selectionStart check is false, sniff is skipped, regular sendMessage path runs.

    5. **Verify the existing handleSend body is preserved** (the const value = inputVal; setInputVal(''); await sendMessage(...) lines at 621-627) — only the sniff is inserted between e.preventDefault() and the trim guard.

    6. **Do NOT modify** the existing handleSlashSelect (lines 673-684) which handles the popup Enter/Tab path; the 5-line sniff is a parallel path for the popup-bypassed case.

    7. **Run `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx`** to verify no regression in the existing 33 tests (Phase 5 + Phase 6 popup tests). Plan 07-02 Task 2 will add the new 5-line sniff test file.
  </action>
  <acceptance_criteria>
    - `grep -n "textareaRef" src/renderer/src/components/ChatArea/ChatArea.tsx` returns at least 3 matches: useRef declaration, JSX ref attribute, AND the textareaRef.current?.selectionStart check in handleSend
    - `grep -n "selectionStart === 0" src/renderer/src/components/ChatArea/ChatArea.tsx` returns 1 match (in handleSend)
    - `grep -n "dispatcherResolve" src/renderer/src/components/ChatArea/ChatArea.tsx` returns at least 1 match (in handleSend OR the existing handleSlashSelect)
    - `grep -n "dispatcherDispatch" src/renderer/src/components/ChatArea/ChatArea.tsx` returns at least 1 match
    - `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` exits 0 with all 33 existing tests still passing
    - `npx tsc --noEmit -p .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx</automated>
  </verify>
  <done>
    - textareaRef = useRef<HTMLTextAreaElement>(null) declared in the useRef cluster
    - <textarea ref={textareaRef} ... /> JSX attribute added (Pitfall P7-4 mitigation)
    - 5-line sniff inserted at the top of handleSend after e.preventDefault(): checks startsWith slash + selectionStart 0, calls dispatcherResolve + dispatcherDispatch if plan is non-null, otherwise falls through to regular sendMessage
    - All 33 existing popup tests still pass
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatArea.handleSend.test.tsx (NEW) — 3 test cases for 5-line sniff (D-15)</name>
  <files>src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx</files>
  <read_first>
    - src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx:1-100 (TestHarness pattern — render + mock + setup; mirror this for handleSend tests)
    - src/renderer/src/components/ChatArea/ChatArea.tsx:1-15 (imports for the chat-input test ID)
    - src/renderer/src/components/ChatArea/ChatArea.tsx:617-628 (handleSend function being tested)
    - src/renderer/src/lib/commands/dispatcher.ts (resolve returns null for slash-then-spaces per A7; dispatch writes to sessionGoals for SystemSilent)
    - src/renderer/src/stores/sessionStore.test.ts:33-74 (electronAPI mock pattern; extend with setSessionGoal + context.currentSession mocks)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:866-922 (3 test case shapes)
    - .planning/phases/07-system-commands-m3-regression-test/07-CONTEXT.md:63-67 (D-15 3 case definitions)
  </read_first>
  <action>
    Per D-15, create a new test file with 3 test cases for the 5-line sniff. Each test mounts ChatArea in a TestHarness, sets the textarea value + selectionStart via fireEvent, presses Enter, and asserts which code path was taken.

    1. **Create `src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx`** (NEW file, ~80-120 lines). Structure:

       a. **Imports**: `import { describe, it, expect, vi, beforeEach } from 'vitest';` + `import { render, screen, fireEvent, act } from '@testing-library/react';` + `import ChatArea from './ChatArea';` + `import { useSessionStore } from '@/stores/sessionStore';` + `import { useProjectStore } from '@/stores/projectStore';`

       b. **Mock infrastructure** at the top of the file (before any describe block):
          ```
          const mockSendMessage = vi.fn();
          const mockSetSessionGoal = vi.fn();
          const mockDispatchResolve = vi.fn();
          const mockDispatchDispatch = vi.fn();

          vi.mock('@/lib/commands/dispatcher', () => ({
            resolve: (...args) => mockDispatchResolve(...args),
            dispatch: (...args) => mockDispatchDispatch(...args),
          }));

          vi.mock('@/hooks/useCommandRegistry', () => ({
            useCommandRegistry: () => ({ commands: [
              { name: 'goal', source: 'system', target: 'goal', sourceLabel: 'system', badge: '[system]', description: 'set goal' },
              { name: 'context', source: 'system', target: 'context', sourceLabel: 'system', badge: '[system]', description: 'show context' },
              { name: 'plan', source: 'system', target: 'plan', sourceLabel: 'system', badge: '[system]', description: 'enter plan' },
            ], conflicts: [], warnings: [] }),
          }));

          vi.mock('sonner', () => ({ toast: { info: vi.fn(), error: vi.fn() } }));
          ```

       c. **Global window.electronAPI mock** in a beforeEach:
          ```
          beforeEach(() => {
            vi.clearAllMocks();
            (globalThis as any).window.electronAPI = {
              llm: { chat: mockSendMessage, onChunk: vi.fn(() => () => {}), stopChat: vi.fn() },
              db: { getMessages: vi.fn().mockResolvedValue([]), getAgentRuns: vi.fn().mockResolvedValue([]), getAgentToolCalls: vi.fn().mockResolvedValue([]), getSessions: vi.fn().mockResolvedValue([]), createSession: vi.fn(), saveMessage: vi.fn() },
              context: { currentSession: vi.fn().mockResolvedValue({ breakdown: { conversation: 100, skills: 50, mcp: 25, workflows: 75 }, total: 250 }) },
              store: { get: vi.fn(), set: vi.fn() },
              platform: 'darwin',
            };
            useSessionStore.setState({
              activeSessionId: 'session-1',
              sessionGoals: new Map(),
              setSessionGoal: mockSetSessionGoal,
              sendMessage: mockSendMessage,
              isStreaming: false,
              sessions: [{ id: 'session-1', project_id: 'project-1', name: 'Test', parent_session_id: null, summary: null, created_at: 0, updated_at: 0 }],
            } as any);
            useProjectStore.setState({ currentProjectId: 'project-1' } as any);
          });
          ```

       d. **3 test cases** (mirror the SlashCommandPopup.test.tsx TestHarness pattern with `render(<ChatArea ... />)` wrapped in necessary providers — check the existing popup test file for the exact provider wrapper):

       - **Test 1: slash-goal X at message start is recognized and routed through dispatcher**:
         - `mockDispatchResolve.mockReturnValue({ kind: 'SystemSilent', command: { name: 'goal', ... }, args: 'write tests' })` (a valid plan)
         - Set textarea value to `'/goal write tests'` + `selectionStart: 0` via fireEvent.change
         - fireEvent.keyDown with key: 'Enter' (no shiftKey)
         - Assert `mockDispatchResolve` was called with `('/goal write tests', [...])`
         - Assert `mockDispatchDispatch` was called with the SystemSilent plan
         - Assert `mockSendMessage` was NOT called (sniff intercepted the message)
         - Assert `mockSetSessionGoal` was called with `('session-1', 'write tests')`

       - **Test 2: mid-text slash with selectionStart > 0 is NOT recognized**:
         - `mockDispatchResolve.mockReturnValue(null)` (or any value; should not be called)
         - Set textarea value to `'/foo bar /baz'` + `selectionStart: 9` (the position right after `'/foo bar '`)
         - fireEvent.keyDown with key: 'Enter'
         - Assert `mockSendMessage` was called with `('project-1', '/foo bar /baz', expect.objectContaining({ providerId: undefined, model: undefined }))`
         - Assert `mockDispatchResolve` was NOT called (sniff short-circuited on selectionStart)

       - **Test 3: slash-then-spaces (no valid command match) falls through to sendMessage**:
         - `mockDispatchResolve.mockReturnValue(null)` (resolve returned null per A7)
         - Set textarea value to `'/  foo'` + `selectionStart: 0`
         - fireEvent.keyDown with key: 'Enter'
         - Assert `mockDispatchResolve` WAS called (sniff fired, just resolve returned null)
         - Assert `mockSendMessage` was called with `('project-1', '/  foo', expect.anything())` (fell through to regular path)

    2. **If ChatArea requires a more complex wrapper** (providers, contexts, mocks for other hooks like useChatScroll, useTheme, useAgentActivity), mirror the wrapper from the existing SlashCommandPopup.test.tsx file (look for `render(<...>...</...>)` patterns and any `<Provider>` wrappers). The exact wrapper shape will be visible by reading the first 80 lines of SlashCommandPopup.test.tsx.

    3. **Run the test**:
       ```
       npx vitest run src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx
       ```
       Expect 3 tests passing (one for each D-15 case).

    4. **If the test fails with "selectionStart is not a valid property"** (jsdom limitation), use the alternative approach: `fireEvent.change(textarea, { target: { value, selectionStart: 0 } })` — jsdom in vitest@3 supports this. If still failing, use `Object.defineProperty(textarea, 'selectionStart', { value: 0, configurable: true })` before fireEvent.keyDown.
  </action>
  <acceptance_criteria>
    - `test -f src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx` returns success
    - `grep -cE "it\\('" src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx` returns 3 (3 test cases for D-15)
    - `npx vitest run src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx` exits 0 with 3 tests passing
    - Test 1 asserts both `mockDispatchDispatch` was called AND `mockSendMessage` was NOT called
    - Test 2 asserts `mockDispatchResolve` was NOT called AND `mockSendMessage` was called
    - Test 3 asserts `mockDispatchResolve` WAS called AND `mockSendMessage` was called (fall-through)
    - `npx tsc --noEmit -p .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx  <verify>
    <automated>npx vitest run src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx</automated>
  </verify>
  <done>
    - New ChatArea.handleSend.test.tsx file exists with 3 test cases covering D-15
    - Test 1 verifies slash-goal X is recognized and routed through dispatcher (SystemSilent plan); sendMessage is NOT called
    - Test 2 verifies mid-text slash (selectionStart > 0) is NOT recognized; sendMessage IS called
    - Test 3 verifies slash-then-spaces (resolve returns null) falls through to sendMessage
    - All 33 existing popup tests still pass; TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 3: SLASH-REGRESSION 3 it blocks (it 7.1 in llm-adapter.test.ts, it 7.2 in runtime.test.ts, it 7.3a+7.3b in llm.test.ts)</name>
  <files>src/main/deepagent/llm-adapter.test.ts, src/main/deepagent/runtime.test.ts, src/main/llm.test.ts</files>
  <read_first>
    - src/main/deepagent/llm-adapter.test.ts:1-159 (full file — existing 9 createLangChainModel tests; new SLASH-REGRESSION it 7.1 goes at the end in a new describe block)
    - src/main/deepagent/anthropic-roundtrip.test.ts:130-180 (v3Events() async generator — the Anthropic SDK mock pattern to copy for it 7.1)
    - src/main/deepagent/runtime.test.ts:1-400 (focus on the createDeepAgentRuntime describe block at line 85+; the new it 7.2 goes at the end of that describe)
    - src/main/deepagent/runtime.ts:453-602 (createDeepAgentRuntime body — verify isPlanMode behavior in test)
    - src/main/llm.test.ts:1-963 (focus on the runLLMChat describe block at line 40+; the new it 7.3 + planOnly propagation test go at the end)
    - src/main/llm.ts:306-425 (runLLMChat body + streamEvents v3 — verify the first message_chunk is think-tag in plan mode)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:670-770 (SLASH-REGRESSION test design + 3 it block patterns)
    - .planning/phases/07-system-commands-m3-regression-test/07-CONTEXT.md:72-78 (D-17 spec for 3 it blocks)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:995-1015 (Pitfall P7-3: patch-package protection + load-bearing test purpose)
  </read_first>
  <action>
    Per D-16/D-17/D-18/D-19, add 3 SLASH-REGRESSION it blocks (plus 1 additional planOnly propagation test) that form the load-bearing test for the 6-hunk patch-package on @langchain/anthropic@1.4.0. If the patch is ever stripped by npm install, these tests fail loudly and force a `npx patch-package` rerun.

    **it 7.1 — first message_chunk in plan mode contains think-tag** (in `src/main/deepagent/llm-adapter.test.ts`)

    1. Append a new describe block at the end of `src/main/deepagent/llm-adapter.test.ts`:
       ```
       describe('SLASH-REGRESSION: 6-hunk patch-package guard (D-17/D-19)', () => {
         it('7.1: first message_chunk in plan mode contains <think> (reasoning roundtrip preserved)', async () => {
           // Mock the Anthropic SDK messages.stream with a v3 stream that emits a reasoning block first.
           // Use the v3Events() async generator pattern from anthropic-roundtrip.test.ts:130-180:
           //   yield { event: 'message-start', id: 'msg_plan_test' }
           //   yield { event: 'content-block-start', index: 0, content: { type: 'reasoning', reasoning: '', index: 0 } }
           //   yield { event: 'content-block-delta', index: 0, delta: { type: 'reasoning-delta', reasoning: 'plan only: analyze problem' } }
           //   yield { event: 'content-block-delta', index: 0, delta: { type: 'block-delta', fields: { type: 'reasoning', signature: 'sig-plan-abc' } } }
           //   yield { event: 'content-block-finish', index: 0, content: { type: 'reasoning', reasoning: 'plan only: analyze problem', signature: 'sig-plan-abc' } }
           //   yield { event: 'content-block-start', index: 1, content: { type: 'text', text: '', index: 1 } }
           //   yield { event: 'content-block-delta', index: 1, delta: { type: 'text-delta', text: 'Here is the plan.' } }
           //   yield { event: 'message-finish', reason: 'stop' }
           //
           // Pipe the v3 stream through the adapter (ChatModelStream pattern from
           // anthropic-roundtrip.test.ts) and assert:
           //   const blocks = message.content as Array<Record<string, unknown>>;
           //   expect(blocks[0].type).toBe('reasoning');
           //   expect(blocks[0].signature).toBe('sig-plan-abc');
           //
           // CRITICAL: if patch-package is not applied, _formatStandardContent's
           // isAnthropicMessage guard drops reasoning blocks and blocks[0] is text
           // (or the array is shorter). This test FAILS if patch is broken.
         });
       });
       ```

    **it 7.2 — plan mode does not invoke write_file/edit_file/bash tools** (in `src/main/deepagent/runtime.test.ts`)

    2. Append a new it at the end of the `createDeepAgentRuntime` describe block in `src/main/deepagent/runtime.test.ts`:
       ```
       it('7.2: plan mode (overrides.planOnly=true) suppresses write_file/edit_file/bash tools', async () => {
         // Mock createDeepAgent to capture the (config) argument.
         // Mock the existing dependencies (db, skill-manager, mcp-connector) as in the other
         // createDeepAgentRuntime tests (see runtime.test.ts:158-389 for patterns).
         //
         // Call: await createDeepAgentRuntime('project-1', 'session-1', { id: 'm1', content: 'plan this' }, 'agent-1', { planOnly: true })
         // The mock of createDeepAgent captures the (config) argument.
         //
         // Assert:
         //   - config.interruptOn === false (plan mode skips interrupt)
         //   - config.tools array length === 1 (only createFetchTool, no bash/delete_file)
         //   - config.tools array does NOT include any tool named 'bash' or 'delete_file'
         //
         // Also add a negative test: call createDeepAgentRuntime WITHOUT planOnly
         // (overrides = { planOnly: false }) and assert tools array length is 3+
         // (bash + delete_file + fetch present) and interruptOn === DEFAULT_INTERRUPT_ON.
       });
       ```

    **it 7.3a + 7.3b — slash-plan preserves M3 thinking chain + planOnly propagation** (in `src/main/llm.test.ts`)

    3. Append 2 new its at the end of the `runLLMChat` describe block in `src/main/llm.test.ts`:
       ```
       it('7.3a: payload.overrides.planOnly=true flows to createDeepAgentRuntime', async () => {
         // Mock createDeepAgentRuntime to capture the (projectId, sessionId, message, agentId, overrides) call.
         // Call: runLLMChat(sender, requestId, { projectId: 'p1', sessionId: 's1', message: { id: 'm1', content: 'plan this' }, overrides: { planOnly: true } })
         // Assert: createDeepAgentRuntime was called with the 5th argument (overrides) containing
         //   { planOnly: true } (use expect.objectContaining({ planOnly: true }) or capture and assert)
       });

       it('7.3b: slash /plan roundtrip emits first message_chunk with <think> (M3 thinking preserved)', async () => {
         // Mock the runtime to emit a v3-style stream with a reasoning block
         // (similar to the existing 'should stream assistant tokens' test at llm.test.ts:58+).
         // Run: runLLMChat with payload.overrides.planOnly = true.
         // Assert: the FIRST sender.send call to the chunk channel is a 'message_chunk' event
         //   whose text starts with '<think>'.
         // (look at llm.ts:376-378: sender.send(channel, { type: 'message_chunk', text: '<think>' })).
         //
         // Pattern: capture all sender.send calls, then assert sender.send.mock.calls[0]
         //   === [channel, { type: 'message_chunk', text: '<think>' }].
         //
         // CRITICAL: this test fails if the 6-hunk patch-package is broken OR if the
         // runLLMChat think-tag emission at lines 376-378 is removed.
       });
       ```

    4. **All 4 new tests use the existing mock patterns** in their respective files (no new mock infrastructure required). For it 7.1, copy the v3Events() generator pattern from anthropic-roundtrip.test.ts:130-180. For it 7.2, reuse the createDeepAgent mock setup at runtime.test.ts:158-389. For it 7.3a/7.3b, reuse the runLLMChat sender mock at llm.test.ts:40-57.

    5. **Run the 3 test files**:
       ```
       npx vitest run src/main/deepagent/llm-adapter.test.ts
       npx vitest run src/main/deepagent/runtime.test.ts
       npx vitest run src/main/llm.test.ts
       ```
       Each should report new tests passing (it 7.1: 1 new; it 7.2: 1 new; it 7.3a + 7.3b: 2 new) + all existing tests still green.

    6. **Full suite green check**:
       ```
       npm test
       ```
       Expect ~155 tests passing (149 Phase 7-01 + 3 handleSend + 3 SLASH-REGRESSION) with 0 regressions.
  </action>
  <acceptance_criteria>
    - `grep -c "it('7.1" src/main/deepagent/llm-adapter.test.ts` returns 1 (it 7.1 added)
    - `grep -c "it('7.2" src/main/deepagent/runtime.test.ts` returns 1 (it 7.2 added)
    - `grep -cE "it\\('7\\.3[ab]?" src/main/llm.test.ts` returns 2 (it 7.3a + it 7.3b added)
    - `npx vitest run src/main/deepagent/llm-adapter.test.ts` exits 0 with at least 10 tests (9 existing + 1 new)
    - `npx vitest run src/main/deepagent/runtime.test.ts` exits 0 with at least 14 tests (13 existing + 1 new)
    - `npx vitest run src/main/llm.test.ts` exits 0 with at least 20 tests (18 existing + 2 new)
    - it 7.1 asserts blocks[0].type === reasoning AND blocks[0].signature === sig-plan-abc
    - it 7.2 asserts config.interruptOn === false AND config.tools.length === 1
    - it 7.3b asserts the first sender.send call is message_chunk with text think-tag
    - `npx tsc --noEmit -p .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/main/deepagent/llm-adapter.test.ts src/main/deepagent/runtime.test.ts src/main/llm.test.ts</automated>
  </verify>
  <done>
    - it 7.1 added to llm-adapter.test.ts: first message_chunk in plan mode contains think-tag (load-bearing 6-hunk patch-package guard)
    - it 7.2 added to runtime.test.ts: plan mode (overrides.planOnly=true) suppresses write_file/edit_file/bash tools via tools array length and interruptOn === false
    - it 7.3a + 7.3b added to llm.test.ts: planOnly flows to createDeepAgentRuntime AND slash-plan roundtrip emits first message_chunk with think-tag
    - All existing tests in the 3 files still pass
    - Full npm test suite green (155+ tests)
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → DOM (textarea ref) | Untrusted user input flows through textarea.onChange; the 5-line sniff reads selectionStart from the DOM (PITFALLS P7-4) |
| main → deepagent runtime (planOnly) | Plan 07-01 wired payload.overrides.planOnly end-to-end; SLASH-REGRESSION it 7.2 verifies the runtime actually consumes it |
| npm install → patched node_modules | The 6-hunk patch-package on @langchain/anthropic@1.4.0 can be stripped by npm install; SLASH-REGRESSION it 7.1 detects this (T-07-T11 mitigation) |
| user input → dispatcher (slash sniff) | User types slash-then-spaces (no valid command); dispatcher.resolve returns null and input falls through to regular sendMessage (A7 + D-15 case 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-T9 | Spoofing | 5-line sniff always returns false | mitigate | textareaRef.current?.selectionStart === 0 — verifies the textarea ref is bound; the 3 D-15 test cases catch ref-binding regressions |
| T-07-T10 | Tampering | handleSend sniff bypassed (e.g., via Tab path) | accept | handleSlashSelect (popup path) routes through dispatcher.resolve + dispatch correctly (Phase 6 verified); the sniff is a parallel path for popup-bypassed input |
| T-07-T11 | Tampering | npm install strips 6-hunk patch-package | mitigate | SLASH-REGRESSION it 7.1 directly asserts v3 stream reasoning roundtrip (blocks[0].type === reasoning, blocks[0].signature === sig-plan-abc); if patch breaks, test fails |
| T-07-T12 | Information Disclosure | slash-plan LLM still invokes write_file/edit_file/bash | mitigate | Plan 07-01 runtime.ts isPlanMode gate strips bash/delete_file from builtInTools and sets interruptOn: false; SLASH-REGRESSION it 7.2 asserts the gate fires |
| T-07-T13 | Tampering | runLLMChat think-tag emission removed | mitigate | SLASH-REGRESSION it 7.3b asserts the first sender.send call is message_chunk with text think-tag; if the lines 376-378 emit is removed, the test fails |
| T-07-T14 | Denial of Service | TestHarness missing providers causes infinite re-render | accept | Tests wrap ChatArea in the same providers as SlashCommandPopup.test.tsx (read the existing wrapper to confirm) |
| T-07-T15 | Information Disclosure | textarea value contains sensitive text passed to dispatcher | accept | Dispatcher is renderer-side; only sonner toast (no network) and useSessionStore.sessionGoals (in-memory) consume the text; v1.2 will add PII scrubbing |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck audit (07-RESEARCH.md section 3) — no new deps in Plan 07-02 (it modifies existing test files + 1 new test file + 1 src file); all packages previously audited and [OK] |

## Hard Do Not Touch Compliance

- runtime.ts: Plan 07-01 modified lines 43-46 (type alias) + 488 (builtInTools) + 599 (interruptOn); Plan 07-02 does NOT modify runtime.ts at all (only adds tests against the existing isPlanMode behavior)
- llm.ts: Plan 07-01 modified lines 32-44 (ChatPayload interface); Plan 07-02 does NOT modify llm.ts at all (only adds tests)
- workflow-runtime.ts: NOT touched in either plan
- LLMStreamEvent union: NOT touched in either plan
- 6-hunk patch-package on @langchain/anthropic@1.4.0: NOT touched; SLASH-REGRESSION it 7.1 is the load-bearing test
- node_modules: NOT modified by either plan
</threat_model>

<verification>
After all 3 tasks complete:

1. **Targeted test runs** (each task verifies its own changes):
   ```
   npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
   npx vitest run src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx
   npx vitest run src/main/deepagent/llm-adapter.test.ts
   npx vitest run src/main/deepagent/runtime.test.ts
   npx vitest run src/main/llm.test.ts
   ```

2. **Full suite green** (no Phase 6 + 7-01 regressions):
   ```
   npm test  # expect ~155 tests passing: 139 Phase 6 + 10 Phase 7-01 + 3 handleSend + 3 SLASH-REGRESSION
   ```

3. **TypeScript clean**:
   ```
   npx tsc --noEmit -p .  # or project-defined typecheck command
   ```

4. **Hard Do Not Touch boundaries**:
   ```
   git diff --stat src/main/llm.ts  # expect changes ONLY from Plan 07-01 (no additional changes)
   git diff --stat src/main/deepagent/runtime.ts  # expect changes ONLY from Plan 07-01
   git diff src/main/workflow-runtime.ts  # expect 0 changes
   git status patches/  # expect clean
   ```

5. **Manual smoke** (electron dev build):
   - `npm run dev` → start app → open a project → create a session
   - Type `/goal write tests` in textarea at the very start (caret at position 0) → press Enter → verify the goal is set (no LLM call)
   - Type `/foo bar /baz` with caret at position 9 → press Enter → verify the message goes to LLM as plain text
   - Type `/  foo` with caret at position 0 → press Enter → verify it goes to LLM (falls through)
   - Type `/plan write tests` with caret at position 0 → press Enter → verify plan toast appears AND first LLM message_chunk contains think-tag
</verification>

<success_criteria>
- [ ] textareaRef = useRef<HTMLTextAreaElement>(null) added to ChatArea
- [ ] <textarea ref={textareaRef} ... /> JSX attribute added (Pitfall P7-4 mitigation)
- [ ] 5-line slash sniff inserted at top of handleSend (D-14)
- [ ] 3 new test cases in ChatArea.handleSend.test.tsx pass (D-15)
- [ ] SLASH-REGRESSION it 7.1 in llm-adapter.test.ts asserts first message_chunk contains think-tag
- [ ] SLASH-REGRESSION it 7.2 in runtime.test.ts asserts plan mode suppresses write_file/edit_file/bash tools
- [ ] SLASH-REGRESSION it 7.3a in llm.test.ts asserts planOnly flows to createDeepAgentRuntime
- [ ] SLASH-REGRESSION it 7.3b in llm.test.ts asserts slash /plan roundtrip emits first message_chunk with think-tag
- [ ] All 3 task verify commands exit 0
- [ ] npm test full suite green (155+ tests)
- [ ] npx tsc --noEmit exits 0
- [ ] Hard Do Not Touch boundaries respected (no additional modifications to runtime.ts/llm.ts/workflow-runtime.ts/LLMStreamEvent union/6-hunk patch-package)
- [ ] Threat model mitigations T-07-T9 (textareaRef binding), T-07-T11 (patch-package detection), T-07-T12 (plan mode tool suppression), T-07-T13 (think-tag emission) all in place via tests
</success_criteria>

<output>
Create `/Users/suntc/project/CDF/.planning/phases/07-system-commands-m3-regression-test/07-02-SUMMARY.md` when done.
</output>
