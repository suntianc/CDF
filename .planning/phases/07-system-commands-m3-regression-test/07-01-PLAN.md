---
phase: 07-system-commands-m3-regression-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/stores/sessionStore.ts
  - src/renderer/src/stores/sessionStore.test.ts
  - src/renderer/src/lib/commands/dispatcher.ts
  - src/renderer/src/lib/commands/dispatcher.test.ts
  - src/main/deepagent/context-aggregator.ts
  - src/main/context-aggregator.test.ts
  - src/main/ipc-handlers.ts
  - src/main/llm.ts
  - src/main/deepagent/runtime.ts
  - src/preload/index.ts
  - src/shared/types.ts
autonomous: true
requirements:
  - SLASH-05
  - SLASH-06
  - SLASH-07
user_setup: []

must_haves:
  truths:
    - User runs `/goal X` and the in-memory `useSessionStore.sessionGoals` Map immediately holds `{ [activeSessionId]: 'X' }` (D-01..D-05: placeholder text `[system] 正在执行 /goal…` per D-01; write happens immediately per D-03, in-memory only per D-05, no async, no LLM call; sessionGoals persists across session switches per D-04)
    - User runs `/context` and a static bubble shows the current session's breakdown: conversation / skills / mcp / workflow tokens + total (D-06..D-09: `/context` and `/context [all]` return current session scope per D-06; token estimation is `String.length * 0.25` per C-08; no LLM call)
    - User runs `/plan X` and the dispatcher invokes `sendMessage` with `{ planOnly: true }` (D-10..D-13: `[plan]` placeholder with description preview per D-10/D-11; planOnly runtime flag per D-12; runtime strips `bash` / `delete_file` from `builtInTools` and sets `interruptOn` to suppress `write_file` / `edit_file` in plan mode per D-13)
    - The 3 integration gap fixes (ChatPayload.overrides → ChatRuntimeOverrides, RuntimeModelOverrides → ChatRuntimeOverrides, runtime.ts planOnly consumption per Gap 1+2+3) are wired so that `planOnly: true` flows end-to-end from dispatcher to deepagent without field loss
    - All Phase 6 tests (139) and the 3 new dispatcher real-impl tests + 3 sessionGoals tests + 4-5 context-aggregator tests stay green
  artifacts:
    - path: src/renderer/src/stores/sessionStore.ts
      provides: "sessionGoals Map field + setSessionGoal action (D-02/D-04)"
      contains: "sessionGoals"
      exports: ["useSessionStore", "setSessionGoal"]
    - path: src/renderer/src/stores/sessionStore.test.ts
      provides: "3 sessionGoals tests: write / overwrite / cross-session persistence"
      min_lines: 80
    - path: src/renderer/src/lib/commands/dispatcher.ts
      provides: "SystemSilent (setSessionGoal + toast) + SystemLocal (IPC + breakdown toast) + PlanMode (sendMessage + planOnly) real implementations"
      contains: "setSessionGoal"
    - path: src/renderer/src/lib/commands/dispatcher.test.ts
      provides: "3 new real-impl tests: SystemSilent → store write + toast; SystemLocal → IPC call + toast; PlanMode → sendMessage with overrides.planOnly"
      min_lines: 240
    - path: src/main/deepagent/context-aggregator.ts
      provides: "aggregateCurrentSessionContext(sessionId) → { breakdown, total } from messages + skills + mcp + workflows (D-07/D-09)"
      exports: ["aggregateCurrentSessionContext"]
    - path: src/main/context-aggregator.test.ts
      provides: "4-5 unit tests for token breakdown: empty session, conversation-only, skills, mcp, workflows, total sum"
      min_lines: 100
    - path: src/main/ipc-handlers.ts
      provides: "context:currentSession handler that wraps aggregateCurrentSessionContext (D-08)"
      contains: "context:currentSession"
    - path: src/main/llm.ts
      provides: "ChatPayload.overrides → ChatRuntimeOverrides import (Gap 1 fix)"
      contains: "ChatRuntimeOverrides"
    - path: src/main/deepagent/runtime.ts
      provides: "RuntimeModelOverrides → ChatRuntimeOverrides import + isPlanMode conditional builtInTools + interruptOn (Gap 2 + Gap 3 fix, D-13)"
      contains: "isPlanMode"
    - path: src/preload/index.ts
      provides: "electronAPI.context.currentSession bridge (D-08)"
      contains: "context: { currentSession"
    - path: src/shared/types.ts
      provides: "ElectronAPI.context namespace + ContextBreakdown type (D-08)"
      contains: "currentSession: (sessionId: string)"
  key_links:
    - from: src/renderer/src/lib/commands/dispatcher.ts
      to: src/renderer/src/stores/sessionStore.ts
      via: "useSessionStore.getState().setSessionGoal(activeSessionId, args)"
      pattern: "setSessionGoal"
    - from: src/renderer/src/lib/commands/dispatcher.ts
      to: src/preload/index.ts
      via: "window.electronAPI.context.currentSession(activeSessionId)"
      pattern: "electronAPI\\.context\\.currentSession"
    - from: src/main/ipc-handlers.ts
      to: src/main/deepagent/context-aggregator.ts
      via: "ipcMain.handle('context:currentSession', ...) → aggregateCurrentSessionContext"
      pattern: "aggregateCurrentSessionContext"
    - from: src/main/llm.ts
      to: src/shared/types.ts
      via: "ChatPayload.overrides: ChatRuntimeOverrides (Gap 1 fix)"
      pattern: "ChatRuntimeOverrides"
    - from: src/main/deepagent/runtime.ts
      to: src/shared/types.ts
      via: "RuntimeModelOverrides = ChatRuntimeOverrides (Gap 2 fix)"
      pattern: "ChatRuntimeOverrides"
    - from: src/main/deepagent/runtime.ts
      to: src/main/deepagent/runtime.ts
      via: "isPlanMode conditional builtInTools + interruptOn (Gap 3 fix, D-13)"
      pattern: "isPlanMode"
---

<objective>
Wire the real implementations for the 3 system command branches (`/goal`, `/context`, `/plan`) and close the 3 integration gaps that prevent `payload.overrides.planOnly` from flowing end-to-end. Phase 6 left dispatcher branches as `console.log` placeholders and Phase 6 typed `ChatPayload.overrides` / `RuntimeModelOverrides` without `planOnly`. This plan:
- Adds `useSessionStore.sessionGoals: Map<sessionId, string>` + `setSessionGoal` action (SLASH-05)
- Adds `aggregateCurrentSessionContext` main-side function + `context:currentSession` IPC + preload bridge + shared type (SLASH-06)
- Fixes `ChatPayload.overrides` and `RuntimeModelOverrides` to import `ChatRuntimeOverrides` (Gaps 1+2)
- Wires `isPlanMode` to strip `bash` / `delete_file` from `builtInTools` and toggle `interruptOn` in `createDeepAgent` (Gap 3, D-13)
- Replaces 3 dispatcher placeholder branches with real implementations (setSessionGoal + toast / IPC + breakdown toast / sendMessage with `{ planOnly: true }`)

Purpose: After this plan, `/goal` writes a per-session goal in memory, `/context` aggregates a static token breakdown, and `/plan` actually suppresses write tools at the deepagent runtime. Plan 07-02 then adds the 5-line sniff that wires the dispatcher to `handleSend` and the SLASH-REGRESSION 3 it blocks that protect the 6-hunk patch-package.

Output: 11 files modified/created (4 new, 7 modified) + ~10 new tests added on top of the 139-test Phase 6 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-system-commands-m3-regression-test/07-CONTEXT.md
@.planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md
@.planning/phases/07-system-commands-m3-regression-test/07-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-02-SUMMARY.md
@.planning/research/PITFALLS.md

# Source files the executor must read before editing
@src/renderer/src/stores/sessionStore.ts (860 lines — focus on lines 45-68 SessionState interface, 82 streamingSessionsCache, 84-96 init, 143-182 selectSession, 338-422 sendMessage)
@src/renderer/src/stores/sessionStore.test.ts (116 lines — pattern for store tests; extend at end)
@src/renderer/src/lib/commands/dispatcher.ts (115 lines — full file: resolve + dispatch)
@src/renderer/src/lib/commands/dispatcher.test.ts (177 lines — 14 existing tests; extend at end)
@src/main/deepagent/skill-manager.ts (focus on `listPhysicalSkills(projectPath)` at lines 80-104)
@src/main/deepagent/mcp-connector.ts (focus on `loadMcpTools(agentId, mcpServers)` at line 129)
@src/main/deepagent/runtime.ts (613 lines — focus on lines 43-46 RuntimeModelOverrides, 55-59 DEFAULT_INTERRUPT_ON, 453-488 createDeepAgentRuntime signature + builtInTools, 590-602 createDeepAgent call with interruptOn)
@src/main/llm.ts (825 lines — focus on lines 32-44 ChatPayload.overrides local type, 306-325 runLLMChat signature with payload.overrides passthrough)
@src/main/ipc-handlers.ts (813 lines — focus on lines 13-26 imports, 784-813 commands:list + commands:readProjectCommands pattern)
@src/preload/index.ts (140 lines — focus on lines 96-118 commands bridge pattern, 119-129 llm bridge pattern)
@src/shared/types.ts (494 lines — focus on lines 189-195 ChatRuntimeOverrides, 457-468 ElectronAPI.llm, 482-491 ElectronAPI.commands pattern)
@src/main/database.ts (focus on lines 63-72 messages table schema with content TEXT, 401-413 workflows table schema with graph_data TEXT)
</context>

<interfaces>
From src/shared/types.ts (used by Gap 1+2 fix):
```typescript
export interface ChatRuntimeOverrides {
  providerId?: string;
  model?: string;
  planOnly?: boolean;  // D-01 PlanMode extension point
}
```

From src/renderer/src/stores/sessionStore.ts (extension point):
- `SessionState` interface at lines 45-68 — add `sessionGoals: Map<string, string>` field and `setSessionGoal: (sessionId: string, goal: string) => void` action
- `useSessionStore = create<SessionState>(...)` at line 84 — add `sessionGoals: new Map()` initial value and `setSessionGoal` implementation in the create callback
- `selectSession` at line 143-182 — DO NOT clear `sessionGoals` (D-04: cross-session persistence)
- `sendMessage` at line 338 — accepts `overrides?: ChatRuntimeOverrides` (3rd param); dispatcher calls it with `{ planOnly: true }`

From src/renderer/src/lib/commands/dispatcher.ts (branch upgrade target):
- `case 'SystemSilent':` at line 89-92 (placeholder) — REPLACE with setSessionGoal + toast
- `case 'SystemLocal':` at line 94-98 (placeholder) — REPLACE with electronAPI.context.currentSession + breakdown toast
- `case 'PlanMode':` at line 100-105 — KEEP `await sendMessage(projectId, plan.args, { planOnly: true })`; ADD `[plan]` toast before it (D-10/D-11)

From src/main/deepagent/runtime.ts (Gap 2+3 fix):
- `interface RuntimeModelOverrides` at lines 43-46 — REPLACE with `type RuntimeModelOverrides = ChatRuntimeOverrides` import
- `DEFAULT_INTERRUPT_ON` at lines 55-59 — keep as default branch
- `builtInTools: any[] = [createDeleteFileTool(...), createBashTool(...), createFetchTool()]` at line 488 — wrap in `isPlanMode` check
- `createDeepAgent({...interruptOn: DEFAULT_INTERRUPT_ON...})` at line 599 — pass `isPlanMode ? false : DEFAULT_INTERRUPT_ON`

From src/main/llm.ts (Gap 1 fix):
- `export interface ChatPayload` at lines 32-44 — DELETE inline `overrides?: { providerId?; model?; }`; use `overrides?: ChatRuntimeOverrides` (import from `../shared/types`)

From src/main/ipc-handlers.ts (IPC insertion point):
- Add `ipcMain.handle('context:currentSession', ...)` after `commands:readProjectCommands` (line 812) — wraps `aggregateCurrentSessionContext(sessionId)` and returns `{ breakdown, total }`

From src/preload/index.ts (preload bridge insertion point):
- Add `context: { currentSession: (sessionId: string) => ipcRenderer.invoke('context:currentSession', sessionId) }` after the `commands` block (line 117)

From src/shared/types.ts (type insertion point):
- Add `context: { currentSession: (sessionId: string) => Promise<{ breakdown: { conversation: number; skills: number; mcp: number; workflows: number }; total: number }>; }` to `ElectronAPI` interface after the `commands` block (line 491)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: sessionStore — add sessionGoals Map field + setSessionGoal action + 3 tests</name>
  <files>src/renderer/src/stores/sessionStore.ts, src/renderer/src/stores/sessionStore.test.ts</files>
  <read_first>
    - src/renderer/src/stores/sessionStore.ts:45-96 (SessionState interface + useSessionStore.create init)
    - src/renderer/src/stores/sessionStore.ts:82 (streamingSessionsCache — separate from sessionGoals, do NOT conflate)
    - src/renderer/src/stores/sessionStore.ts:143-182 (selectSession — must NOT clear sessionGoals per D-04)
    - src/renderer/src/stores/sessionStore.test.ts:1-116 (test pattern — useSessionStore.setState + electronAPI mock)
    - .planning/phases/07-system-commands-m3-regression-test/07-CONTEXT.md:33-40 (D-01..D-05 /goal decisions)
  </read_first>
  <action>
    Per D-02/D-04/D-05, add an in-memory `sessionGoals: Map<string, string>` field to `SessionState` and a `setSessionGoal(sessionId, goal)` action.

    1. In `src/renderer/src/stores/sessionStore.ts`, extend the `SessionState` interface (currently lines 45-68) with two new members: `sessionGoals: Map<string, string>` and `setSessionGoal: (sessionId: string, goal: string) => void`.

    2. In the `useSessionStore.create` init object (currently lines 84-96), set the initial value to `sessionGoals: new Map()` and implement the setter using a `set((state) => ...)` callback that builds a **new** `Map` from `state.sessionGoals` (Zustand shallow-compare requirement — mutating the existing Map will not trigger re-render), then sets the new entry and returns `{ sessionGoals: next }`. The setter signature is `(sessionId: string, goal: string) => void`; do NOT make it async.

    3. Verify `selectSession` (lines 143-182) does NOT clear `sessionGoals`. D-04 says the Map persists across session switches. The current `selectSession` only manipulates `messages / agentRuns / agentToolCalls / delegatedTasks / todos / activeRunId / pendingApproval / streamingMessageId / isStreaming / error` — confirm it does not touch `sessionGoals`. If it does, remove the sessionGoals reset.

    4. Do NOT add `sessionGoals` to `streamingSessionsCache` (line 82) — the cache is for per-session streaming state, not user metadata.

    5. In `src/renderer/src/stores/sessionStore.test.ts`, append 3 tests inside the existing `describe('sessionStore sendMessage', ...)` block (or a new sibling describe block). The tests must:
       - Test A: `setSessionGoal('session-1', 'write tests')` → `useSessionStore.getState().sessionGoals.get('session-1') === 'write tests'`
       - Test B: call `setSessionGoal('session-1', 'a')` then `setSessionGoal('session-1', 'b')` → final value is `'b'` (overwrite)
       - Test C: call `setSessionGoal('s1', 'a')` + `setSessionGoal('s2', 'b')` + invoke `useSessionStore.getState().selectSession('s1')` (mocking electronAPI.db.getMessages + electronAPI.db.getAgentRuns/getAgentToolCalls) → both entries still in Map (D-04 cross-session persistence)

    6. Use the existing test pattern at sessionStore.test.ts:8-26 (the `useSessionStore.setState({...})` reset) and the electronAPI mock at lines 33-74. For Test C, you may need to extend the mock with `db.getAgentRuns: vi.fn().mockResolvedValue([])` and `db.getAgentToolCalls: vi.fn().mockResolvedValue([])` if not already present (to satisfy the `typeof === 'function'` guard in `fetchAgentActivity` at sessionStore.ts:186-189).

    7. Do NOT add TypeScript assertions; rely on existing `vi.fn()` mock infrastructure. Run the test via `npx vitest run src/renderer/src/stores/sessionStore.test.ts` and verify all 4 tests (1 existing + 3 new) pass.
  </action>
  <acceptance_criteria>
    - `grep -n "sessionGoals" src/renderer/src/stores/sessionStore.ts` shows ≥ 4 matches: interface field, init value, setter body, and any cross-reference (TypeScript type / export)
    - `grep -n "setSessionGoal" src/renderer/src/stores/sessionStore.ts` shows ≥ 2 matches: interface member + create init
    - The setter body uses `new Map(state.sessionGoals)` (NOT direct mutation) — `grep -n "new Map(state.sessionGoals)" src/renderer/src/stores/sessionStore.ts` returns 1 match
    - `npx vitest run src/renderer/src/stores/sessionStore.test.ts` passes with 4 tests (1 original + 3 new) — verify the output line `Tests  4 passed (4)` is present
    - `selectSession` (line 143-182) does NOT touch `sessionGoals` — `grep -A 30 "selectSession: async" src/renderer/src/stores/sessionStore.ts | grep -c sessionGoals` returns 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/renderer/src/stores/sessionStore.test.ts</automated>
  </verify>
  <done>
    - `sessionGoals: Map<string, string>` field declared in `SessionState` and initialized to `new Map()`
    - `setSessionGoal(sessionId, goal)` action writes to a NEW Map (immutability); runs synchronously with no async dependencies
    - 3 new tests pass: write / overwrite / cross-session persistence (D-04)
    - All 4 tests in `sessionStore.test.ts` pass (1 original + 3 new)
  </done>
</task>

<task type="auto">
  <name>Task 2: context-aggregator (NEW) + context:currentSession IPC + preload + shared types + 4-5 tests</name>
  <files>src/main/deepagent/context-aggregator.ts, src/main/context-aggregator.test.ts, src/main/ipc-handlers.ts, src/preload/index.ts, src/shared/types.ts</files>
  <read_first>
    - src/main/deepagent/skill-manager.ts:80-104 (listPhysicalSkills signature returning PhysicalSkillView[])
    - src/main/deepagent/mcp-connector.ts:129-155 (loadMcpTools signature returning { client, tools: StructuredToolInterface[] })
    - src/main/database.ts:63-72 (messages table: id, session_id, role, content, created_at, tokens)
    - src/main/database.ts:401-413 (workflows table: id, project_id, name, description, graph_data, status, ...)
    - src/main/ipc-handlers.ts:13-26 (existing imports: listPhysicalSkills, fs, path, os, MCPServer)
    - src/main/ipc-handlers.ts:784-813 (commands:list + commands:readProjectCommands handlers as IPC pattern template)
    - src/preload/index.ts:96-118 (commands bridge pattern — context bridge follows same shape)
    - src/shared/types.ts:189-195 (ChatRuntimeOverrides — for type import)
    - src/shared/types.ts:482-491 (ElectronAPI.commands block — context block follows same shape)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:355-435 (research §5.1 IPC handler design + token math)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:1061-1063 (Pitfall P7-6: per-source try-catch, not single catch-all)
  </read_first>
  <action>
    Per D-06/D-07/D-08/D-09, create the `aggregateCurrentSessionContext` function in main process, wrap it in an IPC channel, expose it on the preload bridge, and declare its type on the shared `ElectronAPI` interface. The breakdown is `{ conversation, skills, mcp, workflows }` with each value being `Math.ceil(chars * 0.25)` (D-09 heuristic) and `total = sum(breakdown)`.

    1. **Create `src/main/deepagent/context-aggregator.ts`** (NEW file). Export a single function:
       ```typescript
       export interface ContextBreakdown { conversation: number; skills: number; mcp: number; workflows: number; }
       export async function aggregateCurrentSessionContext(sessionId: string): Promise<{ breakdown: ContextBreakdown; total: number }>
       ```
       The function must:
       - **Conversation tokens**: `db.prepare('SELECT COALESCE(SUM(LENGTH(content)), 0) AS total FROM messages WHERE session_id = ?').get(sessionId)` → `Math.ceil(chars * 0.25)`
       - **Skills tokens**: query project via `db.prepare('SELECT p.path FROM projects p JOIN sessions s ON s.project_id = p.id WHERE s.id = ?').get(sessionId)`; if found, call `listPhysicalSkills(project.path)` and for each skill read its `SKILL.md` file size via `fs.statSync(path.join(scope-path, 'SKILL.md')).size` (handle ENOENT with `try { return fs.statSync(p).size } catch { return 0 }`); sum chars; `Math.ceil(chars * 0.25)`
       - **MCP tools tokens**: query agent via `db.prepare('SELECT a.id FROM agents a JOIN sessions s ON s.agent_id = a.id WHERE s.id = ?').get(sessionId)`; if found, query `mcp_servers WHERE is_connected = 1`, call `loadMcpTools(agent.id, mcpServers)`, sum `JSON.stringify(tool.inputSchema || tool.schema || {}).length` for each tool; `Math.ceil(chars * 0.25)`
       - **Workflows tokens**: `db.prepare("SELECT COALESCE(SUM(LENGTH(graph_data)), 0) AS total FROM workflows WHERE status = 'active' AND project_id = (SELECT project_id FROM sessions WHERE id = ?)").get(sessionId)`; `Math.ceil(chars * 0.25)`
       - **Total**: `conversation + skills + mcp + workflows`
       - **Error handling** (PITFALLS P7-6): wrap each of the 4 sources in its own try-catch; on failure set that single source to 0 and log warning. Do NOT use a single catch-all that zeros out everything. If `sessionId` is unknown, return `{ breakdown: { 0, 0, 0, 0 }, total: 0 }`.
       - **sessionId validation** (ASVS V5): if `typeof sessionId !== 'string' || sessionId.length > 64 || sessionId.length === 0`, return zero breakdown immediately.

    2. **Create `src/main/context-aggregator.test.ts`** (NEW file, 4-5 tests). Mock `better-sqlite3` (use a `vi.mock('./database', () => ({ default: { prepare: vi.fn() }}))` pattern), mock `skill-manager` (`listPhysicalSkills`), mock `mcp-connector` (`loadMcpTools`). Tests must:
       - Test 1: empty session (no messages, no skills, no mcp, no workflows) → all 0
       - Test 2: 2 messages totaling 800 chars in `messages` table → `breakdown.conversation === 200` (Math.ceil(800 * 0.25))
       - Test 3: 1 SKILL.md file of 400 bytes via mocked `listPhysicalSkills` → `breakdown.skills === 100`
       - Test 4: 1 MCP tool with inputSchema of 200 chars → `breakdown.mcp === 50`
       - Test 5: 1 active workflow with `graph_data` of 1000 chars → `breakdown.workflows === 250`; verify `total === conversation + skills + mcp + workflows`
       - Bonus Test 6: invalid sessionId (empty string OR > 64 chars) → zero breakdown + total 0; verify the function does not throw

    3. **Add IPC handler in `src/main/ipc-handlers.ts`** (after the `commands:readProjectCommands` handler at line 812). Add import `import { aggregateCurrentSessionContext } from './deepagent/context-aggregator';` at the top with the other `deepagent/*` imports (lines 15-20). Then:
       ```typescript
       ipcMain.handle('context:currentSession', async (_evt, sessionId: string) => {
         try {
           return await aggregateCurrentSessionContext(sessionId);
         } catch (err) {
           console.error('[context:currentSession] failed:', err);
           return { breakdown: { conversation: 0, skills: 0, mcp: 0, workflows: 0 }, total: 0 };
         }
       });
       ```

    4. **Add preload bridge in `src/preload/index.ts`** (after the `commands` block at line 117, before the `platform: process.platform` line at line 119). Add:
       ```typescript
       context: {
         currentSession: (sessionId: string) =>
           ipcRenderer.invoke('context:currentSession', sessionId),
       },
       ```

    5. **Add shared type in `src/shared/types.ts`** (after the `commands` block at line 491 in the `ElectronAPI` interface). Add:
       ```typescript
       context: {
         currentSession: (sessionId: string) => Promise<{
           breakdown: { conversation: number; skills: number; mcp: number; workflows: number };
           total: number;
         }>;
       };
       ```

    6. Verify TypeScript compiles cleanly: `npx tsc --noEmit -p .` (or the project's standard type-check command) — must pass with 0 errors.
  </action>
  <acceptance_criteria>
    - `grep -n "export async function aggregateCurrentSessionContext" src/main/deepagent/context-aggregator.ts` returns 1 match
    - `grep -n "context:currentSession" src/main/ipc-handlers.ts` returns 1 match (the handler line)
    - `grep -n "context: {" src/preload/index.ts` returns 1 match (the new bridge block)
    - `grep -n "currentSession: (sessionId: string)" src/shared/types.ts` returns 1 match (the type signature)
    - `npx vitest run src/main/context-aggregator.test.ts` passes with 4-6 tests — verify the output line `Tests  N passed (N)` where N ≥ 4
    - `npx tsc --noEmit -p .` exits 0 (or whatever the project's typecheck command is — confirm in package.json scripts)
    - The context-aggregator function uses 4 separate try-catch blocks (one per source), not a single catch-all — `grep -cE "try \{|catch \(" src/main/deepagent/context-aggregator.ts` returns ≥ 8 (4 try + 4 catch)
    - sessionId validation: `if (typeof sessionId !== 'string' || sessionId.length === 0 || sessionId.length > 64)` is the first check at the top of the function
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/main/context-aggregator.test.ts</automated>
  </verify>
  <done>
    - `src/main/deepagent/context-aggregator.ts` exists with `aggregateCurrentSessionContext(sessionId)` returning `{ breakdown, total }` from 4 sources
    - `src/main/context-aggregator.test.ts` exists with 4-6 unit tests (empty / conversation / skills / mcp / workflows / total / invalid-input) — all pass
    - `ipcMain.handle('context:currentSession', ...)` is registered in `src/main/ipc-handlers.ts`
    - `electronAPI.context.currentSession` is exposed in `src/preload/index.ts`
    - `ElectronAPI.context` namespace is typed in `src/shared/types.ts`
    - TypeScript compiles cleanly with no errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration gap fixes — ChatPayload.overrides + RuntimeModelOverrides + runtime.ts planOnly consumption</name>
  <files>src/main/llm.ts, src/main/deepagent/runtime.ts</files>
  <read_first>
    - src/main/llm.ts:1-44 (imports block + ChatPayload interface — local overrides type is at lines 40-43)
    - src/main/llm.ts:306-325 (runLLMChat signature: payload.overrides passed as 5th arg to createDeepAgentRuntime)
    - src/main/deepagent/runtime.ts:1-60 (imports + RuntimeModelOverrides + DEFAULT_INTERRUPT_ON)
    - src/main/deepagent/runtime.ts:453-602 (createDeepAgentRuntime body: builtInTools at line 488, createDeepAgent call at lines 590-602 with interruptOn: DEFAULT_INTERRUPT_ON at line 599)
    - src/shared/types.ts:189-195 (ChatRuntimeOverrides — contains planOnly?: boolean)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:130-180 (Gap 1 + Gap 2 + Gap 3 analysis with fix designs)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:1063-1074 (Pitfall P7-1 + P7-2: type chain must be fixed before runtime layer)
  </read_first>
  <action>
    Per Gap 1 + Gap 2 + Gap 3 from RESEARCH §1, close the type-chain breaks so that `payload.overrides.planOnly = true` flows end-to-end from `useSessionStore.sendMessage` to `createDeepAgent`'s `interruptOn` and `tools` arrays.

    **Gap 1: `ChatPayload.overrides` → `ChatRuntimeOverrides`**
    1. In `src/main/llm.ts`, add `import type { ChatRuntimeOverrides } from '../shared/types';` to the imports block (lines 7-11). The `AgentApprovalResolution` / `AgentRunStatus` / `AgentToolCallStatus` imports remain.
    2. In `src/main/llm.ts` lines 32-44, REPLACE the local `overrides?: { providerId?: string; model?: string; }` field in the `ChatPayload` interface with `overrides?: ChatRuntimeOverrides;`. The `ChatRuntimeOverrides` type from `shared/types.ts:189-195` already contains `planOnly?: boolean`.

    **Gap 2: `RuntimeModelOverrides` → `ChatRuntimeOverrides`**
    3. In `src/main/deepagent/runtime.ts`, add `import type { ChatRuntimeOverrides } from '../../shared/types';` to the imports block (lines 1-25). Adjust the relative path to match the file's location (3 levels up: `../../shared/types`).
    4. In `src/main/deepagent/runtime.ts` lines 43-46, REPLACE the local `interface RuntimeModelOverrides { providerId?: string; model?: string; }` with `type RuntimeModelOverrides = ChatRuntimeOverrides;`. This is a type alias, not an interface extension, so all 4 call sites (none reference planOnly yet, but the type is now available) work unchanged.

    **Gap 3: `runtime.ts` planOnly consumption (D-13)**
    5. In `src/main/deepagent/runtime.ts`, immediately after line 460 (after the `overrides?: RuntimeModelOverrides` parameter, before the function body) or as the first line of the function body, compute `const isPlanMode = Boolean(overrides?.planOnly);`. The `Boolean()` cast is defensive (handles `undefined` gracefully).

    6. **Tools array modification** (D-13: plan mode strips `bash` + `delete_file`): At line 488, the current line is:
       ```typescript
       const builtInTools: any[] = [createDeleteFileTool(project.path), createBashTool({ workingDir: project.path }), createFetchTool()];
       ```
       REPLACE with:
       ```typescript
       const builtInTools: any[] = [createFetchTool()];
       if (!isPlanMode) {
         builtInTools.push(createDeleteFileTool(project.path));
         builtInTools.push(createBashTool({ workingDir: project.path }));
       }
       ```
       In plan mode, `builtInTools` contains only `createFetchTool()` — LLM cannot see or invoke `bash` / `delete_file`.

    7. **interruptOn modification** (D-13: plan mode rejects `write_file` / `edit_file`): At line 599, the current call is:
       ```typescript
       const deepAgent = createDeepAgent({ ..., interruptOn: DEFAULT_INTERRUPT_ON, ... });
       ```
       REPLACE `interruptOn: DEFAULT_INTERRUPT_ON` with:
       ```typescript
       interruptOn: isPlanMode ? false : DEFAULT_INTERRUPT_ON,
       ```
       The deepagents library accepts `false` (= no interrupt at all — equivalent to "plan mode is silent, no approval flow"). Combined with the stripped tools, the LLM can neither see `bash` / `delete_file` (removed from tools) nor trigger approval for `write_file` / `edit_file` (interruptOn = false means no interrupts, so any tool invocation is a no-op or direct failure).

    8. **Verify no regression**: run `npx vitest run src/main/deepagent/runtime.test.ts` — all 13 existing tests must still pass. Plan 07-02 will add the SLASH-REGRESSION it 7.2 (no-tool-call-in-plan-mode) on top of this. The existing tests at runtime.test.ts:158-389 (the `createDeepAgentRuntime` describe block) may not exercise the new `isPlanMode` branch, so they should pass unchanged.

    9. **Verify llm.ts still works**: run `npx vitest run src/main/llm.test.ts` — the existing 18+ tests must still pass. The test at llm.test.ts:629-670 ("should pass runtime model overrides to deepagent runtime") already verifies `overrides` passthrough; that test now exercises the extended `ChatRuntimeOverrides` type. Plan 07-02 will add SLASH-REGRESSION it 7.3 + a new test verifying `planOnly: true` flows through.

    10. **Hard "Do Not Touch" boundaries respected**:
        - `llm.ts:306-425` (`runLLMChat` body) — NOT modified; only lines 32-44 (ChatPayload interface) are changed
        - `runtime.ts` — modified ONLY at lines 43-46 (type alias), one new line for `isPlanMode`, and the tools/interruptOn changes (lines 488 and 599)
        - `workflow-runtime.ts` — NOT touched
        - `LLMStreamEvent` union — NOT touched
        - 6-hunk patch-package on `@langchain/anthropic@1.4.0` — NOT touched (no `node_modules` modifications)
  </action>
  <acceptance_criteria>
    - `grep -n "ChatRuntimeOverrides" src/main/llm.ts` returns ≥ 1 match (the import) + the field declaration
    - `grep -n "ChatRuntimeOverrides" src/main/deepagent/runtime.ts` returns ≥ 1 match (the import) + the type alias
    - `grep -n "isPlanMode" src/main/deepagent/runtime.ts` returns ≥ 4 matches: declaration, tools gate, interruptOn gate, AND function-body usage
    - `grep -nE "providerId\?: string;\s*$" src/main/llm.ts` returns 0 matches (local overrides type removed)
    - `grep -nE "interface RuntimeModelOverrides" src/main/deepagent/runtime.ts` returns 0 matches (replaced with `type RuntimeModelOverrides = ChatRuntimeOverrides`)
    - `npx vitest run src/main/deepagent/runtime.test.ts` exits 0 with all existing tests passing
    - `npx vitest run src/main/llm.test.ts` exits 0 with all existing tests passing
    - `npx tsc --noEmit -p .` exits 0 (TypeScript clean)
    - `git diff --stat src/main/llm.ts` shows changes ONLY at lines 7-11 (imports) and 32-44 (ChatPayload)
    - `git diff --stat src/main/deepagent/runtime.ts` shows changes ONLY at lines 1-25 (imports), 43-46 (type alias), 486-602 (function body with isPlanMode + tools + interruptOn)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/main/deepagent/runtime.test.ts src/main/llm.test.ts</automated>
  </verify>
  <done>
    - `ChatPayload.overrides` typed as `ChatRuntimeOverrides` (Gap 1 closed) — `planOnly` field flows from renderer to llm.ts without type loss
    - `RuntimeModelOverrides` aliased to `ChatRuntimeOverrides` (Gap 2 closed) — `planOnly` field flows from llm.ts to runtime without type loss
    - `isPlanMode = Boolean(overrides?.planOnly)` computed in `createDeepAgentRuntime` (Gap 3 closed, D-13)
    - In plan mode: `bash` and `delete_file` removed from `builtInTools` (LLM cannot see them)
    - In plan mode: `interruptOn: false` (no approval flow; write_file/edit_file cannot trigger)
    - All existing 13+ runtime.test.ts tests and 18+ llm.test.ts tests still pass
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 4: dispatcher.ts — 3 real implementations (SystemSilent/SystemLocal/PlanMode) + 3 dispatcher.test extensions</name>
  <files>src/renderer/src/lib/commands/dispatcher.ts, src/renderer/src/lib/commands/dispatcher.test.ts</files>
  <read_first>
    - src/renderer/src/lib/commands/dispatcher.ts:79-115 (the `dispatch` function with 3 placeholder branches at lines 89-105)
    - src/renderer/src/lib/commands/dispatcher.test.ts:131-177 (existing dispatch tests for PluginRewrite + PlanMode + no-project)
    - .planning/phases/07-system-commands-m3-regression-test/07-CONTEXT.md:33-62 (D-01..D-13 system command decisions)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:295-330 (dispatcher SystemSilent design)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:475-510 (dispatcher SystemLocal design)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:614-642 (dispatcher PlanMode design)
    - .planning/phases/07-system-commands-m3-regression-test/07-RESEARCH.md:1063-1074 (Pitfalls P7-6 + P7-7: per-source try-catch / plan bubble coexistence)
  </read_first>
  <action>
    Per D-01..D-13, replace the 3 placeholder `console.log` branches in `dispatcher.ts` with real implementations using sonner toasts and the IPC bridge built in Task 2. PlanMode is already partially wired (line 104 calls `sendMessage` with `{ planOnly: true }`); just add the `[plan]` toast before it.

    1. **Add sonner import to `src/renderer/src/lib/commands/dispatcher.ts`**: at the top of the file (after line 6), add `import { toast } from 'sonner';`. sonner is already installed (Phase 6 verified — no new dep).

    2. **Replace `case 'SystemSilent':` (lines 89-92)** with:
       ```typescript
       case 'SystemSilent': {
         // D-02/D-03/D-04: write to sessionGoals Map (in-memory, persists across sessions).
         // D-01: placeholder bubble via sonner toast (Phase 8 polish can swap to MessageItem).
         const { activeSessionId, setSessionGoal } = useSessionStore.getState();
         if (!activeSessionId) {
           console.warn('[dispatcher] SystemSilent: no active session');
           return;
         }
         const goal = (plan.args || '').trim();  // C-02: trim head/tail whitespace
         setSessionGoal(activeSessionId, goal);
         toast.info('[system] 正在执行 /goal…', {
           description: `session=${activeSessionId.slice(0, 8)}  goal=${goal || '(无描述)'}`,
           duration: 2000,
         });
         return;
       }
       ```
       Note: the `setSessionGoal` was added in Task 1; the toast is non-blocking (C-04: disappears after 2s).

    3. **Replace `case 'SystemLocal':` (lines 94-98)** with:
       ```typescript
       case 'SystemLocal': {
         // D-06/D-07/D-08: pull token breakdown from main process IPC; render static bubble.
         const { activeSessionId } = useSessionStore.getState();
         if (!activeSessionId) {
           console.warn('[dispatcher] SystemLocal: no active session');
           return;
         }
         try {
           const result = await window.electronAPI.context.currentSession(activeSessionId);
           toast.info('[system] 上下文', {
             description:
               `对话: ${result.breakdown.conversation} tokens\n` +
               `Skills: ${result.breakdown.skills} tokens\n` +
               `MCP: ${result.breakdown.mcp} tokens\n` +
               `Workflows: ${result.breakdown.workflows} tokens\n` +
               `Total: ${result.total} tokens`,
             duration: 4000,
           });
         } catch (err: any) {
           console.error('[dispatcher] SystemLocal IPC failed:', err);
           toast.error('[system] 上下文拉取失败', { description: err?.message || '未知错误' });
         }
         return;
       }
       ```
       Note: the `electronAPI.context.currentSession` was added in Task 2; the description uses `\n` (sonner renders as multi-line). C-04: 4s duration.

    4. **Update `case 'PlanMode':` (lines 100-105)** to add the `[plan]` toast (D-10/D-11) BEFORE the existing `sendMessage` call:
       ```typescript
       case 'PlanMode': {
         // D-10/D-11: [plan] marker bubble (持续到首个 message_chunk 到达, C-04).
         const description = plan.args.trim() || '(无描述)';
         toast.info(`[plan] 进入 plan 模式：${description}`, {
           description: 'LLM 不会调用 write_file / edit_file / bash',
           duration: 3000,
         });
         // D-12: 透传 planOnly 给 sendMessage → llm.ts → runtime.ts (Gap 1+2+3 chain)
         await sendMessage(projectId, plan.args, { planOnly: true });
         return;
       }
       ```
       The `sendMessage(..., { planOnly: true })` call (already at line 104) is preserved — Task 3 wired the type chain to consume `planOnly` end-to-end.

    5. **Extend `src/renderer/src/lib/commands/dispatcher.test.ts`** with 3 new tests in the existing `describe('dispatcher.dispatch', ...)` block. The tests follow the existing mock pattern at lines 4-13 (mock `useSessionStore` and `useProjectStore` with `vi.mock`). Add to the top of the file (after the existing imports at line 1):
       ```typescript
       const mockSetSessionGoal = vi.fn();
       const mockElectronAPI = { context: { currentSession: vi.fn() } };
       // In the vi.mock for sessionStore, also expose setSessionGoal
       // In a beforeAll or beforeEach, set (globalThis as any).window.electronAPI = mockElectronAPI
       ```
       Then add 3 tests:
       - **Test A — SystemSilent calls setSessionGoal + toast**: mock `useSessionStore.getState()` to return `{ activeSessionId: 'session-1', setSessionGoal: mockSetSessionGoal, sendMessage: mockSendMessage }` + mock `useProjectStore.getState()` to return `{ currentProjectId: 'project-1' }`. Mock `mockSetSessionGoal.mockImplementation(() => {})`. Dispatch `{ kind: 'SystemSilent', command: goalCmd, args: 'write tests' }`. Assert `mockSetSessionGoal` was called with `('session-1', 'write tests')` AND `mockSendMessage` was NOT called. For toast assertion, use `vi.mock('sonner', () => ({ toast: { info: vi.fn() } }))` at the top of the file and assert `toast.info` was called with the `[system] 正在执行 /goal…` string.
       - **Test B — SystemLocal calls electronAPI.context.currentSession + toast**: mock `mockElectronAPI.context.currentSession.mockResolvedValue({ breakdown: { conversation: 100, skills: 50, mcp: 25, workflows: 75 }, total: 250 })`. Dispatch `{ kind: 'SystemLocal', command: contextCmd, args: '' }`. Assert `mockElectronAPI.context.currentSession` was called with `('session-1')` AND `toast.info` was called with `[system] 上下文` + a description containing `Total: 250 tokens`.
       - **Test C — PlanMode calls sendMessage with `{ planOnly: true }` + emits [plan] toast**: this is similar to the existing test at line 156-164, but additionally assert that `toast.info` was called with `[plan] 进入 plan 模式` string before `sendMessage` is called. Use `mockSendMessage.mockImplementation(async () => { /* verify toast fired first */ })`.

    6. **Sonner mock setup**: at the top of `dispatcher.test.ts`, add `vi.mock('sonner', () => ({ toast: { info: vi.fn(), error: vi.fn() } }));` and import `{ toast } from 'sonner'` to access the mock for assertions. The test file is in `src/renderer/src/lib/commands/` and uses vitest's `vi.mock` hoisting (the existing tests at lines 4-13 demonstrate the pattern).

    7. Run `npx vitest run src/renderer/src/lib/commands/dispatcher.test.ts` — verify 17 tests pass (14 existing + 3 new).
  </action>
  <acceptance_criteria>
    - `grep -n "setSessionGoal" src/renderer/src/lib/commands/dispatcher.ts` returns ≥ 1 match (the SystemSilent branch calls it)
    - `grep -n "electronAPI.context.currentSession" src/renderer/src/lib/commands/dispatcher.ts` returns ≥ 1 match (the SystemLocal branch)
    - `grep -n "\\[plan\\] 进入 plan 模式" src/renderer/src/lib/commands/dispatcher.ts` returns 1 match (the PlanMode toast)
    - `grep -n "console.log" src/renderer/src/lib/commands/dispatcher.ts` returns 0 matches (all placeholders replaced — the existing `console.warn` for no-active-project is fine, but `console.log` is gone)
    - `npx vitest run src/renderer/src/lib/commands/dispatcher.test.ts` exits 0 with ≥ 17 tests (14 existing + 3 new)
    - `npx vitest run src/renderer/src/stores/sessionStore.test.ts src/main/context-aggregator.test.ts src/main/deepagent/runtime.test.ts src/main/llm.test.ts` exits 0 (cross-check: no regressions in adjacent files)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/renderer/src/lib/commands/dispatcher.test.ts src/renderer/src/stores/sessionStore.test.ts src/main/context-aggregator.test.ts src/main/deepagent/runtime.test.ts src/main/llm.test.ts</automated>
  </verify>
  <done>
    - `case 'SystemSilent':` writes to `useSessionStore.sessionGoals` (in-memory, no LLM) and emits `[system] 正在执行 /goal…` toast (D-01..D-05)
    - `case 'SystemLocal':` calls `window.electronAPI.context.currentSession(activeSessionId)` and emits a 4-line breakdown toast (D-06..D-09)
    - `case 'PlanMode':` emits `[plan] 进入 plan 模式：X` toast and calls `sendMessage(projectId, args, { planOnly: true })` (D-10..D-13) — the type chain from Task 3 ensures `planOnly` flows to `runtime.ts`'s `isPlanMode` gate
    - 3 new dispatcher tests verify the real implementations; 14 existing tests unchanged
    - All dispatcher, sessionStore, context-aggregator, runtime, llm tests pass (cumulative green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (context:currentSession) | Untrusted renderer passes `sessionId: string` to main; main returns token counts. ASVS V5 input validation required. |
| renderer → main (llm:chat with planOnly) | Renderer sets `payload.overrides.planOnly = true`; main process must respect the flag (Gap 3) |
| user input → renderer (toast.description) | User types `/goal <X>` or `/context`; X is interpolated into `toast.info` description. XSS risk if sonner renders as HTML. |
| main → filesystem (SKILL.md size) | `listPhysicalSkills` reads `~/.cdf/skills/<name>/SKILL.md` and `<projectPath>/.cdf/skills/<name>/SKILL.md`. Symlink/path traversal risk if scope-path is malicious. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-T1 | Tampering | `useSessionStore.sessionGoals` | mitigate | `setSessionGoal` builds a new `Map` (immutability); no cross-session reads implemented in v1.1 (D-04); v1.2 SQLite migration will require session_id ACL check |
| T-07-T2 | Information Disclosure | `context:currentSession` IPC | mitigate | `sessionId` validation: `typeof === 'string' && length > 0 && length <= 64`; main process queries by primary key (no cross-session read); renderer cannot pass arbitrary sessionId to access other users' data because sessionId is a UUID v4 generated by main |
| T-07-T3 | Spoofing | `/plan` user claims plan mode but runtime ignores | mitigate | Gap 3 fix: `runtime.ts` consumes `overrides.planOnly` and strips `bash` / `delete_file` from `builtInTools` + sets `interruptOn: false` — verified by SLASH-REGRESSION it 7.2 in Plan 07-02 |
| T-07-T4 | Tampering | 6-hunk patch-package on `@langchain/anthropic@1.4.0` | mitigate | SLASH-REGRESSION it 7.1 in Plan 07-02 directly asserts v3 stream reasoning roundtrip — if `npm install` strips the patch, the test fails and forces a `npx patch-package` rerun |
| T-07-T5 | Denial of Service | `sessionGoals` Map unbounded growth | accept | Each session has 1 goal; map size = active sessions count (bounded by user's session count, typically < 100); Phase 8 polish may add LRU eviction |
| T-07-T6 | Information Disclosure | `listPhysicalSkills` reads symlink in `~/.cdf/skills/` | accept | Path is anchored at user's home + project root (not user input); Phase 8 polish may add `fs.realpath` check |
| T-07-T7 | XSS | `toast.info` description with user-supplied X | mitigate | sonner 2.0.7 default-escapes description text (not HTML); verified by Phase 6 prior usage — `toast.info('text with X')` renders as plain text |
| T-07-T8 | Tampering | `/context` IPC returns 0 for all sources if any throws | accept | Pitfall P7-6: 4 separate try-catch blocks (one per source) — if `loadMcpTools` fails, conversation/skills/workflows still report real values; only the failed source zeros out |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck audit (07-RESEARCH.md §3) — all 4 Phase 7 dependencies (`@langchain/anthropic@1.4.0`, `sonner@2.0.7`, `cmdk@1.1.1`, `@radix-ui/react-popover@1.1.15`) are `[OK]`. No new deps added. No blocking-human checkpoint required (slopcheck all green). |

## Hard "Do Not Touch" Compliance

- `runtime.ts:306-602` (createDeepAgentRuntime body) — Gap 3 fix modifies lines 488 (builtInTools) and 599 (interruptOn), but the surrounding 100+ lines (model creation, checkpointer, message building, MCP loading, subagent loop) are untouched
- `llm.ts:306-425` (runLLMChat body + streamEvents v3) — Gap 1 fix modifies lines 32-44 (ChatPayload interface); line 324 (payload.overrides passthrough) is already correct and remains untouched
- `workflow-runtime.ts` — NOT modified
- `LLMStreamEvent` union — NOT modified
- 6-hunk patch-package on `@langchain/anthropic@1.4.0` — NOT modified; SLASH-REGRESSION it 7.1 in Plan 07-02 is the load-bearing test
- `slopcheck` audit at 07-RESEARCH.md §3 returns 4 OK / 0 SLOP / 0 SUS; no new deps required
</threat_model>

<verification>
After all 4 tasks complete:

1. **Targeted test runs** (each task verifies its own changes):
   ```bash
   npx vitest run src/renderer/src/stores/sessionStore.test.ts
   npx vitest run src/main/context-aggregator.test.ts
   npx vitest run src/renderer/src/lib/commands/dispatcher.test.ts
   npx vitest run src/main/deepagent/runtime.test.ts
   npx vitest run src/main/llm.test.ts
   ```

2. **Full suite green** (no Phase 6 regressions):
   ```bash
   npm test  # expect ~149 tests passing: 139 Phase 6 + 3 sessionGoals + 4-6 context-aggregator + 3 dispatcher real-impl
   ```

3. **TypeScript clean**:
   ```bash
   npx tsc --noEmit -p .  # or project-defined typecheck command
   ```

4. **Hard "Do Not Touch" boundaries**:
   ```bash
   git diff --stat src/main/llm.ts  # expect changes ONLY in lines 7-11 + 32-44
   git diff --stat src/main/deepagent/runtime.ts  # expect changes ONLY in lines 1-25 + 43-46 + 488 + 599
   git diff src/main/workflow-runtime.ts  # expect 0 changes
   git status patches/  # expect clean (no patch modifications)
   ```

5. **Manual smoke** (electron dev build):
   - `npm run dev` → start app → open a project → create a session
   - Type `/goal write tests` in textarea → press Enter → verify `[system] 正在执行 /goal…` toast appears within 200ms → verify in DevTools console `useSessionStore.getState().sessionGoals.get('<activeSessionId>')` returns `'write tests'`
   - Type `/context` → press Enter → verify breakdown toast shows 4 lines + Total
   - Type `/plan write tests` → press Enter → verify `[plan] 进入 plan 模式：write tests` toast appears → verify in DevTools Network tab the `llm:chat` IPC includes `payload.overrides.planOnly === true`
</verification>

<success_criteria>
- [ ] `sessionGoals: Map<string, string>` field + `setSessionGoal` action added to `useSessionStore` (SLASH-05 data layer)
- [ ] `aggregateCurrentSessionContext(sessionId)` returns `{ breakdown, total }` from 4 sources (SLASH-06 data layer)
- [ ] `ipcMain.handle('context:currentSession', ...)` registered and exposed via `electronAPI.context.currentSession` (SLASH-06 IPC bridge)
- [ ] `ChatPayload.overrides` typed as `ChatRuntimeOverrides` (Gap 1 closed)
- [ ] `RuntimeModelOverrides` aliased to `ChatRuntimeOverrides` (Gap 2 closed)
- [ ] `isPlanMode` gate in `runtime.ts` strips `bash` / `delete_file` from `builtInTools` and sets `interruptOn: false` (Gap 3 closed, D-13)
- [ ] `dispatcher.dispatch` SystemSilent writes to sessionGoals + emits `/goal` toast
- [ ] `dispatcher.dispatch` SystemLocal calls IPC + emits breakdown toast
- [ ] `dispatcher.dispatch` PlanMode emits `[plan]` toast + calls `sendMessage(..., { planOnly: true })`
- [ ] All 4 task `<verify>` commands exit 0
- [ ] `npm test` full suite green (139 Phase 6 + 3 sessionGoals + 4-6 context-aggregator + 3 dispatcher real-impl)
- [ ] `npx tsc --noEmit` exits 0
- [ ] Hard "Do Not Touch" boundaries respected (only ChatPayload type, RuntimeModelOverrides type, runtime.ts line 488 + 599, llm.ts:324 passthrough; workflow-runtime.ts / LLMStreamEvent union / 6-hunk patch untouched)
- [ ] Threat model mitigations: T-07-T1 (sessionGoals immutability), T-07-T2 (sessionId validation), T-07-T3 (planOnly end-to-end), T-07-T4 (load-bearing test in Plan 07-02), T-07-T7 (sonner default-escapes), T-07-T8 (per-source try-catch) all in place
</success_criteria>

<output>
Create `/Users/suntc/project/CDF/.planning/phases/07-system-commands-m3-regression-test/07-01-SUMMARY.md` when done.
</output>
