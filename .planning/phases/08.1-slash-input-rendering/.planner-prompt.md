<planning_context>
**Phase:** 08.1
**Phase name:** 斜杠命令输入框内联渲染 (slash-input-rendering)
**Mode:** standard

**Phase Goal (from ROADMAP.md):**
当用户在 Master Agent 对话输入框选中一个 slash 命令后，原本的纯文本 `/xxx` 替换为胶囊样式 token —— 命令图标（按 source 维度）+ 标题化名称 + 闪烁光标紧随其后；普通文本与 token 混合显示。需要从 `<textarea>` 升级为 `contenteditable` 方案（或等效 overlay 渲染层），并把 Tab/Enter/Esc/Backspace 行为与既有 popup dispatch 契约对齐。

**Phase requirement IDs (MUST address):** None mapped in REQUIREMENTS.md (Phase 8.1 is a v1.1 emergency appendage, requirements are locked in 08.1-SPEC.md as R1..R8 — 8 requirements)

<files_to_read>
- /Users/suntc/project/CDF/.planning/phases/08.1-slash-input-rendering/08.1-CONTEXT.md (USER DECISIONS — locked D-01..D-08)
- /Users/suntc/project/CDF/.planning/phases/08.1-slash-input-rendering/08.1-SPEC.md (LOCKED REQUIREMENTS — 8 requirements R1..R8, 13 acceptance criteria)
- /Users/suntc/project/CDF/.planning/phases/08.1-slash-input-rendering/08.1-DISCUSSION-LOG.md (Audit trail of decisions — DO NOT use as input, decisions are in CONTEXT.md)
- /Users/suntc/project/CDF/.planning/ROADMAP.md (Roadmap)
- /Users/suntc/project/CDF/.planning/STATE.md (Project state)
- /Users/suntc/project/CDF/CLAUDE.md (Project instructions)
- /Users/suntc/project/CDF/src/renderer/src/components/ChatArea/ChatArea.tsx (Critical code — overlay target)
- /Users/suntc/project/CDF/src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (Reference for icon patterns)
- /Users/suntc/project/CDF/src/renderer/src/lib/commands/dispatcher.ts (Read-only contract — must not regress)
- /Users/suntc/project/CDF/src/shared/types.ts (CommandSource + SlashCommand interface)
</files_to_read>

**Critical locked decisions (D-01..D-08 from CONTEXT.md, NON-NEGOTIABLE):**

- **D-01 (Rendering):** Overlay rendering — keep `<textarea>` as input source, add absolutely-positioned `<div>` overlay. textarea text is `color: transparent`. Reuses existing IME guard.
- **D-02 (Source icons):** 5 categories (system/Sparkles, skills/GraduationCap, mcp/Wrench, workflow/Play, command/Terminal). 7-source `CommandSource` collapses to 5 at token layer (skill:project + skill:global → GraduationCap, cmd:project + cmd:system → Terminal).
- **D-03 (Resend scope):** Parser utility only — `parseInputToTokens(text, registry)` exported from `src/renderer/src/lib/commands/parseInputToTokens.ts`. NO resend UI button (out of scope).
- **D-04 (Backspace + click-no-op):** Event interception in `ChatArea.tsx` — `handleKeyDown` extended to detect caret at `len('/cmd-name ')` and `e.preventDefault() + setInputVal('')` for atomic delete. `onMouseDown={(e) => e.preventDefault()}` on SlashToken span.
- **D-05 (Title-Case):** `formatTokenLabel(name)` — first letter of every hyphen-separated segment uppercased; hyphens preserved. `gsd-fast` → `Gsd-Fast`.
- **D-06 (Component file):** New `src/renderer/src/components/SlashCommand/SlashToken.tsx`. Props: `name`, `source: CommandSource`, `data-testid?`.
- **D-07 (Overlay markup):** Wrap each `<textarea>` in relative-positioned container; sibling `<div>` overlay shows leading token + trailing plain text. Z-index: 0 (textarea) vs 1 (overlay).
- **D-08 (Tests):** 3 new test files (SlashToken.test.tsx, parseInputToTokens.test.ts, formatTokenLabel.test.ts) + 1 regression assertion in existing dispatcher.test.ts.

**Files to create:**
- `src/renderer/src/components/SlashCommand/SlashToken.tsx` (new)
- `src/renderer/src/components/SlashCommand/SlashToken.test.tsx` (new)
- `src/renderer/src/lib/commands/iconMap.ts` (new — `getSlashTokenIcon(source)` helper)
- `src/renderer/src/lib/commands/parseInputToTokens.ts` (new — `parseInputToTokens(text, registry)` helper)
- `src/renderer/src/lib/commands/parseInputToTokens.test.ts` (new)
- `src/renderer/src/lib/commands/formatTokenLabel.ts` (new — `formatTokenLabel(name)` helper)
- `src/renderer/src/lib/commands/formatTokenLabel.test.ts` (new)

**Files to modify:**
- `src/renderer/src/components/ChatArea/ChatArea.tsx` (overlay markup, Backspace atomicity, click-no-op wiring, both textareas)
- `src/renderer/src/lib/commands/dispatcher.test.ts` (1 regression assertion: `resolve('/goal fix login', ...)` still returns `SystemSilent` plan)

**Hard constraints (must not regress):**
- IME composition guard (ChatArea.tsx:150-152, 648-680 — 3 refs + 200ms timer)
- Shift+Enter newline path (ChatArea.tsx:733 — `e.key === 'Enter' && !e.shiftKey`)
- dispatcher.resolve() / dispatch() contract: `inputVal` literal `/cmd-name args` is unchanged
- Tailwind v4 static class scan: all utility classes must be statically present in source (no dynamic concatenation)
- No new dependencies (React, lucide-react, Radix Popover, cmdk already available)
- Both textareas share the token component (DRY)
- No DB / IPC / preload changes (renderer-only)

**Anti-patterns to avoid:**
- contentEditable replacement (over-engineering, breaks IME)
- Mid-input slash rendering (preserves v1.1 D-12)
- Multi-token per input (explicitly excluded)
- Editable token body (explicitly excluded)
- Resend UI button (separate feature phase)
- Clipboard paste token reconstruction (out of scope; only resend path)
- Per-source token background color (out of scope; neutral surface)
- Light theme palette (deferred to v1.2+)

**Project instructions:** Read /Users/suntc/project/CDF/CLAUDE.md — follow project-specific guidelines (Chinese-simplified responses, CodeGraph usage, RTK, etc.)
**Project skills:** Check /Users/suntc/.claude/projects/-Users-suntc-project-CDF/memory/ and /Users/suntc/project/CDF/.claude/ — no project-specific skills for this phase found.
</planning_context>

<downstream_consumer>
Output consumed by /gsd:execute-phase. Plans need:
- Frontmatter (wave, depends_on, files_modified, autonomous)
- Tasks in XML format with read_first and acceptance_criteria fields (MANDATORY on every task)
- Verification criteria
- must_haves for goal-backward verification
</downstream_consumer>

<deep_work_rules>
## Anti-Shallow Execution Rules (MANDATORY)

Every task MUST include these fields — they are NOT optional:

1. **`<read_first>`** — Files the executor MUST read before touching anything. Always include:
   - The file being modified (so executor sees current state, not assumptions)
   - Any "source of truth" file referenced in CONTEXT.md (reference implementations, existing patterns, config files, schemas)
   - Any file whose patterns, signatures, types, or conventions must be replicated or respected

2. **`<acceptance_criteria>`** — Verifiable conditions that prove the task was done correctly. Rules:
   - Every criterion must be checkable as a source assertion, behavior assertion, test command, or CLI output
   - NEVER use subjective language ("looks correct", "properly configured", "consistent with")
   - Include exact strings, patterns, values, command outputs, or observable behavior where that is the right proof
   - Examples:
     - Code: `auth.py contains def verify_token(` / `test_auth.py exits 0`
     - Behavior: `POST /api/auth/login returns 200 + httpOnly JWT cookie for valid credentials`
     - Config: `.env.example contains DATABASE_URL=` / `Dockerfile contains HEALTHCHECK`
     - Docs: `README.md contains '## Installation'` / `API.md lists all endpoints`
     - Infra: `deploy.yml has rollback step` / `docker-compose.yml has healthcheck for db`

3. **`<action>`** — Must include CONCRETE values, not references. Rules:
   - NEVER say "align X with Y", "match X to Y", "update to be consistent" without specifying the exact target state
   - Include concrete identifiers and reference values: config keys, function signatures, SQL table names, class names, import paths, env vars, endpoint paths, etc.
   - If CONTEXT.md has a comparison table or expected values, copy only the target identifiers/values needed to remove ambiguity
   - Do not include full file contents, fenced code blocks, or complete implementations in `<action>`
   - The executor should understand the intended target state from `<action>` and use `<read_first>` files for current implementation details, patterns, and source-of-truth context

**Why this matters:** Executor agents work from the plan text. Vague instructions like "update the config to match production" produce shallow one-line changes. Concrete instructions like "add DATABASE_URL, set POOL_SIZE=20, add REDIS_URL, and read config/runtime.ts before editing" produce complete work without turning the planner into the executor.
</deep_work_rules>

<quality_gate>
- [ ] PLAN.md files created in phase directory `.planning/phases/08.1-slash-input-rendering/`
- [ ] Each plan has valid frontmatter (phase, plan, type, wave, depends_on, files_modified, autonomous, requirements)
- [ ] Tasks are specific and actionable
- [ ] Every task has `<read_first>` with at least the file being modified
- [ ] Every task has `<acceptance_criteria>` with behavior, test-command, CLI, or source assertions
- [ ] Every `<action>` contains concrete identifiers without fenced code blocks or full implementations
- [ ] Dependencies correctly identified
- [ ] Waves assigned for parallel execution
- [ ] must_haves derived from phase goal (atomic token rendering, dispatcher contract preserved, IME preserved)
- [ ] All 8 SPEC requirements (R1..R8) covered by at least one task
- [ ] All 13 acceptance criteria from SPEC.md traceable to a task
- [ ] 4 test files mentioned: SlashToken.test.tsx, parseInputToTokens.test.ts, formatTokenLabel.test.ts, dispatcher.test.ts (regression)
- [ ] No new dependencies added
- [ ] Tailwind v4 static class scan constraint surfaced
</quality_gate>
