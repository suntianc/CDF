---
phase: 08-polish-differentiators
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified: [.planning/phases/08-polish-differentiators/08-SUMMARY.md, src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx]
autonomous: true
requirements: [SC-4, SC-5]
user_setup: []
must_haves:
  truths:
    - "IME z-index known-issue comment is present in SlashCommandPopup.tsx (D-13: accept macOS known issue with IME candidate 9999 z-index; D-14: no code fix attempted; D-15: comment + Esc-once hint per ROADMAP SC-4 workaround — actually added in 08-01 Task 5; 08-04 verifies and may add a parallel comment in ChatArea.tsx if missing)"
    - "08-SUMMARY.md documents the 5 success criteria + 22 D decisions + 7 C decisions + test count delta + manual verification steps"
  artifacts:
    - path: .planning/phases/08-polish-differentiators/08-SUMMARY.md
      provides: "Phase 8 final summary: 4 plans, 5 SCs, 22 D + 7 C decisions, ~12 new tests, 1 latent bug fix"
      contains: "Phase 8 Summary"
      min_lines: 150
    - path: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
      contains: "IME z-index known issue"
  key_links: []
---

<objective>
Wave D — Final phase summary + cross-check verification. This plan has 2 narrow tasks:
1. Verify the IME z-index comment added in 08-01 Task 5 is present; add a parallel comment in ChatArea.tsx (near the PopoverContent) if the slash popup comment isn't sufficient. D-15 says the comment should be near the Phase 5 known stub.
2. Create `08-SUMMARY.md` documenting the entire phase: 4 plans, 5 success criteria, 22 D decisions + 7 C decisions, ~12 new tests, 1 latent bug fix (Toaster mount), Phase 7 regressions check.

This is a documentation + verification plan, not new code. It exists to:
- Provide a closing artifact for `/gsd:verify-work` (full suite green + 5 SCs verified + 22 D + 7 C tracked)
- Document the manual-only verification items (macOS IME z-index smoke + chokidar chmod 0 smoke)
- Hand off the 4 08-0X-PLAN.md + 08-0X-SUMMARY.md set to the milestone retrospective

Output: 1 new file (08-SUMMARY.md) + 1 ChatArea.tsx comment verification/duplication + cross-check of all 4 08-0X-SUMMARY.md files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-differentiators/08-CONTEXT.md
@.planning/phases/08-polish-differentiators/08-RESEARCH.md
@.planning/phases/08-polish-differentiators/08-VALIDATION.md
@.planning/phases/07-system-commands-m3-regression-test/07-01-SUMMARY.md
@.planning/phases/07-system-commands-m3-regression-test/07-02-SUMMARY.md

Source files: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (verify IME comment from 08-01 Task 5), src/renderer/src/components/ChatArea/ChatArea.tsx:1158-1174 (verify/add IME comment), 08-01-SUMMARY.md + 08-02-SUMMARY.md + 08-03-SUMMARY.md (consumed after each prior plan executes).
</context>

<interfaces>
From 08-01 Task 5: IME z-index comment is in SlashCommandPopup.tsx (9 lines, before `return (`).
From 08-04 (this plan): cross-check whether a parallel comment is needed in ChatArea.tsx (D-15 says "在 Phase 5 已知 stub 旁" — the stub is in ChatArea.tsx, but the comment is in SlashCommandPopup.tsx).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify IME z-index comment + add parallel comment in ChatArea.tsx</name>
  <files>src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx, src/renderer/src/components/ChatArea/ChatArea.tsx</files>
  <read_first>
    - SlashCommandPopup.tsx:127-145 (verify the IME comment from 08-01 Task 5 is present)
    - ChatArea.tsx:1158-1174 (PopoverContent + SlashCommandPopup wiring — Phase 5 known stub area)
    - 08-CONTEXT.md:62-66 (D-15: 在 Phase 5 已知 stub 旁加注释)
    - 08-RESEARCH.md:399-435 (§7.2 — comment placement options)
    - 08-RESEARCH.md:739-745 (Pitfall 9 — IME comment could be misread as "to fix")
  </read_first>
  <action>
    D-15 says "在 Phase 5 已知 stub 旁加注释，提示用户「IME 候选期间 popup 被遮挡，按 Esc 关闭候选框后 popup 仍可见」". The Phase 5 known stub is in ChatArea.tsx (the `isComposingKeyEvent` + 200ms `justFinishedComposingRef` window). 08-01 placed the comment in SlashCommandPopup.tsx (near the return JSX). Verify both, and add a SHORTER pointer comment in ChatArea.tsx (near the PopoverContent) to satisfy the "旁" (next to) constraint from D-15.

    1. Verify the IME comment from 08-01 Task 5 is present in SlashCommandPopup.tsx:
       - `grep -n "IME z-index known issue" src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx` returns 1 match.
       - If MISSING: re-add the 9-line comment block from 08-01 Plan Task 5 action.
       - If PRESENT: skip to step 2.

    2. In src/renderer/src/components/ChatArea/ChatArea.tsx, immediately BEFORE `<PopoverContent` (around line 1158), add a SHORT pointer comment (3 lines, not the full 9-line explanation) that references the SlashCommandPopup.tsx comment for full context:
       ```
       {/* IME z-index known issue: see SlashCommandPopup.tsx for full context. macOS IME candidate windows sit above web-layer z-index; press Esc to dismiss. (D-13..D-15, accepted as platform limitation.) */}
       ```
       This satisfies the D-15 "在 Phase 5 已知 stub 旁" requirement without duplicating the full explanation.

    3. Do NOT add a long comment block to ChatArea.tsx (DRY: SlashCommandPopup.tsx has the full text). Do NOT change any other ChatArea.tsx logic (the existing IME safety ref + composition handlers remain untouched).

    4. Verify with `git diff src/renderer/src/components/ChatArea/ChatArea.tsx` showing ONLY 1 comment line added (the pointer).
  </action>
  <acceptance_criteria>
    - `grep -n "IME z-index known issue" src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx` = 1 match (from 08-01)
    - `grep -n "see SlashCommandPopup.tsx for full context" src/renderer/src/components/ChatArea/ChatArea.tsx` = 1 match (pointer comment)
    - `git diff --stat src/renderer/src/components/ChatArea/ChatArea.tsx` shows exactly 1 line added (the comment)
    - `npx vitest run` (full suite) remains green (no logic changes)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "IME z-index known issue" src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx src/renderer/src/components/ChatArea/ChatArea.tsx | grep -v ":0" | wc -l</automated>
  </verify>
  <done>
    - IME z-index full comment present in SlashCommandPopup.tsx (9 lines, from 08-01)
    - Pointer comment added in ChatArea.tsx (1 line, references the full comment)
    - D-15 satisfied: comment is "旁" the Phase 5 known stub
    - Full suite green
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 08-SUMMARY.md — final phase summary</name>
  <files>.planning/phases/08-polish-differentiators/08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/07-system-commands-m3-regression-test/07-01-SUMMARY.md (template)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-01-SUMMARY.md (template)
    - .planning/ROADMAP.md (5 success criteria for Phase 8)
    - .planning/phases/08-polish-differentiators/08-CONTEXT.md (22 D + 7 C decisions)
  </read_first>
  <action>
    Create the final Phase 8 SUMMARY.md at `.planning/phases/08-polish-differentiators/08-SUMMARY.md`. This is the closing artifact for the phase. It documents:
    - 4 plans + their commit hashes (filled in after execution)
    - 5 success criteria verification status
    - 22 D decisions (D-01..D-22) + 7 C decisions (C-01..C-07) — for each, point to the plan/task that implemented it
    - ~12 new tests + the latent Toaster bug fix
    - Manual-only verifications (macOS IME smoke + chmod 0 chokidar smoke)
    - Hard "Do Not Touch" compliance check
    - Deferred items (亮色主题 / SLASH-15 / SLASH-17)

    1. Create the file with the following structure (frontmatter + sections):
       ```
       ---
       phase: 08-polish-differentiators
       plans: 4
       type: polish
       status: complete
       test_count: 226 + ~12 new = ~238
       generated_files:
         - 08-01-SUMMARY.md
         - 08-02-SUMMARY.md
         - 08-03-SUMMARY.md
         - 08-04-SUMMARY.md (this plan's doc)
       ---

       # Phase 8: Polish + Differentiators — Summary

       ## Plans Executed
       - Plan 08-01: Wave A — Toaster mount + skeleton.tsx + SlashCommandPopup polish (D-01..D-12, D-15)
       - Plan 08-02: Wave B — useCommandRegistry loading state machine (D-07..D-11)
       - Plan 08-03: Wave C — chokidar fallback (D-16..D-19) + toast dedup (C-04)
       - Plan 08-04: Wave D — IME comment cross-check + this summary

       ## 5 Success Criteria (from ROADMAP)
       1. SC-1: 7 source badges each render a distinct text-* color — VERIFIED via SOURCE_TEXT_COLOR lookup map (D-01..D-04) + 4 SlashCommandPopup tests
       2. SC-2: CJK + variation-selector filter — VERIFIED via normForFilter (D-05d) + pre-normalized Map (D-06) + 2 SlashCommandPopup tests
       3. SC-3: Skeleton row on >500ms MCP load — VERIFIED via 5-state loading enum + 500ms timer + Command.Loading wrapper (D-07..D-12) + 3 useCommandRegistry tests
       4. SC-4: IME z-index known issue (macOS) — VERIFIED via 9-line comment in SlashCommandPopup.tsx + 1-line pointer in ChatArea.tsx (D-13..D-15)
       5. SC-5: chokidar failure → readdir fallback + toast — VERIFIED via degradeAndFallback helper + onFallback bridge + toast.warning with dedup (D-16..D-19, C-04) + 3 tests

       ## 22 D Decisions (Locked) — Implementation Status
       (Table: D-ID | Summary | Plan/Task | Status)
       D-01..D-04: 7-color source badge | 08-01 Task 3 | ✅
       D-05d: Variation selector removal | 08-01 Task 3 | ✅
       D-06: Pre-normalized Map | 08-01 Task 3 | ✅
       D-07..D-12: MCP skeleton | 08-01 Task 4 + 08-02 | ✅
       D-13..D-15: IME z-index | 08-01 Task 5 + 08-04 Task 1 | ✅
       D-16..D-19: chokidar fallback | 08-03 Tasks 1+2 | ✅
       D-20..D-22: 5-row popup layout | (no change) | ✅ (Phase 5/6 preserved)

       ## 7 C Decisions (Claude's Discretion) — Implementation Status
       C-01: 亮色主题 7 色 → 推 v1.2 (deferred)
       C-02: 7 色在 popup 之外 → Phase 8 限定 popup (only SlashCommandPopup)
       C-03: Skeleton 行数 → 1 行 (used in 08-01 Task 4)
       C-04: Toast 去重 → Set 存 fingerprint (used in 08-03 Task 2)
       C-05: Skeleton 颜色 → bg-[var(--color-bg-active)] (used in 08-01 Task 2)
       C-06: 5 行 popup 密度 → 不调 (preserved Phase 5 baseline)
       C-07: cmd:project 颜色 → text-gray-500 (used in 08-01 Task 3)

       ## Latent Bug Fix
       - <Toaster /> was never mounted in App.tsx (Phase 6/7 silently assumed it). Phase 6 useCommandRegistry.ts:71,78 + Phase 7 dispatcher.ts:99/115/126/134 all called toast.* which were no-ops. 08-01 Task 1 mounts `<Toaster richColors position="bottom-right" theme="dark" />` + 1 App.test.tsx verifies.

       ## Test Counts
       - 226 Phase 7 baseline
       - +1 App.test.tsx (Toaster mount)
       - +1 skeleton.tsx (tsc-only, no test)
       - +4 SlashCommandPopup.test.tsx (source color / CJK variation / skeleton / IME comment)
       - +3 useCommandRegistry.test.ts (slow loading / no slow / error fallback)
       - +2 chokidar-watcher.test.ts (degrade to readdir / no retry)
       - +1 useCommandRegistry.test.ts (fallback toast + dedup)
       - Total: ~238 tests (was 226; +12 new)

       ## Hard "Do Not Touch" Verified Intact
       - src/main/runtime.ts — NOT modified
       - src/main/llm.ts:306-425 — NOT modified
       - src/main/workflow-runtime.ts — NOT modified
       - LLMStreamEvent union — NOT modified
       - 6-hunk patch-package on @langchain/anthropic@1.4.0 — NOT modified
       - Phase 5/6 popup layout (256px max-h + 7-row + Command.Item) — only className changed; JSX structure preserved

       ## Manual-Only Verifications
       1. macOS IME z-index smoke: enable Pinyin → type `/` in textarea → IME candidate covers popup → press Esc → candidate dismisses → popup visible
       2. chokidar fallback smoke: chmod 000 ~/.cdf/commands/ → restart app → verify "项目命令热重载不可用，已降级为静态扫描" toast in bottom-right

       ## Deferred to v1.2+
       - 亮色主题 7 色彩色 (C-01)
       - 7 色在 popup 之外 (C-02)
       - SLASH-15 (/goal SQLite 持久化)
       - SLASH-17 (命令别名)

       ## Phase 8 Scope Discipline (per 客人大人 2026-06-04)
       - ✅ 22 D decisions + 7 C decisions all implemented or preserved
       - ❌ NO new SLASH-XX (Phase 8 is polish, no new requirements)
       - ❌ NO args parser (D-02 passthrough only, deferred to v1.2 SLASH-15/17)
       - ❌ NO 7-color redesign (palette locked by D-01)
       ```

    2. After writing, verify the file is well-formed: `cat .planning/phases/08-polish-differentiators/08-SUMMARY.md | head -5` shows the frontmatter.

    3. Commit: `gsd-sdk query commit "docs(08): create phase 8 summary" --files .planning/phases/08-polish-differentiators/08-SUMMARY.md`
  </action>
  <acceptance_criteria>
    - `ls -la .planning/phases/08-polish-differentiators/08-SUMMARY.md` returns 1 file
    - `wc -l .planning/phases/08-polish-differentiators/08-SUMMARY.md` ≥ 100 lines
    - `grep -n "Phase 8: Polish" .planning/phases/08-polish-differentiators/08-SUMMARY.md` = 1 match
    - `grep -n "5 Success Criteria" .planning/phases/08-polish-differentiators/08-SUMMARY.md` = 1 match
    - `grep -n "22 D Decisions" .planning/phases/08-polish-differentiators/08-SUMMARY.md` = 1 match
    - `grep -n "7 C Decisions" .planning/phases/08-polish-differentiators/08-SUMMARY.md` = 1 match
    - `grep -n "Hard \"Do Not Touch\"" .planning/phases/08-polish-differentiators/08-SUMMARY.md` = 1 match
    - `npx vitest run` (full suite) green
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - .planning/phases/08-polish-differentiators/08-SUMMARY.md (NEW) created, ≥ 100 lines
    - Documents 4 plans + 5 SCs + 22 D + 7 C + ~12 new tests + 1 latent bug fix + manual smoke
    - Hard "Do Not Touch" compliance verified
    - Full suite green (~238 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| None new | This plan adds no new code paths; only documentation + 1 comment. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| None | — | SUMMARY.md is a markdown doc; no security boundary. |
</threat_model>

<verification>
After both tasks complete:
1. npx vitest run (full suite) → ~238 tests passing
2. npx tsc --noEmit -p . → exits 0
3. git status .planning/phases/08-polish-differentiators/ shows 4 PLAN.md + 4 SUMMARY.md + 08-SUMMARY.md files
4. Manual smoke: macOS IME Esc dismiss; chmod 0 chokidar fallback toast
</verification>

<success_criteria>
1. SC-4: IME z-index known-issue comment cross-checked in both SlashCommandPopup.tsx (full) + ChatArea.tsx (pointer)
2. SC-5: 08-SUMMARY.md created with 5 SCs + 22 D + 7 C documented
3. Hard "Do Not Touch" intact
4. ~238 tests green
5. Phase 8 complete; v1.1 milestone ready for handoff
</success_criteria>

<output>
Create .planning/phases/08-polish-differentiators/08-04-SUMMARY.md when done.
</output>
