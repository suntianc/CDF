---
phase: 08-polish-differentiators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/renderer/src/App.tsx, src/renderer/src/components/ui/skeleton.tsx, src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx, src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx, src/App.test.tsx, src/renderer/src/components/ChatArea/ChatArea.tsx]
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
user_setup: []
must_haves:
  truths: ["Toaster mounted in App.tsx (T-08-T9 latent fix)", "7 source badges render distinct text-* via SOURCE_TEXT_COLOR (D-01..D-04)", "NFKC + variation-selector removal in filter (D-05d)", "normalizedMap O(1) filter (D-06)", "Skeleton row when loading='slow' (D-08..D-12)", "IME z-index comment near popover (D-15)"]
  artifacts: [{path: src/renderer/src/App.tsx, contains: "<Toaster"}, {path: src/renderer/src/components/ui/skeleton.tsx, exports: [Skeleton], min_lines: 12}, {path: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx, contains: SOURCE_TEXT_COLOR, min_lines: 240}, {path: src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx, contains: "describe('Phase 8"}, {path: src/App.test.tsx, contains: Toaster}, {path: src/renderer/src/components/ChatArea/ChatArea.tsx, contains: "loading={registry.loading}"}]
  key_links: [{from: SlashCommandPopup.tsx, to: skeleton.tsx, pattern: "import.*Skeleton.*from.*skeleton"}, {from: SlashCommandPopup.tsx, to: shared/types.ts, pattern: CommandSource}, {from: App.tsx, to: sonner, pattern: "import.*Toaster.*from.*sonner"}, {from: SlashCommandPopup.tsx, to: SlashCommandPopup.tsx, pattern: normalizedMap}]
---

<objective>
Wave A — Mount `<Toaster />` in App.tsx (latent Phase 6/7 bug fix) + create shadcn Skeleton shim + implement 5 polish dimensions in SlashCommandPopup: 7-color source badge lookup map, CJK NFKC variation-selector removal, pre-normalized Map perf opt, MCP slow-load Skeleton row, IME z-index known-issue comment.

Output: 6 files (1 modified App.tsx + 1 new skeleton.tsx + 1 modified SlashCommandPopup.tsx + 1 modified ChatArea.tsx + 1 extended test file + 1 new App.test.tsx) + ~6 new tests on top of 226-test baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md
@.planning/phases/08-polish-differentiators/08-CONTEXT.md
@.planning/phases/08-polish-differentiators/08-RESEARCH.md
@.planning/phases/08-polish-differentiators/08-VALIDATION.md
@.planning/phases/05-popup-shell-keyboard-spike/05-UI-SPEC.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md
@.planning/phases/07-system-commands-m3-regression-test/07-01-SUMMARY.md

Source files: App.tsx:1-121, SlashCommandPopup.tsx:1-174 (filterCommands 19-31, props 33-47, displayCommands 55-70, Badge 153-155), SlashCommandPopup.test.tsx:1-742, badge.tsx, lib/utils.ts (cn), styles/globals.css, shared/types.ts:140-165 (CommandSource), ChatArea.tsx:1158-1174.
</context>

<interfaces>
CommandSource (shared/types.ts:144-151): 'system'|'mcp'|'skill:project'|'skill:global'|'workflow'|'cmd:project'|'cmd:system'.

SlashCommandPopupProps: add `loading?: 'idle'|'pending'|'slow'|'ready'|'error'` (D-08/D-09).

ChatArea.tsx:1158-1174: add `loading={registry.loading}` to SlashCommandPopup.

cn() from lib/utils.ts. CSS vars (no additions, D-04): --color-bg-surface, --color-bg-active, --color-text-primary.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx mount Toaster + new App.test.tsx</name>
  <files>src/renderer/src/App.tsx, src/App.test.tsx</files>
  <read_first>App.tsx:1-16 (imports), 50-120 (return JSX). 08-RESEARCH.md:576-595 (Toaster), 683-690 (Pitfall 1 CRITICAL).</read_first>
  <action>Per D-16 + Pitfall 1 (CRITICAL), mount Sonner Toaster. Prerequisite for ALL toasts. (1) In App.tsx add `import { Toaster } from 'sonner';` after line 16. (2) Insert `<Toaster richColors position="bottom-right" theme="dark" />` AFTER sidebar toggle button (lines 110-118) and BEFORE closing `</div>` line 119. Must be inside outer div. (3) Do NOT change any other line. (4) Create src/App.test.tsx (NEW, ~30 lines): stub window.electronAPI = {store: {get: vi.fn().mockResolvedValue(null), set: vi.fn()}}, import App, render, query `container.querySelector('[data-sonner-toaster]') ?? document.querySelector('[data-sonner-toaster]')`, expect truthy. (5) Run: npx vitest run src/App.test.tsx → 1 test passes.</action>
  <acceptance_criteria>grep "import { Toaster }" App.tsx = 1; grep "<Toaster" App.tsx = 1; grep "richColors|position=\"bottom-right\"|theme=\"dark\"" App.tsx = 3; git diff --stat App.tsx = 2 lines added; npx vitest run src/App.test.tsx = 1 passing; npx vitest run (full) green.</acceptance_criteria>
  <verify><automated>npx vitest run src/App.test.tsx</automated></verify>
  <done>Toaster mounted in App.tsx; src/App.test.tsx (NEW) verifies; Phase 6/7 silent toasts visible (T-08-T9); full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: skeleton.tsx — new shadcn Skeleton shim</name>
  <files>src/renderer/src/components/ui/skeleton.tsx</files>
  <read_first>badge.tsx:1-36 (cva-free pattern), lib/utils.ts (cn), styles/globals.css (--color-bg-active), 08-RESEARCH.md:343-364 (canonical Skeleton v4), 691-697 (Pitfall 2 file missing).</read_first>
  <action>Per D-12 + C-05, create Skeleton. CDF has no --color-accent; use bg-[var(--color-bg-active)]. (1) Create src/renderer/src/components/ui/skeleton.tsx (NEW, 12 lines): `import { cn } from '@/lib/utils'; function Skeleton({ className, ...props }: React.ComponentProps<'div'>) { return <div data-slot="skeleton" className={cn('animate-pulse rounded-md bg-[var(--color-bg-active)]', className)} {...props} />; } export { Skeleton };`. (2) No new CSS vars or Tailwind classes. (3) Verify: npx tsc --noEmit -p . exits 0.</action>
  <acceptance_criteria>wc -l skeleton.tsx ~12; grep "export { Skeleton }" = 1; grep "bg-\[var(--color-bg-active)\]" = 1; npx tsc --noEmit -p . exits 0; npx vitest run green.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit -p . && echo tsc-ok</automated></verify>
  <done>skeleton.tsx (NEW, 12 lines) exports Skeleton with bg-[var(--color-bg-active)] (C-05); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: SlashCommandPopup — 7-color source badge + CJK NFKC + Map perf</name>
  <files>src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx</files>
  <read_first>SlashCommandPopup.tsx:1-31 (imports + SYSTEM_COMMANDS + filterCommands), 55-70 (useMemos), 145-163 (Badge). shared/types.ts:144-151. 08-CONTEXT.md:30-55 (D-01..D-06). 08-RESEARCH.md:131-198 (§4 lookup map), 204-277 (§5 NFKC + Map).</read_first>
  <action>Per D-01..D-04 + D-05d + D-06, implement 3 polish dimensions. (1) Add `import { cn } from '@/lib/utils';` after line 11. (2) Add `import { Skeleton } from '@/components/ui/skeleton';`. (3) Add `import type { CommandSource } from '../../../../shared/types';`. (4) Add SOURCE_TEXT_COLOR map after SYSTEM_COMMANDS (line 17), before old filterCommands: `const SOURCE_TEXT_COLOR: Record<CommandSource, string> = { 'system':'text-blue-400', 'skill:global':'text-violet-300', 'skill:project':'text-purple-400', 'workflow':'text-green-400', 'mcp':'text-amber-400', 'cmd:system':'text-gray-400', 'cmd:project':'text-gray-500' };`. (5) Add VARIATION_SELECTORS regex + normForFilter helper: `const VARIATION_SELECTORS = /[︀-️\u{E0100}-\u{E01EF}]/gu; const normForFilter = (s: string): string => s.normalize('NFKC').replace(VARIATION_SELECTORS, '').toLowerCase();`. MUST have gu flags (Pitfall P8-8). (6) DELETE old filterCommands (lines 19-31). (7) In component body, after displayCommands useMemo (55-65) and BEFORE filtered useMemo (67-70), add normalizedMap useMemo and refactor filtered: `const normalizedMap = useMemo(() => { const m = new Map<string, string>(); for (const c of displayCommands) m.set(c.name, normForFilter(c.name)); return m; }, [displayCommands]); const filtered = useMemo(() => { const nq = normForFilter(query); if (!nq) return displayCommands.slice(); return displayCommands.filter((c) => { const name = normalizedMap.get(c.name) ?? normForFilter(c.name); return name.includes(nq) || ('/' + name).includes(nq); }); }, [query, displayCommands, normalizedMap]);`. (8) Update Badge (line 153-155): `<Badge variant="secondary" className={cn('text-[10px] px-1.5 py-0 font-mono', SOURCE_TEXT_COLOR[c.source])}>{c.badge}</Badge>`. (9) Do NOT change Command.Item/Empty body. (10) npx vitest run SlashCommandPopup.test.tsx → all 30+ existing pass.</action>
  <acceptance_criteria>grep SOURCE_TEXT_COLOR ≥ 2; grep "text-blue-400|text-violet-300|text-purple-400|text-green-400|text-amber-400|text-gray-400|text-gray-500" = 7; grep VARIATION_SELECTORS ≥ 2; grep normForFilter ≥ 4; grep normalizedMap ≥ 3; grep "filterCommands\\s*(" = 0 (old removed); npx vitest run SlashCommandPopup.test.tsx passes (30+ existing); npx vitest run green.</acceptance_criteria>
  <verify><automated>npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx</automated></verify>
  <done>SOURCE_TEXT_COLOR (7) applied via cn(); VARIATION_SELECTORS + normForFilter (D-05d); normalizedMap O(1) filter (D-06); old filterCommands removed; 0 regressions.</done>
</task>

<task type="auto">
  <name>Task 4: SlashCommandPopup — Skeleton row + loading prop wire</name>
  <files>src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx, src/renderer/src/components/ChatArea/ChatArea.tsx</files>
  <read_first>SlashCommandPopup.tsx:33-47 (props), 135-168 (Command.List). ChatArea.tsx:1158-1174 (PopoverContent + SlashCommandPopup). 08-CONTEXT.md:54-62 (D-08..D-12). 08-RESEARCH.md:281-364 (§6).</read_first>
  <action>Per D-08..D-12, add Skeleton row at top of Command.List when loading='slow'. 08-02 sets the state. (1) In SlashCommandPopupProps (line 33-47), add: `loading?: 'idle'|'pending'|'slow'|'ready'|'error';`. (2) Destructure `loading` from props in component signature (line 52). (3) In Command.List (line 135), AFTER hasMcpWarning block (136-144) and BEFORE filtered.map (145), add: `{loading === 'slow' && (<Command.Loading><div data-testid="mcp-skeleton" className="flex items-center gap-2 px-2 py-1.5 select-none"><Skeleton className="h-3 w-12 rounded" /><Skeleton className="h-3 w-24 rounded" /></div></Command.Loading>)}`. C-03: 1 row. (4) In ChatArea.tsx (line 1158-1174), add `loading={registry.loading}` to SlashCommandPopup. Find existing `const registry = useCommandRegistry(...)`. (5) npx vitest run SlashCommandPopup.test.tsx → existing 30+ pass.</action>
  <acceptance_criteria>grep "loading?:" in SlashCommandPopup.tsx ≥ 1; grep 'data-testid="mcp-skeleton"' = 1; grep "Command.Loading" = 1; grep "loading={registry.loading}" in ChatArea.tsx = 1; npx vitest run SlashCommandPopup.test.tsx passes.</acceptance_criteria>
  <verify><automated>npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx</automated></verify>
  <done>SlashCommandPopup accepts loading prop; loading='slow' renders Skeleton row (data-testid='mcp-skeleton'); ChatArea wires loading prop; 0 regressions.</done>
</task>

<task type="auto">
  <name>Task 5: SlashCommandPopup — IME z-index known-issue comment</name>
  <files>src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx</files>
  <read_first>SlashCommandPopup.tsx:127-134 (Command wrapper). 08-CONTEXT.md:62-66 (D-13..D-15). 08-RESEARCH.md:399-435 (§7).</read_first>
  <action>Per D-15, add explicit "known issue, accepted as platform limitation, Esc-once workaround" comment. D-14 accepted the issue. (1) In SlashCommandPopup.tsx, immediately BEFORE `return (` (line 127), add 9-line comment: `// IME z-index known issue (D-13..D-15, accepted as platform limitation): // macOS IME candidate windows (Pinyin/Hiragana/Kana) render in a separate // NSPanel at NSPopUpMenuWindowLevel (~101), which sits above any web-layer // z-index. PopoverContent uses z-50 (from popover.tsx:23), but web-layer // z-index cannot escape the NSWindow boundary. There is no Chromium/Electron // API to render above the IME panel (as of 2026-06-04). // Workaround: press Esc once to dismiss the IME candidate; the popup // remains visible underneath. Tracked for v1.2 if Apple ships an // IMKCandidates placement API.`. (2) Do NOT use TODO/FIXME markers (Pitfall P9). (3) No other code change.</action>
  <acceptance_criteria>grep "IME z-index known issue" = 1; grep "TODO|FIXME" in new comment = 0; 9 lines added.</acceptance_criteria>
  <verify><automated>grep -c "IME z-index known issue" src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx</automated></verify>
  <done>9-line IME z-index known-issue comment added; no TODO/FIXME markers.</done>
</task>

<task type="auto">
  <name>Task 6: SlashCommandPopup.test.tsx — 4 new Phase 8 tests + App.test.tsx toaster test (already in Task 1)</name>
  <files>src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx</files>
  <read_first>SlashCommandPopup.test.tsx:1-742 (pattern: TestHarness + describe blocks). 08-VALIDATION.md:39-49 (test map).</read_first>
  <action>Per 08-VALIDATION.md Test Map, add 4 new tests in a `describe('Phase 8 polish', ...)` block at the END of SlashCommandPopup.test.tsx (after the existing Phase 6 handleSlashSelect block at line 713-741). Tests follow existing TestHarness pattern (lines 41-169) with extended props (loading, commands).
    - Test A — Source color class: render TestHarness with 7 commands (system/skill:global/skill:project/workflow/mcp/cmd:system/cmd:project). Query Badge element by closest('[cmdk-item]') for each. Assert each Badge's className includes the expected text-* class (e.g. 'text-blue-400' for system). Use `expect(badge.className).toMatch(/text-blue-400/)`.
    - Test B — CJK variation-selector filter: render TestHarness with `commands: [{name: 'party-emoji', source: 'system', badge: '[system]', description: '', target: 'party-emoji', sourceLabel: 'system'}]`. Type `/🎉` (U+FE0F) and verify row visible. Then change to `/🎉︎` (U+FE0F) and verify row still visible (D-05d: variation selector removal makes them match).
    - Test C — Skeleton row when loading='slow': extend TestHarness to accept `loading` prop. Render with `loading='slow'`. Query `getByTestId('mcp-skeleton')` → expect truthy. Verify 2 Skeleton divs inside (h-3 w-12 + h-3 w-24).
    - Test D — IME z-index comment exists (string-match): `expect(SlashCommandPopup.toString()).toContain('IME z-index known issue')` OR import source via fs.readFileSync + assert. (Note: forwardRef components are React objects, not strings — use fs.readFileSync + grep pattern.)
  Run: npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx → 30+ existing + 4 new = 34+ passing.</action>
  <acceptance_criteria>grep "describe('Phase 8 polish" in SlashCommandPopup.test.tsx = 1; grep "text-blue-400" in test file = 1; grep "data-testid=\"mcp-skeleton\"" in test file = 1; grep "U+FE0F\|\\\\uFE0F\|🎉" in test file ≥ 2; npx vitest run SlashCommandPopup.test.tsx = 34+ tests passing (30+ existing + 4 new).</acceptance_criteria>
  <verify><automated>npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx</automated></verify>
  <done>4 new Phase 8 tests pass; all 30+ existing Phase 5/6/7 tests pass; 0 regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| renderer → main (commands:list) | Untrusted renderer passes projectId+agentId to main; main returns commands. ASVS V5 input validation. |
| renderer → sonner (toast calls) | User input may flow into toast.description; sonner default-escapes (XSS safe per Phase 6 verification). |
| filterCommands input → output | User keystroke string + SlashCommand[].name. NFKC + variation-selector removal are well-defined transforms; no injection risk. |
| Badge text-* class → CSS | Tailwind v4 JIT scans source. Lookup map uses static class strings — no template interpolation, no XSS via className. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-T1 | Tampering | SOURCE_TEXT_COLOR lookup | mitigate | Record<CommandSource, string> TypeScript type ensures all 7 keys present; Phase 6 CommandSource union unchanged. |
| T-08-T2 | Information Disclosure | filterCommands input | mitigate | NFKC + variation-selector removal are normalization transforms; no data leaks. |
| T-08-T3 | Spoofing | Badge text-* class | mitigate | Static class strings only; cn() merges with twMerge; no template literal interpolation. |
| T-08-T4 | Denial of Service | normalizedMap rebuild | mitigate | useMemo dependency is [displayCommands]; rebuild only on actual list change. |
| T-08-T5 | Information Disclosure | Skeleton DOM | accept | data-testid is a test attribute; no PII. |
| T-08-T6 | XSS | toast.description | accept | sonner 2.0.7 default-escapes (Phase 6 verified); no HTML rendering. |
| T-08-T7 | Tampering | <Toaster /> mount | mitigate | Import in App.tsx is a static string; not user-controllable. |
| T-08-T8 | Tampering | IME z-index comment | mitigate | Static comment text; not parsed by app. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added in this plan. Skeleton shim is hand-written (no shadcn CLI). |
</threat_model>

<verification>
After all 6 tasks complete:

1. **Targeted test runs**:
   - npx vitest run src/App.test.tsx
   - npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
   - npx tsc --noEmit -p .

2. **Full suite green** (no Phase 7 regressions):
   - npx vitest run → expect ~232 tests passing (226 Phase 7 + 1 App.test + 4 SlashCommandPopup + 1 from 08-02/08-03)

3. **Hard "Do Not Touch" boundaries**:
   - git diff src/main/runtime.ts → 0 changes
   - git diff src/main/llm.ts → 0 changes
   - git diff src/main/workflow-runtime.ts → 0 changes
   - git diff src/shared/types.ts → 0 changes (CommandSource union unchanged)
   - git status patches/ → clean

4. **Manual smoke** (electron dev build):
   - npm run dev → start app → open project → type `/g` in textarea
   - Verify 7 different command sources render in distinct text colors (system blue, skill:global violet, etc.)
   - Verify Chinese CJK name (e.g. add a skill named `代码审查`) → type `/代` → matches
   - Verify emoji variation selector: add a command with name `party` + badge containing U+FE0F → type with and without → both match
   - macOS: enable Pinyin → type `/` in textarea → IME candidate covers popup → press Esc → candidate dismisses → popup still visible
</verification>

<success_criteria>
1. SC-1: 7 source badges each render a distinct text-* color class via SOURCE_TEXT_COLOR lookup map; no other JSX change
2. SC-2: CJK names match via NFKC + variation-selector removal; emoji with U+FE0F and without both match identically
3. SC-3: SlashCommandPopup accepts `loading` prop; when 'slow', renders 1-row Skeleton at top of Command.List with data-testid='mcp-skeleton'
4. SC-4: IME z-index known-issue comment present in SlashCommandPopup.tsx; documents macOS limitation + Esc-once workaround
5. SC-5: <Toaster /> mounted in App.tsx — Phase 6/7 silent toasts now visible; 1 App.test.tsx verifies
6. All 226 Phase 7 tests remain green + 4 new Phase 8 tests pass
7. TypeScript compiles cleanly (npx tsc --noEmit -p . exits 0)
</success_criteria>

<output>
Create .planning/phases/08-polish-differentiators/08-01-SUMMARY.md when done.
</output>
