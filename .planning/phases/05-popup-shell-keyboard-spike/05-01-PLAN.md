---
phase: 05-popup-shell-keyboard-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/renderer/src/components/ui/popover.tsx
  - src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
  - src/renderer/src/components/ChatArea/ChatArea.tsx
  - src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
autonomous: true
requirements:
  - SLASH-01
  - SLASH-02
user_setup: []

must_haves:
  truths:
    - User types `/` in the Master Agent chat textarea and a popup appears anchored to the form
    - User continues typing after `/` and the popup list filters by case-insensitive substring match on the command name (D-05: substring + case-insensitive + NFKC-normalized; D-06: name-only)
    - User presses ArrowDown/ArrowUp to cycle the highlighted row (wraps at top/bottom)
    - User presses Enter or Tab and the highlighted command text plus a trailing space is inserted into the textarea and the popup closes (D-07: Tab and Enter identical — insert text + close + preventDefault, do NOT execute)
    - User presses Escape and the popup closes with focus remaining in the textarea
    - User presses Backspace when the textarea contains only `/` and the popup closes
    - During IME composition the popup does NOT open from `onChange`
    - The 3 hardcoded command names `/goal`, `/context`, `/plan` always render on first open with NO description, NO source badge, NO keybind hint (D-01: name-only; D-02: 3 placeholders always)
    - An empty filter result renders the hint line `无匹配命令 / 试试输入 /goal · /context · /plan 查看可用命令` (D-03 + UI-SPEC)
  artifacts:
    - path: src/renderer/src/components/ui/popover.tsx
      provides: shadcn-style wrapper around @radix-ui/react-popover primitives
      exports: ["Popover", "PopoverTrigger", "PopoverAnchor", "PopoverContent"]
    - path: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
      provides: cmdk <Command> renderer with manual NFKC filter, ↑↓/Enter/Tab/Esc handleKeyDown imperative API
      exports: ["SlashCommandPopup", "SlashCommandPopupHandle"]
    - path: src/renderer/src/components/ChatArea/ChatArea.tsx
      provides: slashOpen useState, PopoverAnchor wrapping the form, extended handleKeyDown, handleSlashSelect
      contains: "slashOpen"
    - path: src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
      provides: 8 foundational unit tests covering open/close, filter, Tab/Enter, D-03, D-04, focus retention
      min_lines: 120
  key_links:
    - from: src/renderer/src/components/ChatArea/ChatArea.tsx
      to: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
      via: "ref={slashRef} + slashRef.current.handleKeyDown(nativeEvent)"
      pattern: "slashRef\\.current\\.handleKeyDown"
    - from: src/renderer/src/components/ChatArea/ChatArea.tsx
      to: src/renderer/src/components/ui/popover.tsx
      via: "Popover open={slashOpen} onOpenChange={setSlashOpen} wraps PopoverAnchor asChild={<form>}"
      pattern: "PopoverAnchor"
    - from: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
      to: cmdk Command
      via: "value={selectedValue} onValueChange={setSelectedValue} shouldFilter={false}"
      pattern: "shouldFilter=\\{false\\}"
---

<objective>
Wire up the `/` command popup shell on the existing bare `<textarea>` in `ChatArea.tsx` using `cmdk@1.1.1` (filter + list rendering) and `@radix-ui/react-popover@1.1.15` (positioning + focus management). This is a SPIKE — deliver the popup UI layer, the keyboard bridge to the textarea, and the IME safety gates. Do NOT touch dispatcher, IPC, plugin sources, or any main-process code. D-01..D-07 + C-01..C-03 are locked.

Purpose: Phase 6 will populate the popup with 4 plugin sources and wire the dispatcher on top of this exact shell. Locking the shell + keyboard contract now is the load-bearing spike that rejects `unstable_useSlashCommandAdapter` and proves the cmdk + Radix Popover path works on the bare textarea.

Output: 4 modified/created files (popover shim, SlashCommandPopup, ChatArea wire, test file with 8 foundational tests) + `package.json` dep entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-popup-shell-keyboard-spike/05-CONTEXT.md
@.planning/phases/05-popup-shell-keyboard-spike/05-UI-SPEC.md
@.planning/phases/05-popup-shell-keyboard-spike/05-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

# Source files the executor must read before editing
@src/renderer/src/components/ChatArea/ChatArea.tsx (1097 lines — focus on lines 121-218 for state declarations, 611-673 for handleSend/handleKeyDown, 990-1091 for the composer form)
@src/renderer/src/components/ui/dropdown-menu.tsx (shim pattern reference)
@src/renderer/src/components/ChatArea/MessageItem.test.ts (vitest test pattern)
@vitest.config.ts
@package.json
</context>

<interfaces>
From src/renderer/src/components/ChatArea/ChatArea.tsx (state at lines 137-147, refs at 143-149):
- `const [inputVal, setInputVal] = useState<string>('');` — composer textarea value
- `const isComposingRef = useRef(false);` — IME composition flag (must be consulted in slashOpen gate)
- `const justFinishedComposingRef = useRef(false);` — 200ms post-composition window
- `const compositionEndTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);` — timer for the 200ms window
- `const isComposingKeyEvent = (e: React.KeyboardEvent<HTMLTextAreaElement>) => boolean;` — helper at line 653
- `const handleSend = async (e?: React.FormEvent) => Promise<void>;` — at line 611; SlashCommandPopup must NOT call this (D-07)

From src/renderer/src/components/ChatArea/ChatArea.tsx (composer form at lines 1002-1091):
- `<form onSubmit={(e) => e.preventDefault()} className="relative z-10 flex flex-col bg-[var(--color-bg-surface)] border border-[var(--color-border)] focus-within:border-[var(--color-accent)] focus-within:ring-1 focus-within:ring-[var(--color-accent)]/20 rounded-xl p-3 transition-all shadow-lg">` — line 1002; wrap this in `<PopoverAnchor asChild>`
- `<textarea value={inputVal} onChange={(e) => setInputVal(e.target.value)} onCompositionStart={handleCompositionStart} onCompositionEnd={handleCompositionEnd} onKeyDown={handleKeyDown} placeholder="给 Master Agent 发送消息..." rows={2} className="w-full bg-transparent text-[var(--color-text-primary)] placeholder:text-[var(--color-text-muted)] outline-none resize-none text-sm min-h-[56px] max-h-40 py-1" />` — line 1004-1013
- `const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>) => { ... }` — at line 658; extend this with the slashOpen branch

From src/renderer/src/components/ui/dropdown-menu.tsx (shim pattern):
- `import * as DropdownMenuPrimitive from "@radix-ui/react-dropdown-menu";` then forwardRef wrappers for each sub-component
- z-50 + border + bg-[var(--color-bg-sidebar)] for Content; portal via `<DropdownMenuPrimitive.Portal>` for Content
- Display names via `DropdownMenuPrimitive.X.displayName`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install cmdk + @radix-ui/react-popover, create popover.tsx shim</name>
  <files>package.json, src/renderer/src/components/ui/popover.tsx</files>
  <read_first>
    - src/renderer/src/components/ui/dropdown-menu.tsx (shim pattern reference; line 1-5 imports + line 6-15 Root/Trigger/Group/Portal aliases + line 56-71 Content with Portal + line 74-90 Item forwardRef)
    - package.json (existing dependencies block lines 17-49; @radix-ui/react-dropdown-menu is the template for naming)
    - .planning/research/STACK.md (cmdk + @radix-ui/react-popover versions confirmed; @radix-ui/react-popover is already in node_modules as transitive dep)
  </read_first>
  <action>
    1. Run `npm install --save cmdk@^1.1.1 @radix-ui/react-popover@^1.1.15` from /Users/suntc/project/CDF. Verify both lines appear in `package.json` `dependencies` block (cmdk and @radix-ui/react-popover; sort to maintain dependency order; cmdk alphabetically belongs after clsx, @radix-ui/react-popover belongs with the other @radix-ui/* entries near line 29-32). Verify `node_modules/cmdk/package.json` exists with `"version": "1.1.1"` and `node_modules/@radix-ui/react-popover/package.json` exists with `"version": "1.1.15"`.
    2. Create `src/renderer/src/components/ui/popover.tsx` that mirrors the dropdown-menu.tsx shim pattern. Imports: `* as React from "react"`, `* as PopoverPrimitive from "@radix-ui/react-popover"`, `cn` from `@/lib/utils`. Exported sub-components (each aliased from `PopoverPrimitive` for Root/Trigger/Anchor; Content wrapped in `PopoverPrimitive.Portal` like dropdown-menu Content; the same for non-primitive):
       - `Popover` = `PopoverPrimitive.Root` (named export)
       - `PopoverTrigger` = `PopoverPrimitive.Trigger`
       - `PopoverAnchor` = `PopoverPrimitive.Anchor`
       - `PopoverContent` = forwardRef wrapping `PopoverPrimitive.Portal` > `PopoverPrimitive.Content`. Default `sideOffset = 8` (8px gap from anchor per UI-SPEC sm spacing token). Default className via `cn(...)`: `"z-50 w-72 rounded-md border border-[var(--color-border)] bg-[var(--color-bg-surface)] p-1 text-[var(--color-text-primary)] shadow-lg outline-none"`. Set `displayName = PopoverPrimitive.Content.displayName`.
       - `PopoverClose` = `PopoverPrimitive.Close`
       Do NOT add animation classes (no data-[state=open]:animate-in) — the existing ChatArea CSS is not Tailwind-animate-aware. Set z-index to 50 (UI-SPEC requires ≥ 50; TodoList is z-10).
  </action>
  <verify>
    <automated>
      cat /Users/suntc/project/CDF/package.json | grep -E '"cmdk"|"@radix-ui/react-popover"' && \
      cat /Users/suntc/project/CDF/node_modules/cmdk/package.json | grep '"version"' && \
      cat /Users/suntc/project/CDF/node_modules/@radix-ui/react-popover/package.json | grep '"version"' && \
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'popover\.tsx|error TS' | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` block contains exactly the lines `"cmdk": "^1.1.1",` and `"@radix-ui/react-popover": "^1.1.15",`
    - `node_modules/cmdk/package.json` field `"version"` is `"1.1.1"`
    - `node_modules/@radix-ui/react-popover/package.json` field `"version"` is `"1.1.15"`
    - `npx tsc --noEmit` exits 0 with no errors mentioning `popover.tsx`
    - `grep -E "export \\{" /Users/suntc/project/CDF/src/renderer/src/components/ui/popover.tsx` returns lines exporting `Popover`, `PopoverTrigger`, `PopoverAnchor`, `PopoverContent`, `PopoverClose`
  </acceptance_criteria>
  <done>deps installed, popover shim compiles and exports all 5 sub-components with z-50 + bg-[var(--color-bg-surface)] + border-[var(--color-border)] styling matching the dropdown-menu convention</done>
</task>

<task type="auto">
  <name>Task 2: Create SlashCommandPopup.tsx with cmdk + manual NFKC filter + keyboard handleKeyDown bridge</name>
  <files>src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx</files>
  <read_first>
    - .planning/phases/05-popup-shell-keyboard-spike/05-CONTEXT.md (D-01..D-07 + C-01..C-03)
    - .planning/phases/05-popup-shell-keyboard-spike/05-UI-SPEC.md (Interaction Contract section 1-4, Copywriting Contract for hint text)
    - .planning/research/PITFALLS.md (P6 5 case study: `.` / CJK / `//` / description / selectedIndex wrap)
    - node_modules/cmdk/dist/index.d.ts (verify Command, Command.List, Command.Item, Command.Empty exports and prop signatures)
  </read_first>
  <action>
    Create `src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx`. Structure:

    1. Imports: `forwardRef, useImperativeHandle, useMemo, useRef, useState, useEffect` from `react`; `Command` namespace from `"cmdk"` (verified in node_modules); `cn` from `"@/lib/utils"`.

    2. Hardcoded commands array (constant at module scope, NOT inside the component — no re-creation per render):
       ```
       const SYSTEM_COMMANDS: ReadonlyArray<{ value: string; label: string }> = [
         { value: '/goal', label: '/goal' },
         { value: '/context', label: '/context' },
         { value: '/plan', label: '/plan' },
       ];
       ```
       Per D-01: label is the command name ONLY, no description, no source badge, no keybind.

    3. Filter helper (module-scope pure function):
       ```
       const filterCommands = (query: string, items: typeof SYSTEM_COMMANDS): typeof SYSTEM_COMMANDS => {
         const normalized = query.normalize('NFKC').toLowerCase();
         if (!normalized) return items;
         return items.filter((c) => c.value.normalize('NFKC').toLowerCase().includes(normalized));
       };
       ```
       Per D-05 (substring + case-insensitive + NFKC) + D-06 (filter searches only name). Both query and item value are NFKC-normalized. This is the implementation that addresses PITFALLS P6a (`.` literal char), P6b (CJK), P6c (`//` substring).

    4. Exported handle interface + props:
       ```
       export interface SlashCommandPopupHandle {
         handleKeyDown: (e: { key: string; preventDefault: () => void }) => boolean;
         // returns true if event was consumed, false to let parent re-dispatch
       }
       export interface SlashCommandPopupProps {
         query: string;            // text after the leading `/` (ChatArea passes inputVal.slice(1))
         onSelect: (command: string) => void;  // Tab/Enter picked this command; does NOT execute
         onClose: () => void;      // Esc
       }
       ```

    5. Component body (forwardRef):
       - `const [selectedValue, setSelectedValue] = useState<string>(SYSTEM_COMMANDS[0].value);` — D-04 initial highlight on first row.
       - `const filtered = useMemo(() => filterCommands(query, SYSTEM_COMMANDS), [query]);`
       - `useEffect(() => { if (filtered[0]) setSelectedValue(filtered[0].value); }, [query]);` — D-04 reset to top on filter change; also addresses PITFALLS P6e (selectedIndex bounds — when filter reduces, selectedValue resets to a valid item).
       - `useImperativeHandle(ref, () => ({ handleKeyDown: (e) => { if (filtered.length === 0) { if (e.key === 'Escape' || e.key === 'Enter' || e.key === 'Tab') { e.preventDefault(); onClose(); return true; } return false; } const idx = filtered.findIndex((c) => c.value === selectedValue); if (e.key === 'ArrowDown') { e.preventDefault(); setSelectedValue(filtered[(idx + 1) % filtered.length].value); return true; } if (e.key === 'ArrowUp') { e.preventDefault(); setSelectedValue(filtered[(idx - 1 + filtered.length) % filtered.length].value); return true; } if (e.key === 'Enter' || e.key === 'Tab') { e.preventDefault(); onSelect(selectedValue); return true; } if (e.key === 'Escape') { e.preventDefault(); onClose(); return true; } return false; } }), [filtered, selectedValue, onSelect, onClose]);`
       - JSX: `<Command value={selectedValue} onValueChange={setSelectedValue} shouldFilter={false} label="Slash commands" className="w-full"><Command.List className="max-h-64 overflow-y-auto p-0">{filtered.map((c) => (<Command.Item key={c.value} value={c.value} onSelect={() => onSelect(c.value)} className="flex cursor-default select-none items-center gap-2 rounded-sm px-2 py-1.5 text-[13px] text-[var(--color-text-primary)] outline-none data-[selected=true]:bg-[var(--color-accent)]/15 data-[selected=true]:text-[var(--color-text-primary)]">{c.label}</Command.Item>))}<Command.Empty className="px-2 py-2 text-[12px] text-[var(--color-text-muted)]"><div>无匹配命令</div><div>试试输入 /goal · /context · /plan 查看可用命令</div></Command.Empty></Command.List></Command>` — D-03 hint text from UI-SPEC Copywriting Contract; 13px body from UI-SPEC Typography; accent-15 highlight per UI-SPEC color token #7c3aed.
       - displayName: `'SlashCommandPopup'`.

    6. Export `SlashCommandPopup` (the forwardRef result), `SlashCommandPopupHandle`, `SlashCommandPopupProps`. The filter helper and SYSTEM_COMMANDS are module-private (not exported).

    Constraint reminders (anti-patterns to avoid): do NOT use fuzzy filter; do NOT split query by `.` or `/`; do NOT call `onSelect` AND `handleSend`; do NOT add description or source badge columns (D-01).
  </action>
  <verify>
    <automated>
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'SlashCommandPopup|error TS' | head -5 && \
      grep -E 'shouldFilter=\\{false\\}|NFKC|ArrowDown' /Users/suntc/project/CDF/src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no errors referencing SlashCommandPopup
    - The file contains the string `shouldFilter={false}` (manual filter, not cmdk's default)
    - The file contains `.normalize('NFKC')` (NFKC normalization per D-05)
    - The file contains the literal hint text `无匹配命令` and `试试输入 /goal · /context · /plan 查看可用命令` (D-03 + UI-SPEC Copywriting Contract)
    - The file exports `SlashCommandPopup`, `SlashCommandPopupHandle`, `SlashCommandPopupProps`
    - The file does NOT contain the strings `description`, `badge`, `skill:`, `workflow:`, `mcp:` (D-01: no description/source badge)
    - The file does NOT contain `fuse`, `fuzzy`, or `Fuse` (no fuzzy search per spike)
    - `useImperativeHandle` returns a `handleKeyDown` that handles `ArrowUp`, `ArrowDown`, `Enter`, `Tab`, `Escape` keys with `e.preventDefault()` before mutating state
  </acceptance_criteria>
  <done>SlashCommandPopup component compiles, exports handle + props, uses cmdk with manual NFKC filter, implements ↑↓ wrap + Tab/Enter identical + Esc, and renders D-03 hint via Command.Empty</done>
</task>

<task type="auto">
  <name>Task 3: Wire into ChatArea.tsx + create SlashCommandPopup.test.tsx with 8 foundational tests</name>
  <files>src/renderer/src/components/ChatArea/ChatArea.tsx, src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx</files>
  <read_first>
    - src/renderer/src/components/ChatArea/ChatArea.tsx lines 1-22 (imports — add Popover + SlashCommandPopup import), 121-149 (state declarations — add slashOpen useState + slashRef), 611-622 (handleSend — do NOT modify this; D-07), 624-673 (IME refs and handleKeyDown — extend the latter), 1002-1091 (composer form — wrap in PopoverAnchor and add PopoverContent)
    - src/renderer/src/components/ChatArea/MessageItem.test.ts (vitest + @testing-library import pattern; `describe` + `it` + `expect`)
    - vitest.config.ts (jsdom + globals + @ alias)
    - .planning/phases/05-popup-shell-keyboard-spike/05-VALIDATION.md (test cases 5-01-03, 5-01-04, 5-01-05, 5-01-06, 5-01-07, 5-01-08, 5-01-09, 5-01-10, 5-01-11)
  </read_first>
  <action>
    **Modify ChatArea.tsx in 5 surgical insertion points (no other edits):**

    1. **Imports** (line 1-22 area): Add 2 imports: `import { Popover, PopoverAnchor, PopoverContent } from '@/components/ui/popover';` and `import { SlashCommandPopup, SlashCommandPopupHandle } from '@/components/SlashCommand/SlashCommandPopup';`

    2. **State** (after line 138 `composerModelSelectorOpen`): Add `const [slashOpen, setSlashOpen] = useState(false);`

    3. **Refs** (after line 147 `compositionEndTimerRef`): Add `const slashRef = useRef<SlashCommandPopupHandle>(null);`

    4. **Callbacks** (after `isComposingKeyEvent` at line 656, before `handleKeyDown`): Add the handleSlashSelect + a slashOpen query helper. Use `useCallback` (or const arrow if not yet imported — do NOT add useCallback import if not used elsewhere; check line 1 imports; if not there, add to React imports):
       ```
       const handleSlashSelect = (cmd: string) => {
         setInputVal(cmd + ' ');
         setSlashOpen(false);
       };
       ```
       D-07 enforcement: this does NOT call handleSend. It only inserts text and closes the popup.

    5. **onChange** (line 1006): Replace `onChange={(e) => setInputVal(e.target.value)}` with a handler that also manages slashOpen per UI-SPEC Trigger Condition:
       ```
       onChange={(e) => {
         const value = e.target.value;
         setInputVal(value);
         if (isComposingRef.current) return; // PITFALLS P13: IME composition guard
         const shouldOpen = value.startsWith('/') && !value.includes(' ') && value.length <= 32;
         setSlashOpen(shouldOpen);
       }}
       ```

    6. **handleKeyDown** (line 658-673): Prepend a slashOpen branch BEFORE the existing Enter handling:
       ```
       const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>) => {
         if (isComposingKeyEvent(e)) return;
         if (slashOpen) {
           if (e.key === 'Backspace' && inputVal === '/') {
             e.preventDefault();
             setSlashOpen(false);
             return;
           }
           const handled = slashRef.current?.handleKeyDown(e.nativeEvent) ?? false;
           if (handled) return;
         }
         if (e.key === 'Enter' && !e.shiftKey) { ... existing ... }
       };
       ```
       The `e.nativeEvent` carries `key` and `preventDefault()`, both of which the SlashCommandPopup handle reads. Shift+Enter naturally falls through to the original code path (no SlashCommandPopup branch for it; it does newline).

    7. **Composer form** (line 1002-1091): Wrap the entire `<form>` in a Popover. The form on line 1002 currently is `<form onSubmit={(e) => e.preventDefault()} className="...">`. Replace the form open + close tags with:
       ```
       <Popover open={slashOpen} onOpenChange={setSlashOpen} modal={false}>
         <PopoverAnchor asChild>
           <form onSubmit={(e) => e.preventDefault()} className="...existing classes...">
             ... existing textarea + toolbar inside ...
           </form>
         </PopoverAnchor>
         <PopoverContent
           onOpenAutoFocus={(e) => e.preventDefault()}
           align="start"
           side="top"
           sideOffset={8}
           className="w-[var(--radix-popover-anchor-width)]"
         >
           <SlashCommandPopup
             ref={slashRef}
             query={inputVal.startsWith('/') ? inputVal.slice(1) : ''}
             onSelect={handleSlashSelect}
             onClose={() => setSlashOpen(false)}
           />
         </PopoverContent>
       </Popover>
       ```
       Position: `side="top"` because the form is at `bottom-0` of the viewport; above is the only visible space. `modal={false}` to keep textarea interactive. `onOpenAutoFocus={e => e.preventDefault()}` per PITFALLS P1 (focus steal prevention). Use `var(--radix-popover-anchor-width)` so the popover matches the form's width.

    8. **NO changes to**: `handleSend` (line 611), `welcomeModelSelectorOpen`/`composerModelSelectorOpen` (line 138-139), `TodoList` block (line 995-1001), `useChatScroll` (line 151), the form's existing textarea + toolbar classes, the `<form onSubmit>` itself, the `<div className="absolute bottom-0">` wrapper at line 991.

    **Create `src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx`** with 8 foundational tests using vitest + @testing-library/react. The test file builds a minimal harness component (NOT importing ChatArea — that's too coupled to stores; the test verifies the cmdk + wiring contract):
    - Test harness (defined inside the test file): `<TestHarness>` component holds `inputVal`/`slashOpen` state, renders `<Popover open={slashOpen} ...>` + `<PopoverAnchor asChild><textarea .../></PopoverAnchor>` + `<PopoverContent><SlashCommandPopup ref={slashRef} query={inputVal.slice(1)} onSelect={handleSlashSelect} onClose={...} /></PopoverContent>`. Wires onChange + handleKeyDown the same way ChatArea does (copy the 5 lines from Task 3 step 5+6 above into the harness). The harness also tracks an `inputVal` change counter or exposes inputVal via a ref so tests can assert text was inserted.
    - Use `render(<TestHarness />)` from `@testing-library/react`; query via `screen.getByRole('textbox')` for the textarea; query popup rows via `screen.getByText('/goal')`; query the empty hint via `screen.getByText('无匹配命令')`.
    - Use `fireEvent.change(textarea, { target: { value: '/' } })` and `fireEvent.keyDown(textarea, { key: 'Enter' })`.

    The 8 tests (matches validation 5-01-03, 5-01-04, 5-01-05, 5-01-06, 5-01-07 partial, 5-01-08 partial, 5-01-09, 5-01-10, 5-01-11):
    1. `it('keeps textarea focus when popup opens', ...)` — fireEvent.change to '/', assert `document.activeElement === textarea` (PITFALLS P1 / 5-01-03)
    2. `it('opens on slash', ...)` — type '/', assert `/goal` is in the document (5-01-06)
    3. `it('closes when value does not start with /', ...)` — type 'hello', assert no `/goal` in document (5-01-04 + 5-01-05)
    4. `it('filters case-insensitive on command name', ...)` — type '/GO', assert `/goal` still visible (D-05 + 5-01-07)
    5. `it('inserts command text and closes popup on Enter', ...)` — type '/', ArrowDown (cycles to /context), Enter, assert inputVal === '/context ' AND `/goal` not in document (5-01-09)
    6. `it('inserts command text and closes popup on Tab (identical to Enter)', ...)` — type '/', Tab, assert inputVal === '/goal ' (D-07 + 5-01-09)
    7. `it('shows first row highlighted on initial open', ...)` — type '/', assert `/goal` has `data-selected="true"` (D-04 + 5-01-10)
    8. `it('shows hint line when filter matches nothing', ...)` — type '/zzz', assert screen has text `无匹配命令` (D-03 + 5-01-11)

    **Constraint reminders:** Do NOT add `vite-plugin-testing-library` or any new dev dep; vitest + jsdom + @testing-library/react@16.0.0 are already in package.json. Do NOT touch `runtime.ts` / `llm.ts` / `workflow-runtime.ts` (Hard Do Not Touch list). Do NOT call `handleSend` from the slash path (D-07).
  </action>
  <verify>
    <automated>
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'ChatArea|SlashCommandPopup\\.test|error TS' | head -5 && \
      grep -nE 'PopoverAnchor|slashOpen|setSlashOpen|handleSlashSelect|slashRef' /Users/suntc/project/CDF/src/renderer/src/components/ChatArea/ChatArea.tsx | head -10 && \
      npx vitest run /Users/suntc/project/CDF/src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no errors referencing ChatArea or SlashCommandPopup.test
    - `grep "slashOpen" ChatArea.tsx` returns ≥ 5 lines (state declaration, onChange gate, handleKeyDown check, Popover open prop, PopoverContent wiring)
    - `grep "PopoverAnchor" ChatArea.tsx` returns ≥ 1 line (form wrap)
    - `grep "handleSlashSelect" ChatArea.tsx` returns ≥ 2 lines (declaration + onSelect prop)
    - `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` exits 0 and prints `8 passed`
    - The 8 test names match exactly: `keeps textarea focus when popup opens`, `opens on slash`, `closes when value does not start with /`, `filters case-insensitive on command name`, `inserts command text and closes popup on Enter`, `inserts command text and closes popup on Tab (identical to Enter)`, `shows first row highlighted on initial open`, `shows hint line when filter matches nothing`
    - `handleSend` (line 611) is unmodified (D-07): no `handleSend` reference appears inside `handleSlashSelect`
  </acceptance_criteria>
  <done>ChatArea wires PopoverAnchor + PopoverContent + slashOpen useState + handleSlashSelect + extended handleKeyDown; the 8 foundational tests pass green; type check is clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer-input | untrusted user keystrokes (CJK, IME, control chars) cross here into ChatArea onChange + onKeyDown |
| renderer-popover | SlashCommandPopup receive `query` (derived from inputVal) and `onSelect` callback that mutates inputVal — controlled by parent, not user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-P1 | Tampering (focus) | PopoverContent autoFocus | mitigate | `onOpenAutoFocus={e => e.preventDefault()}` on PopoverContent in Task 3 step 7 — verified by test `keeps textarea focus when popup opens` |
| T-05-P6a | Tampering (filter) | SlashCommandPopup filter | mitigate | Manual `String#includes` with NFKC normalize, no `split('.')`/`split('/')` — verified by tests `filters case-insensitive on command name` and `shows hint line when filter matches nothing` |
| T-05-P6b | Information disclosure | SlashCommandPopup filter | accept | CJK skills not in scope until Phase 6; the 3 hardcoded commands are ASCII, NFKC is a no-op for them — manual filter still NFKC-normalizes for Phase 6 forward-compat |
| T-05-P6e | Tampering (state) | selectedValue wrap | mitigate | `useEffect` resets `selectedValue` to `filtered[0].value` on query change; `useImperativeHandle` uses `(idx + 1) % filtered.length` and `(idx - 1 + filtered.length) % filtered.length` for wrap — verified by Phase 6 Plan 02 tests `arrow navigation` and `selectedIndex bounds` |
| T-05-P13 | Tampering (IME) | ChatArea onChange gate | mitigate | `if (isComposingRef.current) return;` BEFORE the slashOpen mutation in Task 3 step 5; existing 200ms `justFinishedComposingRef` window preserved for subsequent phases — verified by Phase 6 Plan 02 test `ime safe` |
| T-05-SC | Tampering (supply chain) | `cmdk@^1.1.1` + `@radix-ui/react-popover@^1.1.15` install | accept | Both packages verified via `npm view` in research/STACK.md (lines 325-326): cmdk peer `react ^18 || ^19` confirmed; @radix-ui/react-popover peer `react ^16.8 || ^17 || ^18 || ^19` confirmed; @radix-ui/react-popover 1.1.15 already in node_modules as transitive dep from @radix-ui/react-dropdown-menu. No npm install of unknown packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` exits 0 with 8 passed
- `cat package.json | grep -E '"cmdk"|"@radix-ui/react-popover"'` returns 2 lines
- `npm test` does not regress any pre-existing test
- `grep "handleSend" src/renderer/src/components/ChatArea/ChatArea.tsx | grep -v "const handleSend"` returns 0 lines (D-07: slash path does not invoke handleSend)
</verification>

<success_criteria>
- All 5 must_haves.truths verifiable by manual interaction in dev build (start app, type `/` in chat input)
- All 5 must_haves.artifacts exist at the listed paths and pass their listed exports/contains/min_lines checks
- 8 foundational tests green; type check clean; package.json updated; node_modules contains both packages at verified versions
- ROADMAP.md Phase 5 plan list updated to mark 05-01 as ready
</success_criteria>

<output>
Create `.planning/phases/05-popup-shell-keyboard-spike/05-01-SUMMARY.md` when done
</output>
