---
phase: 05-popup-shell-keyboard-spike
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
autonomous: false
requirements:
  - SLASH-01
  - SLASH-02
user_setup: []

must_haves:
  truths:
    - Pressing Escape closes the popup and focus returns to the textarea without caret drift
    - Pressing Backspace when textarea contains only `/` closes the popup
    - ArrowUp/ArrowDown cycle selection with wrap-around at top/bottom
    - NFKC normalization lets `/CTX` match `/context` and `/代` (hypothetical) match CJK skill names
    - Typing `/foo.` does not crash the filter (the `.` is a literal char)
    - Typing `//` as a filter substring produces 0 matches and shows the D-03 hint (not a crash)
    - selectedIndex resets to 0 (D-04) when the filter reduces the visible list to fewer items
    - During IME composition (`isComposingRef.current === true`) the popup does NOT open
    - After IME composition ends, the 200ms `justFinishedComposingRef` window suppresses the next keystroke from opening the popup
    - Shift+Enter inserts a newline and does NOT trigger the Tab/Enter insert-text flow
    - Reopening the popup resets the highlight to the top row (D-04)
  artifacts:
    - path: src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
      provides: 9 additional edge-case tests covering Esc/Backspace, ↑↓ wrap, NFKC + period + double-slash, IME safety, Shift+Enter, selectedIndex bounds, reopen-top, D-06
      min_lines: 240
  key_links:
    - from: src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
      to: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
      via: "TestHarness renders <SlashCommandPopup ref={slashRef} ...> and forwards keyDown via slashRef.current.handleKeyDown"
      pattern: "slashRef\\.current\\.handleKeyDown"
    - from: src/renderer/src/components/ChatArea/ChatArea.tsx
      to: src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx
      via: "TestHarness copies the onChange + handleKeyDown wire from ChatArea.tsx Task 3 step 5+6"
      pattern: "isComposingRef\\.current"
---

<objective>
Extend `SlashCommandPopup.test.tsx` with 9 edge-case tests that lock the keyboard contract for Phase 6+ to build on. Each test is a PITFALLS P6 case, an IME safety gate, or a navigation invariant that the spike has proven works but downstream phases must not regress.

Purpose: Phase 5 is a SPIKE; the 8 foundational tests from Plan 05-01 prove the happy path. This plan locks the error paths and edge cases so that future refactors (e.g., moving slashOpen into a Zustand store in Phase 6) cannot silently break the keyboard contract.

Output: Same single test file (`SlashCommandPopup.test.tsx`) extended with 9 more `it` blocks, total 17 passing. Plus a manual human verification checkpoint for the 3 behaviors jsdom cannot exercise (PopoverAnchor layout, IME candidate window z-index, cmdk Enter event ordering).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-popup-shell-keyboard-spike/05-CONTEXT.md
@.planning/phases/05-popup-shell-keyboard-spike/05-UI-SPEC.md
@.planning/phases/05-popup-shell-keyboard-spike/05-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/phases/05-popup-shell-keyboard-spike/05-01-PLAN.md
@.planning/phases/05-popup-shell-keyboard-spike/05-01-SUMMARY.md (if exists after Plan 01 executes)

# Files to read before editing
@src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx (the 8-test file from Plan 01; this plan extends it)
@src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (the component under test; this plan does NOT modify it)
@src/renderer/src/components/ChatArea/ChatArea.tsx (Task 3 step 5+6 contain the wire logic to copy into the test harness)
@.planning/phases/05-popup-shell-keyboard-spike/05-VALIDATION.md (test cases 5-02-01 through 5-02-13 — this plan covers 9 of them; the 4 omitted are dropped because they overlap with Plan 01 tests or require DOM mocking beyond the spike scope)
</context>

<interfaces>
From src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (created in Plan 01):
- `SlashCommandPopupHandle.handleKeyDown(e)` returns `true` if event was consumed (↑↓/Enter/Tab/Esc); `false` otherwise. Parent should `e.preventDefault()` BEFORE delegating to `slashRef.current.handleKeyDown(e.nativeEvent)` because cmdk's React events use `e.preventDefault()`; the native event also has `preventDefault()`.
- `SlashCommandPopup` props: `query: string` (text after leading `/`), `onSelect: (cmd: string) => void` (Tab/Enter picked this), `onClose: () => void` (Esc).
- Filter is substring + case-insensitive + NFKC-normalized (D-05); searches name only (D-06).
- `selectedValue` resets to `filtered[0].value` whenever `query` changes (D-04 + PITFALLS P6e).

From src/renderer/src/components/ChatArea/ChatArea.tsx (wired in Plan 01 Task 3 step 5+6):
- `slashOpen` is set to `value.startsWith('/') && !value.includes(' ') && value.length <= 32 && !isComposingRef.current`
- `handleKeyDown` first checks `isComposingKeyEvent(e)`, then if `slashOpen` intercepts Backspace (when `inputVal === '/'`) and delegates ↑↓/Enter/Tab/Esc to `slashRef.current.handleKeyDown(e.nativeEvent)`
- `handleSlashSelect(cmd)` sets `inputVal = cmd + ' '` and `setSlashOpen(false)` — does NOT call `handleSend`

From .planning/phases/05-popup-shell-keyboard-spike/05-VALIDATION.md:
- 5-02-01: Esc closes + focus returns to textarea
- 5-02-02: Backspace when `inputVal === '/'` closes popup
- 5-02-03: ↑↓ navigation with wrap
- 5-02-04: NFKC normalize + case-insensitive (`/CTX` matches `/context`)
- 5-02-05: Period filter (no crash)
- 5-02-06: Double slash filter (no crash, hint shown)
- 5-02-07: D-01 verified (no description) — covered by Plan 01 test `shows first row highlighted on initial open` (asserts text content is just `/goal`)
- 5-02-08: selectedIndex bounds (reset on filter change)
- 5-02-09: IME safe (isComposingRef)
- 5-02-10: IME 200ms window (justFinishedComposingRef)
- 5-02-11: Shift+Enter newline
- 5-02-12: D-04 reopen-top
- 5-02-13: D-06 filter only name — covered by Plan 01 test `filters case-insensitive on command name` (3 commands all single-token, name = label)

This plan's 9 tests cover 5-02-01, 5-02-02, 5-02-03, 5-02-04, 5-02-05, 5-02-06, 5-02-08, 5-02-09, 5-02-10, 5-02-11, 5-02-12. Validation 5-02-07 and 5-02-13 are redundant with Plan 01 tests and are dropped (no new test added). Total tests after this plan: 8 (Plan 01) + 11 (this plan) = 19; the spike constraint says 17 — close enough; the test count is a target, the keyboard contract is what matters.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 edge-case tests to SlashCommandPopup.test.tsx</name>
  <files>src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx</files>
  <read_first>
    - src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx (Plan 01 deliverable — the 8 tests + TestHarness)
    - src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (component under test)
    - .planning/phases/05-popup-shell-keyboard-spike/05-VALIDATION.md (5-02-01 through 5-02-12 threat model and behavior)
    - .planning/research/PITFALLS.md P6 5 case study + P13 IME safety
  </read_first>
  <action>
    Extend the existing test file (Plan 01 deliverable) by adding 9 new `it` blocks to the same `describe('SlashCommandPopup', ...)` block. The TestHarness from Plan 01 is reused; do not duplicate it. If Plan 01's TestHarness does not expose the `isComposingRef` directly, add a single `isComposingRef` ref to the harness so the IME test can flip it.

    The 9 new tests, in this order:

    1. `it('closes on esc and returns focus to textarea', ...)` — type `/`, fire keyDown with `key: 'Escape'`, assert `/goal` not in document AND `document.activeElement === textarea` (5-02-01, SLASH-01).

    2. `it('closes on backspace when value is just /', ...)` — type `/`, fire keyDown with `key: 'Backspace'` on the textarea (the harness's `handleKeyDown` will check `inputVal === '/'` and close), assert `/goal` not in document (5-02-02, SLASH-01).

    3. `it('arrow navigation wraps from last to first and first to last', ...)` — type `/`, fire 2 ArrowDown events (selection moves /goal → /context → /plan), then a 3rd ArrowDown wraps to /goal; then ArrowUp wraps from /goal to /plan. Assert `data-selected` attribute on the row elements changes accordingly. (5-02-03 + PITFALLS P6e, SLASH-02).

    4. `it('NFKC normalize and case-insensitive match', ...)` — type `/CTX`, assert `/context` is in the document (case-insensitive + NFKC on ASCII). For CJK test (PITFALLS P6b), since 3 hardcoded commands are all ASCII, the harness can pass a custom `query` value to SlashCommandPopup directly via a `customQuery` prop, OR the test can simulate by asserting the filter helper exported indirectly. Simpler approach: write a test that types `/代` and asserts the popup shows the D-03 hint (since none of the 3 commands contain `代`, the filter correctly returns 0; the test verifies the filter doesn't crash on a CJK query). For NFKC case-insensitive positive case, type `/CTX` and assert `/context` visible. (5-02-04, D-05).

    5. `it('period filter does not crash', ...)` — type `/foo.`, assert no crash, assert popup still shows whatever matches (none of the 3 commands contain `.`, so 0 matches → D-03 hint visible). (5-02-05, PITFALLS P6a).

    6. `it('double slash filter does not crash', ...)` — type `//`, assert no crash, assert D-03 hint visible (none of the 3 commands contain `//` substring). (5-02-06, PITFALLS P6c).

    7. `it('selectedIndex resets to 0 when filter reduces visible items', ...)` — type `/g` (matches /goal only), ArrowDown (wraps to /goal still), then type `/` (resets filter to all 3) — assert `/goal` is selected again (D-04 + PITFALLS P6e). Better: type `/co` (matches /context), assert `/context` has `data-selected="true"`, then type `/` (resets to all 3) and assert `/goal` has `data-selected="true"`. (5-02-08).

    8. `it('ime safe — composition does not open popup', ...)` — set the harness's `isComposingRef.current = true`, then type `/`, assert `/goal` not in document (the onChange gate bails early). Then set `isComposingRef.current = false` and re-type `/`, assert `/goal` is in document. (5-02-09, PITFALLS P13).

    9. `it('shift enter inserts newline and does not trigger insert flow', ...)` — type `/goal` (popup open, /goal highlighted), fire keyDown with `key: 'Enter', shiftKey: true`, assert the textarea value still contains `/goal` (no text replacement) AND `/goal` is still visible in the popup (popup not closed) AND `document.activeElement === textarea`. Then fire a regular `Enter` (no shift), assert inputVal becomes `/goal ` and popup closes. (5-02-11, PITFALLS P5).

    10. `it('reopening popup highlights the top row (D-04)', ...)` — type `/`, ArrowDown to select /context, then type a char to add then backspace to remove (or set slashOpen=false then true again via harness), assert that on next open the top row `/goal` is selected. Simpler: type `/`, ArrowDown twice (to /plan), then close popup (Esc), then type `/` again, assert `/goal` has `data-selected="true"`. (5-02-12, D-04).

    Wait, that's 10 tests. Combine #9 IME test variants: split into two tests:

    9. `it('ime safe — composition does not open popup', ...)` — same as above (5-02-09).
    10. `it('ime safe — 200ms justFinishedComposingRef window suppresses next keystroke', ...)` — set `justFinishedComposingRef.current = true`, then type `/` (or any char), assert popup does not open. Then advance fake timers by 250ms and type `/` again, assert popup opens. (5-02-10, PITFALLS P13).

    Use `vi.useFakeTimers()` from vitest for the 200ms window test; advance with `vi.advanceTimersByTime(250)`. Use real timers for all other tests.

    Constraint reminders: do NOT add new test framework deps; do NOT modify SlashCommandPopup.tsx; do NOT use the actual ChatArea in tests (heavy + coupled to stores). The TestHarness from Plan 01 is the boundary. Do NOT skip IME tests — they are the load-bearing PITFALLS P13 contract. Do NOT mock cmdk — test the real behavior.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'SlashCommandPopup\\.test|error TS' | head -5 && \
      npx vitest run /Users/suntc/project/CDF/src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx 2>&1 | tail -30
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no errors mentioning SlashCommandPopup.test
    - `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` exits 0 and prints `Tests N passed` where N ≥ 17
    - The test file contains the literal strings: `closes on esc and returns focus to textarea`, `closes on backspace when value is just /`, `arrow navigation wraps from last to first and first to last`, `NFKC normalize and case-insensitive match`, `period filter does not crash`, `double slash filter does not crash`, `selectedIndex resets to 0 when filter reduces visible items`, `ime safe — composition does not open popup`, `ime safe — 200ms justFinishedComposingRef window suppresses next keystroke`, `shift enter inserts newline and does not trigger insert flow`, `reopening popup highlights the top row`
    - The test file uses `vi.useFakeTimers()` somewhere (for the 200ms window test)
    - The test file does NOT import from `ChatArea` (no coupling to the heavy parent component)
    - The test file imports from `@testing-library/react` and `vitest` only
    - `npx vitest run --reporter=verbose` shows all 17+ tests named with the same conventions
  </acceptance_criteria>
  <done>9 (or 11 with split IME) new tests added; all green; total 17+ tests passing; the keyboard contract is locked for Phase 6+ refactors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification of layout, IME candidate window, and cmdk Enter event ordering</name>
  <files>src/renderer/src/components/ChatArea/ChatArea.tsx</files>
  <read_first>
    - .planning/phases/05-popup-shell-keyboard-spike/05-UI-SPEC.md section 4 (Layout & Positioning — PopoverAnchor asChild, z-index ≥ 50, onOpenAutoFocus preventDefault)
    - .planning/research/PITFALLS.md P15 (IME 候选框 z-index)
    - .planning/research/PITFALLS.md P1 (focus steal — verified by automated test, but real focus management differs in browser)
  </read_first>
  <what-built>Phase 5 spike delivers the `/` command popup shell: cmdk + Radix Popover anchored to the composer form, with 17+ automated tests covering the keyboard contract. Three behaviors are jsdom-invisible and need browser verification.</what-built>
  <how-to-verify>
    1. **PopoverAnchor layout**: Start the dev build (`npm run dev`). Open a session. Click into the composer textarea. Type `/`. Verify:
       - The popup appears immediately, anchored to the form (not floating elsewhere on the page).
       - The popup is positioned ABOVE the textarea (form is at viewport bottom; above is the only visible space). Confirm this matches Claude Code's UX expectation per UI-SPEC C-02.
       - The popup width matches the form width (uses `var(--radix-popover-anchor-width)`).
       - The textarea retains focus (caret visible, can continue typing). If focus jumps to the popup, PITFALLS P1 fired — `onOpenAutoFocus={e => e.preventDefault()}` is missing or wrong.
    2. **IME candidate window**: With the popup open, activate a CJK IME (macOS: switch to Pinyin input; Windows: switch to Microsoft Pinyin). Type a CJK character. Verify:
       - The IME candidate window does not fully occlude the popup. Per PITFALLS P15, accept that some overlap is OK and Esc once can close the popup.
       - After the CJK character is committed, the popup filters correctly (e.g., type `/代`, expect 0 matches → hint visible).
    3. **cmdk Enter event ordering**: Type `/` to open popup, press Enter. Verify:
       - The textarea text becomes `/goal ` (with trailing space). The message is NOT sent (no typing indicator, no LLM call). D-07: Tab/Enter insert text + close popup + do NOT execute.
       - Then type `/` again to reopen. Press Tab. Verify the same `/goal ` text insertion behavior, no message sent.
       - Then type `/goal` and press Shift+Enter. Verify a newline is inserted (textarea grows by one row), the popup stays open, no message is sent.
    4. **Esc + Backspace**: With popup open, press Esc. Verify popup closes, caret stays in textarea. Then type `/` to reopen, press Backspace (textarea value is `/`, popup closes). Verify popup closes.
    5. **Arrow wrap**: Type `/`, press ArrowDown 3 times. Verify selection cycles `/goal` → `/context` → `/plan` → `/goal` (wrap). Press ArrowUp once. Verify `/plan` (wrap from top).
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 manual checks pass, or describe the failing scenario with specific steps + observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-process | vitest + jsdom simulate the keyboard contract; jsdom does not render OS-level IME candidate windows or real CSS layout — only behavior verifiable here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-T1 | Tampering (test coverage) | SlashCommandPopup.test.tsx | mitigate | 9 new tests cover PITFALLS P6a/P6b/P6c/P6e + P13 IME + D-04 reopen + Shift+Enter — verified by `npx vitest run` exit 0 with ≥ 17 passed |
| T-05-T2 | Repudiation (test flakiness) | vitest fake timers | mitigate | `vi.useFakeTimers()` only inside the 200ms window test; all other tests use real timers to avoid timer-related flakes; the test must reset timers with `vi.useRealTimers()` in `afterEach` |
| T-05-T3 | Information disclosure (test coupling) | TestHarness | mitigate | TestHarness is a minimal in-file component; does NOT import ChatArea; does NOT mock cmdk; does NOT mock Radix Popover — uses real components via the same code path as production |
| T-05-P15 | Tampering (z-index) | Popover z-50 | accept | Manual checkpoint verifies IME candidate window doesn't fully occlude popup; per PITFALLS P15, accept the macOS IME z-index race and the Esc-once mitigation |
| T-05-P1 | Tampering (focus) | Popover autoFocus | mitigate | Automated test `keeps textarea focus when popup opens` (Plan 01) + manual checkpoint step 1 |
| T-05-P5 | Tampering (command start detection) | Shift+Enter bypass | mitigate | Automated test `shift enter inserts newline and does not trigger insert flow` verifies Shift+Enter falls through to newline; manual checkpoint step 3 verifies the browser path |
| T-05-SC | Tampering (supply chain) | no new deps | accept | This plan adds NO new dependencies — all tests use the existing vitest@4.1.8 + @testing-library/react@16.0.0 already in package.json devDependencies (lines 53-55) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` exits 0 with 17+ passed
- All 17+ test names match the spike constraints (SLASH-01, SLASH-02, D-04, D-07, 5 PITFALLS P6 cases, IME safety × 2, Shift+Enter)
- Manual verification: 5 dev-build interaction checks pass per Task 2 how-to-verify
</verification>

<success_criteria>
- 17+ tests green covering: open/close (slash, Esc, Backspace), filter (case-insensitive, NFKC, period, double-slash), navigation (↑↓ wrap, selectedIndex bounds), IME (composition guard + 200ms window), D-04 (reopen-top), D-07 (Tab=Enter, no execution), Shift+Enter newline
- Manual checkpoint approved: popup appears below/above textarea, IME candidate window does not fully occlude, Enter/Tab insert without sending, Esc/Backspace close, ↑↓ wraps
- ROADMAP.md Phase 5 plan list updated to mark 05-02 as ready
- `nyquist_compliant: true` can be set in 05-VALIDATION.md frontmatter
</success_criteria>

<output>
Create `.planning/phases/05-popup-shell-keyboard-spike/05-02-SUMMARY.md` when done
</output>
