# Phase 2: AI Chat Engine - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-05-20
**Phase:** 02-ai-chat-engine
**Areas discussed:** Chat UI layout, Streaming UX, Input area, GSD commands, History persistence, Markdown rendering, Error handling, Image upload, Thinking display, Tool calls, Message queue, Message status

---

## Chat UI Layout & Conversation Management

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 上下结构（输入框固定底部） | 类似 ChatGPT/Claude 的上下结构 | ✓ |
| 左右分栏 | 类似 Slack/Discord 的左右结构 | |
| 极简模式（输入框隐藏） | 主内容区整个是对话，无固定输入框 | |

**用户的选择:** 上下结构（输入框固定底部）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 标准对话气泡（头像+气泡） | 用户头像/图标在左，文字气泡在右（用户）vs 在左（AI） | ✓ |
| 极简气泡（纯文字，无头像） | 用户文字靠右（灰底），AI 文字靠左（白底），无头像 | |
| 类 iMessage 风格 | 用户气泡在右（蓝色），AI 在左（灰色），头像仅在 AI 侧显示 | |

**用户的选择:** 标准对话气泡（头像+气泡）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 标题 + 点击加载 | 侧边栏对话列表只显示标题，用户点击后主内容区加载完整对话 | ✓ |
| 标题 + 消息预览 | 侧边栏显示标题和最后一条消息的摘要（截断） | |
| 仅标题（按时间排序） | 侧边栏只按时间排序，高亮当前对话 | |

**用户的选择:** 标题 + 点击加载

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 单对话模式 | 一次只能有一个活跃对话 | ✓ |
| 多对话标签页 | 可以同时打开多个对话（类似浏览器标签页） | |

**用户的选择:** 单对话模式

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| AI 自动生成标题 | 保存后自动以第一句话生成标题，支持重命名 | ✓ |
| 序号/时间命名，支持重命名 | 不命名，以「对话 1」「对话 2」或日期时间标识 | |
| 手动命名（保存时提示） | 保存时弹出命名对话框，要求用户输入标题 | |

**用户的选择:** AI 自动生成标题，支持重命名

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 清空当前对话，显示欢迎 | 切换工作区时清空当前对话，显示欢迎对话框 | ✓ |
| 自动加载最近对话 | 切换工作区时自动加载该工作区最近打开的对话 | |
| 显示对话列表，用户手动选择 | 切换工作区时，在侧边栏显示该工作区的对话列表 | |

**用户的选择:** 清空当前对话，显示欢迎

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 显示欢迎，手动选择 | 启动时显示欢迎对话框，用户需要手动从侧边栏选择对话恢复 | ✓ |
| 自动恢复上次对话 | 启动时自动恢复上次使用的对话，显示在聊天面板中 | |
| 显示欢迎 + 继续上次按钮 | 启动时显示欢迎对话框，但如果上次有活跃对话，在欢迎框中显示「继续上次对话」按钮 | |

**用户的选择:** 显示欢迎，手动选择

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 自动保存 + 清空 + 显示欢迎 | 用户点击「新建对话」后，当前对话自动保存（如果有内容），然后清空，显示欢迎对话框 | ✓ |
| 清空当前对话，显示欢迎 | 用户点击「新建对话」后，清空当前对话，显示欢迎对话框 | |
| 清空当前对话，输入框聚焦 | 用户点击「新建对话」后，清空当前对话，输入框聚焦 | |

**用户的选择:** 自动保存 + 清空 + 显示欢迎

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 最近 10 条 + 查看更多 | 显示最近 10 条对话，超出时隐藏。用户点击「查看更多」加载历史 | ✓ |
| 全部显示 + 按时间排序 | 显示所有对话，按最后活动时间倒序排列 | |
| 全部显示 + 搜索过滤 | 显示所有对话，支持搜索过滤（输入关键词筛选对话标题） | |

**用户的选择:** 最近 10 条 + 查看更多

---

## Streaming UX & Cancellation

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 立即显示光标/「思考中...」 | AI 开始响应时立即显示闪烁光标或「思考中...」动画 | ✓ |
| 等待第一个 token 后再显示 | 不显示中间状态，直到第一个 token 到达才开始渲染 | |
| 脉冲动画 → 渐入文字 | 显示一个微妙的脉冲动画表示正在处理，第一个 token 到达后过渡到文字 | |

**用户的选择:** 立即显示光标/「思考中...」+ 脉冲动画 → 渐入文字（结合方案 1 和 3）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 逐 chunk 追加（最实时） | 每个 MessageChunk 到达时立即追加到 DOM | ✓ |
| 批量渲染（定时刷新） | 每 50-100ms 批量渲染一次累积的 chunks | |
| 按语义单位刷新 | 以完整句子或代码块为单位刷新 | |

**用户的选择:** 逐 chunk 追加（agent 推荐）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 发送按钮变为停止按钮 | 发送按钮在生成期间变为停止按钮（红色） | ✓ |
| 停止按钮（回复区域右下角） | AI 回复区域右下角显示圆形停止按钮 | |
| 仅 Escape 快捷键 | 用户按 Escape 键中断，无 UI 按钮 | |

**用户的选择:** 发送按钮变为停止按钮 + Escape 快捷键

---

## Input Area Design

| Option | Description | Selected |
| ------ | ----------- | -------- |
| Enter 发送，Shift+Enter 换行 | Enter 发送消息，Shift+Enter 换行 | ✓ |
| Ctrl+Enter 发送，Enter 换行 | Ctrl/Cmd+Enter 发送，Enter 换行 | |
| 点击按钮发送，Enter 换行 | Enter 换行，点击按钮发送 | |
| 同时支持 | 同时支持 Enter 发送和 Shift+Enter 换行 | |

**用户的选择:** Enter 发送，Shift+Enter 换行

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 自动增高（1→多行），限高滚动 | 固定 1 行高度，用户打字时自动增高（最多 6-8 行），超过后出现滚动条 | ✓ |
| 固定单行，始终不增高 | 固定 1 行，始终不增高 | |
| 无限增高（无上限） | 无高度限制，可无限增高 | |

**用户的选择:** 自动增高（1→多行），限高滚动（6-8 行）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 固定：输入消息，或 /gsd-* 执行命令 | 占位符固定显示「输入消息，或 /gsd-* 执行命令」 | ✓ |
| 固定：向 AI 提问... | 占位符固定显示「向 AI 提问...」 | |
| 动态变化 | 动态：输入时当检测到 `/` 时显示「输入命令」提示 | |

**用户的选择:** 固定：输入消息，或 /gsd-* 执行命令

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 发送后保持可用 + 支持连续发送 | 点击发送后，输入框保持可用，用户可以继续输入 | ✓ |
| 发送中状态（输入框禁用） | 点击发送后，输入框显示「发送中...」状态，按钮变为禁用 | |
| 发送后保持可用 | 点击发送后，输入框保持可用，用户可以继续输入 | |

**用户的选择:** 发送后保持可用 + 支持连续发送（消息队列模式）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 确认 | 确认发送按钮逻辑 | ✓ |
| 再问一个 | 再补充一个细节：停止按钮的样式（红色？灰色？） | |

**用户的选择:** 确认

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| AI 回复中，输入框为空时显示「停止」按钮；有内容时显示「发送」按钮（进入队列） | AI 回复中，输入框中如果没有内容，则显示停止按钮，如果有输入，显示发送按钮，点击发送进入消息队列 | ✓ |

**用户的选择:** AI 回复中，输入框为空时显示「停止」按钮；有内容时显示「发送」按钮（进入队列）

---

## GSD Command Integration

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 拦截 + 命令补全 | 自动检测 /gsd- 命令，拦截到主进程执行 + 输入时提示可用命令 | ✓ |
| 纯拦截 | 消息正常发给 AI agent，agent 通过工具调用执行 GSD 命令 | |

**用户的选择:** 拦截 + 命令补全

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 命令名 + 描述 + 参数提示 | 命令菜单显示：命令名 + 简短描述 + 参数提示 | ✓ |
| 仅命令名，描述在输入框内 | 命令菜单显示：仅命令名，描述在选中后显示在输入框内 | |

**用户的选择:** 命令名 + 描述 + 参数提示

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 卡片形式，可视化更强 | GSD 结果以卡片形式展示 | ✓ |
| 文本消息，与普通对话一致 | GSD 命令的执行结果以正常文本消息展示 | |
| 实时日志流 + 最终结果卡片 | GSD 执行过程中实时输出日志流，完成后格式化展示结果 | |

**用户的选择:** 卡片形式，可视化更强

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 状态 + 摘要 + 展开 + 复制 | 卡片显示：命令名、执行状态（成功/失败）、输出摘要。底部有「展开」按钮查看完整输出，支持复制 | ✓ |
| 状态 + 完整输出（始终展开） | 卡片显示：命令名、执行状态、完整输出直接展开 | |
| 状态 + 摘要 + 复制按钮 | 卡片显示：命令名、执行状态、输出摘要。支持点击复制输出 | |

**用户的选择:** 状态 + 摘要 + 展开 + 复制（方案 1 和 3 结合）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 智能重试（主进程判断错误类型） | GSD 命令执行失败时，卡片根据错误类型决定是否显示重试按钮 | ✓ |
| 所有失败都显示重试 | GSD 命令执行失败时，卡片显示错误信息 + 重试按钮 | |
| 不显示重试按钮 | GSD 命令执行失败时，卡片仅显示错误信息 | |

**用户的选择:** 所有失败都显示重试（用户修正：智能重试）

---

## History Persistence

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 复用 pi SDK SessionManager | 用 pi SDK 的 SessionManager 管理持久化 | ✓ |
| 自定义路径，复用 SDK 逻辑 | 现在就用 SessionManager.open(path) 自定义路径，但复用 SDK 的存储逻辑 | |
| 自己实现持久化 | 完全自己实现持久化，不依赖 pi SDK 存储 | |

**用户的选择:** 先用方案 A（复用 pi SDK SessionManager）

---

## Markdown Rendering

| Option | Description | Selected |
| ------ | ----------- | -------- |
| shiki + shadcn Card | 使用 shiki 做代码高亮，配合 shadcn 的 Card 样式包裹代码块 | ✓ |
| shadcn + prismjs | 使用 shadcn 已有的 shadcn 组件 + Tailwind，配合 remark/rehype 处理 Markdown | |
| shadcn + highlight.js | 使用 shadcn 已有的 shadcn 组件 + Tailwind，配合 remark/rehype 处理 Markdown | |

**用户的选择:** shiki + shadcn Card（agent 推荐）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 确认 | 确认：用 shiki 做代码高亮？ | ✓ |
| highlight.js | 用 highlight.js，支持更多语言，但高亮效果不如 shiki | |
| prismjs | 用 prismjs，最轻量，但高亮效果一般 | |

**用户的选择:** shiki + shadcn Card

---

## Error Handling

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 错误卡片 + 重试按钮 | AI 回复出错时，在消息流中显示一个红色错误卡片，包含错误信息和「重试」按钮 | ✓ |
| 错误卡片（无重试） | AI 回复出错时，在消息流中显示一个红色错误卡片，仅显示错误信息 | |
| 错误卡片 + 重试 + 查看日志 | AI 回复出错时，在消息流中显示一个红色错误卡片，包含错误信息、重试按钮和「查看日志」按钮 | |

**用户的选择:** 错误卡片 + 重试按钮

---

## Image Upload

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 支持图片上传与分析 | 支持用户拖拽或粘贴图片到输入框，图片以缩略图形式显示在消息中。AI 可以分析图片内容 | ✓ |
| 支持图片上传但不分析 | 支持用户拖拽或粘贴图片到输入框，图片仅作为附件显示，AI 不分析图片内容 | |
| 暂不支持图片 | 暂不支持图片上传，仅支持纯文本输入 | |

**用户的选择:** 支持图片上传，根据模型类型动态显示（多模态显示，纯 LLM 隐藏）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 缩略图嵌入 + 点击放大 | 图片以缩略图形式嵌入在消息气泡中，用户点击可放大查看 | ✓ |
| 附件形式（不嵌入气泡） | 图片以上传附件的形式显示在消息中（类似文件附件），不嵌入气泡内容 | |

**用户的选择:** 缩略图嵌入 + 点击放大

---

## Thinking Display

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 显示 thinking 过程（可折叠） | 当模型启用 thinking 模式时，在消息气泡中显示思考过程（折叠/展开） | ✓ |
| 显示 thinking 过程（始终展开） | 当模型启用 thinking 模式时，在消息气泡中显示 thinking 过程（始终展开） | |
| 不显示 thinking 过程 | 不显示 thinking 过程，用户看不到模型的思考步骤 | |

**用户的选择:** 在有限的可折叠卡片范围内思考，思考完毕自动折叠

---

## Tool Calls

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 工具调用卡片（工具名+参数+状态+结果） | AI 执行工具时，在消息流中显示工具调用的可视化卡片 | ✓ |
| 工具调用卡片（仅工具名+结果） | AI 执行工具时，在消息流中显示工具调用的可视化卡片，但仅显示工具名和结果 | |
| 不显示工具调用过程 | 不显示工具调用过程，用户只看到最终结果 | |

**用户的选择:** 工具调用卡片（工具名+参数+状态+结果）

---

## Message Queue

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 卡片式队列（浅灰背景） | 队列列表以卡片形式显示在输入框上方，每条队列项有浅灰色背景 | ✓ |
| 紧凑列表（悬停显示操作） | 队列列表以紧凑列表形式显示在输入框上方，每条队列项仅显示消息预览 + 状态 | |
| 标签式队列（点击展开操作） | 队列列表以简洁标签形式显示在输入框上方，每条队列项显示消息预览 + 状态 | |

**用户的选择:** 卡片式队列（浅灰背景）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 消息预览（最多 50 字符）+ 操作按钮 | 队列中每条消息显示：消息预览 + 状态 + 操作按钮 | ✓ |
| 仅消息内容（状态在队列） | 队列中每条消息仅显示消息内容，状态在侧边栏队列中显示 | |
| 消息 + 状态图标 | 队列中每条消息显示：消息内容 + 状态图标 | |

**用户的选择:** 消息预览（最多 50 字符）+ 操作按钮（无需状态标签）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 引导 → 标记已引导 + 立即发送 | 队列中每条消息有「引导」按钮。点击后，AI 当前输出被标记为「已引导」，队列消息立即发送 | ✓ |
| 引导 → 标记已引导 + 等当前完成 | 队列中每条消息有「引导」按钮。点击后，AI 当前输出被标记为「已引导」，队列消息发送后，AI 需要完成当前输出再处理新消息 | |
| 引导 → 标记已引导 + 立即发送 + 等当前完成 | 队列中每条消息有「引导」按钮。点击后，AI 当前输出被标记为「已引导」，队列消息立即发送，但 AI 需要完成当前输出再处理新消息 | |

**用户的选择:** 引导 → 标记已引导 + 立即发送

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 队列中消息按正序排列（旧在上） | 队列中消息按发送时间正序排列（最早的在最上面） | ✓ |
| 队列中消息按倒序排列（新在上） | 队列中消息按发送时间倒序排列（最新的在最上面） | |
| 队列中消息按优先级排列 | 队列中消息按优先级排列（引导优先，普通消息在后） | |

**用户的选择:** 正序（旧在上）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 无限制 | 队列中消息无限制，用户手动管理 | ✓ |
| 最多 5 条 | 队列中最多保留 5 条消息，超出时最早的消息自动丢弃 | |
| 最多 10 条 | 队列中最多保留 10 条消息，超出时最早的消息自动丢弃 | |

**用户的选择:** 无限制

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 自动滚动到底部 | 队列中消息超出可视区域时，自动滚动到最新消息（底部） | ✓ |
| 保持当前滚动位置 | 队列中消息超出可视区域时，保持当前滚动位置 | |
| 自动滚动到顶部 | 队列中消息超出可视区域时，自动滚动到最旧消息（顶部） | |

**用户的选择:** 自动滚动到底部

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 默认展开，可折叠 | 队列默认展开，但用户可点击折叠按钮隐藏队列 | ✓ |
| 始终展开 | 队列始终展开显示所有消息 | |
| 默认折叠 | 队列默认折叠，用户点击展开按钮后显示所有消息 | |

**用户的选择:** 默认展开，可折叠

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 显示图标 | 队列折叠时，在输入框上方显示一个图标，点击展开 | ✓ |
| 显示「有 N 条消息排队」提示 | 队列折叠时，在输入框上方显示「有 N 条消息排队」提示，点击展开 | |
| 不显示提示 | 队列折叠时，不显示任何提示，用户需要点击输入框上方的区域展开 | |

**用户的选择:** 显示图标（⏫ 符号）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 圆角 + 浅灰背景 + 边框 | 队列卡片有圆角（8px），浅灰色背景（#f5f5f5），边框（#ebebeb） | ✓ |
| 无圆角 + 浅灰背景 + 无边框 | 队列卡片无圆角，浅灰色背景，无边框 | |
| 圆角 + 无背景 + 边框 | 队列卡片有圆角（8px），无背景色，边框（#ebebeb） | |

**用户的选择:** 圆角 + 浅灰背景 + 边框

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 右侧横向排列 | 队列项右侧显示 ↩︎ 和 × 按钮，横向排列 | ✓ |
| 右侧纵向排列 | 队列项右侧显示 ↩︎ 和 × 按钮，纵向排列 | |
| 右侧悬停显示 | 队列项右侧显示 ↩︎ 和 × 按钮，悬停时显示 | |

**用户的选择:** 右侧横向排列

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 始终可用 | 队列中每条消息的 ↩︎ 按钮始终可用，点击后立即发送 | ✓ |
| AI 回复中可用 | 队列中每条消息的 ↩︎ 按钮在 AI 未回复时禁用，AI 回复中可用 | |
| 发送中时禁用 | 队列中每条消息的 ↩︎ 按钮在消息处于「发送中」状态时禁用 | |

**用户的选择:** 始终可用

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 始终可用 | 队列中每条消息的 × 按钮始终可用，点击后移除队列 | ✓ |
| 发送中时禁用 | 队列中每条消息的 × 按钮在消息处于「发送中」状态时禁用 | |
| 已完成时禁用 | 队列中每条消息的 × 按钮在消息处于「已完成」状态时禁用 | |

**用户的选择:** 始终可用

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| ↩︎ 符号 | 引导按钮用 ↩︎ 符号（返回箭头），简洁直观 | ✓ |
| 白色背景 + 灰色边框 | 引导按钮用白色背景 + 灰色边框，文字为深灰色 | |
| 浅灰色背景 + 深灰色文字 | 引导按钮用浅灰色背景 + 深灰色文字，无边框 | |

**用户的选择:** ↩︎ 符号

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| × 符号（红色） | 删除按钮用 × 符号，红色文字 | ✓ |
| 🗑 符号（灰色） | 删除按钮用 🗑 符号，灰色文字 | |
| ↻ 符号（灰色） | 删除按钮用 ↻ 符号，灰色文字 | |

**用户的选择:** × 符号（红色）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 引导 ↩︎，立即发送 ▶ | 引导用 ↩︎ 符号，立即发送用 ▶ 符号，区分两个操作 | ✓ |
| 都用 ↩︎ 符号 | 引导和立即发送都用 ↩︎ 符号，用户通过上下文区分 | |
| 引导 ↩︎，立即发送 ⏩ | 引导用 ↩︎ 符号，立即发送用 ⏩ 符号，区分两个操作 | |

**用户的选择:** 立即发送就是引导，这俩是一个概念（都用 ↩︎ 符号）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 立即发送 → 停止当前 + 发送新消息 | AI 回复中点击「立即发送」→ 当前输出被标记为「已停止」，队列消息立即发送 | ✓ |
| 立即发送 → 直接截断 | AI 回复中点击「立即发送」→ 当前输出直接截断，新消息立即发送，不显示停止标记 | |

**用户的选择:** 立即发送 → 停止当前 + 发送新消息（显示「已引导」标记）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 已发送提示（2 秒后消失） | 消息发送后，输入框显示「已发送」绿色提示，持续 2 秒后消失 | ✓ |
| 无提示 | 消息发送后，输入框清空，不显示任何提示 | |
| 发送中状态 | 消息发送后，输入框清空，显示「发送中...」状态 | |

**用户的选择:** 已发送提示（2 秒后消失）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 消息 + 状态标签 | 用户消息在对话流中显示：消息内容 + 状态标签（发送中/已发送/已引导/已停止） | ✓ |
| 仅消息内容 | 用户消息在对话流中仅显示消息内容，状态只在队列中显示 | |
| 消息 + 状态图标 | 用户消息在对话流中显示：消息内容 + 状态图标 | |

**用户的选择:** 消息 + 状态标签

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 在对话流中显示 | 「已引导」标记显示在对话流中用户消息旁边，队列中不显示 | ✓ |
| 在队列中显示 | 「已引导」标记显示在队列中消息旁边，对话流中不显示 | |
| 同时在对话流和队列中显示 | 「已引导」标记同时在对话流和队列中显示 | |

**用户的选择:** 在对话流中显示

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 平滑动画（200ms） | 队列展开/折叠使用平滑动画（高度渐变，200ms） | ✓ |
| 瞬间切换 | 队列展开/折叠瞬间切换，无动画 | |
| 淡入淡出（150ms） | 队列展开/折叠使用淡入淡出动画（150ms） | |

**用户的选择:** 平滑动画（200ms）

---

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 悬停变浅灰背景 | 引导按钮悬停时背景变为浅灰色，文字颜色加深 | ✓ |
| 悬停无效果 | 引导按钮悬停时无效果，保持原样 | |
| 悬停变白背景 | 引导按钮悬停时背景变为白色，边框变为深灰色 | |

**用户的选择:** 悬停变浅灰背景

---

## Code Block Copy

| Option | Description | Selected |
| ------ | ----------- | -------- |
| 每个代码块独立复制按钮 | 每个代码块右上角显示「复制」按钮，点击复制代码到剪贴板 | ✓ |
| 全局复制按钮 | 所有代码块共享一个复制按钮 | |
| 无复制按钮 | 代码块不显示复制按钮，用户需要手动选择文本复制 | |

**用户的选择:** 每个代码块独立复制按钮

---

## the agent's Discretion

以下领域用户明确表示由 agent 决定：

- 加载动画/骨架屏的具体设计
- 欢迎对话框的具体样式和文案
- 空状态、错误状态的具体 UI 处理
- 工作区列表的排序和展示细节
- 主题切换的具体 CSS 变量命名方案
- 文件选择对话框的初始目录策略
- 消息气泡的头像来源（用户头像 vs 默认图标）
- 消息时间戳的显示方式
- 代码块的语言检测逻辑
- 队列卡片的具体尺寸和间距
- 输入框的具体高度和边距
- 消息状态标签的具体样式和颜色

---

## Deferred Ideas

None — 讨论保持在 Phase 2 范围内。

---

*Phase: 02-ai-chat-engine*
*Discussion log created: 2026-05-20*