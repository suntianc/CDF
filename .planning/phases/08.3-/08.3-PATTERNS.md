# Phase 08.3: 增加@引用文件功能 - 模式映射

**映射日期:** 2026-06-07
**分析文件数:** 11 (5 个新建 renderer + 3 个新建 main + 4 个修改)
**找到的类比文件:** 9 / 11

## 文件分类

| 新建/修改文件 | 角色 | 数据流 | 最近似类比 | 匹配质量 |
|----------------|------|---------|-------------|----------|
| `src/renderer/src/components/AtMention/AtToken.tsx` | component | presentational | `src/renderer/src/components/SlashCommand/SlashToken.tsx` | 精确 |
| `src/renderer/src/components/AtMention/AtMentionPopup.tsx` | component | event-driven | `src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx` | 精确 |
| `src/renderer/src/stores/atMentionStore.ts` | store | state-management | (新增独立 store，参考 `useSlashCommandStore` 模板) | role-match |
| `src/renderer/src/lib/commands/pathUtils.ts` | utility | transform | `src/renderer/src/lib/commands/parseInputToTokens.ts` | role-match |
| `src/main/at-mention/gitignore-loader.ts` | service | file-I/O | `src/main/commands/project-commands.ts` (fs + 解析) | role-match |
| `src/main/at-mention/candidate-lister.ts` | service | file-I/O (BFS) | `src/main/commands/chokidar-watcher.ts` (FS enumeration) | role-match |
| `src/main/at-mention/at-mention-handler.ts` | service | IPC handler | `src/main/ipc-handlers.ts:commands:list` | 精确 |
| `src/main/ipc-handlers.ts` (修改) | IPC registration | request-response | 现有 `commands:list` / `commands:readProjectCommands` 模式 | 精确 |
| `src/preload/index.ts` (修改) | contextBridge | request-response | 现有 `commands.*` 桥接模式 | 精确 |
| `src/shared/types.ts` (修改) | type-declaration | type-only | `SlashCommand` / `ParsedFrontmatter` 接口定义模式 | 精确 |
| `src/renderer/src/components/ChatArea/ChatArea.tsx` (修改) | component | state + event-driven | 现有 slash trigger / overlay 模式 | 精确 |
| `src/renderer/src/components/ChatArea/MessageItem.tsx` (修改) | component | render-only | 现有 `renderMessageContent` 模式 | 精确 |

## 模式分配

### `src/renderer/src/components/AtMention/AtToken.tsx` (component, presentational)

**类比:** `src/renderer/src/components/SlashCommand/SlashToken.tsx`

**整体结构 (lines 49-75):**
```tsx
export function SlashToken({ name, source, ...rest }: SlashTokenProps) {
  const Icon = getSlashTokenIcon(source);
  const testId = rest['data-testid'] ?? 'slash-token';

  return (
    <span
      data-testid={testId}
      data-slash-token=""
      contentEditable={false}
      onMouseDown={(e) => e.preventDefault()}
      className={cn(
        'inline-flex items-center gap-1 align-middle',
        'rounded-full px-2.5 py-0.5',
        'bg-gradient-to-r from-[var(--color-accent)]/12 to-[var(--color-accent)]/6 border border-[var(--color-accent)]/20',
        'font-semibold text-[var(--color-accent)] text-xs',
        'select-none whitespace-nowrap',
        'pointer-events-auto shadow-sm shadow-[var(--color-accent)]/5'
      )}
    >
      <Icon className="w-3 h-3 text-[var(--color-accent)]" aria-hidden="true" />
      <span className="tracking-wide">{formatTokenLabel(name)}</span>
    </span>
  );
}
```

**关键模式要点:**
- 使用 `<span>` 内联渲染（与 textarea 共享 inline-flex 布局）
- `onMouseDown={(e) => e.preventDefault()}` 阻止 focus 转移 (Phase 08.1 D-04/SPEC R5)
- `data-testid` 默认值 + 可选覆盖
- `contentEditable={false}` 防御性属性
- Tailwind v4 静态类（不可拼接动态类）

**AtToken 适配 (08.3 增量):**
- Props: `{ path: string; kind: 'file' | 'dir'; ignored?: boolean }`
- Icon: `File` / `Folder` (lucide-react 0.x) — 取代 `getSlashTokenIcon(source)`
- 路径尾 `/` 视觉提示（目录）— `formatTokenLabel` 不适用；改用 `path` 直接显示或截断
- `data-at-token=""` 语义标记
- 同样 `onMouseDown preventDefault` + `contentEditable={false}`

**测试类比:** `src/renderer/src/components/SlashCommand/SlashToken.test.tsx`
- mock `lucide-react` 为 sentinel `<svg data-testid="...">`
- 测试覆盖：Title-Case、icon 分类、onMouseDown preventDefault、data-testid 覆盖、snapshot 基线

---

### `src/renderer/src/components/AtMention/AtMentionPopup.tsx` (component, event-driven)

**类比:** `src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx`

**forwardRef + handleKeyDown 模式 (lines 68-192):**
```tsx
export const SlashCommandPopup = forwardRef<
  SlashCommandPopupHandle,
  SlashCommandPopupProps
>(({ query, onSelect, onInsert, onClose, commands, hasMcpWarning, mcpWarningMessage, loading }, ref) => {
  // 内部 useImperativeHandle 暴露键盘处理
  useImperativeHandle(ref, () => ({
    handleKeyDown: (e) => {
      if (filtered.length === 0) {
        if (e.key === 'Escape' || e.key === 'Enter' || e.key === 'Tab') {
          e.preventDefault();
          onClose();
          return true;
        }
        return false;
      }
      const idx = filtered.findIndex((c) => c.name === selectedValue);
      if (e.key === 'ArrowDown') { /* wrap around */ }
      if (e.key === 'ArrowUp') { /* wrap around */ }
      if (e.key === 'Enter') { e.preventDefault(); (onInsert ?? onSelect)('/' + selectedValue); return true; }
      if (e.key === 'Tab') { e.preventDefault(); (onInsert ?? onSelect)('/' + selectedValue); return true; }
      if (e.key === 'Escape') { e.preventDefault(); onClose(); return true; }
      return false;
    },
  }), [filtered, selectedValue, onSelect, onInsert, onClose]);

  return (
    <Command value={selectedValue} onValueChange={setSelectedValue} shouldFilter={false} label="Slash commands" className="w-full">
      <Command.List className="max-h-64 overflow-y-auto p-0">
        {filtered.map((c) => (
          <Command.Item key={`${c.source}-${c.name}`} value={c.name} data-source={c.source} onSelect={() => onSelect('/' + c.name)} className="...">
            <Badge variant="secondary" className={cn('text-[10px] px-1.5 py-0 font-mono', SOURCE_TEXT_COLOR[c.source])}>
              {c.badge}
            </Badge>
            <span className="font-mono">/{c.name}</span>
          </Command.Item>
        ))}
        <Command.Empty className="px-2 py-2 text-[12px] text-[var(--color-text-muted)]">
          <div>无匹配命令</div>
          <div>试试输入 /goal · /context 查看可用命令</div>
        </Command.Empty>
      </Command.List>
    </Command>
  );
});
```

**NFKC + Variation Selector 工具 (lines 37-40):**
```typescript
const VARIATION_SELECTORS = /[︀-️\u{E0100}-\u{E01EF}]/gu;
const normForFilter = (s: string): string =>
  s.normalize('NFKC').replace(VARIATION_SELECTORS, '').toLowerCase();
```

**AtMentionPopup 适配 (08.3 增量):**
- Props: `{ query: string; candidates: string[]; loading: boolean; truncated: boolean; onSelect: (path: string) => void; onClose: () => void }`
- `candidates` 是 `string[]`（不是 `SlashCommand[]`）— 路径后缀 `/` 推断 kind（pitfall #4 推荐方案 2）
- handleKeyDown: `Enter` 选 + insert，`Tab` 同样选 + insert（08.3 无 dispatch 概念），`Esc` 关，`↑/↓` 导航，`Backspace` 当 query 为空时关 popup（08.3 沿用 08.1 模式）
- normForFilter：从 `SlashCommandPopup.tsx:37-40` 抽出到 `pathUtils.ts` 共享（或 inline 拷贝）
- `Command.Empty`: `未找到匹配文件`
- `Command.List` 顶部 truncated 提示行：`共 5000+ 个文件，已截断显示前 5000 个`

**测试类比:** `src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx`
- 完整 TestHarness 模式（IME refs、composition 状态、slash open state）
- 测试覆盖：opens/closes、arrow nav、Enter vs Tab、NFKC、IME 安全
- `jsdom` polyfill: `ResizeObserver`、`Element.prototype.scrollIntoView`

---

### `src/renderer/src/stores/atMentionStore.ts` (store, state-management)

**类比模式:** `useSlashCommandStore`（文件不在现存代码中——STORE 模式参考 `useGoalJudge` 文档中描述的"独立 store 模式"）

**现存的 zustand store 模板** (`src/renderer/src/stores/contextModalStore.ts` 等):
```typescript
import { create } from 'zustand';

interface ModalState {
  isOpen: boolean;
  payload: unknown | null;
  open: (payload?: unknown) => void;
  close: () => void;
}

export const useContextModalStore = create<ModalState>((set) => ({
  isOpen: false,
  payload: null,
  open: (payload) => set({ isOpen: true, payload: payload ?? null }),
  close: () => set({ isOpen: false, payload: null }),
}));
```

**atMentionStore 状态形态 (08.3 设计):**
```typescript
interface AtMentionState {
  isOpen: boolean;
  query: string;
  candidates: string[];           // 候选路径（path 字符串数组）
  loading: boolean;               // IPC 调用中
  truncated: boolean;             // 超过 5000 上限
  cursorPos: number;              // textarea 光标位置（用于 `@` 触发检测）
  open: (cursorPos: number) => void;
  close: () => void;
  setQuery: (q: string) => void;
  setCandidates: (paths: string[], truncated: boolean) => void;
  setLoading: (loading: boolean) => void;
}
```

**关键模式要点:**
- 独立 store（不复用 `useSlashCommandStore`，避免 `@` ↔ `/` 双向状态同步 — RESEARCH 推荐方案）
- `close()` 主动 `setCandidates([])` 释放内存（pitfall #2）
- `setCandidates` 同时设置 `truncated` 标志

---

### `src/renderer/src/lib/commands/pathUtils.ts` (utility, transform)

**类比:** `src/renderer/src/lib/commands/parseInputToTokens.ts` (整体模板)

**parseInputToTokens 结构 (lines 1-65):**
```typescript
import type { CommandSource, SlashCommand } from '../../../../shared/types';

/**
 * Phase 08.1 — D-03: parse a user-typed string for the leading slash
 * command and the trailing free text. Mirrors `dispatcher.resolve()`'s
 * matcher logic ...
 */
export function parseInputToTokens(
  text: string,
  registry: ReadonlyArray<SlashCommand>
): { token?: { name: string; source: CommandSource }; text: string } {
  if (!text.startsWith('/')) {
    return { text };
  }
  // ...纯函数，无副作用
}
```

**pathUtils 导出函数 (08.3 设计):**
1. `getRelativePath(absolutePath: string, projectRoot: string): string`
   - 内部: `path.relative(projectRoot, absolutePath).replace(/\\/g, '/')`
   - 拒绝 `..`、绝对路径前缀泄露
2. `parseAtTokens(inputVal: string): { path: string; kind: 'file' | 'dir'; start: number; end: number }[]`
   - 正则扫描 `@(\S+?)` 后接空白/标点/行尾
   - 跳过邮箱形式（`user@example.com` 模式豁免可选）
3. `formatAtLabel(path: string): string`
   - 显示优化（截断超长路径、`/` 分段着色可选）
4. `normForFilter(s: string): string`（从 SlashCommandPopup 抽出共享）
5. `VARIATION_SELECTORS` 常量（从 SlashCommandPopup 抽出共享）

**关键模式要点:**
- 纯函数，无副作用（不调 IPC、不读 fs）
- 路径处理用 `path.relative` + 正则替换 `\\` 为 `/`（pitfall #5 跨平台）
- JSDoc 注释说明语义边界（08.1 `parseInputToTokens` 模式）

**测试类比:** `src/renderer/src/lib/commands/parseInputToTokens.test.ts` / `formatTokenLabel.test.ts`
- vitest 单元测试
- 覆盖边界：空字符串、`..` 路径、Windows 路径（mock）、目录 `kind='dir'` 推断

---

### `src/main/at-mention/gitignore-loader.ts` (service, file-I/O)

**类比:** `src/main/commands/project-commands.ts` (fs 读取 + 解析模式)

**project-commands.ts 模板 (lines 36-48):**
```typescript
import fs from 'fs';
import os from 'os';
import path from 'path';
import YAML from 'yaml';
import type { ParsedFrontmatter, SlashCommand } from '../../shared/types';

export function parseFrontmatter(filePath: string): CommandFrontmatter {
  if (!fs.existsSync(filePath)) return {};
  const content = fs.readFileSync(filePath, 'utf-8');
  if (!content.startsWith(FRONTMATTER_START)) return {};
  // ...解析
}
```

**gitignore-loader.ts 设计:**
```typescript
import ignore from 'ignore'; // E-04 锁定 ignore@5.3.2
import fs from 'fs';
import path from 'path';

const DEFAULT_IGNORED = ['node_modules', 'out', 'dist', '.git', '.next', '.cache'];

export function loadGitignore(projectRoot: string): ReturnType<typeof ignore> {
  const ig = ignore();
  const rootGitignore = path.join(projectRoot, '.gitignore');
  if (fs.existsSync(rootGitignore)) {
    ig.add(fs.readFileSync(rootGitignore, 'utf-8'));
  }
  ig.add(DEFAULT_IGNORED);
  return ig;
}

export function toPosix(p: string): string {
  return p.split(path.sep).join('/');
}
```

**关键模式要点:**
- `fs.existsSync` 防御性检查
- `fs.readFileSync(..., 'utf-8')` 同步读
- POSIX 路径转换（pitfall #10）
- 工厂函数返回 `ig` 实例（避免全局状态）

**测试要点 (Wave 0):**
- `loadGitignore` 接受空字符串
- 默认排除 `node_modules` 等
- 嵌套 `.gitignore` 合并
- negation `!pattern` 正确处理
- `toPosix` Windows 路径转换

---

### `src/main/at-mention/candidate-lister.ts` (service, file-I/O BFS)

**类比:** `src/main/commands/chokidar-watcher.ts` (FS enumeration + 防抖 + 降级)

**chokidar-watcher 模板 (lines 18-50):**
```typescript
function debounce<T extends (...args: any[]) => any>(fn: T, ms: number): T { ... }
function emitFallbackEvent(scope: 'system' | 'project', dir: string, error: string): void { ... }
function readdirFallback(dir: string, scope: 'system' | 'project'): void { ... }
```

**candidate-lister.ts 设计:**
```typescript
import fs from 'fs';
import path from 'path';
import { loadGitignore, toPosix } from './gitignore-loader';

const MAX_DEPTH = 6;
const MAX_COUNT = 5000;

export interface ListResult {
  candidates: string[];   // relative path 数组，目录带 '/' 后缀
  truncated: boolean;
}

export function listCandidates(projectRoot: string): ListResult {
  const ig = loadGitignore(projectRoot);
  const result: string[] = [];
  let truncated = false;
  
  const queue: { dir: string; depth: number }[] = [{ dir: projectRoot, depth: 0 }];
  while (queue.length > 0) {
    const { dir, depth } = queue.shift()!;
    if (depth > MAX_DEPTH) continue;
    
    let entries;
    try {
      entries = fs.readdirSync(dir, { withFileTypes: true });
    } catch (err) {
      continue; // 跳过无权限目录
    }
    
    for (const entry of entries) {
      if (result.length >= MAX_COUNT) { truncated = true; break; }
      const relPosix = toPosix(path.relative(projectRoot, path.join(dir, entry.name)));
      if (ig.ignores(relPosix + (entry.isDirectory() ? '/' : ''))) continue;
      
      result.push(entry.isDirectory() ? relPosix + '/' : relPosix);
      if (entry.isDirectory()) {
        queue.push({ dir: path.join(dir, entry.name), depth: depth + 1 });
      }
    }
  }
  
  return { candidates: result.sort(), truncated };
}
```

**关键模式要点:**
- 显式 BFS 队列（避免 DFS 栈溢出 — pitfall #3）
- `withFileTypes: true` 一次 syscall 拿 dirent（Node 20+）
- `path.relative` + `toPosix` 统一 POSIX 风格（pitfall #10）
- `try/catch` 静默跳过无权限目录（chokidar-watcher 模式）
- 浅层枚举 B-05: `MAX_DEPTH=6`, `MAX_COUNT=5000`
- 目录带 `/` 后缀（pitfall #4 推荐方案 — 减少 IPC payload）

---

### `src/main/at-mention/at-mention-handler.ts` (service, IPC handler)

**类比:** `src/main/ipc-handlers.ts:commands:list` (lines 799-812)

**commands:list 模板:**
```typescript
ipcMain.handle('commands:list', async (_evt, projectId: string, agentId: string) => {
  try {
    const project = db.prepare('SELECT path FROM projects WHERE id = ?').get(projectId) as { path: string } | undefined;
    if (!project) {
      return { commands: [], conflicts: [], warnings: [] };
    }
    ensureProjectWatcher(project.path);
    return await collectAllCommands(project.path, agentId);
  } catch (err) {
    console.error('[commands:list] failed:', err);
    return { commands: [], conflicts: [], warnings: [] };
  }
});
```

**at-mention-handler.ts 设计:**
```typescript
import { ipcMain } from 'electron';
import db from '../database';
import { listCandidates } from './candidate-lister';

export function registerAtMentionHandlers() {
  ipcMain.handle('project:listAtMentionCandidates', async (_evt, projectId: string) => {
    try {
      const project = db.prepare('SELECT path FROM projects WHERE id = ?').get(projectId) as { path: string } | undefined;
      if (!project) {
        return { candidates: [], truncated: false };
      }
      return listCandidates(project.path);
    } catch (err) {
      console.error('[project:listAtMentionCandidates] failed:', err);
      return { candidates: [], truncated: false };
    }
  });
}
```

**关键模式要点:**
- `try/catch` + console.error + 降级返回（永不抛 — IPC handler 约定）
- 通过 `db` 校验 projectId 存在（ASVS V4 — 跨项目枚举防护）
- 返回结构 `{ candidates: string[]; truncated: boolean }`

**集成位置:** `src/main/ipc-handlers.ts` 中 `registerAtMentionHandlers()` 调用（在 `registerWorkflowIpcHandlers()` 之后）

---

### `src/main/ipc-handlers.ts` (修改)

**修改位置:** `registerIpcHandlers()` 函数末尾，`registerWorkflowIpcHandlers()` 调用之后

**修改模式 (Phase 6/7/8 模式):**
```typescript
// 现有
registerWorkflowIpcHandlers();

// ===== Phase 6 Plan 02: Slash Command Registry IPC (D-15 O(1) memory read) =====
ipcMain.handle('commands:list', async (_evt, projectId: string, agentId: string) => { ... });

// 08.3 增量
// ===== Phase 08.3: @Mention file candidate IPC (E-01..E-05) =====
registerAtMentionHandlers();
```

**关键模式要点:**
- import 新模块（不直接在 `ipc-handlers.ts` 写业务逻辑）
- 模块导出 `registerXxxHandlers()` 函数聚合 IPC 通道

---

### `src/preload/index.ts` (修改)

**修改位置:** `electronAPI.commands` 之后，`electronAPI.context` 之前

**修改模式 (Phase 6 模式):**
```typescript
// 现有
commands: {
  list: (projectId: string, agentId: string) => ipcRenderer.invoke('commands:list', projectId, agentId),
  // ...
},

// 08.3 增量
// ===== Phase 08.3: @Mention file candidate bridge =====
project: {
  listAtMentionCandidates: (projectId: string) =>
    ipcRenderer.invoke('project:listAtMentionCandidates', projectId),
},
```

**关键模式要点:**
- `contextBridge` 暴露为 `electronAPI.project.*`
- 返回 `Promise<{ candidates: string[]; truncated: boolean }>`
- TypeScript 类型补全在 `src/shared/types.ts` 的 `ElectronAPI` 接口同步添加

---

### `src/shared/types.ts` (修改)

**修改位置:** `ElectronAPI` 接口（lines 490-591），在 `commands:` 之后添加

**类型定义模式 (SlashCommand 接口模式):**
```typescript
export interface AtMentionCandidate {
  path: string;           // relative POSIX path
  kind: 'file' | 'dir';   // 由 '/' 后缀推断
}

export interface AtMentionCandidateList {
  candidates: string[];   // 极简 payload（pitfall #4 推荐）
  truncated: boolean;
}

// 在 ElectronAPI 中添加
project: {
  listAtMentionCandidates: (projectId: string) => Promise<AtMentionCandidateList>;
};
```

**关键模式要点:**
- 接口命名 PascalCase
- `AtMentionCandidate` 为可选类型（如果 renderer 端用 `endsWith('/')` 推断 kind，可省略）
- `ElectronAPI` 接口与 preload 同步

---

### `src/renderer/src/components/ChatArea/ChatArea.tsx` (修改)

**修改位置:** 多处 — 09.3 增量散落在 3 个 textarea 实例中

**修改 1: 新增 `currentProjectRoot` 计算 (lines 257-259 附近)**
```typescript
// 现有
const currentProjectName = useMemo(() => {
  return projects.find(p => p.id === currentProjectId)?.name || '未知项目';
}, [currentProjectId, projects]);

// 08.3 增量
const currentProject = useMemo(
  () => projects.find(p => p.id === currentProjectId) || null,
  [projects, currentProjectId]
);
const currentProjectRoot = currentProject?.path || null;
```

**修改 2: 新增 `atOpen` state + ref**
```typescript
const [atOpen, setAtOpen] = useState(false);
const atRef = useRef<AtMentionPopupHandle>(null);
```

**修改 3: 在 inline-flex overlay 中追加 `<AtTokenSequence>` (line 879-883 模式)**
```tsx
// 现有
<div className="flex items-start gap-1.5 w-full relative z-0" style={{ fontSize: '15px' }}>
  {parsedToken?.token && (
    <div className="flex-shrink-0 pt-[3px]">
      <SlashToken name={parsedToken.token.name} source={parsedToken.token.source} />
    </div>
  )}
  {/* 08.3 增量 — inline-flex 序列支持多个 at token */}
  {parsedAtTokens.length > 0 && (
    <AtTokenSequence tokens={parsedAtTokens} />
  )}
  <textarea ... />
</div>
```

**修改 4: textarea onChange 触发 `@` 检测 (line 892-901 模式)**
```tsx
onChange={(e) => {
  const value = parsedToken?.token ? '/' + parsedToken.token.name + ' ' + tail : tail;
  setInputVal(value);
  if (isComposingRef.current) return;
  // 现有 slash 触发
  const shouldOpen = value.startsWith('/') && !value.includes(' ') && value.length <= 32 && !parsedToken?.token;
  setSlashOpen(shouldOpen);
  // 08.3 增量 — @ 触发（光标在 @ 后）
  const cursor = e.target.selectionStart;
  const textBeforeCursor = value.slice(0, cursor);
  const atMatch = textBeforeCursor.match(/(?:^|\s)@(\S*)$/);
  if (atMatch && currentProjectRoot) {
    setAtOpen(true);
    setAtQuery(atMatch[1]);
  } else {
    setAtOpen(false);
  }
}}
```

**修改 5: 原子 Backspace 扩展 (line 762-769 模式)**
```tsx
// 现有 slash token 原子删除
if (e.key === 'Backspace' && parsedToken?.token) {
  if (e.currentTarget.selectionStart === 0 && e.currentTarget.selectionEnd === 0) {
    e.preventDefault();
    setInputVal('');
    setSlashOpen(false);
    return;
  }
}
// 08.3 增量 — at token 原子删除
if (e.key === 'Backspace' && parsedAtTokens.length > 0) {
  const lastAt = parsedAtTokens[parsedAtTokens.length - 1];
  const cursor = e.currentTarget.selectionStart;
  // 光标在最后一个 at token 的末尾（包括尾随空格）
  if (cursor === lastAt.end + 1 && e.currentTarget.selectionEnd === cursor) {
    e.preventDefault();
    setInputVal(value.slice(0, lastAt.start));
    setAtOpen(false);
    return;
  }
}
```

**修改 6: Popover 互斥 (line 876 / 1190 模式)**
```tsx
// 现有
<Popover open={slashOpen && !activeSessionId} ...>

// 08.3 增量 — at popup 互斥（同一时刻只有一个 popup open）
<Popover open={atOpen && !atSlash && !activeSessionId} ...>
```

**关键模式要点:**
- inline-flex overlay 与 08.1 SlashToken 共存（同 div 内 flex sibling）
- `useMemo` 缓存 `currentProjectRoot` / `parsedAtTokens`
- `isComposingKeyEvent` 防护（pitfall #1）
- 互斥：slash + at popup 不同时 open

---

### `src/renderer/src/components/ChatArea/MessageItem.tsx` (修改)

**修改位置:** `renderMessageContent` 函数 (line 162)

**修改模式 (现有 think 标签扫描模式 line 184-198):**
```tsx
// 现有 think 标签清洗
let cleanContent = content;
const thinkCount = (cleanContent.match(/<think>/g) || []).length;
// ...

// 08.3 增量 — @ path token 渲染（在 cleanContent 后、renderMain 前）
const atTokenRegex = /@([\w./-]+)/g;
const renderContentWithAtTokens = (text: string) => {
  if (!text.includes('@')) return renderMain(text);
  const parts: React.ReactNode[] = [];
  let lastIndex = 0;
  let match: RegExpExecArray | null;
  while ((match = atTokenRegex.exec(text)) !== null) {
    if (match.index > lastIndex) {
      parts.push(...renderMain(text.slice(lastIndex, match.index)) ? [renderMain(text.slice(lastIndex, match.index))] : []);
    }
    const path = match[1];
    const kind = path.endsWith('/') ? 'dir' : 'file';
    parts.push(<AtToken key={`at-${match.index}`} path={path} kind={kind} />);
    lastIndex = match.index + match[0].length;
  }
  if (lastIndex < text.length) {
    parts.push(renderMain(text.slice(lastIndex)));
  }
  return <>{parts}</>;
};
```

**关键模式要点:**
- 优先于 `renderMain` 扫描（避免 markdown 解析吞掉 `@`）
- 正则 `@([\w./-]+)` 捕获路径（与 Phase 8 `parseInputToTokens` 风格一致）
- 复用 `<AtToken>` 组件（输入框 + 历史消息视觉一致）

---

## 共享模式

### 1. 模式: `forwardRef` + `useImperativeHandle` 键盘处理

**来源:** `src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx:136-192`

**应用到:**
- `AtMentionPopup.tsx`
- 任何需要拦截 textarea 键盘事件的 popup 组件

```typescript
export const SlashCommandPopup = forwardRef<Handle, Props>(({ ... }, ref) => {
  useImperativeHandle(ref, () => ({
    handleKeyDown: (e) => {
      if (filtered.length === 0) { /* Esc/Enter/Tab 关 */ return true; }
      // ArrowUp/Down/Enter/Tab/Escape 处理
      return false;
    },
  }), [filtered, selectedValue, ...]);
  // ...
});
```

### 2. 模式: Popover 互斥（同一时刻只一个 popup open）

**来源:** `ChatArea.tsx:876, 1190`

**应用到:**
- 任何在 ChatArea 内新增的 popup（AtMention、潜在 @-mentions）

```tsx
<Popover open={slashOpen && !activeSessionId} onOpenChange={setSlashOpen} modal={false}>
  {/* welcome 屏 */}
</Popover>
<Popover open={slashOpen && !!activeSessionId} onOpenChange={setSlashOpen} modal={false}>
  {/* composer 屏 */}
</Popover>
```

08.3 增量：
```tsx
<Popover open={atOpen && !slashOpen && !activeSessionId} ...>  // welcome
<Popover open={atOpen && !slashOpen && !!activeSessionId} ...>  // composer
```

### 3. 模式: IME 组合期防护

**来源:** `ChatArea.tsx:663-695` + `SlashCommandPopup.test.tsx:460-515`

**应用到:**
- 所有 ChatArea 内 onChange / onKeyDown 监听

```typescript
const isComposingKeyEvent = (e: React.KeyboardEvent<HTMLTextAreaElement>) => {
  const nativeEvent = e.nativeEvent as KeyboardEvent & { isComposing?: boolean; keyCode?: number; which?: number };
  return isComposingRef.current || e.isComposing || nativeEvent.isComposing || nativeEvent.keyCode === 229 || nativeEvent.which === 229;
};

const handleCompositionEnd = () => {
  isComposingRef.current = false;
  justFinishedComposingRef.current = true;
  // 200ms 后清除
  compositionEndTimerRef.current = setTimeout(() => {
    justFinishedComposingRef.current = false;
  }, 200);
};
```

### 4. 模式: 静态类扫描（Tailwind v4）

**来源:** `SlashToken.tsx:59-66` + `SlashCommandPopup.tsx:23-31` (SOURCE_TEXT_COLOR 静态 Map)

**应用到:**
- 所有 08.3 新增组件的 className

```typescript
// GOOD — 静态字面量
className="inline-flex items-center gap-1 rounded-full bg-blue-100"

// BAD — 动态拼接（Tailwind v4 不会扫描）
className={`bg-${color}-100/20`}
```

### 5. 模式: token 视觉包装（literal-string 契约）

**来源:** `SlashToken.tsx:49-75`

**应用到:**
- `AtToken.tsx` — 同样的 inline-flex span 模式

```tsx
<span
  data-testid="at-token"
  data-at-token=""
  contentEditable={false}
  onMouseDown={(e) => e.preventDefault()}
  className="inline-flex items-center gap-1 align-middle rounded-full px-2.5 py-0.5 bg-gradient-to-r from-[var(--color-accent)]/12 ..."
>
  <Icon className="w-3 h-3" />
  <span className="tracking-wide">{path}</span>
</span>
```

### 6. 模式: IPC handler 错误降级

**来源:** `ipc-handlers.ts:799-826` (commands:list / commands:readProjectCommands)

**应用到:**
- `at-mention-handler.ts`

```typescript
ipcMain.handle('xxx', async (_evt, projectId: string) => {
  try {
    const project = db.prepare('SELECT path FROM projects WHERE id = ?').get(projectId);
    if (!project) return { /* 空降级 */ };
    return doWork(project.path);
  } catch (err) {
    console.error('[xxx] failed:', err);
    return { /* 空降级 */ };
  }
});
```

### 7. 模式: contextBridge 安全边界

**来源:** `preload/index.ts:106-132` (commands namespace)

**应用到:**
- `preload/index.ts` 新增 `project.*` namespace

```typescript
project: {
  listAtMentionCandidates: (projectId: string) =>
    ipcRenderer.invoke('project:listAtMentionCandidates', projectId),
},
```

### 8. 模式: NFKC + Variation Selector 过滤

**来源:** `SlashCommandPopup.tsx:37-40`

**应用到:**
- `AtMentionPopup.tsx`（路径模糊匹配）
- 抽出到 `pathUtils.ts` 共享

```typescript
const VARIATION_SELECTORS = /[︀-️\u{E0100}-\u{E01EF}]/gu;
const normForFilter = (s: string): string =>
  s.normalize('NFKC').replace(VARIATION_SELECTORS, '').toLowerCase();
```

### 9. 模式: 单元测试 jsdom polyfill

**来源:** `SlashCommandPopup.test.tsx:21-35`

**应用到:**
- `AtMentionPopup.test.tsx`

```typescript
beforeAll(() => {
  if (typeof globalThis.ResizeObserver === 'undefined') {
    globalThis.ResizeObserver = class { observe() {} unobserve() {} disconnect() {} } as unknown as typeof ResizeObserver;
  }
  if (typeof Element !== 'undefined' && !Element.prototype.scrollIntoView) {
    Element.prototype.scrollIntoView = function () {};
  }
});
```

### 10. 模式: lucide-react mock sentinel

**来源:** `SlashToken.test.tsx:24-47`

**应用到:**
- `AtToken.test.tsx`（File / Folder / AlertTriangle）

```typescript
vi.mock('lucide-react', () => {
  const sentinel = (testId: string) => {
    const Cmp = (props: any) => <svg data-testid={testId} {...props} />;
    Cmp.displayName = `MockIcon(${testId})`;
    return Cmp;
  };
  return {
    File: sentinel('file'),
    Folder: sentinel('folder'),
    AlertTriangle: sentinel('alert-triangle'),
  };
});
```

---

## 关键约束（不破坏 08.1/08.2 回归）

- `src/renderer/src/lib/commands/parseInputToTokens.ts` **不动** — 08.3 用独立 `parseAtTokens` 避免签名冲突
- `src/renderer/src/lib/commands/dispatcher.ts` **不动** — `sendMessage` 原文发送含 `@path`
- `src/renderer/src/components/SlashCommand/SlashToken.tsx` **不动** — 视觉参考，08.3 AtToken 沿用同结构
- `src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx` **可选修改** — 抽出 `normForFilter` 到 `pathUtils.ts`（建议 modify 08.1 import path 走向 DRY）
- `src/renderer/src/lib/commands/parseInputToTokens.test.ts` / `dispatcher.test.ts` / `formatTokenLabel.test.ts` 全部 **不动** — 回归保护

---

## 元数据

**类比搜索范围:**
- `src/renderer/src/components/SlashCommand/`
- `src/renderer/src/components/ChatArea/`
- `src/renderer/src/lib/commands/`
- `src/renderer/src/stores/`
- `src/main/commands/`
- `src/main/ipc-handlers.ts`
- `src/preload/index.ts`
- `src/shared/types.ts`

**扫描文件数:** 30+

**模式提取日期:** 2026-06-07

**覆盖率:**
- 精确类比: 7 个文件（SlashToken, SlashCommandPopup, ChatArea overlay, ipc-handler 模式, preload, types, MessageItem render）
- 角色匹配: 4 个文件（store, utility, gitignore-loader, candidate-lister）
- 无类比: 0
