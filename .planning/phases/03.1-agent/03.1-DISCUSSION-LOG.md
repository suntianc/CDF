# Phase 03.1: agent - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-26
**Phase:** 03.1-agent
**Areas discussed:** 委派触发, 白名单配置, Agent映射, 任务包, responseFormat, 异常包装, TaskPanel

---

## 委派触发机制

| Option | Description | Selected |
|--------|-------------|----------|
| System Prompt 指令 | Master Agent 根据 system prompt 判断是否委派 | ✓（混合模式） |
| 前端显式触发 | 用户点击按钮显式触发 | ✓（混合模式） |
| 纯系统指令 | 无用户干预 | |

**User's choice:** 混合模式
**Notes:** System Prompt 指令 + 用户可干预

---

## 子Agent白名单

| Option | Description | Selected |
|--------|-------------|----------|
| subagentIds 白名单 | 只允许调用白名单内的 Agent | ✓ |
| 全部 Agent 可委派 | Agent 资产库中所有 Agent 都可作为子Agent | |
| 动态发现 | 运行时根据任务内容匹配 | |

**User's choice:** subagentIds 白名单
**Notes:** 未配置 subagentIds 时 task tool 不可用

---

## Agent → subagent 映射

| Topic | Decision |
|-------|----------|
| task(name) 标识符 | 使用 Agent.slug（稳定 slug/id） |
| general-purpose | 保持禁用 |
| responseFormat | 统一 DelegatedTaskResultSchema |

**Notes:**
- D-07: 映射结构（name/description/systemPrompt/tools/model/responseFormat）
- D-08: `name` 使用 Agent.slug
- D-09: generalPurposeSubagent 保持 disabled

---

## 任务包（Task Payload）

| Topic | Decision |
|-------|----------|
| checkpoint 继承 | 不继承父级完整 checkpoint |
| 传递方式 | 通过 task 参数传递 JSON 字符串 |
| 内容 | 7 要素：任务目标、用户约束、文件路径、根目录、期望产物、必要历史摘要 |

**User's choice:** 只通过 task 参数接收任务包，不继承 checkpoint

---

## responseFormat

| Topic | Decision |
|-------|----------|
| Schema | 统一 DelegatedTaskResultSchema |
| 定义 | status/artifacts/summary/error |
| 强制 | 所有子Agent 强制使用 |

**User's choice:** 统一 Zod schema，不依赖 system prompt

---

## 异常状态包装

| Error Type | Handler |
|-----------|---------|
| timeout | Runtime 包装为 `{ status: "failure", error: { code: "TIMEOUT" } }` |
| interrupted | Runtime 包装为 `{ status: "failure", error: { code: "INTERRUPTED" } }` |
| parse_failed | Runtime 包装为 `{ status: "failure", error: { code: "PARSE_FAILED" } }` |

**User's choice:** Runtime 负责所有异常状态包装

---

## TaskPanel

| Topic | Decision |
|-------|----------|
| 识别方式 | stream metadata 和 task events |
| 节点类型 | delegated-task |
| 状态 | running / success / failure（含 timeout/interrupted/parse_failed） |

**User's choice:** 扩展 TaskPanel 支持 delegated-task 节点

---

## 审查意见（第二版）

**Suntc 君第二版审查要点：**
1. Master 通过 subagentIds 白名单加载可委派 Agent ✓
2. 使用稳定 slug/id 作为 task(name) ✓
3. 默认 general-purpose 保持禁用 ✓
4. 子 Agent 不继承完整 checkpoint，通过 task 参数接收任务包 ✓
5. 统一 DelegatedTaskResultSchema responseFormat ✓
6. Runtime 负责 timeout / interrupted / parse_failed 状态包装 ✓
7. TaskPanel 根据 stream metadata 和 task events 展示 ✓

---

## Deferred Ideas

- 子Agent 并发执行数量限制 — future phase
- 复杂重试策略 — future phase
- Agent 资产库「可委派」标志位 UI — future phase
- 自定义超时阈值配置 UI — future phase
