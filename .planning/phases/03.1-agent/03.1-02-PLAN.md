---
phase: "03.1"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/renderer/src/components/TaskPanel/TaskPanel.tsx
  - src/main/llm.ts
  - src/shared/types.ts
autonomous: true
requirements:
  - AGNT-03
  - AGNT-05
must_haves:
  truths:
    - "TaskPanel displays delegated task nodes with running/success/failure status"
    - "Delegated task errors show error.code (TIMEOUT/INTERRUPTED/PARSE_FAILED/other)"
    - "LLM stream emits delegated_task_start/delegated_task_end events"
  artifacts:
    - path: src/renderer/src/components/TaskPanel/TaskPanel.tsx
      provides: Delegated task node rendering in TaskPanel
      min_lines: 60
    - path: src/main/llm.ts
      provides: delegated-task stream event emission
      min_lines: 40
  key_links:
    - from: src/main/llm.ts
      to: src/renderer/src/components/TaskPanel/TaskPanel.tsx
      via: LLMStreamEvent type delegated_task_start/end
      pattern: "delegated_task"
---

<objective>
实现 TaskPanel 对委派任务的展示，以及 LLM stream 对 delegated-task 事件的发射。

TaskPanel 需要：
1. 识别 delegated_task_start / delegated_task_chunk / delegated_task_end 事件
2. 展示委派任务节点，显示 agent 名称、状态（running/success/failure）
3. 失败时显示 error.code

LLM 层需要：
1. 在工具调用为 'task' 时发射 delegated_task_start 事件
2. 在工具调用完成时发射 delegated_task_end 事件
3. 对于 timeout/interrupted/parse 异常进行包装
4. 实现 buildTaskPackage 辅助函数用于构建 task 工具输入
</objective>

<context>
@src/renderer/src/components/TaskPanel/TaskPanel.tsx
@src/main/llm.ts
@src/shared/types.ts

# Event types (added in Plan 01)

From src/shared/types.ts (DelegatedTaskResult and LLMStreamEvent):
```typescript
export const DelegatedTaskResultSchema = DELEGATED_TASK_RESULT_SCHEMA;  // imported from runtime.ts
export type DelegatedTaskResult = z.infer<typeof DelegatedTaskResultSchema>;

export type LLMStreamEvent =
  | { type: 'delegated_task_start'; taskId: string; agentSlug: string; agentName: string; goal: string }
  | { type: 'delegated_task_chunk'; taskId: string; text: string }
  | { type: 'delegated_task_end'; taskId: string; status: 'success' | 'failure'; result?: DelegatedTaskResult; errorCode?: string }
  // ... existing events
```

# Existing LLMStreamEvent handling in llm.ts

The llm.ts runLLMChat function streams events to the renderer via sender.send(channel, { type: '...', ... }).

Tool calls are handled in the toolStreamPromise:
```typescript
for await (const call of run.toolCalls) {
  const toolCallId = call.callId || crypto.randomUUID();
  upsertToolCall(runId, toolCallId, call.name, call.input);
  sender.send(channel, {
    type: 'tool_start',
    id: toolCallId,
    name: call.name,
    input: call.input,
  });
  // ...
}
```

# Existing TaskPanel structure

From TaskPanel.tsx:
```typescript
interface TaskPanelProps {
  isOpen: boolean;
  onClose: () => void;
  width: number;
  onResize: (width: number) => void;
}

function RunStatusIcon({ status }: { status?: AgentRunStatus }) {
  switch (status) {
    case 'completed':
      return <CheckCircle className="w-4 h-4 text-[var(--color-success)]" />;
    case 'failed':
      return <XCircle className="w-4 h-4 text-[var(--color-danger)]" />;
    // ...
  }
}
```

TaskPanel currently only handles AgentRunStatus (run_started, run_updated, approval_required). It needs to handle delegated-task events.

# buildTaskPackage context

Per D-03, Agent.slug is the task(name) unique stable key. The task tool input is constructed as:
```typescript
interface TaskPackage {
  name: string;  // subagent's slug (stable key)
  goal: string;  // task description
}
```

buildTaskPackage should be implemented in the IPC/preload layer to construct this package when the renderer initiates a delegated task.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sessionStore to track delegated tasks</name>
<files>src/renderer/src/stores/sessionStore.ts</files>
<action>
在 sessionStore.ts 中添加对 delegated-task 事件的处理。

1. 定义 DelegatedTask 类型（在 store 内部或单独导出）：
```typescript
interface DelegatedTask {
  taskId: string;
  agentSlug: string;
  agentName: string;
  goal: string;
  status: 'running' | 'success' | 'failure';
  chunks: string[];
  result?: {
    status: 'success' | 'failure';
    artifacts: string[];
    summary: string;
    error?: { code: string; message: string };
  };
  errorCode?: string;
}
```

2. 在 SessionState 接口中添加 delegatedTasks 状态：
```typescript
delegatedTasks: DelegatedTask[];
```

3. 在 useSessionStore initial state 中添加：
```typescript
delegatedTasks: [],
```

4. 在 sendMessage 的 streamPromise 中，添加对 delegated_task_start / delegated_task_chunk / delegated_task_end 事件的处理（放在 tool_start 处理附近）：
```typescript
// delegated_task_start
if (data.type === 'delegated_task_start') {
  set((state) => ({
    delegatedTasks: [
      ...state.delegatedTasks,
      {
        taskId: data.taskId,
        agentSlug: data.agentSlug,
        agentName: data.agentName,
        goal: data.goal,
        status: 'running',
        chunks: [],
      },
    ],
  }));
  return;
}

// delegated_task_chunk
if (data.type === 'delegated_task_chunk') {
  set((state) => ({
    delegatedTasks: state.delegatedTasks.map((task) =>
      task.taskId === data.taskId
        ? { ...task, chunks: [...task.chunks, data.text] }
        : task
    ),
  }));
  return;
}

// delegated_task_end
if (data.type === 'delegated_task_end') {
  set((state) => ({
    delegatedTasks: state.delegatedTasks.map((task) =>
      task.taskId === data.taskId
        ? {
            ...task,
            status: data.status,
            result: data.result,
            errorCode: data.errorCode,
          }
        : task
    ),
  }));
  return;
}
```
</action>
<verify>
<automated>npx tsc --noEmit src/renderer/src/stores/sessionStore.ts 2>&1 | head -10</automated>
</verify>
<done>sessionStore tracks delegatedTasks array with running/success/failure states</done>
</task>

<task type="auto">
  <name>Task 2: Update TaskPanel to render delegated task nodes</name>
<files>src/renderer/src/components/TaskPanel/TaskPanel.tsx</files>
<action>
修改 TaskPanel.tsx 以展示委派任务节点。

1. 导入 DelegatedTask 类型（从 sessionStore 或定义内联）：
```typescript
import type { DelegatedTask } from '../../stores/sessionStore';
```

2. 在 TaskPanel 组件中添加 delegatedTasks 读取：
```typescript
const {
  // ... existing
  delegatedTasks,
} = useSessionStore();
```

3. 添加 DelegatedTask 状态图标（放在 RunStatusIcon 附近）：
```typescript
function DelegatedTaskStatusIcon({ status }: { status: 'running' | 'success' | 'failure' }) {
  switch (status) {
    case 'running':
      return <Loader className="w-4 h-4 animate-spin text-[var(--color-accent)]" />;
    case 'success':
      return <CheckCircle className="w-4 h-4 text-[var(--color-success)]" />;
    case 'failure':
      return <XCircle className="w-4 h-4 text-[var(--color-danger)]" />;
  }
}
```

4. 在 TaskPanel 的渲染部分，找到 `{activeRun && toolSummary.total === 0 && !pendingApproval && (...)}` 之后，添加 delegated tasks 渲染：
```typescript
{delegatedTasks.length > 0 && (
  <div className="space-y-3">
    <div className="text-xs font-semibold text-[var(--color-text-primary)]">委派任务</div>
    {delegatedTasks.map((task) => (
      <div
        key={task.taskId}
        className="rounded-lg border border-[var(--color-border)] bg-[var(--color-bg-surface)] p-3 space-y-2"
      >
        <div className="flex items-center gap-2">
          <DelegatedTaskStatusIcon status={task.status} />
          <div className="text-sm font-medium text-[var(--color-text-primary)] truncate">
            {task.agentName}
          </div>
          <div className="text-[10px] text-[var(--color-text-muted)] font-mono">
            {task.agentSlug}
          </div>
        </div>
        {task.goal && (
          <div className="text-[11px] text-[var(--color-text-muted)] line-clamp-2">
            {task.goal}
          </div>
        )}
        {task.status === 'failure' && task.errorCode && (
          <div className="text-xs text-[var(--color-danger)]">
            任务执行失败: {task.errorCode}
            {task.result?.error?.message && ` - ${task.result.error.message}`}
          </div>
        )}
        {task.status === 'success' && task.result?.summary && (
          <div className="text-[11px] text-[var(--color-text-muted)] line-clamp-2">
            {task.result.summary}
          </div>
        )}
      </div>
    ))}
  </div>
)}

{delegatedTasks.length === 0 && activeRun && toolSummary.total === 0 && !pendingApproval && (
  <div className="text-xs text-[var(--color-text-muted)]">暂无委派子任务</div>
)}
```

5. 确保导入 Loader, CheckCircle, XCircle（已在文件顶部导入）
</action>
<verify>
<automated>npx tsc --noEmit src/renderer/src/components/TaskPanel/TaskPanel.tsx 2>&1 | head -10</automated>
</verify>
<done>TaskPanel renders delegated tasks with agent name, status icon, goal, and error code on failure</done>
</task>

<task type="auto">
  <name>Task 3: Emit delegated-task stream events in llm.ts</name>
<files>src/main/llm.ts</files>
<action>
修改 src/main/llm.ts 以发射 delegated-task 事件。

1. 导入 DELEGATED_TASK_RESULT_SCHEMA（从 runtime.ts）：
```typescript
import { DELEGATED_TASK_RESULT_SCHEMA } from './deepagent/runtime';
```

2. 在 toolStreamPromise 中，找到 `for await (const call of run.toolCalls)` 循环，在工具调用开始处添加对 'task' 工具的检测：

在 `sender.send(channel, { type: 'tool_start', ... })` 之后添加：
```typescript
// D-12: 检测 task 工具调用，发射 delegated_task_start
if (call.name === 'task') {
  const taskId = toolCallId;
  const input = call.input as { name?: string; task?: string };
  let agentSlug = input?.name || 'unknown';
  let goal = '';

  // D-03: task 输入的 task 字段是 JSON 字符串，包含 goal
  if (input?.task) {
    try {
      const taskPackage = JSON.parse(input.task);
      goal = taskPackage.goal || '';
    } catch {
      // ignore parse error
    }
  }

  // 从 subagents 列表中查找 agentName（如果可用）
  const agentName = agentSlug;  // fallback to slug

  sender.send(channel, {
    type: 'delegated_task_start',
    taskId,
    agentSlug,
    agentName,
    goal,
  });
}
```

3. 在工具调用输出处理处（`const output = await call.output` 附近），对 'task' 工具的输出进行解析并发射 delegated_task_end：

找到 `updateToolCall(toolCallId, 'success', output)` 之后添加：
```typescript
// D-11/D-14: 对 task 工具输出进行异常包装和 delegated_task_end 发射
if (call.name === 'task') {
  let parsedResult;
  let errorCode: string | undefined;
  let status: 'success' | 'failure' = 'success';

  try {
    // 尝试解析为 JSON
    const rawOutput = typeof output === 'string' ? output : JSON.stringify(output);
    const parsed = DELEGATED_TASK_RESULT_SCHEMA.safeParse(JSON.parse(rawOutput));
    if (parsed.success) {
      parsedResult = parsed.data;
      if (parsedResult.status === 'failure') {
        status = 'failure';
        errorCode = parsedResult.error?.code;
      }
    } else {
      // 解析失败，包装为 PARSE_FAILED
      status = 'failure';
      errorCode = 'PARSE_FAILED';
      parsedResult = {
        status: 'failure',
        artifacts: [],
        summary: '',
        error: { code: 'PARSE_FAILED', message: `无法解析子Agent返回: ${rawOutput.slice(0, 200)}` },
      };
    }
  } catch (err: any) {
    status = 'failure';
    errorCode = 'PARSE_FAILED';
    parsedResult = {
      status: 'failure',
      artifacts: [],
      summary: '',
      error: { code: 'PARSE_FAILED', message: err?.message || 'unknown parse error' },
    };
  }

  sender.send(channel, {
    type: 'delegated_task_end',
    taskId: toolCallId,
    status,
    result: parsedResult,
    errorCode,
  });
}
```

4. 对于工具调用错误（catch block），同样需要发射 delegated_task_end：
在 `updateToolCall(toolCallId, 'error', undefined, error?.message || String(error))` 之后添加：
```typescript
if (call.name === 'task') {
  // D-11: 判断错误类型并包装
  let errorCode = 'UNKNOWN';
  const msg = error?.message || '';
  if (msg.toLowerCase().includes('timeout')) {
    errorCode = 'TIMEOUT';
  } else if (msg.toLowerCase().includes('interrupt') || msg.toLowerCase().includes('cancel')) {
    errorCode = 'INTERRUPTED';
  }

  sender.send(channel, {
    type: 'delegated_task_end',
    taskId: toolCallId,
    status: 'failure',
    result: {
      status: 'failure',
      artifacts: [],
      summary: '',
      error: { code: errorCode, message: msg },
    },
    errorCode,
  });
}
```
</action>
<verify>
<automated>npx tsc --noEmit src/main/llm.ts 2>&1 | head -10</automated>
</verify>
<done>llm.ts emits delegated_task_start on task tool call, delegated_task_end on task completion with parsed result or wrapped error</done>
</task>

<task type="auto">
  <name>Task 4: Add buildTaskPackage helper function</name>
<files>src/main/llm.ts</files>
<action>
在 src/main/llm.ts 中添加 buildTaskPackage 辅助函数，用于构建 task 工具的输入参数。

在文件顶部（导入区域之后）添加：

```typescript
/**
 * 构建 task 工具的输入参数包
 * D-03: Agent.slug 是 task(name) 的唯一稳定键
 *
 * @param agentSlug - 子 Agent 的 slug（稳定键）
 * @param goal - 任务描述
 * @returns task 工具输入对象 { name, task: jsonString }
 */
export function buildTaskPackage(agentSlug: string, goal: string): { name: string; task: string } {
  const taskPackage = {
    name: agentSlug,
    goal,
  };
  return {
    name: agentSlug,
    task: JSON.stringify(taskPackage),
  };
}
```

此函数在 IPC 层使用，当渲染进程需要主动触发一个委派任务时调用。
</action>
<verify>
<automated>npx tsc --noEmit src/main/llm.ts 2>&1 | head -10</automated>
</verify>
<done>buildTaskPackage function exported from llm.ts, constructs { name, task } input for task tool</done>
</task>

</tasks>

<verification>
1. TypeScript 编译通过：
   - `npx tsc --noEmit src/shared/types.ts` — 检查类型定义
   - `npx tsc --noEmit src/main/llm.ts` — 检查 llm.ts 编译
   - `npx tsc --noEmit src/renderer/src/components/TaskPanel/TaskPanel.tsx` — 检查 UI 编译
   - `npx tsc --noEmit src/renderer/src/stores/sessionStore.ts` — 检查 store 编译
2. sessionStore 正确处理 delegated_task_* 事件
3. TaskPanel 渲染 delegated tasks（验证 UI 组件存在）
4. llm.ts 在 task 工具调用时发射正确的事件类型
5. buildTaskPackage 函数存在且导出
</verification>

<success_criteria>
- [ ] delegated_task_start 事件在 task 工具调用开始时发射，包含 taskId, agentSlug, agentName, goal
- [ ] delegated_task_end 事件在 task 工具调用完成时发射，包含 status, result, errorCode
- [ ] 解析失败时 errorCode 为 PARSE_FAILED
- [ ] TaskPanel 展示委派任务节点，状态图标正确
- [ ] 失败时显示 errorCode
- [ ] 空状态显示"暂无委派子任务"
- [ ] buildTaskPackage 函数正确构建 task 输入包
</success_criteria>

<output>
创建 `.planning/phases/03.1-agent/03.1-02-SUMMARY.md`
</output>
