---
phase: 03.2-deepagents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/deepagent/runtime.ts
autonomous: false
requirements:
  - REVIEW-03.2-RUNTIME
must_haves:
  truths:
    - "createDeepAgent 参数与官方文档完全一致"
    - "FilesystemBackend 使用 CompositeBackend 包装，项目文件与内部数据隔离"
    - "HarnessProfile 注册利用了所有可用字段"
    - "自定义 middleware 不与默认 middleware 冲突"
  artifacts:
    - path: "src/main/deepagent/runtime.ts"
      provides: "Runtime 创建与配置"
      contains: "CompositeBackend"
  key_links:
    - from: "src/main/deepagent/runtime.ts"
      to: "deepagents SDK"
      via: "createDeepAgent"
      pattern: "createDeepAgent"
---

<objective>
模块 1 复核：任务规划与协调 — Runtime 创建、Agent 配置、createDeepAgent 参数

Purpose: 对照 deepagents.js 官方文档，检查 createDeepAgent 调用参数完整性、FilesystemBackend 是否正确使用 CompositeBackend 包装、HarnessProfile 是否充分利用、自定义 middleware 是否与默认 middleware 冲突。发现问题直接修复。

Output: 模块 1 复核报告 + 代码修复
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-deepagents/03.2-CONTEXT.md
@.planning/phases/03.2-deepagents/03.2-RESEARCH.md
@src/main/deepagent/runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 复核 createDeepAgent 参数与 Backend 体系</name>
  <files>src/main/deepagent/runtime.ts</files>
  <read_first>
    - src/main/deepagent/runtime.ts (当前 Runtime 创建代码)
    - .planning/phases/03.2-deepagents/03.2-RESEARCH.md (Module 1 部分：问题 1.1-1.7)
    - node_modules/deepagents/dist/index.d.ts (createDeepAgent 参数签名、FilesystemBackend/CompositeBackend/StateBackend 类型定义)
  </read_first>
  <action>
按以下步骤逐一检查并修复（per D-03, D-04, D-05）：

**检查项 1: createDeepAgent 参数完整性**
对照 `node_modules/deepagents/dist/index.d.ts` 中 `createDeepAgent` 的参数类型定义，确认当前 `runtime.ts` 第 535-547 行的调用是否覆盖了所有应该传入的参数。重点关注：
- `responseFormat` — 当前未传入，确认主 Agent 是否需要
- `store` — 当前未传入，确认是否需要长期记忆
- `contextSchema` — 当前未传入，确认是否需要

**检查项 2: CompositeBackend 替换 FilesystemBackend**（per 研究问题 1.4 / 2.1）
当前代码第 437 行 `new FilesystemBackend({ rootDir: project.path, virtualMode: true })` 使用裸 FilesystemBackend。官方文档明确推荐使用 CompositeBackend 包装：
- 默认 backend 使用 StateBackend（线程作用域，内部数据存在 langgraph state 中）
- 项目文件路由到 `/workspace/` 前缀，使用 FilesystemBackend

修改 `runtime.ts`：
1. 从 `deepagents` 导入 `CompositeBackend` 和 `StateBackend`（与现有 `FilesystemBackend` 同源）
2. 将第 437 行替换为 CompositeBackend 配置：
   - `new CompositeBackend(new StateBackend(), { "/workspace/": new FilesystemBackend({ rootDir: project.path, virtualMode: true }) })`
3. 同时需要更新 `buildProjectContext` 函数中的路径说明（第 294-296 行），将 `/` 改为 `/workspace/` 作为项目文件的虚拟根目录
4. 检查 `resolveAgentSkillsConfig` 中的 `toVirtualPath` 函数是否需要调整路径前缀

**检查项 3: HarnessProfile 字段利用**（per 研究问题 1.2）
当前 `registerCdfHarnessProfile` 第 216-244 行只设置了 `generalPurposeSubagent` 和 `excludedTools`。检查 `node_modules/deepagents/dist/index.d.ts` 中 HarnessProfile 类型定义，确认以下字段是否应该配置：
- `baseSystemPrompt`
- `systemPromptSuffix`
- `toolDescriptionOverrides`
如果这些字段有实际用途，在 profile 中添加合理的默认值。

**检查项 4: 自定义 middleware 与默认 middleware**（per 研究问题 1.6）
当前第 543 行只传入了 `createRecoverableToolErrorMiddleware()`。确认 deepagents 内置的默认 middleware（TodoListMiddleware、FilesystemMiddleware 等）是否在 createDeepAgent 内部自动添加，还是需要显式传入。查看 `node_modules/deepagents/dist/index.d.ts` 或源码中 createDeepAgent 的实现逻辑。
  </action>
  <verify>
    <automated>grep -c "CompositeBackend" src/main/deepagent/runtime.ts && grep -c "StateBackend" src/main/deepagent/runtime.ts</automated>
  </verify>
  <acceptance_criteria>
    - CompositeBackend 和 StateBackend 出现在 runtime.ts 的 import 语句中
    - createDeepAgent 调用中 `backend` 参数使用 CompositeBackend 包装，不再是裸 FilesystemBackend
    - buildProjectContext 中的路径说明更新为 `/workspace/` 前缀
    - grep 输出 `CompositeBackend` 计数 >= 2（import + 使用）
  </acceptance_criteria>
  <done>createDeepAgent 参数与官方文档一致；FilesystemBackend 使用 CompositeBackend 包装；项目文件与内部数据路径隔离</done>
</task>

<task type="auto">
  <name>Task 2: 生成模块 1 复核报告并修复剩余问题</name>
  <files>src/main/deepagent/runtime.ts</files>
  <read_first>
    - src/main/deepagent/runtime.ts (Task 1 修复后的最新版本)
    - .planning/phases/03.2-deepagents/03.2-RESEARCH.md (Module 1 部分，确认所有问题已覆盖)
  </read_first>
  <action>
完成 Task 1 中剩余未修复的检查项（HarnessProfile 字段、middleware 冲突确认）。

然后在终端输出模块 1 复核报告，格式如下（per D-12）：

```
=== 模块 1：任务规划与协调 复核报告 ===

【检查项清单】
| # | 检查项 | 官方文档依据 | 结果 |
|---|--------|-------------|------|
| 1.1 | createDeepAgent 参数完整性 | docs.langchain.com/oss/javascript/deepagents/customization | ✓/✗ |
| 1.2 | HarnessProfile 字段利用 | node_modules/deepagents/dist/index.d.ts | ✓/✗ |
| 1.3 | FilesystemBackend virtualMode | docs.langchain.com/oss/javascript/deepagents/backends | ✓/✗ |
| 1.4 | CompositeBackend 包装 | docs.langchain.com/oss/javascript/deepagents/backends | ✓/✗ |
| 1.5 | SqliteSaver checkpointer | docs.langchain.com/oss/javascript/deepagents/customization | ✓/✗ |
| 1.6 | 自定义 middleware 兼容性 | docs.langchain.com/oss/javascript/deepagents/customization | ✓/✗ |
| 1.7 | registerHarnessProfile API | node_modules/deepagents/dist/index.d.ts | ✓/✗ |

【发现的问题】
| # | 严重程度 | 问题描述 | 修复状态 |
|---|----------|----------|----------|
（列出所有发现的问题及修复状态：已修复 / 不需要修复 / 待确认）

【建议的后续关注点】
（如有）
```
  </action>
  <verify>
    <automated>echo "Module 1 review report generated"</automated>
  </verify>
  <acceptance_criteria>
    - 终端输出包含完整的「模块 1：任务规划与协调 复核报告」
    - 报告包含检查项清单、发现的问题、修复状态三个部分
    - 每个检查项有明确的 ✓ 或 ✗ 标记
    - CompositeBackend 替换已在代码中完成（如 Task 1 验证通过）
  </acceptance_criteria>
  <done>模块 1 复核报告已输出，所有可修复的问题已修复</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>模块 1「任务规划与协调」复核完成，已修复 createDeepAgent 参数配置、将 FilesystemBackend 替换为 CompositeBackend 包装</what-built>
  <how-to-verify>
    1. 查看上方终端输出的模块 1 复核报告
    2. 确认所有检查项的标记是否正确
    3. 确认发现的问题修复状态是否合理
    4. 如有疑问或需要调整，在回复中说明
  </how-to-verify>
  <resume-signal>确认无问题后回复 "approved"；如有问题请描述</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deepagents SDK → 项目代码 | SDK API 变更可能破坏集成 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-01 | Tampering | npm/pip/cargo installs | accept | 已在 Phase 3 审核过，本阶段无新包安装 |
| T-03.2-02 | Information Disclosure | FilesystemBackend rootDir | mitigate | CompositeBackend 包装隔离项目文件与内部数据 |
</threat_model>

<verification>
- CompositeBackend 和 StateBackend 正确导入并使用
- createDeepAgent 参数与官方文档类型定义一致
- 项目文件虚拟路径从 `/` 更新为 `/workspace/`
</verification>

<success_criteria>
- CompositeBackend 替换完成，项目文件路径更新
- 模块 1 复核报告输出，用户确认
</success_criteria>

<output>
Create `.planning/phases/03.2-deepagents/03.2-01-SUMMARY.md` when done
</output>
