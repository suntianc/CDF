---
phase: 03.2-deepagents
plan: 02
type: execute
wave: 2
depends_on:
  - 03.2-01
files_modified:
  - src/main/deepagent/runtime.ts
  - src/main/llm.ts
autonomous: false
requirements:
  - REVIEW-03.2-CONTEXT
must_haves:
  truths:
    - "memory 参数正确加载 AGENTS.md 文件"
    - "checkpoint namespace 不与 deepagents 内部冲突"
    - "buildInputMessages 不会导致消息重复注入"
    - "SummarizationMiddleware 自动处理上下文窗口管理"
  artifacts:
    - path: "src/main/deepagent/runtime.ts"
      provides: "上下文管理逻辑"
      contains: "buildInputMessages"
  key_links:
    - from: "src/main/deepagent/runtime.ts"
      to: "deepagents SummarizationMiddleware"
      via: "createDeepAgent 内置"
      pattern: "SummarizationMiddleware"
---

<objective>
模块 2 复核：环境操作与上下文管理 — 上下文窗口管理、总结策略、会话持久化

Purpose: 对照官方文档检查 Backend 体系使用（CompositeBackend 已在 Plan 01 修复）、memory 参数配置、checkpoint namespace 设置、buildInputMessages 消息注入逻辑是否与 deepagents 内部管理冲突。确认 SummarizationMiddleware 是否自动处理 CHAT-03 需求。

Output: 模块 2 复核报告 + 代码修复
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-deepagents/03.2-CONTEXT.md
@.planning/phases/03.2-deepagents/03.2-RESEARCH.md
@.planning/phases/03.2-deepagents/03.2-01-SUMMARY.md
@src/main/deepagent/runtime.ts
@src/main/llm.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 复核上下文管理与会话持久化机制</name>
  <files>src/main/deepagent/runtime.ts, src/main/llm.ts</files>
  <read_first>
    - src/main/deepagent/runtime.ts (当前版本，含 Plan 01 修复)
    - src/main/llm.ts (流式处理与上下文管理)
    - .planning/phases/03.2-deepagents/03.2-RESEARCH.md (Module 2 部分：问题 2.1-2.6)
    - node_modules/deepagents/dist/index.d.ts (SummarizationMiddleware 类型定义、memory 参数类型)
  </read_first>
  <action>
按以下步骤逐一检查并修复（per D-03, D-04, D-05）：

**检查项 1: memory 参数配置**（per 研究问题 2.2）
确认 runtime.ts 第 443-446 行的 memory 配置：
- `['AGENTS.md', 'Claude.md'].filter(...)` 只取第一个文件（`.slice(0, 1)`）
- 检查官方文档中 memory 参数是否支持多个文件路径
- 如果支持多个，评估是否应该移除 `.slice(0, 1)` 限制

**检查项 2: DEEPAGENT_CHECKPOINT_NAMESPACE**（per 研究问题 2.4）
当前第 51 行 `export const DEEPAGENT_CHECKPOINT_NAMESPACE = ''` 为空字符串。
- 在 deepagents 源码中搜索 `checkpoint_ns` 或 `checkpointNamespace` 的默认值
- 确认空字符串是否与 deepagents 内部默认 namespace 冲突
- 如有冲突，设置为项目专用 namespace（如 `'cdf-agent'`）

**检查项 3: buildInputMessages 消息重复风险**（per 研究问题 2.6）
当前 runtime.ts 第 260-274 行：
- 无 checkpoint 时注入完整历史消息
- 有 checkpoint 时只注入当前消息

检查 deepagents 内部是否也管理消息历史。如果 deepagents 的 checkpointer 已经保存了完整对话历史，buildInputMessages 在无 checkpoint 时注入历史可能导致重复。

需要确认：
1. deepagents 的 `agent.invoke()` / `agent.streamEvents()` 是否会从 checkpointer 加载历史消息
2. 如果会，buildInputMessages 是否应该始终只注入当前消息（移除历史注入逻辑）

**检查项 4: SummarizationMiddleware 确认**（per 研究问题 2.5）
检查 deepagents 源码或文档确认：
- createDeepAgent 是否自动添加 SummarizationMiddleware
- 如果自动添加，CHAT-03（85% 阈值自动总结）需求是否已满足
- 如果未自动添加，需要显式配置

**检查项 5: todo 轮询机制**
llm.ts 第 293-312 行使用 500ms 间隔轮询 todo 状态。检查 deepagents 内置的 TodoListMiddleware 是否提供类似功能（通过事件流推送 todo 更新），如果提供，自定义轮询机制可以移除。
  </action>
  <verify>
    <automated>grep -n "checkpoint_ns\|DEEPAGENT_CHECKPOINT_NAMESPACE" src/main/deepagent/runtime.ts</automated>
  </verify>
  <acceptance_criteria>
    - checkpoint namespace 值已确认不会与 deepagents 内部冲突（如果冲突则已修改为专用值）
    - buildInputMessages 逻辑已确认不会导致消息重复（如确认重复则已修复）
    - SummarizationMiddleware 自动添加状态已确认
    - memory 参数 `.slice(0, 1)` 限制已评估是否合理
  </acceptance_criteria>
  <done>上下文管理机制与官方文档一致，checkpoint namespace 无冲突，消息注入无重复</done>
</task>

<task type="auto">
  <name>Task 2: 生成模块 2 复核报告</name>
  <files>src/main/deepagent/runtime.ts</files>
  <read_first>
    - src/main/deepagent/runtime.ts (Task 1 修复后的最新版本)
    - .planning/phases/03.2-deepagents/03.2-RESEARCH.md (Module 2 部分)
  </read_first>
  <action>
完成 Task 1 中剩余未修复的检查项。

然后在终端输出模块 2 复核报告，格式如下（per D-12）：

```
=== 模块 2：环境操作与上下文管理 复核报告 ===

【检查项清单】
| # | 检查项 | 官方文档依据 | 结果 |
|---|--------|-------------|------|
| 2.1 | FilesystemBackend → CompositeBackend | docs.langchain.com/.../backends | ✓ (Plan 01 已修复) |
| 2.2 | memory 参数配置 | docs.langchain.com/.../memory | ✓/✗ |
| 2.3 | SqliteSaver checkpointer | docs.langchain.com/.../human-in-the-loop | ✓/✗ |
| 2.4 | DEEPAGENT_CHECKPOINT_NAMESPACE | — | ✓/✗ |
| 2.5 | SummarizationMiddleware 自动处理 | docs.langchain.com/.../customization | ✓/✗ |
| 2.6 | buildInputMessages 无重复注入 | — | ✓/✗ |

【发现的问题】
| # | 严重程度 | 问题描述 | 修复状态 |
|---|----------|----------|----------|

【建议的后续关注点】
```
  </action>
  <verify>
    <automated>echo "Module 2 review report generated"</automated>
  </verify>
  <acceptance_criteria>
    - 终端输出包含完整的「模块 2：环境操作与上下文管理 复核报告」
    - 报告包含检查项清单、发现的问题、修复状态三个部分
    - 每个检查项有明确的 ✓ 或 ✗ 标记
  </acceptance_criteria>
  <done>模块 2 复核报告已输出，所有可修复的问题已修复</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>模块 2「环境操作与上下文管理」复核完成，检查了 memory 参数、checkpoint namespace、消息注入逻辑、SummarizationMiddleware</what-built>
  <how-to-verify>
    1. 查看上方终端输出的模块 2 复核报告
    2. 确认所有检查项的标记是否正确
    3. 确认发现的问题修复状态是否合理
  </how-to-verify>
  <resume-signal>确认无问题后回复 "approved"；如有问题请描述</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint 数据 → 会话恢复 | checkpoint namespace 冲突可能导致数据混乱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-03 | Tampering | checkpoint namespace | mitigate | 确认 namespace 不与 deepagents 内部冲突 |
| T-03.2-04 | Information Disclosure | message history injection | mitigate | 确认不会重复注入历史消息 |
</threat_model>

<verification>
- checkpoint namespace 不与 deepagents 内部默认值冲突
- buildInputMessages 不会导致消息重复
- SummarizationMiddleware 自动处理状态已确认
</verification>

<success_criteria>
- 上下文管理机制检查完成
- 模块 2 复核报告输出，用户确认
</success_criteria>

<output>
Create `.planning/phases/03.2-deepagents/03.2-02-SUMMARY.md` when done
</output>
