---
phase: 06-4-source-command-registry-dispatcher
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - src/main/commands/chokidar-watcher.ts
  - src/main/ipc-handlers.ts
  - src/preload/index.ts
  - src/shared/types.ts
  - src/renderer/src/lib/commands/system-commands.ts
  - src/renderer/src/lib/commands/dispatcher.ts
  - src/renderer/src/hooks/useCommandRegistry.ts
  - src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
  - src/renderer/src/components/ChatArea/ChatArea.tsx
  - src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx
  - src/renderer/src/lib/commands/dispatcher.test.ts
  - src/renderer/src/hooks/useCommandRegistry.test.ts
  - src/main/commands/chokidar-watcher.test.ts
autonomous: true
requirements:
  - SLASH-04
  - SLASH-13
  - SLASH-DISPATCH
user_setup: []

must_haves:
  truths:
    - Renderer can call `window.electronAPI.commands.list(projectId, agentId)` and receive `{ commands, conflicts, warnings }`; preload bridges to main IPC `commands:list` (D-15: O(1) memory read, not lazy IPC)
    - chokidar 3.6.0 watcher on `~/.cdf/commands/*.md` + `<projectPath>/.cdf/commands/*.md` with `awaitWriteFinish: { stabilityThreshold: 200 }`; 100ms debounce; on add/change/unlink fires `mainWindow.webContents.send('commands:changed', { source: 'chokidar' })` (D-23 init timing; D-14 incremental updates — custom commands dir + workflow DB listen is Phase 8 polish)
    - chokidar init lives in `app.whenReady` block (P6.6: `os.homedir()` must be ready)
    - chokidar failure is logged via electron-log only (D-24: NO toast, deferred to Phase 8 polish)
    - dispatcher.resolve(inputVal, commands) returns one of 4 CommandDispatchAction kinds based on matched command's source + name (D-01, D-02 args passthrough, D-03 mapping)
    - dispatcher.dispatch(plan) routes SystemSilent/SystemLocal to Phase 7 placeholders (console.log), PlanMode to `sendMessage(content, { overrides: { planOnly: true } })` (via ChatRuntimeOverrides.planOnly field added in Plan 06-01), PluginRewrite to `sendMessage(\`请调用 ${name} 工具，参数：${args || '(无参数)'}\`)` (D-02 args passthrough — string slicing only, no flag parsing; D-18 args not passed to tool schema, only as natural-language `message.content`)
    - useCommandRegistry hook fetches `commands:list` on mount, sets state, fires sonner toasts for each `mcp_health_warning` and each `CommandConflictError`; subscribes to `commands:changed` push event and re-fetches on chokidar or mcp-health source
    - ChatArea.tsx handleSlashSelect extended to call `dispatcher.resolve(inputVal, registry.commands)` on Enter; if resolved, calls `dispatcher.dispatch(plan)`, clears input + closes popup; if not resolved (Tab path or unknown), falls back to old behavior (insert text + close) preserving Phase 5 D-07 Tab-only-inserts-text contract
    - SlashCommandPopup.tsx Command.Item extended to render `<Badge variant="secondary">{cmd.badge}</Badge>` + `/{cmd.name}` + optional description (hidden for MCP per D-09); `data-source` attribute for testing; 7-row layout per Phase 5 UI-SPEC 11px label color-muted (Phase 6 inherits UI-SPEC; only ADDITIONS are source badge column + mcp_health_warning row)
    - mcp_health_warning row renders in SlashCommandPopup above the command list (C-02 choice: top) with AlertCircle icon + 灰行 styling using existing CSS variables
    - Phase 5 known stub (handleKeyDown shiftKey check in SlashCommandPopup) — Plan 06-02 verifies this remains stable; no new shiftKey bypass
  artifacts:
    - path: src/main/commands/chokidar-watcher.ts
      provides: "watchSystemCommandsDir(onChange) + watchProjectCommandsDir(projectPath, onChange); both return stop function; 100ms debounce; awaitWriteFinish 200ms; depth 0; emits 'commands:changed' via mainWindow.webContents.send"
      exports: ["watchSystemCommandsDir", "watchProjectCommandsDir"]
    - path: src/main/ipc-handlers.ts
      provides: "ipcMain.handle('commands:list', ...) + ipcMain.handle('commands:readProjectCommands', ...) + BrowserWindow push channel registration for 'commands:changed'"
      contains: "commands:list"
    - path: src/preload/index.ts
      provides: "electronAPI.commands.{list, readProjectCommands, onChanged} methods; onChanged returns cleanup function"
      contains: "commands:changed"
    - path: src/renderer/src/lib/commands/system-commands.ts
      provides: "3 hardcoded SlashCommand entries matching main collectSystemCommands output (D-08 badge format)"
      exports: ["SYSTEM_COMMANDS"]
    - path: src/renderer/src/lib/commands/dispatcher.ts
      provides: "resolve(inputVal, commands): CommandDispatchAction | null + dispatch(plan): Promise<void>"
      exports: ["resolve", "dispatch"]
    - path: src/renderer/src/hooks/useCommandRegistry.ts
      provides: "useCommandRegistry(projectId, agentId): { commands, conflicts, warnings, loading, reload } with sonner toasts on warnings/conflicts + chokidar event subscription"
      exports: ["useCommandRegistry"]
    - path: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
      provides: "Command.Item renders Badge + name + optional description; data-source attribute; mcp_health_warning row at top when warnings.length > 0; new prop `commands: SlashCommand[]` and `hasMcpWarning: boolean`"
      exports: ["SlashCommandPopup", "SlashCommandPopupHandle", "SlashCommandPopupProps"]
    - path: src/renderer/src/components/ChatArea/ChatArea.tsx
      provides: "handleSlashSelect calls dispatcher.resolve + dispatch; reads commands from useCommandRegistry; on Enter fires dispatcher; on Tab (or no match) falls back to text insert"
      contains: "dispatcher.resolve"
  key_links:
    - from: src/renderer/src/components/ChatArea/ChatArea.tsx
      to: src/renderer/src/lib/commands/dispatcher.ts
      via: "import { resolve, dispatch } from '@/lib/commands/dispatcher'; handleSlashSelect invokes resolve(inputVal, registry.commands)"
      pattern: "dispatcher\\.resolve|import.*dispatcher"
    - from: src/renderer/src/hooks/useCommandRegistry.ts
      to: src/preload/index.ts
      via: "window.electronAPI.commands.list(projectId, agentId) + window.electronAPI.commands.onChanged(callback)"
      pattern: "electronAPI\\.commands"
    - from: src/main/ipc-handlers.ts
      to: src/main/commands/command-registry.ts
      via: "ipcMain.handle('commands:list') calls collectAllCommands(projectPath, agentId) and returns { commands, conflicts, warnings }"
      pattern: "collectAllCommands"
    - from: src/main/commands/chokidar-watcher.ts
      to: src/main/ipc-handlers.ts
      via: "mainWindow.webContents.send('commands:changed', { source: 'chokidar' })"
      pattern: "commands:changed"
    - from: src/renderer/src/lib/commands/dispatcher.ts
      to: src/renderer/src/stores/sessionStore.ts
      via: "useSessionStore.getState().sendMessage(projectId, content, overrides) — for PlanMode passes { planOnly: true }"
      pattern: "useSessionStore.*sendMessage"
    - from: src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
      to: src/renderer/src/components/ui/badge.tsx
      via: "import { Badge } from '@/components/ui/badge'; <Badge variant=\"secondary\">{cmd.badge}</Badge>"
      pattern: "import.*Badge.*badge"
---

<objective>
Wire the renderer-side dispatcher on top of Plan 06-01's main-process data layer: add chokidar double-watch + IPC push channel + preload bridge, build the 4-kind dispatcher (`resolve` + `dispatch`), build the `useCommandRegistry` hook with sonner toasts and chokidar subscription, extend `SlashCommandPopup` to render source badges + description column + `mcp_health_warning` row, and extend `ChatArea.handleSlashSelect` to call dispatcher on Enter (preserve Tab text-insert per D-07). This is the load-bearing integration layer; Plan 06-01's data layer stays unchanged.

Purpose: This is the visible Phase 6 deliverable — `/goal` / `/context` / `/plan` / `/<mcp_tool>` / `/<skill_name>` / `/<workflow>` / `/<custom_command>` all work end-to-end with the source-aware popup. Dispatcher is renderer-side per C-04 (Architectural Responsibility Map line 109-114). PluginRewrite routes through existing `useSessionStore.sendMessage` with no new dispatch IPC channel (D-18 + PITFALLS P2). `/plan` writes the existing `payload.overrides.planOnly` extension point at `llm.ts:324` (do NOT modify llm.ts).

Output: 9 modified/created files in main + preload + renderer + 4 test files extending the Phase 5 test suite by 12+ tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-VALIDATION.md
@.planning/phases/05-popup-shell-keyboard-spike/05-UI-SPEC.md
@.planning/phases/05-popup-shell-keyboard-spike/05-VERIFICATION.md (known stub: SlashCommandPopup handleKeyDown shiftKey check)
@.planning/phases/05-popup-shell-keyboard-spike/05-01-SUMMARY.md
@.planning/phases/05-popup-shell-keyboard-spike/05-02-SUMMARY.md
@.planning/research/PITFALLS.md (P1 focus steal, P2 bypass llm:chat, P5 shift+enter, P6.4 mcpCache, P6.6 homedir timing, P13 IME, P15 IME z-index)

# Source files the executor must read before editing
@.planning/phases/06-4-source-command-registry-dispatcher/06-01-PLAN.md (data layer this plan builds on)
@src/main/commands/command-registry.ts (collectAllCommands shape from Plan 06-01)
@src/shared/types.ts (Plan 06-01 added SlashCommand, CommandDispatchAction, ElectronAPI.commands)
@src/main/ipc-handlers.ts (existing handler pattern at line 43+)
@src/main/index.ts (line 61 app.whenReady — chokidar init hooks here)
@src/preload/index.ts (existing contextBridge.exposeInMainWorld pattern; electronAPI namespace line 1-105)
@src/renderer/src/components/ChatArea/ChatArea.tsx (line 132 state; line 663-666 handleSlashSelect; line 1022 Popover wrap; line 1131 SlashCommandPopup onSelect)
@src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (line 1-130 — extension target)
@src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx (Phase 5 19-test base; this plan EXTENDS not replaces)
@src/renderer/src/stores/sessionStore.ts (line 338 sendMessage signature; line 782 overrides field)
@src/renderer/src/components/ui/badge.tsx (Badge component — variant="secondary" + className override)
</context>

<interfaces>
From src/main/commands/command-registry.ts (Plan 06-01 output):
- `collectAllCommands(projectPath: string, agentId: string): Promise<{ commands: SlashCommand[]; conflicts: CommandConflictError[]; warnings: Array<{ type: 'mcp_health_warning'; message: string }> }>` — IPC handler wraps this and returns the result to renderer.

From src/shared/types.ts (Plan 06-01 output):
- `CommandSource = 'system' | 'mcp' | 'skill:project' | 'skill:global' | 'workflow' | 'cmd:project' | 'cmd:system'`
- `SlashCommand = { name, description, source, target, sourceLabel, badge, argumentHint? }`
- `CommandDispatchAction` discriminated union with 4 kinds: SystemSilent | SystemLocal | PluginRewrite | PlanMode
- `CommandConflictError` class: constructor `(commandName, conflicts: Array<{ source, badge }>)`
- `ChatRuntimeOverrides` now has `planOnly?: boolean` (Plan 06-01 added)
- `ElectronAPI.commands` namespace (Plan 06-01 did NOT add; this plan adds): `{ list, readProjectCommands, onChanged }`

From src/renderer/src/components/ChatArea/ChatArea.tsx:
- `inputVal` state (line 139), `slashOpen` state (line 142), `setInputVal` (line 139), `setSlashOpen` (line 142), `activeSession` (line 243), `activeSessionAgent` (line 560), `currentProjectId` (line 132)
- `sendMessage` is destructured from `useSessionStore` (line 134); signature `(projectId, content, overrides?: ChatRuntimeOverrides) => Promise<void>`.
- `handleSlashSelect = (cmd: string) => { setInputVal(cmd + ' '); setSlashOpen(false); }` (line 663-666) — Phase 5 contract. THIS PLAN EXTENDS it.
- Popover wrap (line 1022) and `<SlashCommandPopup>` (line 1125-1134) — both touched by this plan.

From src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (Phase 5 base):
- Hardcoded `SYSTEM_COMMANDS: ReadonlyArray<{ value, label }>` (line 10-14) — Phase 6 keeps this as a `name` only fallback AND moves to `src/renderer/src/lib/commands/system-commands.ts` with full `SlashCommand` shape for dispatcher.
- Props (line 31-35): `query`, `onSelect`, `onClose` — Phase 6 ADDS: `commands: SlashCommand[]`, `hasMcpWarning?: boolean`.
- `filterCommands` (line 16-25) — Phase 6 extends to search by `name` (not `value`).
- Command.Item (line 110-119) — Phase 6 extends with Badge + name + description.

From src/renderer/src/components/ui/badge.tsx:
- `<Badge variant="secondary" className="...">` — accepts className for sizing override (`text-[10px] px-1.5 py-0` per Phase 5 UI-SPEC label size).

From src/renderer/src/stores/sessionStore.ts (line 338):
- `sendMessage: (projectId: string, content: string, overrides?: ChatRuntimeOverrides) => Promise<void>` — signature unchanged; this plan's dispatcher passes `{ planOnly: true }` for PlanMode, nothing for PluginRewrite (D-18).

From src/main/index.ts (line 61):
- `app.whenReady().then(() => { log.info('App is ready'); registerIpcHandlers(); createWindow(); });` — this plan INSERTS chokidar init into the `then` block after `registerIpcHandlers` (P6.6: `os.homedir()` ready).

From src/main/ipc-handlers.ts (line 43-100 pattern):
- `registerIpcHandlers()` registers handlers + returns void. This plan adds 2 handlers: `commands:list` + `commands:readProjectCommands`.

From src/preload/index.ts (line 1-105):
- `contextBridge.exposeInMainWorld('electronAPI', { ... })` — this plan APPENDS `commands: { list, readProjectCommands, onChanged }` namespace.
</interfaces>

<tasks>

<task type="auto">
  name: "Task 1: Build chokidar-watcher.ts + IPC handlers + preload bridge"
  type: auto
  files: "src/main/commands/chokidar-watcher.ts, src/main/ipc-handlers.ts, src/preload/index.ts, src/main/commands/chokidar-watcher.test.ts"
  read_first:
    - .planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md (D-23 chokidar double-watch + awaitWriteFinish 200ms; D-24 chokidar failure log only; D-25 CI matrix)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md (Pattern 4 chokidar double-watch + debounce; chokidar Cross-Platform section; Pitfall P6.3 debounce; Pitfall P6.6 homedir timing)
    - src/main/commands/command-registry.ts (Plan 06-01 collectAllCommands shape)
    - src/main/index.ts (line 61 app.whenReady hook for chokidar init)
    - src/main/ipc-handlers.ts (line 43-100 handler registration pattern)
    - src/preload/index.ts (line 1-105 electronAPI surface)
    - src/main/logger.ts (electron-log `log.error` import path)
  action: |
    1. Create `src/main/commands/chokidar-watcher.ts` exporting TWO functions (BLOCKER 2 fix — single source of truth; D-23 "两路都用" satisfied by two separate watchers started at two different lifecycle points):
       - `export function watchSystemCommandsDir(onChange: () => Promise<void>): () => void`
         - Watches ONLY `path.join(os.homedir(), '.cdf', 'commands')` (D-19).
         - **WARNING (P6.6):** `os.homedir()` must be ready. Call site MUST be inside `app.whenReady` block in `src/main/index.ts` (P6.6 fix; see step 4). Add a top-of-file `// P6.6: os.homedir() must be called from app.whenReady — see src/main/index.ts init site` comment so the constraint is visible.
         - Implementation: `chokidar.watch(systemDir, { awaitWriteFinish: { stabilityThreshold: 200, pollInterval: 100 }, ignoreInitial: true, depth: 0, usePolling: false })`. `depth: 0` — only top-level `*.md` files, not recursive into subdirs (Pitfall T-06-T6). `usePolling: false` is the default on macOS/Linux (fsevents/inotify); chokidar auto-falls back to polling on Windows when needed.
         - 100ms debounce: define a module-private `debounce<T>(fn: T, ms: number): T` that uses `setTimeout` to coalesce multiple add/change/unlink events. P6.3: chokidar event storm protection.
         - Register listeners: `['add', 'change', 'unlink'].forEach(evt => handle.on(evt, fireDebounced))` where `fireDebounced` is the debounced callback that calls `await onChange()` and then `BrowserWindow.getAllWindows().forEach(w => w.webContents.send('commands:changed', { source: 'chokidar' }))`.
         - On error: `handle.on('error', (err) => log.error('[commands-watcher] system error:', err))`. Per D-24: log only, no toast.
         - Return `() => handle.close()` (sync wrapper around async `chokidar.FSWatcher.close()`).
       - `export function watchProjectCommandsDir(projectPath: string, onChange: () => Promise<void>): () => void`
         - Watches ONLY `path.join(projectPath, '.cdf', 'commands')` (D-19). `projectPath` is REQUIRED (not `''`).
         - Same chokidar options (`awaitWriteFinish: 200ms`, `depth: 0`, `usePolling: false`, `ignoreInitial: true`).
         - Same debounce + listener registration + error handling pattern.
         - Same return type: `() => handle.close()`.
       - Module-private `debounce<T>(fn: T, ms: number): T` helper shared by both functions.
       - Exports list: `["watchSystemCommandsDir", "watchProjectCommandsDir"]` (NOT `watchCommandsDir` — that ambiguous signature was the BLOCKER 2 source of confusion).

    2. Edit `src/main/ipc-handlers.ts` to ADD 2 handlers (do NOT modify existing handlers):
       - `ipcMain.handle('commands:list', async (_, projectId: string, agentId: string) => { ... })`:
         - Look up project path: `const project = db.prepare('SELECT path FROM projects WHERE id = ?').get(projectId) as { path: string } | undefined;`
         - If !project, return `{ commands: [], conflicts: [], warnings: [] }` (no throw).
         - Call `const result = await collectAllCommands(project.path, agentId);` (from Plan 06-01).
         - Return `result` directly.
       - `ipcMain.handle('commands:readProjectCommands', async (_, projectId: string) => { ... })`:
         - Look up project path same as above.
         - If !project, return `{ commands: [] }`.
         - Call `const commands = await collectProjectCommands(project.path);` (re-export from collectors).
         - Return `{ commands }`.
       - Both handlers use `try/catch` for safety (log + return empty result on unexpected error).

    3. Edit `src/preload/index.ts` to ADD a `commands` namespace:
       - Inside the `contextBridge.exposeInMainWorld('electronAPI', { ... })` object, after the `workflow` block (line 420 area), add:
         ```
         commands: {
           list: (projectId: string, agentId: string) =>
             ipcRenderer.invoke('commands:list', projectId, agentId),
           readProjectCommands: (projectId: string) =>
             ipcRenderer.invoke('commands:readProjectCommands', projectId),
           onChanged: (callback: (event: any, data: { source: string }) => void) => {
             const listener = (event: any, data: any) => callback(event, data);
             ipcRenderer.on('commands:changed', listener);
             return () => ipcRenderer.removeListener('commands:changed', listener);
           },
         },
         ```
       - The TypeScript type for this comes from `ElectronAPI.commands` in `src/shared/types.ts`. This plan MUST also update `src/shared/types.ts` `ElectronAPI` interface to add the `commands: { list, readProjectCommands, onChanged }` namespace (since Plan 06-01 only added the `CommandConflictError` etc types, NOT the `commands` namespace on `ElectronAPI`).

    4. Edit `src/main/index.ts` to add chokidar init in the `app.whenReady` block (BLOCKER 2 fix — single deterministic strategy):
       - After `registerIpcHandlers();` and before `createWindow();`.
       - **System watcher** (init in `app.whenReady`, runs for app lifetime): `const stopSystemWatcher = watchSystemCommandsDir(async () => { /* no-op or trigger registry reload */ });` — this fires when `~/.cdf/commands/*.md` changes. Satisfies D-23 "session 启动时两路都用" partially (system dir).
       - **Project watcher** (init lazily on first `commands:list` IPC call): in the `commands:list` handler, after resolving `project.path`, call `let stopProjectWatcher: (() => void) | null = null;` (module-scope or closure-scope). On first invocation, if no project watcher is active, call `stopProjectWatcher = watchProjectCommandsDir(project.path, async () => { /* notify renderer */ });` and store the stop fn. On subsequent calls, check if `project.path` changed; if so, call `stopProjectWatcher()`, then create a new one with the new path. This avoids the previous contradictory "pass '' / updateWatcherProjectPath / defer" three-strategy confusion.
       - **Module-level state pattern** (in `src/main/index.ts`): keep `let projectWatcherStop: (() => void) | null = null;` and `let currentProjectPath: string | null = null;` outside the `app.whenReady` block so the IPC handler closure can read/mutate them.
       - **P6.6 verify check (BLOCKER 2 acceptance):** both `watchSystemCommandsDir(...)` and `watchProjectCommandsDir(...)` call sites MUST be inside `app.whenReady` (system) and `ipcMain.handle('commands:list', ...)` (project) respectively. Add a verification step in Task 4: `grep -E "watchSystemCommandsDir|watchProjectCommandsDir" src/main/index.ts` must return 2+ lines, and the file MUST contain `app.whenReady` block containing `watchSystemCommandsDir`.

    5. Test `src/main/commands/chokidar-watcher.test.ts` (5+ tests):
       - Setup: create `os.tmpdir() + '/cdf-test-' + Date.now()'/.cdf/commands/` dir.
       - Test (a): write a `test.md` file; verify chokidar fires `add` event after `awaitWriteFinish: 200` window (~250ms wait).
       - Test (b): modify the file; verify `change` event fires after debounce (~100ms + 200ms).
       - Test (c): unlink the file; verify `unlink` event fires.
       - Test (d): create a non-md file `notes.txt`; verify NO event (depth-0 + filter behavior).
       - Test (e): simulate watcher error: pass invalid path; verify log.error called, no crash.
       - Use `vi.useFakeTimers()` carefully — the `awaitWriteFinish` uses real timers internally; mixing fake + real is fragile. Use real timers + `await new Promise(r => setTimeout(r, 300))` for the 200ms window.
       - Mock `BrowserWindow.getAllWindows` to return an array with a mock `webContents.send`; verify the `commands:changed` event is emitted.

    Constraint reminders: do NOT modify `runtime.ts` / `llm.ts` / `workflow-runtime.ts` (Hard Do Not Touch); do NOT add a `commands:execute` IPC channel (PITFALLS P2); do NOT throw on chokidar errors (D-24: log only).
  verify:
    automated: |
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'chokidar-watcher|ipc-handlers|preload|error TS' | head -5 && \
      grep -E "awaitWriteFinish|debounce|commands:changed" /Users/suntc/project/CDF/src/main/commands/chokidar-watcher.ts | head -5 && \
      grep -E "commands:list|commands:readProjectCommands" /Users/suntc/project/CDF/src/main/ipc-handlers.ts | head -3 && \
      grep -E "commands:" /Users/suntc/project/CDF/src/preload/index.ts | head -5 && \
      npx vitest run /Users/suntc/project/CDF/src/main/commands/chokidar-watcher.test.ts 2>&1 | tail -20
  acceptance_criteria:
    - `npx tsc --noEmit` exits 0 with no errors in `chokidar-watcher.ts`, `ipc-handlers.ts`, or `preload/index.ts`
    - `src/main/commands/chokidar-watcher.ts` contains the literal string `awaitWriteFinish` AND `stabilityThreshold: 200` (D-23 lock)
    - `src/main/commands/chokidar-watcher.ts` contains a debounce helper (closure with `setTimeout`)
    - `src/main/commands/chokidar-watcher.ts` does NOT contain the string `toast` or `sonner` (D-24: log only)
    - `src/main/ipc-handlers.ts` contains `ipcMain.handle('commands:list'` and `ipcMain.handle('commands:readProjectCommands'`
    - `src/preload/index.ts` contains `commands:` block with `list:`, `readProjectCommands:`, `onChanged:`
    - `src/shared/types.ts` `ElectronAPI` interface contains `commands: { list, readProjectCommands, onChanged }`
    - `npx vitest run src/main/commands/chokidar-watcher.test.ts` exits 0 with ≥ 5 tests passed
    - The chokidar test file includes a test named `fires on add event after awaitWriteFinish` (200ms verify)
    - The chokidar test file includes a test named `ignores non-md files` (depth-0 + .md filter)
  done: chokidar-watcher.ts with debounce + awaitWriteFinish + commands:changed push; 2 IPC handlers added; preload bridge exposes commands namespace; 5+ tests covering add/change/unlink/error/non-md
</parameter>

<task type="auto">
  name: "Task 2: Build dispatcher.ts + useCommandRegistry.ts + system-commands.ts"
  type: auto
  files: "src/renderer/src/lib/commands/system-commands.ts, src/renderer/src/lib/commands/dispatcher.ts, src/renderer/src/hooks/useCommandRegistry.ts, src/renderer/src/lib/commands/dispatcher.test.ts, src/renderer/src/hooks/useCommandRegistry.test.ts"
  read_first:
    - .planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md (D-01 4 kinds; D-02 args passthrough; D-03 name mapping; D-13 chokidar push; D-15 O(1) memory; D-18 MCP args not in tool; C-04 renderer-side dispatcher)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md (Pattern 3 PluginRewrite natural language rewrite; Dispatcher Architecture; mcp_health_warning section)
    - src/shared/types.ts (SlashCommand, CommandDispatchAction, ElectronAPI.commands — Task 1 added)
    - src/renderer/src/stores/sessionStore.ts (line 338 sendMessage signature with overrides)
    - src/renderer/src/components/ChatArea/ChatArea.tsx (activeSession destructuring at line 132; currentProjectId state at line 132)
  action: |
    1. Create `src/renderer/src/lib/commands/system-commands.ts`:
       - Export `SYSTEM_COMMANDS: ReadonlyArray<SlashCommand>` matching the 3 main `collectSystemCommands` entries: `goal` (description: '设置 session 目标'), `context` (description: '查看 session token 用量'), `plan` (description: '进入 plan 模式').
       - Each entry: `{ name, description, source: 'system', target: name, sourceLabel: 'system', badge: '[system]' }`.
       - This is the renderer-side fallback / Phase 5 compat. Plan 06-02 keeps the Phase 5 `SlashCommandPopup.SYSTEM_COMMANDS` constant compatible (it's a separate `{ value, label }` shape used by the popup filter).
       - The dispatcher's `resolve()` can take either a flat `SlashCommand[]` from registry OR fall back to SYSTEM_COMMANDS if registry is empty.

    2. Create `src/renderer/src/lib/commands/dispatcher.ts`:
       - Import `useSessionStore` from `@/stores/sessionStore` and `toast` from `sonner` (for any user feedback in Phase 7, NOT Phase 6).
       - Export `resolve(inputVal: string, commands: ReadonlyArray<SlashCommand>): CommandDispatchAction | null`:
         - If `!inputVal.startsWith('/')`, return `null`.
         - Find matching command: `const match = commands.find(c => inputVal === '/' + c.name || inputVal.startsWith('/' + c.name + ' '));` — match exact command name OR command name + space.
         - If no match, return `null`.
         - Compute args: `const args = inputVal.slice(('/' + match.name).length).trim();` (D-02 passthrough, no flag parsing).
         - If `match.source === 'system'`:
           - If name is `goal`, return `{ kind: 'SystemSilent', command: match, args }`.
           - If name is `context`, return `{ kind: 'SystemLocal', command: match, args }`.
           - If name is `plan`, return `{ kind: 'PlanMode', command: match, args }`.
         - Else (mcp / skill / workflow / cmd): return `{ kind: 'PluginRewrite', command: match, args, prompt: \`请调用 \${match.name} 工具，参数：\${args || '(无参数)'}\` }`.
       - Export `async dispatch(plan: CommandDispatchAction): Promise<void>`:
         - **BLOCKER 1 fix:** `currentProjectId` lives in `useProjectStore` (NOT `useSessionStore`). Import: `import { useProjectStore } from '@/stores/projectStore';` from `@/stores/projectStore` (verified at `src/renderer/src/stores/projectStore.ts:11`).
         - Get state: `const { sendMessage } = useSessionStore.getState();` (sendMessage + activeSessionId from sessionStore) and `const projectId = useProjectStore.getState().currentProjectId;` (projectId from projectStore — single source of truth for active project).
         - Switch on `plan.kind`:
           - `SystemSilent`: Phase 7 placeholder — `console.log('[dispatcher] SystemSilent:', plan.command.name, plan.args);` (do NOT call sendMessage — silent write to store later).
           - `SystemLocal`: Phase 7 placeholder — `console.log('[dispatcher] SystemLocal:', plan.command.name, plan.args);` (no LLM call).
           - `PlanMode`: `await sendMessage(projectId, plan.args, { planOnly: true });` — passes `planOnly: true` through `ChatRuntimeOverrides` to `llm.ts:324` extension point.
           - `PluginRewrite`: `await sendMessage(projectId, plan.prompt);` — NO `overrides` argument (D-18: args go to message.content, not tool schema).
         - If `projectId` is null in any branch, `console.warn('[dispatcher] No active project; cannot dispatch');` and return (graceful no-op). DO NOT use `useSessionStore.getState().currentProjectId` — that field does not exist on sessionStore and would always resolve to null, breaking all 4 dispatch kinds.

    3. Create `src/renderer/src/hooks/useCommandRegistry.ts`:
       - Export `useCommandRegistry(projectId: string | null, agentId: string | null): { commands: SlashCommand[]; conflicts: CommandConflictError[]; warnings: Array<{ type: 'mcp_health_warning'; message: string }>; loading: boolean; reload: () => void }`.
       - State: `useState` for commands, conflicts, warnings, loading.
       - On mount AND when `projectId`/`agentId` changes: call `window.electronAPI.commands.list(projectId, agentId)` (or `'commands:list'` via preload bridge). Set state.
       - Fire toasts:
         - For each warning: `toast.warning(w.message, { description: 'MCP 工具未加载' });` (sonner `warning` variant).
         - For each conflict: `toast.warning(\`命令冲突: /\${c.commandName} 来自 \${c.conflicts.length} 个源\`, { description: '请手动选择' });` — Phase 6.5 user-visible feedback.
       - On mount: subscribe to `window.electronAPI.commands.onChanged((_, data) => { if (data.source === 'chokidar' || data.source === 'mcp-health') reload(); })`. Return cleanup function from useEffect.
       - Expose `reload` function that re-invokes `commands.list`.
       - **Note**: This is renderer-side. Test by mocking `window.electronAPI.commands` in tests.

    4. Tests in `src/renderer/src/lib/commands/dispatcher.test.ts` (8+ tests):
       - `resolve('/goal', [goalCmd])` → `{ kind: 'SystemSilent', command: goalCmd, args: '' }`.
       - `resolve('/goal write tests', [goalCmd])` → `{ kind: 'SystemSilent', command: goalCmd, args: 'write tests' }` (D-02 args passthrough).
       - `resolve('/context', [contextCmd])` → `{ kind: 'SystemLocal', ... }`.
       - `resolve('/plan', [planCmd])` → `{ kind: 'PlanMode', ... }`.
       - `resolve('/plan --priority=high', [planCmd])` → `args: '--priority=high'` (D-02: NO flag parsing, raw passthrough).
       - `resolve('/arxiv_search foo bar', [mcpCmd])` → `{ kind: 'PluginRewrite', ..., args: 'foo bar', prompt: '请调用 arxiv_search 工具，参数：foo bar' }` (D-18 MCP args).
       - `resolve('hello', [goalCmd])` → `null` (no slash).
       - `resolve('/unknown', [goalCmd])` → `null` (no match).
       - `resolve('/goal', [])` → `null` (empty registry).
       - `dispatch({ kind: 'PlanMode', ..., args: 'x' })` mocks `useSessionStore.sendMessage` and verifies it was called with `('projectId', 'x', { planOnly: true })`.
       - `dispatch({ kind: 'PluginRewrite', ..., args: 'foo' })` verifies `sendMessage('projectId', '请调用 tool 工具，参数：foo')` was called WITHOUT overrides arg.

    5. Tests in `src/renderer/src/hooks/useCommandRegistry.test.ts` (4+ tests):
       - Mounts with projectId+agentId, calls `electronAPI.commands.list`, sets state.
       - Receives mcp_health_warning → fires `toast.warning` (mock sonner).
       - Receives CommandConflictError → fires `toast.warning` with conflict message.
       - Mounts cleanup unsubscribes from `onChanged` (verify the returned cleanup function was called).
       - Re-fetches on `commands:changed` event with `source: 'chokidar'`.

    Use `vi.mock('sonner', () => ({ toast: { warning: vi.fn(), error: vi.fn() } }))`, `vi.mock('@/stores/sessionStore', ...)`, AND `vi.mock('@/stores/projectStore', () => ({ useProjectStore: { getState: vi.fn(() => ({ currentProjectId: 'projectId' })) } }))` for store isolation (BLOCKER 1 fix — dispatcher reads `useProjectStore.getState().currentProjectId`; without mocking it, `projectId` resolves to null and all 4 dispatch kinds fail with "No active project; cannot dispatch").

    Constraint reminders: do NOT parse args (D-02); do NOT call sendMessage for SystemSilent/SystemLocal (Phase 7); do NOT add new IPC channels; do NOT use `process.platform` (P6.7: renderer sandboxed).
  verify:
    automated: |
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'dispatcher|useCommandRegistry|system-commands|error TS' | head -5 && \
      grep -E "请调用.*工具，参数" /Users/suntc/project/CDF/src/renderer/src/lib/commands/dispatcher.ts && \
      grep -E "planOnly" /Users/suntc/project/CDF/src/renderer/src/lib/commands/dispatcher.ts && \
      npx vitest run /Users/suntc/project/CDF/src/renderer/src/lib/commands/dispatcher.test.ts /Users/suntc/project/CDF/src/renderer/src/hooks/useCommandRegistry.test.ts 2>&1 | tail -25
  acceptance_criteria:
    - `npx tsc --noEmit` exits 0 with no errors in `dispatcher.ts`, `useCommandRegistry.ts`, or `system-commands.ts`
    - `src/renderer/src/lib/commands/dispatcher.ts` exports BOTH `resolve` AND `dispatch`
    - `dispatcher.ts` contains the template string `请调用 ${...} 工具，参数：${...}` (PluginRewrite prompt format)
    - `dispatcher.ts` PlanMode case calls `sendMessage` with `{ planOnly: true }` (D-01 + llm.ts:324 extension point)
    - `dispatcher.ts` PluginRewrite case calls `sendMessage` WITHOUT third argument (D-18 + PITFALLS P7 mitigation)
    - `dispatcher.ts` SystemSilent + SystemLocal cases do NOT call sendMessage (Phase 7 placeholder — console.log only)
    - `useCommandRegistry.ts` imports `toast` from `sonner` AND `window.electronAPI.commands`
    - `useCommandRegistry.ts` subscribes to `onChanged` and returns cleanup function
    - `npx vitest run src/renderer/src/lib/commands/dispatcher.test.ts` exits 0 with ≥ 11 tests passed
    - `npx vitest run src/renderer/src/hooks/useCommandRegistry.test.ts` exits 0 with ≥ 4 tests passed
    - Test files include named cases `args passthrough no flag parsing` and `plugin rewrite does not pass overrides`
  done: dispatcher.ts 4 kinds with Phase 7 placeholders for SystemSilent/SystemLocal; useCommandRegistry hook with sonner toasts + chokidar subscription; 15+ tests covering all dispatch paths + warning/conflict toast firing
</parameter>

<task type="auto">
  name: "Task 3: Extend SlashCommandPopup.tsx with source badge column + mcp_health_warning row + 12+ new tests"
  type: auto
  files: "src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx, src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx"
  read_first:
    - src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx (Phase 5 base 130 lines — extension target)
    - src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx (Phase 5 19-test base — this plan extends NOT replaces)
    - src/renderer/src/components/ui/badge.tsx (Badge component signature)
    - src/shared/types.ts (SlashCommand type with badge + source fields)
    - .planning/phases/05-popup-shell-keyboard-spike/05-UI-SPEC.md (Phase 5 inherited design contract — 11px label, 13px body, color-muted for description; Phase 6 ADDITIONS only)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md (D-08 badge format; D-09 MCP no description in popup; C-02 mcp_health_warning position)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md (mcp_health_warning Row section; Source Badge Rendering)
    - .planning/phases/05-popup-shell-keyboard-spike/05-VERIFICATION.md (known stub: handleKeyDown shiftKey check — DO NOT add; preserve)
  action: |
    Extend `src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx` (the file is 130 lines; modifications are surgical):
    1. Imports (line 1-7): ADD `import { Badge } from '@/components/ui/badge';` and `import type { SlashCommand } from '../../../../shared/types';` and `import { AlertCircle } from 'lucide-react';` (lucide-react is already in project per Phase 5).
    2. KEEP the Phase 5 module-scope `SYSTEM_COMMANDS: ReadonlyArray<{ value, label }>` (line 10-14) for back-compat — Phase 5 test harness imports it indirectly via the existing popup tests. Rename internal to `PHASE5_PLACEHOLDER` ONLY if needed for clarity; otherwise keep as `SYSTEM_COMMANDS` and merge with new `commands` prop at render time.
    3. Update `SlashCommandPopupProps` (line 31-35): ADD `commands?: SlashCommand[];` and `hasMcpWarning?: boolean;` and `mcpWarningMessage?: string;`. The existing `query` / `onSelect` / `onClose` stay.
    4. Update `filterCommands` (line 16-25): when `commands` prop is provided, use it; otherwise fall back to Phase 5 SYSTEM_COMMANDS mapped to SlashCommand shape. Filter logic: substring + case-insensitive + NFKC-normalized (D-05 + D-06) on `cmd.name` (was `value`).
    5. Component body: compute `displayCommands` = `commands ? commands : SYSTEM_COMMANDS_FALLBACK`. Use this in the existing filtered + render logic.
    6. JSX render (line 101-126): replace `Command.Item` with extended version:
       ```
       <Command.Item
         key={`${cmd.source}-${cmd.name}`}
         value={cmd.name}
         data-source={cmd.source}
         onSelect={() => onSelect(cmd.name)}
         className="flex cursor-default select-none items-center gap-2 rounded-sm px-2 py-1.5 text-[13px] text-[var(--color-text-primary)] outline-none data-[selected=true]:bg-[var(--color-accent)]/15 data-[selected=true]:text-[var(--color-text-primary)]"
       >
         <Badge variant="secondary" className="text-[10px] px-1.5 py-0 font-mono">
           {cmd.badge}
         </Badge>
         <span className="font-mono">/{cmd.name}</span>
         {cmd.source !== 'mcp' && cmd.description && (
           <span className="text-[11px] text-[var(--color-text-muted)] truncate max-w-[200px]">
             {cmd.description.slice(0, 60)}
           </span>
         )}
       </Command.Item>
       ```
       D-09 enforced: MCP commands skip the description column. The `data-source` attribute enables testing.
    7. Inside `Command.List` (line 109-119), ADD the mcp_health_warning row ABOVE the `filtered.map(...)`:
       ```
       {hasMcpWarning && (
         <div
           data-testid="mcp-health-warning"
           className="px-2 py-1.5 text-[11px] text-[var(--color-text-muted)] bg-[var(--color-bg-surface)] border-b border-[var(--color-border)] flex items-center gap-2 select-none"
         >
           <AlertCircle className="w-3 h-3 flex-shrink-0" />
           <span>{mcpWarningMessage || 'MCP 工具未加载，请检查服务器连接'}</span>
         </div>
       )}
       ```
       C-02 choice: top of the list. `select-none` to make it unselectable. `data-testid` for testability.
    8. PRESERVE the existing `Command.Empty` hint (line 120-123) and the `useImperativeHandle` keyboard logic. DO NOT add a shiftKey check — that's the Phase 5 known stub documented in 05-VERIFICATION.md; this plan's job is to NOT regress it.

    Tests in `src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx` — EXTEND the existing file with 12+ new tests in a new `describe('Phase 6 source badges + warnings', ...)` block (do NOT delete the Phase 5 19 tests; the existing TestHarness already supports the new `commands` and `hasMcpWarning` props):
    1. `it('renders source badge for system command', ...)` — pass `commands: [goalSlashCmd]`; assert `getByText('[system]')` and `getByText('/goal')` both visible.
    2. `it('renders source badge for mcp tool', ...)` — pass `commands: [mcpCmd]`; assert `getByText('[mcp:arxiv_search]')`; assert description is NOT rendered (D-09).
    3. `it('renders source badge for workflow', ...)` — `commands: [workflowCmd]`; assert `getByText('[workflow]')`.
    4. `it('renders source badge for skill:project', ...)` — `commands: [skillProjectCmd]`; assert `getByText('[skill:project]')`.
    5. `it('renders source badge for cmd:system', ...)` — `commands: [cmdSystemCmd]`; assert `getByText('[cmd:system]')`.
    6. `it('does not render description for mcp tools', ...)` — assert mcpCmd.description is NOT in document (D-09).
    7. `it('renders description for non-mcp commands', ...)` — assert systemCmd.description IS in document (truncated to 60 chars).
    8. `it('renders mcp_health_warning row at top when hasMcpWarning is true', ...)` — pass `hasMcpWarning: true`; assert `getByTestId('mcp-health-warning')` is in document.
    9. `it('does not render mcp_health_warning row when hasMcpWarning is false', ...)` — pass `hasMcpWarning: false`; assert `queryByTestId('mcp-health-warning')` is null.
    10. `it('preserves D-04 — opens with top row highlighted even with 5+ commands', ...)` — pass 5 commands; assert first has `data-selected="true"`.
    11. `it('key prop includes source — duplicate names from different sources render as 2 separate rows', ...)` — pass `[systemGoal, workflowGoal]`; assert both rows in document.
    12. `it('data-source attribute matches command source for testing', ...)` — pass 3 commands from 3 sources; assert `data-source` attribute values match.

    Update TestHarness in the existing test file to accept `commands` and `hasMcpWarning` props and pass them through to `<SlashCommandPopup>`. The Phase 5 tests should continue to pass (they use the Phase 5 SYSTEM_COMMANDS fallback path).

    Constraint reminders: do NOT add 7-color badge logic (Phase 8 polish); do NOT add shiftKey check (Phase 5 known stub; preserve); do NOT change the Phase 5 keyboard contract (D-04 D-07 must still hold); do NOT remove the empty hint (`Command.Empty` block stays).
  verify:
    automated: |
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'SlashCommandPopup|error TS' | head -5 && \
      grep -E "data-source|mcp-health-warning" /Users/suntc/project/CDF/src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx | head -3 && \
      grep -E "import.*Badge" /Users/suntc/project/CDF/src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx && \
      npx vitest run /Users/suntc/project/CDF/src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx 2>&1 | tail -25
  acceptance_criteria:
    - `npx tsc --noEmit` exits 0 with no errors in `SlashCommandPopup.tsx`
    - `SlashCommandPopup.tsx` imports `Badge` from `@/components/ui/badge`
    - `SlashCommandPopup.tsx` contains the string `data-source` (testability attribute)
    - `SlashCommandPopup.tsx` contains the string `mcp-health-warning` (data-testid for the warning row)
    - `SlashCommandPopup.tsx` Command.Item includes `<Badge variant="secondary"` AND `<span className="font-mono">/{cmd.name}</span>` (badge + name pattern)
    - `SlashCommandPopup.tsx` does NOT contain `shiftKey` (preserves Phase 5 known stub as documented)
    - `npx vitest run src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx` exits 0 with ≥ 31 tests passed (19 Phase 5 + 12 Phase 6)
    - Test file contains the named case `renders source badge for mcp tool` and `does not render description for mcp tools` (D-09 verify)
    - Test file contains the named case `renders mcp_health_warning row at top when hasMcpWarning is true` (C-02 verify)
    - All 19 Phase 5 tests still pass (no regressions)
  done: SlashCommandPopup extended with source badges + description column (mcp excluded) + mcp_health_warning row at top; 12+ new tests + 19 Phase 5 tests all green; no shiftKey check added (preserves Phase 5 known stub)
</parameter>

<task type="auto">
  name: "Task 4: Extend ChatArea.tsx handleSlashSelect to dispatcher; verify all Phase 6 wiring"
  type: auto
  files: "src/renderer/src/components/ChatArea/ChatArea.tsx, src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx"
  read_first:
    - src/renderer/src/components/ChatArea/ChatArea.tsx (line 132 state; line 663-666 handleSlashSelect; line 1022 Popover; line 1125-1134 SlashCommandPopup)
    - src/renderer/src/lib/commands/dispatcher.ts (Task 2 output)
    - src/renderer/src/hooks/useCommandRegistry.ts (Task 2 output)
    - src/renderer/src/stores/sessionStore.ts (sendMessage signature; currentProjectId; activeSessionId)
    - .planning/phases/05-popup-shell-keyboard-spike/05-VERIFICATION.md (D-07: Tab still only inserts text; Enter triggers new dispatcher path)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md (D-07 Phase 5 lock; integration point at ChatArea line 663)
  action: |
    Edit `src/renderer/src/components/ChatArea/ChatArea.tsx` in 4 surgical insertion points (no other edits):
    1. **Imports** (line 1-22 area): ADD `import { resolve, dispatch } from '@/lib/commands/dispatcher';` and `import { useCommandRegistry } from '@/hooks/useCommandRegistry';` and `import type { SlashCommand } from '../../../../shared/types';` (if not already imported).
    2. **Hook usage** (after line 149 refs, before line 153 useChatScroll): ADD `const registry = useCommandRegistry(currentProjectId, activeSession?.agent_id || null);` — provides `commands` to the popup and triggers sonner toasts on mount.
    3. **handleSlashSelect update** (replace line 663-666):
       ```
       const handleSlashSelect = (cmd: string) => {
         // D-07 Phase 5 contract: Tab (or unknown) still just inserts text
         // Phase 6: Enter path runs through dispatcher
         const plan = resolve(inputVal, registry.commands);
         if (plan) {
           setInputVal('');
           setSlashOpen(false);
           dispatch(plan).catch((err) => console.error('[dispatcher] error:', err));
         } else {
           setInputVal(cmd + ' ');
           setSlashOpen(false);
         }
       };
       ```
       **WARNING 4 fix — Tab/Enter distinction mechanism (do NOT regress):** Phase 5 `ChatArea.tsx` `handleKeyDown` (around line 670-679) intercepts the Tab key BEFORE cmdk's `Command.Item onSelect` fires — it calls `e.preventDefault()` on Tab to keep focus in the textarea and triggers the legacy text-insert path directly. The Phase 6 dispatcher MUST only run on Enter (which is what triggers cmdk's `onSelect` callback). Verify by reading Phase 5's `handleKeyDown` and confirming: (a) Tab branch calls `e.preventDefault()` and `setInputVal(cmd + ' '); setSlashOpen(false);` without invoking `handleSlashSelect`; (b) Enter branch does NOT preventDefault and falls through to cmdk's `onSelect` → `handleSlashSelect`. If Phase 5's intercept is missing or broken, the dispatcher will fire on Tab too and D-07 will be violated.
       The `resolve(inputVal, ...)` call uses the FULL inputVal (not just `cmd`); if inputVal is `/goal write tests`, resolve extracts the args. If `cmd` is just `/goal` and inputVal is just `/goal`, args is `''`.
       The unknown-command / Tab path falls back to Phase 5 behavior (text insert).
    4. **SlashCommandPopup props** (line 1125-1134): add `commands={registry.commands}` and `hasMcpWarning={registry.warnings.some(w => w.type === 'mcp_health_warning')}` and `mcpWarningMessage={registry.warnings.find(w => w.type === 'mcp_health_warning')?.message}`.

    DO NOT modify:
    - `handleSend` (D-07: slash path does not call handleSend)
    - `slashOpen` useState or `slashRef` useRef (Phase 5 lock; C-06 keeps useState, not Zustand)
    - `handleKeyDown` branch (Phase 5 keyboard contract intact)
    - The Popover wrap (line 1022) — anchor + content structure unchanged
    - The 5 surgical insertions from Phase 5 (lines 1022-1132)

    Add 4+ tests in `src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` (extend the existing file; these tests live alongside the Phase 5 + Phase 6 popup tests):
    1. `it('handleSlashSelect routes to dispatcher when plan resolves', ...)` — TestHarness mock: `electronAPI.commands.list` returns `[goalSlashCmd]`; type `/goal` in textarea; press Enter; assert inputVal becomes `''` (cleared) and a mock `dispatcher.dispatch` was called with `{ kind: 'SystemSilent', command: goalSlashCmd, args: '' }`. NOTE: For this test, the TestHarness needs to be updated to mock `window.electronAPI.commands` (or the test imports `dispatcher.ts` and uses `vi.spyOn` on `dispatch`).
    2. `it('handleSlashSelect falls back to text insert when resolve returns null', ...)` — registry returns empty `[]`; type `/unknown`; press Enter; assert inputVal becomes `/unknown ` and dispatcher.dispatch was NOT called.
    3. `it('handleSlashSelect preserves Tab text-insert contract (D-07)', ...)` — type `/goal`; press Tab (not Enter); assert inputVal becomes `/goal ` (Phase 5 behavior; no dispatcher call).
    4. `it('PlanMode dispatch passes planOnly override', ...)` — registry returns `[planCmd]`; type `/plan write tests`; press Enter; mock `useSessionStore.sendMessage`; assert it was called with `('projectId', 'write tests', { planOnly: true })`.

    For these tests, you may need to extend the TestHarness to mock `window.electronAPI.commands.list`. Alternative: import `dispatcher.ts` directly and use `vi.mock('@/lib/commands/dispatcher', ...)` to spy on `dispatch`.

    Run final verification:
    - `npx tsc --noEmit` — must exit 0
    - `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` — must show all tests passing (Phase 5 19 + Phase 6 12 popup + Phase 6 4 handleSlashSelect = ≥ 35 tests)
    - `npm test` — full project suite green

    Constraint reminders: do NOT call `handleSend` from handleSlashSelect; do NOT modify llm.ts / runtime.ts (Hard Do Not Touch); do NOT remove the Phase 5 fallback path; do NOT break the 19 Phase 5 tests.
  verify:
    automated: |
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'ChatArea|error TS' | head -5 && \
      grep -E "dispatcher\\.resolve|useCommandRegistry|registry\\.commands" /Users/suntc/project/CDF/src/renderer/src/components/ChatArea/ChatArea.tsx | head -10 && \
      grep -E "planOnly" /Users/suntc/project/CDF/src/renderer/src/components/ChatArea/ChatArea.tsx || echo "planOnly in dispatcher (not ChatArea) is OK" && \
      npx vitest run /Users/suntc/project/CDF/src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx 2>&1 | tail -25 && \
      npm test 2>&1 | tail -15
  acceptance_criteria:
    - `npx tsc --noEmit` exits 0 with no errors in `ChatArea.tsx`
    - `ChatArea.tsx` imports `resolve, dispatch` from `@/lib/commands/dispatcher` AND `useCommandRegistry` from `@/hooks/useCommandRegistry`
    - `ChatArea.tsx` line 663-666 `handleSlashSelect` is REPLACED with a version that calls `dispatcher.resolve(inputVal, registry.commands)` and conditionally calls `dispatch(plan)`
    - `ChatArea.tsx` does NOT contain `handleSend` in the handleSlashSelect function body (D-07 verify)
    - `ChatArea.tsx` SlashCommandPopup invocation now passes `commands={registry.commands}` AND `hasMcpWarning={...}` (3 new props)
    - `npx vitest run src/renderer/src/components/ChatArea/SlashCommandPopup.test.tsx` exits 0 with ≥ 35 tests passed (19 P5 + 12 P6 popup + 4 P6 handleSlashSelect)
    - Test file contains the named case `routes to dispatcher when plan resolves` AND `falls back to text insert when resolve returns null` AND `preserves Tab text-insert contract` AND `PlanMode dispatch passes planOnly override`
    - `npm test` exits 0 with no regressions across the full project (main + renderer tests)
  done: ChatArea.tsx fully wired: handleSlashSelect dispatches via resolve+dispatch; SlashCommandPopup receives registry.commands; PlanMode passes planOnly to llm.ts:324 extension point via existing sendMessage + ChatRuntimeOverrides; D-07 Tab text-insert preserved; full Phase 6 + Phase 5 test suite green
</parameter>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer-user-input | Untrusted text from ChatArea textarea crosses into dispatcher.resolve + SlashCommandPopup.render |
| main-fs | chokidar watches `~/.cdf/commands/*.md` + `<projectPath>/.cdf/commands/*.md`; untrusted user-written markdown files |
| main-ipc | renderer calls `commands:list` / `commands:readProjectCommands`; main pushes `commands:changed` |
| main-window-bridge | `BrowserWindow.webContents.send` for `commands:changed` — only fires after debounce + awaitWriteFinish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-T1 | Tampering (dispatcher bypass) | dispatcher.ts | mitigate | `resolve` is pure function; matches by full `name` or `name + space` — prevents `args` from being interpreted as a different command (e.g., `/plan` vs `/planner`). Verified by dispatcher.test.ts |
| T-06-T2 | Elevation of Privilege (planOnly misuse) | dispatcher.ts + ChatArea.tsx | mitigate | PlanMode is the ONLY kind that writes `planOnly: true`; other 3 kinds do not pass overrides arg. Verified by `dispatcher.test.ts` "plugin rewrite does not pass overrides" + "plan mode passes planOnly override" |
| T-06-T3 | Tampering (args injection) | dispatcher.ts PluginRewrite | mitigate | D-18: args go to `message.content` (LLM natural language), NOT to tool args schema. PITFALLS P7: tool schema is the injection vector; Phase 6 dispatcher completely avoids it for MCP commands. Verified by dispatcher.test.ts "arxiv_search args go to prompt not tool" |
| T-06-T4 | Tampering (XSS in badge / description) | SlashCommandPopup.tsx | mitigate | All values rendered as React text children (`{cmd.badge}` `{cmd.description}`); NEVER `dangerouslySetInnerHTML`. Verified by code review (no `dangerouslySetInnerHTML` in the file) |
| T-06-T5 | Denial of service (chokidar event storm) | chokidar-watcher.ts | mitigate | 100ms debounce + `awaitWriteFinish: { stabilityThreshold: 200 }`; VSCode atomic-write triggers at most 1 event per save. Verified by chokidar-watcher.test.ts (timing assertions) |
| T-06-T6 | Repudiation (silent chokidar failure) | chokidar-watcher.ts | mitigate | D-24: errors logged via `electron-log`; failure is observable in dev tools console. Phase 8 polish will add user-facing toast. Verified by chokidar-watcher.test.ts "logs error on invalid path" |
| T-06-T7 | Information disclosure (homedir race) | main/index.ts chokidar init | mitigate | P6.6: chokidar init in `app.whenReady` block AFTER `os.homedir()` is ready. Verified by code review (init order) |
| T-06-T8 | Tampering (registry race / stale state) | useCommandRegistry.ts | mitigate | `commands:changed` event triggers `reload()`; chokidar debounce coalesces bursts; registry is the source of truth. P6.4 (mcpCache stale) addressed in Plan 06-01: collector flushes mcpCache on health event. Documented |
| T-06-T9 | Tampering (handleSlashSelect state race) | ChatArea.tsx | mitigate | `setInputVal('')` + `setSlashOpen(false)` BEFORE `dispatch(plan)`; prevents double-fire if dispatch is async. Verified by SlashCommandPopup.test.tsx new tests |
| T-06-T10 | Tampering (Phase 5 keyboard regression) | SlashCommandPopup.tsx | mitigate | DO NOT add `shiftKey` check; preserve Phase 5 known stub as documented in 05-VERIFICATION.md. The 19 Phase 5 tests must still pass. Verified by `npm test` exit 0 |
| T-06-SC | Tampering (supply chain) | `chokidar@^3.6.0` | accept | Plan 06-01 installed + slopcheck [OK] verified; chokidar 3.6.0 CommonJS-compatible (4.x is ESM-only); awaitWriteFinish is the load-bearing option for VSCode atomic-write |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/main/commands/chokidar-watcher.test.ts` exits 0 with ≥ 5 tests passed
- `npx vitest run src/renderer/src/lib/commands/dispatcher.test.ts` exits 0 with ≥ 11 tests passed
- `npx vitest run src/renderer/src/hooks/useCommandRegistry.test.ts` exits 0 with ≥ 4 tests passed
- `npx vitest run src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx` exits 0 with ≥ 35 tests passed (19 P5 + 12 P6 popup + 4 P6 ChatArea)
- `npm test` exits 0 with no Phase 5 regressions
- `cat src/main/index.ts | grep -E "watchCommandsDir|app\\.whenReady"` confirms chokidar init in app.whenReady block
- `grep -r "from.*runtime\|from.*llm\|from.*workflow-runtime" src/renderer/src/lib/commands/ src/renderer/src/hooks/` returns 0 lines (renderer does not import from Hard Do Not Touch)
- `grep "handleSend" src/renderer/src/components/ChatArea/ChatArea.tsx | grep -A 5 "handleSlashSelect"` returns 0 references (D-07 verify)
</verification>

<success_criteria>
- All must_haves.truths verifiable: chokidar double-watch with debounce; dispatcher 4 kinds; useCommandRegistry hook with toasts; ChatArea wired; SlashCommandPopup source badges + mcp_health_warning row
- All must_haves.artifacts exist at listed paths with correct exports
- 35+ tests green across 4 test files; type check clean; full project test suite green
- Hard Do Not Touch list respected (no changes to runtime.ts / llm.ts:306-425 / workflow-runtime.ts / LLMStreamEvent union; llm.ts:324 extension point consumed but not modified)
- D-07 preserved: Tab still only inserts text; Enter runs through dispatcher
- Phase 5 known stub preserved: no shiftKey check added; 19 Phase 5 tests still pass
- ROADMAP.md Phase 6 plan list updated to mark 06-02 as ready
- Phase 6 exit: `/goal` / `/context` / `/plan` / `/<mcp_tool>` / `/<skill_name>` / `/<workflow>` / `/<custom_command>` all dispatch correctly with source-aware popup
</success_criteria>

<output>
Create `.planning/phases/06-4-source-command-registry-dispatcher/06-02-SUMMARY.md` when done
</output>
