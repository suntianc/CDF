---
phase: 06-4-source-command-registry-dispatcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/shared/types.ts
  - src/main/commands/collectors/system.ts
  - src/main/commands/collectors/mcp.ts
  - src/main/commands/collectors/skill.ts
  - src/main/commands/collectors/workflow.ts
  - src/main/commands/collectors/project.ts
  - src/main/commands/project-commands.ts
  - src/main/commands/conflict-detector.ts
  - src/main/commands/command-registry.ts
  - src/main/commands/command-registry.test.ts
  - src/main/commands/collectors/mcp.test.ts
  - src/main/commands/collectors/skill.test.ts
  - src/main/commands/collectors/workflow.test.ts
  - src/main/commands/collectors/project.test.ts
  - src/main/commands/project-commands.test.ts
  - src/main/commands/conflict-detector.test.ts
  - .github/workflows/phase-6-commands.yml
autonomous: true
requirements:
  - SLASH-03
  - SLASH-08
  - SLASH-09a
  - SLASH-09b
  - SLASH-10
  - SLASH-11a
  - SLASH-11b
  - SLASH-12
user_setup: []

must_haves:
  truths:
    - Main process exposes a 5-source registry that merges system (3 hardcoded) + MCP cache + Skills × 2 sub-sources + Workflows (status='active' only, valid name regex per D-10) + Custom Commands × 2 sub-sources into a flat SlashCommand[] (D-13)
    - registry build is failure-isolated per source: any single collector throwing does not prevent the other 4 from returning (P6.1) — uses Promise.allSettled, not Promise.all
    - Build phase detects same-name conflicts across sources with different priority and returns CommandConflictError[] (not throw — D-07 lock: both rows preserved per D-05; D-04 priority only controls sort; D-06 priority order: system > skill:project > skill:global > workflow > mcp > cmd:project > cmd:system; D-12 workflow conflicts follow the same general rules)
    - Workflow collector uses lightweight SQL `SELECT id, name, description FROM workflows WHERE project_id = (SELECT id FROM projects WHERE path = ?) AND status = 'active'` (D-11) — does NOT use `db:getWorkflows` IPC which carries heavy `graph_data`
    - Project commands parser reads `~/.cdf/commands/*.md` (system) and `<projectPath>/.cdf/commands/*.md` (project) (D-19); YAML frontmatter parses `name` / `description` / `argument-hint` (D-20); same-name project wins (D-21)
    - Skills collector reuses `listPhysicalSkills(projectPath)` from `src/main/deepagent/skill-manager.ts:80-104`; D-22 same-name project wins
    - MCP collector reuses `loadMcpTools(agentId, mcpServers)` from `src/main/deepagent/mcp-connector.ts:129`; reuses existing mcpCache per D-16 (no reconnect); P6.5 distinguishes "agent never bound MCP" vs "tools empty" (only latter triggers mcp_health_warning per D-17)
    - chokidar 3.6.0 awaits: `npm install --save chokidar@^3.6.0 sonner@^2.0.7` succeeds; node_modules/chokidar/package.json shows `"version": "3.6.0"`; node_modules/sonner/package.json shows `"version": "2.0.7"`
    - CI matrix workflow `.github/workflows/phase-6-commands.yml` defines macos-latest + windows-latest + ubuntu-latest run (D-25)
  artifacts:
    - path: src/shared/types.ts
      provides: "SlashCommand, CommandSource, CommandDispatchAction, CommandConflictError types + ElectronAPI.commands namespace + ChatRuntimeOverrides.planOnly? field"
      contains: "CommandConflictError"
    - path: src/main/commands/collectors/system.ts
      provides: "3 hardcoded system commands (goal/context/plan) — no IO, pure const"
      exports: ["collectSystemCommands"]
    - path: src/main/commands/collectors/mcp.ts
      provides: "MCP tool collector reusing loadMcpTools + mcpCache; returns SlashCommand[] + hasAgentMcp boolean for mcp_health_warning discrimination"
      exports: ["collectMcpCommands"]
    - path: src/main/commands/collectors/skill.ts
      provides: "Skills collector reusing listPhysicalSkills; maps to skill:project | skill:global"
      exports: ["collectSkillCommands"]
    - path: src/main/commands/collectors/workflow.ts
      provides: "Workflow collector: lightweight SQL, /^[a-z0-9-]+$/ name validation"
      exports: ["collectWorkflowCommands"]
    - path: src/main/commands/collectors/project.ts
      provides: "Custom commands collector (thin wrapper over project-commands.ts) returning SlashCommand[]"
      exports: ["collectProjectCommands"]
    - path: src/main/commands/project-commands.ts
      provides: "YAML frontmatter parser + listSystemCommands/listProjectCommands"
      exports: ["listSystemCommands", "listProjectCommands", "parseFrontmatter"]
    - path: src/main/commands/conflict-detector.ts
      provides: "Pure function: detectConflicts(commands) → CommandConflictError[]; NFKC-normalized grouping; NO throw (D-07 lock)"
      exports: ["detectConflicts"]
    - path: src/main/commands/command-registry.ts
      provides: "collectAllCommands(projectPath, agentId) using Promise.allSettled; buildRegistry helper for chokidar incremental updates"
      exports: ["collectAllCommands", "collectAllWithWarnings"]
    - path: package.json
      provides: "chokidar@^3.6.0 + sonner@^2.0.7 entries in dependencies"
      contains: '"chokidar": "^3.6.0"'
    - path: .github/workflows/phase-6-commands.yml
      provides: "CI matrix: macos-latest + windows-latest + ubuntu-latest runs `npm test -- commands`"
      contains: "matrix"
  key_links:
    - from: src/main/commands/command-registry.ts
      to: src/main/commands/collectors/{system,mcp,skill,workflow,project}.ts
      via: "Promise.allSettled([collectSystemCommands, collectMcpCommands, collectSkillCommands, collectWorkflowCommands, collectProjectCommands])"
      pattern: "Promise\\.allSettled"
    - from: src/main/commands/collectors/skill.ts
      to: src/main/deepagent/skill-manager.ts
      via: "import { listPhysicalSkills } from '../../deepagent/skill-manager'"
      pattern: "listPhysicalSkills"
    - from: src/main/commands/collectors/mcp.ts
      to: src/main/deepagent/mcp-connector.ts
      via: "import { loadMcpTools } from '../../deepagent/mcp-connector'; reuses mcpCache via loadMcpTools"
      pattern: "loadMcpTools"
    - from: src/main/commands/collectors/workflow.ts
      to: src/main/database.ts
      via: "db.prepare('SELECT id, name, description FROM workflows WHERE project_id = (SELECT id FROM projects WHERE path = ?) AND status = ?')"
      pattern: "db\\.prepare"
    - from: src/main/commands/conflict-detector.ts
      to: src/shared/types.ts
      via: "import { CommandConflictError, type SlashCommand } from '../../shared/types'"
      pattern: "CommandConflictError"
---

<objective>
Build the main-process foundation of the 4-Source Command Registry: install Phase 6 dependencies (chokidar 3.6.0 + sonner 2.0.7), add the 4 new shared types, implement the 5 source collectors (system / mcp / skill / workflow / project), the project-commands parser, the conflict detector, and the `collectAllCommands` merger that uses `Promise.allSettled` for failure isolation. Set up the cross-platform CI matrix (D-25). Do NOT touch dispatcher / renderer / IPC / chokidar watcher — those are Plan 06-02.

Purpose: This plan locks the load-bearing data layer. Plan 06-02 builds the renderer-side hook + dispatcher on top of these types. The Hard "Do Not Touch" list (runtime.ts / llm.ts:306-425 / workflow-runtime.ts / LLMStreamEvent union / 6-hunk patch-package) is respected — no changes to those files in this plan.

Output: 10 new main-process files + 4 type additions in `src/shared/types.ts` + 1 CI workflow file + 7 test files (one per collector + registry + conflict detector + project-commands).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md
@.planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md
@.planning/phases/06-4-source-registry-dispatcher/06-VALIDATION.md
@.planning/phases/05-popup-shell-keyboard-spike/05-01-SUMMARY.md
@.planning/research/PITFALLS.md

# Source files the executor must read before editing
@src/shared/types.ts (140-143 ChatRuntimeOverrides; 350-431 ElectronAPI; add 4 types + planOnly field)
@src/main/deepagent/mcp-connector.ts (line 12 mcpCache, line 129 loadMcpTools; reuse mcpCache — do not reconnect)
@src/main/deepagent/skill-manager.ts (line 80 getScopePath returns ~/.cdf/skills + <projectPath>/.cdf/skills; line 89 listPhysicalSkills)
@src/main/database.ts (workflows table: id, project_id, name, description, status, ...; projects table has path column)
@src/main/ipc-handlers.ts (line 43 registerIpcHandlers; pattern for adding new handler)
@src/main/index.ts (line 61 app.whenReady — chokidar init hooks here in Plan 06-02)
@package.json (dependencies block)
@vitest.config.ts
</context>

<interfaces>
From src/shared/types.ts (existing; will be MODIFIED in Task 1):
- `ChatRuntimeOverrides` (line 140-143): currently `{ providerId?: string; model?: string }` — Task 1 adds `planOnly?: boolean` per Phase 6 D-01.
- `ElectronAPI.llm.chat` (line 406): payload signature `{ projectId, sessionId, agentId?, message: { id, content }, overrides?: ChatRuntimeOverrides }` — adding `planOnly` to ChatRuntimeOverrides is the only change needed for `/plan` dispatch to reach `llm.ts:324` extension point.
- `MCPServer` (line 84-93): has `is_connected: boolean`; collector joins with `agent_mcp_servers` to filter.
- `Workflow` (line 274-283): has `status: 'draft' | 'active'`; collector filters `status='active'`.

From src/main/deepagent/mcp-connector.ts:
- `loadMcpTools(agentId: string, mcpServers: MCPServer[]): Promise<{ client: any; tools: any[] }>` (line 129) — reuses `mcpCache: Map<string, McpCacheEntry>` (line 12). Phase 6 collector calls this once per registry build. DO NOT call `connectMcpServer` (would bypass cache).

From src/main/deepagent/skill-manager.ts:
- `getScopePath(projectPath: string, scope: 'global' | 'project'): string` (line 80) — returns `path.join(os.homedir(), '.cdf/skills')` for global, `path.join(projectPath, '.cdf/skills')` for project.
- `listPhysicalSkills(projectPath: string): PhysicalSkillView[]` (line 89) — returns merged global + project skills; `scope` field on each result already discriminates (D-22).
- `PhysicalSkillView` (line 17-26): `{ id, name, description, scope, resourceFiles, created_at, updated_at }`.

From src/main/database.ts:
- `workflows` table columns: `id`, `project_id`, `name`, `description`, `graph_data`, `status`, `created_at`, `updated_at`. Query joins via `projects.path`.
- `projects` table: `id`, `path` (absolute), `name`, etc. Use subquery `WHERE project_id = (SELECT id FROM projects WHERE path = ?)`.
- `agent_mcp_servers` join table: `agent_id`, `mcp_server_id`. Filter to `mcp_servers.is_connected = 1`.

From src/main/ipc-handlers.ts (line 43-100 — `registerIpcHandlers` pattern):
- Each handler: `ipcMain.handle('db:getX', async (_, ...args) => { ... })` returning the result or throwing on error. Phase 6 adds `commands:list` (Plan 06-02) — this plan does NOT touch ipc-handlers.ts.

From package.json (existing dependencies):
- `better-sqlite3@^12.10.0` (used in workflow collector lightweight SQL).
- `cmdk@^1.1.1` + `@radix-ui/react-popover@^1.1.15` (Phase 5, used in Plan 06-02).
- No `chokidar` / `sonner` yet — Task 1 installs them.
</interfaces>

<tasks>

<task type="auto">
  name: "Task 1 (Wave 0): Install chokidar + sonner; add 4 shared types; add CI matrix workflow"
  type: auto
  files: "package.json, src/shared/types.ts, .github/workflows/phase-6-commands.yml"
  read_first:
    - src/shared/types.ts (140-143 ChatRuntimeOverrides; 350-431 ElectronAPI; 274-283 Workflow; 84-93 MCPServer)
    - package.json (dependencies block lines 17-49; sort key for alphabetical insert: chokidar belongs after clsx area; sonner alphabetically after radix-ui/*)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md (D-19 D-20 D-21 D-22 D-23 D-25)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md (Package Legitimacy Audit; Standard Stack section)
    - .planning/research/PITFALLS.md (P6.1 Promise.allSettled; P6.5 mcp health warning discrimination)
  action: |
    1. Run from /Users/suntc/project/CDF: `npm install --save chokidar@^3.6.0 sonner@^2.0.7`. Verify both lines appear in `package.json` `dependencies` block. Verify `node_modules/chokidar/package.json` `"version": "3.6.0"` and `node_modules/sonner/package.json` `"version": "2.0.7"` are present (D-25 verification; both verified via `npm view` in research §Standard Stack).
    2. Edit `src/shared/types.ts` to add 4 new types. Insert AFTER the existing `LLMStreamEvent` union (line 138) and BEFORE the `ChatRuntimeOverrides` interface (line 140):
       - `export type CommandSource = 'system' | 'mcp' | 'skill:project' | 'skill:global' | 'workflow' | 'cmd:project' | 'cmd:system';` — D-06 priority order matches this declaration order semantically (priority numbers live in renderer `useCommandRegistry.ts`).
       - `export interface SlashCommand { name: string; description: string; source: CommandSource; target: string; sourceLabel: string; badge: string; argumentHint?: string; }` — `argumentHint` is optional since only custom commands have it (D-20).
       - `export type CommandDispatchAction = { kind: 'SystemSilent'; command: SlashCommand; args: string } | { kind: 'SystemLocal'; command: SlashCommand; args: string } | { kind: 'PluginRewrite'; command: SlashCommand; args: string; prompt: string } | { kind: 'PlanMode'; command: SlashCommand; args: string };` — D-01 four kinds.
       - `export class CommandConflictError extends Error { constructor(public readonly commandName: string, public readonly conflicts: ReadonlyArray<{ source: CommandSource; badge: string }>) { super(\`Command conflict: \${commandName} registered from \${conflicts.length} sources\`); this.name = 'CommandConflictError'; } }` — D-07 lock: build phase returns errors in an array, does NOT throw.
    3. Edit `ChatRuntimeOverrides` (line 140-143) to add the `planOnly?: boolean` field: `export interface ChatRuntimeOverrides { providerId?: string; model?: string; planOnly?: boolean; }` — D-01 PlanMode extension point; lets Plan 06-02 dispatcher pass `{ planOnly: true }` to `sessionStore.sendMessage`, which forwards through preload's existing `overrides?` field at line 406 to `llm.ts:324` (existing extension point — do not modify llm.ts).
    4. Create `.github/workflows/phase-6-commands.yml`:
       ```yaml
       name: Phase 6 Slash Command Tests
       on: [push, pull_request]
       jobs:
         test:
           strategy:
             matrix:
               os: [macos-latest, windows-latest, ubuntu-latest]
           runs-on: ${{ matrix.os }}
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with:
                 node-version: '22'
             - run: npm ci
             - run: npm test -- commands
       ```
       D-25 lock: macOS + Windows + Linux at least 1 run each. No need to filter by branch — full `commands` test pattern matches this plan's test files via `*.test.ts` naming.
  verify:
    automated: |
      cat /Users/suntc/project/CDF/package.json | grep -E '"chokidar"|"sonner"' && \
      cat /Users/suntc/project/CDF/node_modules/chokidar/package.json | grep '"version"' | head -1 && \
      cat /Users/suntc/project/CDF/node_modules/sonner/package.json | grep '"version"' | head -1 && \
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'types\.ts|error TS' | head -5 && \
      grep -E "CommandConflictError|SlashCommand|CommandDispatchAction|CommandSource" /Users/suntc/project/CDF/src/shared/types.ts | head -10 && \
      grep -E "planOnly" /Users/suntc/project/CDF/src/shared/types.ts && \
      cat /Users/suntc/project/CDF/.github/workflows/phase-6-commands.yml | grep -E "macos-latest|windows-latest|ubuntu-latest"
  acceptance_criteria:
    - `package.json` dependencies block contains exactly `"chokidar": "^3.6.0",` and `"sonner": "^2.0.7",`
    - `node_modules/chokidar/package.json` field `"version"` is `"3.6.0"`
    - `node_modules/sonner/package.json` field `"version"` is `"2.0.7"`
    - `npx tsc --noEmit` exits 0 with no errors mentioning `types.ts`
    - `src/shared/types.ts` contains the 4 new exported symbols: `CommandSource`, `SlashCommand`, `CommandDispatchAction`, `CommandConflictError`
    - `ChatRuntimeOverrides` interface contains `planOnly?: boolean` field
    - `.github/workflows/phase-6-commands.yml` exists and contains exactly the strings `macos-latest`, `windows-latest`, `ubuntu-latest`
    - `.github/workflows/phase-6-commands.yml` runs `npm test -- commands` (matches all `commands*.test.ts` + `collectors/*.test.ts` files from Tasks 2-4)
  done: deps installed and version-verified, 4 new types added to shared/types.ts, planOnly field added to ChatRuntimeOverrides, CI matrix workflow committed
</parameter>

<task type="auto">
  name: "Task 2: Implement 5 source collectors + project-commands parser"
  type: auto
  files: "src/main/commands/collectors/system.ts, src/main/commands/collectors/mcp.ts, src/main/commands/collectors/skill.ts, src/main/commands/collectors/workflow.ts, src/main/commands/collectors/project.ts, src/main/commands/project-commands.ts, src/main/commands/collectors/mcp.test.ts, src/main/commands/collectors/skill.test.ts, src/main/commands/collectors/workflow.test.ts, src/main/commands/collectors/project.test.ts, src/main/commands/project-commands.test.ts"
  read_first:
    - src/main/deepagent/mcp-connector.ts (line 12 mcpCache, line 129 loadMcpTools signature + behavior)
    - src/main/deepagent/skill-manager.ts (line 17 PhysicalSkillView, line 80 getScopePath, line 89 listPhysicalSkills)
    - src/main/database.ts (workflows table schema; projects.path; agent_mcp_servers join table)
    - src/shared/types.ts (Task 1 added SlashCommand, MCPServer, Workflow types)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md (D-08 badge format, D-09 MCP no description in popup, D-10 workflow name regex, D-11 lightweight SQL, D-19/20/21/22 paths + frontmatter + project wins)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md (Pattern 1 Promise.allSettled; Pitfall P6.5 MCP health discrimination; 5-collector architecture diagram)
  action: |
    Create 6 source files (collectors/{system,mcp,skill,workflow,project}.ts + project-commands.ts) and 5 test files.

    1. `src/main/commands/collectors/system.ts` — pure const, no IO. Export `collectSystemCommands(): SlashCommand[]` returning 3 hardcoded entries: `{ name: 'goal', description: '设置 session 目标', source: 'system', target: 'goal', sourceLabel: 'system', badge: '[system]' }` + `context` (description: '查看 session token 用量') + `plan` (description: '进入 plan 模式'). Per D-08 C-01: badge text `[system]`. Description is for internal log only; popup does not show it (D-09 excepted only for MCP; system commands show description in popup per Phase 5 popup plan).

    2. `src/main/commands/collectors/mcp.ts` — reuse `loadMcpTools` (mcpCache, no reconnect). Export `collectMcpCommands(agentId: string): Promise<{ commands: SlashCommand[]; hasAgentMcp: boolean }>` (return shape returns BOTH so IPC layer can discriminate P6.5). Implementation:
       - Query `db` for agent-bound MCP servers: `SELECT mcp_servers.* FROM mcp_servers JOIN agent_mcp_servers ON mcp_servers.id = agent_mcp_servers.mcp_server_id WHERE agent_mcp_servers.agent_id = ? AND mcp_servers.is_connected = 1`.
       - If 0 servers: return `{ commands: [], hasAgentMcp: false }`.
       - Else call `await loadMcpTools(agentId, servers)`.
       - Map `tools` to `SlashCommand[]`: `{ name: tool.name, description: tool.description || '', source: 'mcp', target: tool.name, sourceLabel: \`mcp:\${tool.name}\`, badge: \`[mcp:\${tool.name}]\` }`. Description is collected for internal log; per D-09 popup does not show MCP description.
       - Return `{ commands, hasAgentMcp: true }`.

    3. `src/main/commands/collectors/skill.ts` — reuse `listPhysicalSkills`. Export `collectSkillCommands(projectPath: string): Promise<SlashCommand[]>`. Implementation:
       - `const skills = listPhysicalSkills(projectPath)`.
       - Map each to: `{ name: skill.name, description: skill.description, source: skill.scope === 'project' ? 'skill:project' : 'skill:global', target: \`\${skill.scope}:\${skill.name}\`, sourceLabel: \`skill:\${skill.scope}\`, badge: \`[skill:\${skill.scope}]\` }`.
       - D-22 same-name project wins: `listPhysicalSkills` already returns project-wins merge (deepagents `listSkills` behavior); collector just maps.

    4. `src/main/commands/collectors/workflow.ts` — lightweight SQL, name regex filter. Export `collectWorkflowCommands(projectPath: string): Promise<SlashCommand[]>`. Implementation:
       - Use `db.prepare('SELECT id, name, description FROM workflows WHERE project_id = (SELECT id FROM projects WHERE path = ?) AND status = ?').all(projectPath, 'active')` (D-11: lightweight SQL — does NOT use `db:getWorkflows` IPC which carries heavy `graph_data`).
       - Filter `r => /^[a-z0-9-]+$/.test(r.name)` (D-10 valid name regex).
       - Map to `{ name: r.name, description: r.description || '', source: 'workflow', target: r.id, sourceLabel: 'workflow', badge: '[workflow]' }`.
       - If SQL query returns no rows because no project matches `path`, return `[]` (do not throw — P6.1: single source failure should not block others).

    5. `src/main/commands/project-commands.ts` — frontmatter parser + 2 path readers. Export `parseFrontmatter(filePath: string): CommandFrontmatter`, `listSystemCommands(): SlashCommand[]`, `listProjectCommands(projectPath: string): SlashCommand[]`. Implementation:
       - `CommandFrontmatter` interface: `{ name?: string; description?: string; 'argument-hint'?: string }` (D-20).
       - `parseFrontmatter`: read file UTF-8; check `content.startsWith('---\n')`; find end marker `content.indexOf('\n---', 4)`; if missing return `{}`. Slice `content.slice(4, end)`, split lines, parse `key: value` into result object. Trim keys and values.
       - `listCommandsInDir(dir: string, source: 'cmd:system' | 'cmd:project'): SlashCommand[]` (module-private helper): if `!fs.existsSync(dir)`, return `[]`. Else `fs.readdirSync(dir).filter(f => f.endsWith('.md'))`, for each build `{ name: fm.name || f.replace(/\.md$/, ''), description: fm.description || '', source, target: filePath, sourceLabel: source, badge: \`[\${source}]\`, argumentHint: fm['argument-hint'] }`.
       - `listSystemCommands`: `listCommandsInDir(path.join(os.homedir(), '.cdf', 'commands'), 'cmd:system')` (D-19 path is `.cdf/commands/` NOT `.claude/commands/`).
       - `listProjectCommands(projectPath)`: `listCommandsInDir(path.join(projectPath, '.cdf', 'commands'), 'cmd:project')`.

    6. `src/main/commands/collectors/project.ts` — thin wrapper. Export `collectProjectCommands(projectPath: string): Promise<SlashCommand[]>` returning `[...listProjectCommands(projectPath), ...listSystemCommands()]` (D-21 same-name project wins: project listed first, then system — D-06 priority order means project (priority 40) > system (priority 30) when sorted, but detectConflicts runs on raw list; project-wins is enforced by collector ordering). Wrap in Promise.resolve since the underlying functions are sync.

    Tests (5 files; ~5-7 tests each, 30+ total):
    - `collectors/mcp.test.ts`: mock `loadMcpTools` and `db.prepare`; assert (a) 0 servers → `{ commands: [], hasAgentMcp: false }`; (b) servers but tools empty → `{ commands: [], hasAgentMcp: true }` (P6.5 discrimination); (c) 2 tools → 2 SlashCommand entries with `source: 'mcp'` and `badge: '[mcp:toolname]'`; (d) tools[i].description undefined → SlashCommand.description is `''`.
    - `collectors/skill.test.ts`: mock `listPhysicalSkills` to return 1 project + 1 global skill; assert both mapped with correct `source`; assert `description` and `target` correct; assert `badge` format `[skill:project]` and `[skill:global]`.
    - `collectors/workflow.test.ts`: use in-memory better-sqlite3; insert 3 workflows (1 active valid name + 1 active invalid name + 1 draft); mock `db`; assert only 1 returned; assert `source: 'workflow'`, `target: row.id`; assert description preserves `''` fallback for NULL.
    - `collectors/project.test.ts`: use `os.tmpdir()` + temp directories; create `<tmp>/.cdf/commands/foo.md` with frontmatter; create `<tmp>/.cdf/commands/bar.md` (no frontmatter); create `<tmp>/.cdf/commands/notes.txt` (not .md, skip); call `collectProjectCommands(tmp)`, assert 2 commands. For system side: skip on CI for now OR use `process.env.HOME` override — Phase 6 system commands test uses `jest.mock`/vi.mock on `os.homedir` to return temp dir.
    - `project-commands.test.ts`: 5+ tests: (a) parseFrontmatter returns `{}` for non-existent file; (b) parseFrontmatter returns `{}` for file without frontmatter; (c) parseFrontmatter returns name/description/argument-hint from valid frontmatter; (d) parseFrontmatter handles colons in values (`description: http://foo.com`); (e) listSystemCommands returns [] when `~/.cdf/commands/` doesn't exist (mocked); (f) listProjectCommands returns 0 results when `<projectPath>/.cdf/commands/` doesn't exist.

    Constraint reminders: do NOT add new runtime dependencies beyond chokidar/sonner; do NOT use `Promise.all` (P6.1) — collectors individually return sync or Promise; do NOT bypass `mcpCache` (no direct `connectMcpServer` call); do NOT include `graph_data` in workflow SQL.
  verify:
    automated: |
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'commands/(collectors|project-commands)|error TS' | head -5 && \
      grep -E "Promise\\.allSettled" /Users/suntc/project/CDF/src/main/commands/command-registry.ts | head -3 || echo "registry not yet created (Task 3)" && \
      npx vitest run /Users/suntc/project/CDF/src/main/commands/collectors/ /Users/suntc/project/CDF/src/main/commands/project-commands.test.ts 2>&1 | tail -20
  acceptance_criteria:
    - `npx tsc --noEmit` exits 0 with no errors in `src/main/commands/collectors/*.ts` or `project-commands.ts`
    - `src/main/commands/collectors/mcp.ts` exports `collectMcpCommands` returning `Promise<{ commands: SlashCommand[]; hasAgentMcp: boolean }>` (NOT plain SlashCommand[] — discrimination for P6.5)
    - `src/main/commands/collectors/skill.ts` imports `listPhysicalSkills` from `../../deepagent/skill-manager` (verified by `grep "listPhysicalSkills"` returns 1+ line)
    - `src/main/commands/collectors/workflow.ts` SQL string contains `SELECT id, name, description FROM workflows` and `status = ?` (NOT `getWorkflows` IPC; NOT selecting `graph_data`)
    - `src/main/commands/collectors/workflow.ts` contains the regex `/^[a-z0-9-]+$/` (D-10 name validation)
    - `src/main/commands/project-commands.ts` `listSystemCommands` uses `path.join(os.homedir(), '.cdf', 'commands')` (D-19 verified; not `.claude/commands/`)
    - `src/main/commands/collectors/project.ts` calls BOTH `listSystemCommands` and `listProjectCommands`; project listed first (D-21 project wins via ordering)
    - All 5 test files pass; `npx vitest run` exits 0 with ≥ 25 tests passed across the 5 files
    - The string `Promise.all` does NOT appear in `src/main/commands/collectors/*.ts` (P6.1: use Promise.allSettled at the registry level, not in collectors)
    - No collector imports from `src/main/llm.ts`, `src/main/runtime.ts`, or `src/main/workflow-runtime.ts` (Hard Do Not Touch)
  done: 5 collectors + project-commands parser created; each collector unit-tested; failure isolation per source; lightweight workflow SQL; D-19 path correct; D-21 ordering correct; P6.5 discrimination in mcp collector
</parameter>

<task type="auto">
  name: "Task 3: Build command-registry.ts merger + conflict-detector.ts"
  type: auto
  files: "src/main/commands/command-registry.ts, src/main/commands/conflict-detector.ts, src/main/commands/command-registry.test.ts, src/main/commands/conflict-detector.test.ts"
  read_first:
    - .planning/phases/06-4-source-command-registry-dispatcher/06-CONTEXT.md (D-04 priority only controls sort; D-05 conflicts preserve both rows; D-06 priority order; D-07 build phase returns errors in array, does NOT throw)
    - .planning/phases/06-4-source-command-registry-dispatcher/06-RESEARCH.md (Pattern 1 Promise.allSettled; Pattern 2 Conflict-Aware sort; Pitfall P6.1 P6.2 P6.8)
    - src/main/commands/collectors/{system,mcp,skill,workflow,project}.ts (Task 2 output)
    - src/shared/types.ts (Task 1 added SlashCommand + CommandConflictError)
    - vitest.config.ts (test runner config)
  action: |
    1. Create `src/main/commands/conflict-detector.ts`:
       - Export `detectConflicts(commands: ReadonlyArray<SlashCommand>): CommandConflictError[]`.
       - Group commands by NFKC-normalized name (`cmd.name.normalize('NFKC').toLowerCase()` as key, per P6 case 5: CJK collision case).
       - For each group with `length > 1`, build a `CommandConflictError(name, conflicts)` where `conflicts` is `[{ source: c.source, badge: c.badge }, ...]`.
       - Return the errors array. **DO NOT throw** — D-07 lock: build phase returns errors; renderer consumes them to fire sonner toasts. P6.2 warning.
       - Pure function: no IO, no mutation of input. Deterministic ordering: iterate groups by insertion order (Map preserves insertion order in V8).

    2. Create `src/main/commands/command-registry.ts`:
       - Export `collectAllCommands(projectPath: string, agentId: string): Promise<{ commands: SlashCommand[]; conflicts: CommandConflictError[]; warnings: Array<{ type: 'mcp_health_warning'; message: string }> }>`.
       - Use `Promise.allSettled` (P6.1: failure isolation; P6.8: check `.status === 'fulfilled'`, do not use `??` short-circuit) for the 5 collectors.
       - system is sync, wrap in `Promise.resolve(collectSystemCommands())`.
       - mcp returns `{ commands, hasAgentMcp }` — extract commands and hasAgentMcp.
       - After all settled, build flat `commands: SlashCommand[]` array.
       - Compute `conflicts = detectConflicts(commands)`.
       - Compute `warnings`: if `hasAgentMcp && mcpCommands.length === 0` then `[{ type: 'mcp_health_warning', message: 'MCP 工具加载失败，请检查连接' }]`. Per P6.5: if agent never bound MCP, do NOT warn.
       - Return `{ commands, conflicts, warnings }`.
       - **Commands array preserves ALL rows** (D-05 + P6.2: do NOT dedupe; conflicts are info, not removal signal).

    3. Tests in `src/main/commands/conflict-detector.test.ts` (5+ tests per Phase 6 validation strategy):
       - Case 1: `system /goal` + `workflow /goal` (2 rows) → 1 `CommandConflictError` with 2 entries.
       - Case 2: `skill:global /simplify` + `cmd:system /simplify` → 1 error, 2 entries.
       - Case 3: `mcp /arxiv_search` + `skill:project /arxiv_search` → 1 error, 2 entries.
       - Case 4: 3 sources same name → 1 error with 3 entries.
       - Case 5: CJK collision: `skill /上下文` + `workflow /上下文` (NFKC normalized `name` equal) → 1 error, 2 entries.
       - Negative: distinct names → `[]`.
       - Negative: same name SAME source (theoretically impossible but verify detector doesn't double-count) → still 1 error with 2 entries (no special handling).

    4. Tests in `src/main/commands/command-registry.test.ts` (5+ tests):
       - All 5 collectors succeed: mock each collector; assert `commands.length === sum of all mock returns`; assert `conflicts` computed across all rows.
       - One collector throws (e.g., `collectMcpCommands` rejects with `Error('mcp server down')`): other 4 still return their commands; total commands = sum of 4 mock returns. The failed source contributes 0 commands. (P6.1)
       - MCP empty + hasAgentMcp: mcp collector returns `{ commands: [], hasAgentMcp: true }`; assert `warnings` has 1 entry with `type: 'mcp_health_warning'`.
       - MCP empty + no agent binding: mcp collector returns `{ commands: [], hasAgentMcp: false }`; assert `warnings` is `[]` (P6.5 discrimination).
       - Conflicts across sources: 2 collectors return same name → `conflicts` has 1 entry; commands array has BOTH rows (D-05 + P6.2 verify).
       - All collectors fail: `Promise.allSettled` all rejected → commands is `[]`; conflicts is `[]`; warnings is `[]`. No throw (P6.1 + P6.2 verify).

    Use `vi.mock('../../commands/collectors/mcp', ...)` style mocks (vitest `vi.mock` API). For the registry test, mock all 5 collectors at the module level.

    Constraint reminders: do NOT use `Promise.all` (P6.1); do NOT throw on conflicts (D-07 + P6.2); do NOT dedupe commands (D-05); do NOT add mcp_health_warning when hasAgentMcp is false (P6.5).
  verify:
    automated: |
      npx tsc --noEmit -p /Users/suntc/project/CDF/tsconfig.json 2>&1 | grep -E 'command-registry|conflict-detector|error TS' | head -5 && \
      grep -E "Promise\\.allSettled" /Users/suntc/project/CDF/src/main/commands/command-registry.ts && \
      grep -E "Promise\\.all[^S]" /Users/suntc/project/CDF/src/main/commands/command-registry.ts || echo "no bare Promise.all found" && \
      npx vitest run /Users/suntc/project/CDF/src/main/commands/command-registry.test.ts /Users/suntc/project/CDF/src/main/commands/conflict-detector.test.ts 2>&1 | tail -25
  acceptance_criteria:
    - `npx tsc --noEmit` exits 0 with no errors in `command-registry.ts` or `conflict-detector.ts`
    - `src/main/commands/command-registry.ts` contains the string `Promise.allSettled` (failure isolation per P6.1)
    - `src/main/commands/command-registry.ts` does NOT contain `Promise.all(` (only the settled variant)
    - `src/main/commands/conflict-detector.ts` does NOT contain `throw` keyword (D-07 lock: returns array, no throw)
    - `collectAllCommands` returns shape `{ commands: SlashCommand[]; conflicts: CommandConflictError[]; warnings: Array<{ type: 'mcp_health_warning'; message: string }> }` (matches the shape Plan 06-02 IPC handler will forward)
    - `npx vitest run src/main/commands/conflict-detector.test.ts` exits 0 with ≥ 7 tests passed (5 conflict cases + 2 negative cases)
    - `npx vitest run src/main/commands/command-registry.test.ts` exits 0 with ≥ 6 tests passed (5 source + Promise.allSettled isolation + MCP health discrimination + multi-collector conflict)
    - Test file `command-registry.test.ts` includes a test named `does not throw when one collector rejects` (P6.1 verify)
    - Test file `command-registry.test.ts` includes a test named `mcp_health_warning only when hasAgentMcp is true` (P6.5 verify)
  done: command-registry.ts merger with Promise.allSettled isolation + conflict-detector.ts pure detector; 13+ tests covering 5 conflict cases + 6 registry integration cases; Phase 6 data layer is fully testable in isolation
</parameter>

<task type="auto">
  name: "Task 4: Run full collector + registry + conflict test suite; verify CI workflow file"
  type: auto
  files: "(no new files — verification + any small fixes)"
  read_first:
    - .planning/phases/06-4-source-command-registry-dispatcher/06-VALIDATION.md (per-task verification map; sampling rate)
    - .github/workflows/phase-6-commands.yml (Task 1 created)
    - vitest.config.ts
  action: |
    1. Run the full Phase 6 test suite for this plan: `npx vitest run src/main/commands/ 2>&1 | tail -50`. Should show all tests green.
    2. Run type check: `npx tsc --noEmit 2>&1 | tail -10`. Should exit 0.
    3. Run the full project test suite to ensure no regressions: `npm test 2>&1 | tail -30`. (Phase 5's 19 tests in `SlashCommandPopup.test.tsx` must still pass — Plan 06-01 does NOT touch Phase 5 files, but verifies nothing accidentally broke.)
    4. Verify the CI workflow file: `cat .github/workflows/phase-6-commands.yml`. Confirm:
       - 3 OS matrix entries: `macos-latest`, `windows-latest`, `ubuntu-latest` (D-25 lock).
       - Uses `actions/checkout@v4` + `actions/setup-node@v4` (current pinned versions).
       - `npm test -- commands` matches all `commands*.test.ts` + `collectors/*.test.ts` files.
    5. Verify dependency version locks: `cat package.json | grep -E '"chokidar"|"sonner"'`.
    6. (Optional, time-permitting) Local chokidar smoke test: create a temp dir, write a `.md` file, instantiate chokidar watcher in a small script, verify `add` event fires after 200ms. This is NOT a persisted test — Phase 6 chokidar watcher + integration test live in Plan 06-02. Skip if time-constrained.
    7. Document in the SUMMARY (when this plan's SUMMARY is written): all test counts, CI matrix definition, dependency versions.

    Constraint reminders: do NOT add new dependencies; do NOT modify Phase 5 files; do NOT touch runtime.ts / llm.ts / workflow-runtime.ts; do NOT create the chokidar watcher file (that's Plan 06-02).
  verify:
    automated: |
      npx vitest run /Users/suntc/project/CDF/src/main/commands/ 2>&1 | tail -10 && \
      npx tsc --noEmit 2>&1 | tail -3 && \
      npm test 2>&1 | tail -15 && \
      cat /Users/suntc/project/CDF/.github/workflows/phase-6-commands.yml | grep -cE "macos-latest|windows-latest|ubuntu-latest" && \
      cat /Users/suntc/project/CDF/package.json | grep -E '"chokidar"|"sonner"'
  acceptance_criteria:
    - `npx vitest run src/main/commands/` exits 0 with all collector + registry + conflict tests passing (≥ 38 tests: 25 collector + 7 conflict + 6 registry = 38)
    - `npx tsc --noEmit` exits 0 across the full project
    - `npm test` exits 0 with no regressions in Phase 5 tests (the 19 SlashCommandPopup tests still pass)
    - `.github/workflows/phase-6-commands.yml` contains exactly 3 OS matrix entries (`macos-latest`, `windows-latest`, `ubuntu-latest`) — verified by `grep -c` returning `3`
    - `package.json` shows `"chokidar": "^3.6.0"` and `"sonner": "^2.0.7"`
    - No file under `src/main/commands/` imports from `src/main/llm.ts`, `src/main/runtime.ts`, or `src/main/workflow-runtime.ts` (Hard Do Not Touch verified by `grep -L` inverse search)
  done: full Phase 6 main-process data layer green; CI matrix file committed; no Phase 5 regressions; ready for Plan 06-02 to wire dispatcher + chokidar + IPC + renderer hook
</parameter>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fs-user | untrusted user-edited `.cdf/commands/*.md` files contain YAML frontmatter; untrusted skill SKILL.md files contain frontmatter |
| db-main | `workflows` table + `mcp_servers` table + `agent_mcp_servers` join — all read from main process only |
| npm-registry | `chokidar@^3.6.0` and `sonner@^2.0.7` install from npm; verified via `npm view` in RESEARCH §Standard Stack; slopcheck [OK] in §Package Legitimacy Audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-T1 | Tampering (registry) | command-registry.ts | mitigate | `detectConflicts` returns `CommandConflictError[]` (NOT throw per D-07 + P6.2) — verified by `conflict-detector.test.ts` "does not throw" assertion |
| T-06-T2 | Tampering (single source failure) | collectAllCommands | mitigate | `Promise.allSettled` isolation per P6.1 — verified by `command-registry.test.ts` "does not throw when one collector rejects" |
| T-06-T3 | Tampering (workflow SQL injection) | collectors/workflow.ts | mitigate | `db.prepare(...).all(projectPath, 'active')` uses parameter binding; never string-concat user input — verified by code review (parameter `?` placeholders) |
| T-06-T4 | Tampering (frontmatter injection) | project-commands.ts | mitigate | `parseFrontmatter` returns plain string values; no `eval`; no shell exec. `$ARGUMENTS` placeholder is replaced as a string in the dispatcher (Plan 06-02), not at parse time — verified by `project-commands.test.ts` "parses colons in values" |
| T-06-T5 | Information disclosure (mcp health) | collectors/mcp.ts | mitigate | P6.5: `mcp_health_warning` only fires when `hasAgentMcp === true`; if agent never bound MCP, no warning — verified by `mcp.test.ts` "hasAgentMcp false" case |
| T-06-T6 | Tampering (path traversal) | project-commands.ts | mitigate | `listCommandsInDir` uses `path.join` + `fs.readdirSync` only on `~/.cdf/commands/` and `<projectPath>/.cdf/commands/`; NEVER joins user input into the path. `depth: 0` enforced in Plan 06-02 chokidar watcher |
| T-06-T7 | Repudiation (conflict silent) | conflict-detector.ts | mitigate | Conflict errors are returned to renderer (not throw) → renderer fires sonner toast per Plan 06-02 — verified by `conflict-detector.test.ts` "case 1..5" all returning non-empty array |
| T-06-T8 | Denial of service (mcpCache stale) | mcp-connector.ts | accept | Phase 6 reuses `mcpCache`; `configHash` cache may return stale tools on tool-list change. Plan 06-02 chokidar + MCP health events trigger `mcpCache.delete(agentId)`; documented in RESEARCH §Pitfall P6.4 |
| T-06-SC | Tampering (supply chain) | `chokidar@^3.6.0` + `sonner@^2.0.7` install | accept | Both packages slopcheck [OK] in RESEARCH §Package Legitimacy Audit (lines 175-186); npm registry verified via `npm view`; chokidar 3.6.0 CommonJS-compatible (4.x is ESM-only — locked to 3.x). Verified by `node_modules/chokidar/package.json` `"version": "3.6.0"` and `node_modules/sonner/package.json` `"version": "2.0.7"` |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/main/commands/` exits 0 with ≥ 38 tests passing (25 collector + 7 conflict + 6 registry)
- `npm test` exits 0 with no Phase 5 regressions (19 SlashCommandPopup tests still green)
- `cat package.json | grep -E '"chokidar"|"sonner"'` returns 2 lines
- `.github/workflows/phase-6-commands.yml` contains exactly the 3-OS matrix
- `grep -r "from.*runtime\|from.*llm\|from.*workflow-runtime" src/main/commands/` returns 0 lines (Hard Do Not Touch respected)
</verification>

<success_criteria>
- All must_haves.truths verifiable: registry collects from 5 sources with Promise.allSettled isolation; conflicts return as array; workflow SQL is lightweight; MCP health warning discriminates; chokidar + sonner installed; CI matrix committed
- All must_haves.artifacts exist at listed paths with correct exports
- 38+ tests green; type check clean; package.json updated; node_modules contains both packages at verified versions
- ROADMAP.md Phase 6 plan list updated to mark 06-01 as ready
- Hard Do Not Touch list respected (no changes to runtime.ts / llm.ts:306-425 / workflow-runtime.ts / LLMStreamEvent union)
</success_criteria>

<output>
Create `.planning/phases/06-4-source-command-registry-dispatcher/06-01-SUMMARY.md` when done
</output>
