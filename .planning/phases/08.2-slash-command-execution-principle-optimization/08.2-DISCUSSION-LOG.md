# Phase 08.2: Slash command execution principle optimization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-05
**Phase:** 08.2-slash-command-execution-principle-optimization
**Areas discussed:** A (.md body loading), B (MCP dimension), C-1 (/goal), C-2 (/context), C-3 (/plan), D (frontmatter), E (goal inject, folded into C-1), F (real bubbles, folded into C), G (direct tool call)

---

## A. .md body 加载 + $ARGUMENTS 替换

### A1: 加载时机
| Option | Description | Selected |
|--------|-------------|----------|
| 1 | 打开时全读 (eager cache) | |
| 2 | 按 Enter 才读 (lazy fresh) | ✓ |
| 3 | 打开时读 + chokidar invalidate | (initial pick, then changed) |
| 4 | 动态阈值 (Claude 决定) | |

**User's choice:** 选项 2（按 Enter 才读）。最初选 3，后改回 2。
**Notes:** 用户偏好 lazy load + 命令文件改 chokidar 主动监控 + 失败时 LLM 兜底。

### A2: 替换语法
| Option | Description | Selected |
|--------|-------------|----------|
| 只 $ARGUMENTS | 整串参数 | |
| $ARGUMENTS + $0/$1 | 位置参数 | |
| 完整 Claude Code 对等 | $ARGUMENTS + $0/$1 + $name 命名参数 | ✓ |

**User's choice:** 完整 Claude Code 对等。
**Notes:** 用户问 "Claude Code 怎么做的" — 蕾姆给了栗子后用户选择完整对标。

### A3: body 替换后角色
| Option | Description | Selected |
|--------|-------------|----------|
| 代替用户消息 | 命令名不出现 | ✓ |
| 跟在用户消息后面 | 原文 + body 补充 | |
| 作为 system 消息 | body 是 context | |

**User's choice:** 代替用户消息。
**Notes:** 用户场景是「命令后面有中文补充语」——整段都是 args。

### A4: body 读不到处理
| Option | Description | Selected |
|--------|-------------|----------|
| 静默降级 | fallback 到 prompt 模板 | |
| 报错 toast | 不 dispatch | |
| LLM 兜底 | LLM 收到「命令不存在」 | |
| chokidar 主动 + LLM 兜底 | 实时 unlink 监听 + 失败时 LLM | ✓ |

**User's choice:** chokidar 主动 + LLM 兜底。
**Notes:** 用户原话「不应该提示用户失效...对命令进行监听，只要删了就触发从内存移除操作」。

---

## B. MCP 命名维度

### B1: 维度
| Option | Description | Selected |
|--------|-------------|----------|
| per-tool | 改回 SLASH-08 原设计 | |
| per-server (现状) | 一个命令 = 一个服务器 | ✓ |
| 并存 | 两种都提供 | |
| 动态阈值 | Claude 决定 | |

**User's choice:** 保持 per-server 现状。
**Notes:** 与 G 配合讨论时进一步确认：MCP 斜杠命令 = 告诉 LLM 用这个 MCP，**内部工具怎么用是 LLM 的事**。

---

## C-1. /goal 理想形态

### C-1a: 谁判断完成
| Option | Description | Selected |
|--------|-------------|----------|
| 小模型 judge | 对标 Claude Code | |
| 主模型自己说 | 简单 | |
| 用户手动 /goal done | | |
| 提议 + 10s 超时 | | |
| 专用内置 agent + 复用当前模型 | 隔离 + 简单 | ✓ |

**User's choice:** 专用内置 agent + 复用当前对话模型。
**Notes:** 用户原话「应该就是方案 1，但是对于我们来说，这就是一个专门的 agent，一个内置、用户不可见的 agent」。

### C-1b: goal 怎么传给主 agent
| Option | Description | Selected |
|--------|-------------|----------|
| A 告诉一次 | agent 自己记 | |
| B 每轮都念 | token 浪费 | |
| C judge 不通过时再提醒 | | |
| D A + C 结合 | 对标 Claude Code | ✓ |

**User's choice:** D。
**Notes:** 用户问「Claude Code 怎么做的」——蕾姆查到 first message + judge reason 循环。

### C-1c: judge 何时触发
| Option | Description | Selected |
|--------|-------------|----------|
| 主 agent 说「做完了」时才判 | 蕾姆推荐 | ✓ |
| 每轮 tool 调用后都判 | 粒度细 | |
| 每条 stream 结束 | 对标 Claude Code | |
| 双重信号 | | |

**User's choice:** 主 agent 说「做完了」时才判。

### C-1d: /goal 怎么显示
| Option | Description | Selected |
|--------|-------------|----------|
| 系统气泡（更新式） | 状态实时更新 | ✓ |
| 原样 toast | 不可回看 | |
| 专属 GoalCard | 视觉权重高 | |

**User's choice:** 系统气泡（更新式）。
**Notes:** 蕾姆给了 3 个 ASCII mockup 后用户选 1。

---

## C-2. /context 理想形态

### C-2a: 数据范围
| Option | Description | Selected |
|--------|-------------|----------|
| 现状 4 项 | breakdown | |
| breakdown + 明细 | 详细 | |
| breakdown + 提醒 | | |
| Claude Code 完整版 + CDF 专属项 | 蕾姆推荐 | ✓ |

**User's choice:** Claude Code 完整版 + CDF 专属项。
**Notes:** 用户上传了 Claude Code `/context` 截图作为参考。

### C-2b: 怎么呈现
| Option | Description | Selected |
|--------|-------------|----------|
| 系统气泡 | | |
| 独立侧栏面板 | 不占聊天气泡位 | |
| 弹窗 modal | 桌面应用正常 | ✓ |
| 内嵌状态条 | | |

**User's choice:** 弹窗 modal。

### C-2c: LLM 可见性
| Option | Description | Selected |
|--------|-------------|----------|
| LLM 不知道 | 现状 | ✓ |
| 每次都贴提醒 | | |
| 85% 阈值时 | | |

**User's choice:** LLM 不知道。

### C-2d: 触发方式
| Option | Description | Selected |
|--------|-------------|----------|
| slash 命令 | | |
| 按钮 | UI affordance | |
| slash 命令 + 按钮双入口 | | ✓ |

**User's choice:** slash 命令 + 按钮双入口。
**Notes:** 用户原话「/context 命令也做进来吧，虽然是按钮功能，但是也是命令的变体」。最初蕾姆建议只做按钮，用户坚持保留 slash 命令。

---

## C-3. /plan 理想形态

### C-3a: 模式
| Option | Description | Selected |
|--------|-------------|----------|
| 一次性（现状） | | |
| 持久模式（对标 Claude Code） | | |
| Codex 风格弹窗 + modify loop | 蕾姆推荐 | ✓ |
| 3 档权限开关 /permissions | | |

**User's choice:** Codex 风格弹窗 + modify loop。
**Notes:** 用户描述了 Codex 桌面版 plan 模式行为：+ 按钮 → 选 plan → 描述任务 → 弹窗显示计划 → 修改 loop / 立即执行。

### C-3b: 计划生成
| Option | Description | Selected |
|--------|-------------|----------|
| 仅 planOnly flag | | |
| flag + system 提示双保险 | | |
| 专用子 agent | | |
| planOnly flag + 工具分级 | 读允许写屏蔽 | ✓ |

**User's choice:** planOnly flag + 工具分级。
**Notes:** 用户原话「只是不能让他调用操作类的工具，读类的工具还是可以调用的」。

### C-3c: 怎么触发执行
| Option | Description | Selected |
|--------|-------------|----------|
| 模拟用户消息（Codex 风格） | 「立即执行」 | ✓ |
| sessionStore flag | | |
| 重发原任务 | | |

**User's choice:** 模拟用户消息。

### C-3d: modify loop
| Option | Description | Selected |
|--------|-------------|----------|
| 循环「修改意见」+ 重新 planOnly | 蕾姆推荐 | ✓ |
| 在原计划上 edit | | |

**User's choice:** 循环「修改意见」+ 重新 planOnly。

---

## D. Frontmatter 字段执行

### D1: 选哪些字段
| Field | Description | Selected |
|-------|-------------|----------|
| disable-model-invocation | 阻止 LLM 自动调 | ✓ |
| allowed-tools | 限制可用工具 | ✓ |
| user-invocable | 隐藏 popup 但 LLM 可调 | ✓ |
| when_to_use | 语义自动触发 | ✓ |

**User's choice:** 全选 4 个。
**Notes:** 用户先问「Claude Code 怎么做的」，蕾姆给出 4 字段行为描述后用户全选。

### D2: 解析机制
| Option | Description | Selected |
|--------|-------------|----------|
| A. lazy 读 body + YAML | 与 A1 一致 | |
| B. Registry 预读 + cache | | |
| C. frontmatter 预读 + body lazy | 蕾姆推荐 | ✓ |

**User's choice:** C。
**Notes:** 蕾姆找到 Claude Code 文档原话 "a skill's body loads only when it's used" 确认 C。

### D3: 生效机制
| Option | Description | Selected |
|--------|-------------|----------|
| 全 LLM 提示 | 软 | |
| 全代码层（对标 Claude Code） | 蕾姆推荐 | ✓ |
| 混合 | | |

**User's choice:** 全代码层。
**Notes:** 用户原话「我建议与 claude code 保持一致，毕竟它是业内标杆」。

### D4: 默认值
| Option | Description | Selected |
|--------|-------------|----------|
| 对齐 Claude Code 默认值 | 蕾姆推荐 | ✓ |
| CDF 保守默认 | | |

**User's choice:** 对齐 Claude Code 默认值。

---

## E. Goal 自动注入 LLM 上下文

(合入 C-1 讨论)
- 不单独实现
- C-1a (专用 judge) + C-1b (D 方案：first message + judge reason) 已涵盖
- 「每轮都念」被否决（token 浪费），「不告诉」被否决（写后即忘）

---

## F. 真实气泡反馈

(按 command 区分，不统一)
- `/goal` → 系统气泡（C-1d 决定）
- `/context` → 弹窗 modal（C-2b 决定）
- `/plan` → Codex 风格弹窗（C-3a 决定）
- 3 个 command 用途不同，**不**统一为一种呈现

---

## G. Plugin dispatch direct tool call

(无需架构变更)
**User's choice:** 现状（LLM 始终在 loop）。
**Notes:** 用户原话「正常用户不直接指定的时候，llm 自己就会根据需求去使用对应 tool 了，用户指定，那 llm 就根据用户指定的去做呗」。后澄清「只是 tool 是这样，MCP 不是」——MCP 斜杠命令 = 告诉 LLM 用这个 MCP，**MCP 内部工具怎么用是 LLM 的事**，我们不操心。

---

## Claude's Discretion
- 无（每个分歧都让用户做选择）

## Deferred Ideas

参见 CONTEXT.md `<deferred>` 段：
- v1.2+: popup 预览 body、$ARGUMENTS 默认追加、SLASH-15/17、gpt-tokenizer、/plan 持久模式
- UI polish: /context 按钮位置
- 取消: MCP 改 per-tool、/plan 持久模式、3 system command 统一模型
