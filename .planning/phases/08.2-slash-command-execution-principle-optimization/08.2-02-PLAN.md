---
phase: 08.2-slash-command-execution-principle-optimization
plan: 02
type: execute
wave: 2
depends_on: [08.2-01]
files_modified:
  - src/renderer/src/stores/planPopupStore.ts
  - src/renderer/src/stores/planPopupStore.test.ts
  - src/renderer/src/components/PlanPopup/PlanPopup.tsx
  - src/renderer/src/components/PlanPopup/PlanPopup.test.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/lib/commands/dispatcher.ts
  - src/renderer/src/lib/commands/dispatcher.test.ts
  - src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
  - src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx
  - src/main/deepagent/planOnly-tool-gate.test.ts
  - src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "用户输入 /plan X 弹出 Radix Dialog 显示 LLM 生成的计划 markdown"
    - "弹窗内含「修改计划」和「立即执行」两个按钮（修改 = inline 输入 + 重新触发 planOnly=true 请求）"
    - "modify loop 在第 20 次迭代被强制截停：「修改计划」按钮变灰，「立即执行」保持可点；UI 在第 18 次起显示剩余警告"
    - "点击「立即执行」会模拟一条「立即执行」user message 入对话流，下次 sendMessage 不带 planOnly 标志（agent 进入执行态）"
    - "planOnly=true 期间，写类工具（Edit/Write/Bash 写操作）被 deepagent runtime 拒绝；读类工具（Read/Grep/Glob）允许 — 由 planOnly-tool-gate 单测守门（pre-execution probe 决定测试形态，详见 Task 3 step 1）"
    - "frontmatter `user-invocable: false` 的命令不在 / popup 中显示（D-09 user-invocable 强制）"
    - "dispatcher 在所有 PluginRewrite 路径上把 frontmatter.allowedTools 写入 sendMessage overrides（D-09 allowed-tools，P1 已做；本 plan 验收回归）"
    - "SLASH-REGRESSION (llm-adapter.test.ts) 仍 green"
  artifacts:
    - path: "src/renderer/src/stores/planPopupStore.ts"
      provides: "usePlanPopupStore Zustand state machine: isOpen / status / planContent / iterationCount (≤20) / modifyHistory + open/close/startModify/execute/cancelCurrent actions"
      exports: ["usePlanPopupStore", "PlanStatus", "PlanPopupState"]
    - path: "src/renderer/src/components/PlanPopup/PlanPopup.tsx"
      provides: "Radix Dialog 展示 plan markdown + 两按钮 + inline modify input + 迭代计数器"
      exports: ["PlanPopup"]
    - path: "src/renderer/src/lib/commands/dispatcher.ts"
      provides: "PlanMode 分支调 usePlanPopupStore.open()（popupOpen !== false 时）；resolve() 对 /plan 设置 popupOpen: true"
    - path: "src/main/deepagent/planOnly-tool-gate.test.ts"
      provides: "回归测试：planOnly=true 拒写、允读；planOnly=false（或 undefined）放行所有工具（详见 Task 3 pre-execution probe）"
    - path: "src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx"
      provides: "user-invocable: false 过滤（D-09 UI 强制）"
  key_links:
    - from: "src/renderer/src/lib/commands/dispatcher.ts (PlanMode case)"
      to: "src/renderer/src/stores/planPopupStore.ts open()"
      via: "Zustand store.getState().open(description)"
      pattern: "usePlanPopupStore\\.getState\\(\\)\\.open"
    - from: "src/renderer/src/components/PlanPopup/PlanPopup.tsx"
      to: "src/renderer/src/stores/planPopupStore.ts"
      via: "selector subscriptions (usePlanPopupStore((s) => ...))"
      pattern: "usePlanPopupStore\\("
    - from: "src/renderer/src/stores/planPopupStore.ts startModify/execute"
      to: "useSessionStore.sendMessage + window.electronAPI.llm.stopChat"
      via: "Zustand cross-store + IPC"
      pattern: "sendMessage\\(|stopChat\\("
    - from: "src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx"
      to: "command.frontmatter.userInvocable"
      via: "filter before cmdk render"
      pattern: "userInvocable"
---

<objective>
**P2 — /plan Codex 弹窗模式 + frontmatter 运行时强制 (user-invocable + allowed-tools 回归 + planOnly tool gate 回归)。**

把 `/plan` 从 Phase 7 的 toast + planOnly flag 升级为 Codex 桌面版风格弹窗：
1. usePlanPopupStore Zustand 状态机（状态：closed/generating/reviewing/modifying/executing/cap-reached；迭代上限 20）— C3-04/C3-05
2. PlanPopup Radix Dialog 组件（markdown 渲染、两按钮、inline 修改输入、计数器、临界值警告）— UI-SPEC.md Surface 3
3. dispatcher PlanMode 分支接入 store；resolve() 对 /plan 设 popupOpen: true — C3-01/C3-05
4. SlashCommandPopup user-invocable 过滤（D-09）
5. planOnly tool gate 回归测试新建（C3-02）：验证写类工具拒、读类工具允（**pre-execution probe 决定测试形态**，详见 Task 3 step 1）
6. 「立即执行」= 模拟「立即执行」user message + 下次 sendMessage 无 planOnly — C3-03

**Purpose:** /plan 是 3 system command 中风险最低、UI 改动最直接的。本 plan 先行验证整套「dispatcher 调 store → 组件订阅 store」模式，为 P3/P4 趟路。

**Output:** 4 个新文件（store + store.test + component + component.test）+ planOnly-tool-gate.test.ts（新）+ 6 个扩展。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-slash-command-execution-principle-optimization/08.2-CONTEXT.md
@.planning/phases/08.2-slash-command-execution-principle-optimization/08.2-RESEARCH.md
@.planning/phases/08.2-slash-command-execution-principle-optimization/08.2-PATTERNS.md
@.planning/phases/08.2-slash-command-execution-principle-optimization/08.2-UI-SPEC.md
@.planning/phases/08.2-slash-command-execution-principle-optimization/08.2-VALIDATION.md

# P1 已落地的 type seam（必须读以确认 PlanMode.popupOpen / allowedTools 字段已存在）
@src/shared/types.ts

# P1 已落地的 dispatcher（本 plan 在此基础上修改 PlanMode 分支）
@src/renderer/src/lib/commands/dispatcher.ts
@src/renderer/src/lib/commands/dispatcher.test.ts

# Zustand 极简模式参考
@src/renderer/src/stores/themeStore.ts
@src/renderer/src/stores/llmStore.ts
@src/renderer/src/stores/flowStore.test.ts

# SessionStore（cross-store 调用 sendMessage 的契约）
@src/renderer/src/stores/sessionStore.ts

# Radix Dialog 包装器（已经存在，直接用）
@src/renderer/src/components/ui/dialog.tsx
@src/renderer/src/components/ui/button.tsx
@src/renderer/src/components/ui/skeleton.tsx

# Markdown 渲染（已用于 ChatArea，直接复用）
@src/renderer/src/components/ChatArea/MarkdownRenderer.tsx

# RTL 测试模板（高质量参考）
@src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx
@src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx

# planOnly 工具拒绝点（runtime 层，**只读** — 严禁修改 — Issue 3+4 修复）
@src/main/deepagent/runtime.ts
@src/main/deepagent/runtime.test.ts

# Mount 点
@src/renderer/src/App.tsx
@src/renderer/src/components/ChatArea/ChatArea.tsx

# Hard Do Not Touch（再次提醒）
@src/main/llm.ts
@src/main/deepagent/llm-adapter.ts
</context>

<interfaces>
<!-- usePlanPopupStore contract（PATTERNS.md §planPopupStore） -->

types：
- PlanStatus = 'closed' | 'generating' | 'reviewing' | 'modifying' | 'executing' | 'cap-reached'
- PlanPopupState 字段：
  - isOpen: boolean
  - status: PlanStatus
  - description: string  // /plan X 中 X 的 trim()
  - planContent: string  // LLM 流式输出累积
  - currentRequestId: string | null  // 当前 runLLMChat 请求 id（用于 stopChat）
  - iterationCount: number  // 0..20
  - modifyHistory: Array<{ opinion: string; planAfter: string }>
- actions：
  - open(description: string): void  // closed → generating，重置 planContent/iterationCount/history
  - close(): void  // any → closed，清 planContent / currentRequestId
  - appendChunk(chunk: string): void  // 流式 chunk 累积；首个非空 chunk 时把 generating → reviewing
  - startModify(opinion: string): Promise<void>  // 先 cancelCurrent → 再 sendMessage(`修改意见：${opinion}`, { planOnly: true })；iterationCount += 1；status = 'modifying'；若 newCount >= 20 → status = 'cap-reached' 之后再触发一次（保留可见执行入口）
  - execute(): Promise<void>  // sendMessage('立即执行', undefined)；status = 'executing'；then close()
  - cancelCurrent(): Promise<void>  // 若 currentRequestId 非空 → window.electronAPI.llm.stopChat(currentRequestId)

PlanPopup 组件（UI-SPEC.md Surface 3）：
- 用 @/components/ui/dialog 的 Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription
- DialogTitle: `📋 计划：${description}`（description 截断 60 字符 + 省略号）
- DialogDescription（top-right 计数器）：`第 ${iterationCount} 次迭代 · ${iterationCount}/20 轮`
- 内容区：status === 'generating' 时 `<Skeleton className="h-32 w-full" />`；否则 MarkdownRenderer 渲染 planContent
- 中部 banner：iterationCount >= 1 显示「💡 你已修改 ${iterationCount} 次。点击「立即执行」会关闭弹窗并把"立即执行"作为新消息发送给 agent。」
- 临界警告：iterationCount === 18 或 19 显示 `⚠️ 剩余 ${20 - iterationCount} 次修改机会。`；iterationCount === 20 显示 `已用尽 20 次修改。请点击「立即执行」或关闭弹窗。`
- 底部右下：两按钮
  - Button variant="secondary"（"修改计划"）→ 点击切换 inline modify 输入；status === 'cap-reached' 时 disabled
  - Button variant="default"（"立即执行"）→ 点击 execute()；始终 enabled（即使 cap-reached）
- inline modify input：Enter 提交（startModify(value)），Shift+Enter 换行，placeholder `输入修改意见，Enter 重新生成计划，Shift+Enter 换行`
- 关闭语义：Esc / 点 overlay → close()（不自动 execute；C3-03 已说明）
- 颜色 token：「立即执行」按钮 = accent (#7c3aed)（UI-SPEC.md accent reservation list）；「修改计划」= secondary outline；DialogContent 顶部 4px accent 横线（标识 plan 弹窗品牌）
- data-testid: "plan-popup"（外层）

planOnly tool gate 回归（C3-02 — Issue 3 修订）：
- **pre-execution probe**（Task 3 step 1 新增）：先 `grep -n "planOnly" src/main/deepagent/runtime.ts` + `grep -n "writeTools\\|filterTools\\|toolGate\\|setAllowedTools" src/main/deepagent/runtime.ts` 探明 Phase 7 是否真的实现 planOnly 工具拒绝
- **probe 结果 1（enforced）**：测试按现有 3 组 case 写：`planOnly=true 拒 Edit/Write/Bash`、`planOnly=true 允 Read/Grep/Glob`、`planOnly=false/undefined 全允`；测试 PASS 表示 C3-02 已落地
- **probe 结果 2（NOT enforced）**：把 3 组 case 改为 `it.todo` 并在 SUMMARY 标注「PLAN ONLY GATE GAP — Phase 7 未实现运行时拒绝，需 v1.2 补」
- 位置：src/main/deepagent/planOnly-tool-gate.test.ts（**只新建此测试文件，不修改 runtime.ts**）

SlashCommandPopup user-invocable 过滤（D-09）：
- 在 cmdk render 之前用 `commands.filter(c => c.frontmatter?.userInvocable !== false)` 排除
- frontmatter 未声明 → 视为 true（与 D-10 默认值对齐）

dispatcher PlanMode 修改：
- resolve() 中 `/plan` 分支返回追加 `popupOpen: true`
- dispatch() 中 `case 'PlanMode'` 分支：
  - 若 `plan.popupOpen !== false`：调 `usePlanPopupStore.getState().open(description)`；await sendMessage(projectId, plan.args, { planOnly: true })
  - 若 `plan.popupOpen === false`：保留 Phase 7 toast 行为（用于测试 escape hatch）
</interfaces>

<tasks>

<task type="auto">
<name>Task 1: usePlanPopupStore Zustand 状态机 + 单测</name>
<read_first>
- src/renderer/src/stores/themeStore.ts（极简 Zustand 模式）
- src/renderer/src/stores/llmStore.ts（actions + state 模式）
- src/renderer/src/stores/sessionStore.ts（cross-store sendMessage 契约 + setActiveSessionId 模式）
- src/renderer/src/stores/flowStore.test.ts（co-located store test 模板）
- src/shared/types.ts（确认 ChatRuntimeOverrides 含 planOnly + allowedTools）
- .planning/phases/08.2-slash-command-execution-principle-optimization/08.2-PATTERNS.md §"planPopupStore"
- .planning/phases/08.2-slash-command-execution-principle-optimization/08.2-UI-SPEC.md §"State Machines / plan modify loop"
- .planning/phases/08.2-slash-command-execution-principle-optimization/08.2-CONTEXT.md §"C-3" (C3-01..C3-05)
</read_first>
<action>
1. **新建 src/renderer/src/stores/planPopupStore.ts**：
   - 导出 type `PlanStatus`、interface `PlanPopupState`
   - 用 `create<PlanPopupState>((set, get) => ({...}))` 创建 store
   - 初始 state：isOpen=false, status='closed', description='', planContent='', currentRequestId=null, iterationCount=0, modifyHistory=[]
   - 实现 6 个 action（语义见 <interfaces>）：
     - **open(description)**：set 全部初始字段 + isOpen=true + status='generating' + description（已 trim/截断）
     - **close()**：把 currentRequestId 临时存起，先调 cancelCurrent，再 set 全部回初始 + isOpen=false
     - **appendChunk(chunk)**：把 chunk append 到 planContent；若 status === 'generating' 且 planContent.length === 0 之前 → set status='reviewing'
     - **startModify(opinion)**：
       - 先 await get().cancelCurrent()
       - 若 get().iterationCount >= 20 → set status='cap-reached'，return（拒绝再触发，C3-04 P7 兜底）
       - set status='modifying', planContent=''（清空让流式重写）, iterationCount = get().iterationCount + 1
       - 把 history 追加 { opinion, planAfter: '' } 占位（流式回来后由 appendChunk 累积）
       - 调 `useSessionStore.getState().sendMessage(projectId, '修改意见：' + opinion, { planOnly: true })`（projectId 从 useProjectStore.getState().currentProjectId 拿）
       - 注：currentRequestId 的实际写入由 sessionStore.sendMessage 之后的 runLLMChat 触发，本 plan 不强求拿到；可读 useSessionStore.activeRequestId 或类似入口；若该入口不存在则在 store 中留 placeholder 字段、SUMMARY 中注明 P11 风险后续解决
     - **execute()**：调 `useSessionStore.getState().sendMessage(projectId, '立即执行', undefined)`（**不带 planOnly**，C3-03）；set status='executing'；then await get().close()
     - **cancelCurrent()**：若 get().currentRequestId 非空 → `await window.electronAPI.llm.stopChat(get().currentRequestId)`；不拋错（catch 内 console.warn）
   - 写明所有 set 使用浅拷贝（无 Map/Set 嵌套，直接 set 对象即可）
   - 顶部 `import { create } from 'zustand'`；不引 React（store 是纯 Zustand）

2. **新建 src/renderer/src/stores/planPopupStore.test.ts**：
   - mock `useSessionStore`、`useProjectStore`、`window.electronAPI.llm.stopChat`（参考 dispatcher.test.ts 第 5-23 行 mock 模式）
   - 6 个 case：
     - "open() transitions closed → generating + resets iterationCount/planContent/history"
     - "appendChunk() transitions generating → reviewing on first non-empty chunk"
     - "startModify() increments iterationCount, calls cancelCurrent then sendMessage with planOnly=true, status → modifying"
     - "startModify() at iterationCount === 20: rejects (no sendMessage call), sets status='cap-reached'"
     - "execute() calls sendMessage('立即执行', undefined) without planOnly, status → executing, then close() reached"
     - "cancelCurrent() calls window.electronAPI.llm.stopChat with currentRequestId; null id is no-op"
   - 测试 vitest reset：每个 it 之前 `usePlanPopupStore.setState({ isOpen: false, status: 'closed', planContent: '', iterationCount: 0, modifyHistory: [], currentRequestId: null, description: '' })` 重置
</action>
<verify>
<automated>npx vitest run src/renderer/src/stores/planPopupStore.test.ts 2>&1 | tail -30</automated>
<automated>npx tsc --noEmit 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria>
- 文件存在：planPopupStore.ts + planPopupStore.test.ts
- 导出 usePlanPopupStore + PlanStatus + PlanPopupState：`grep -cE "^export (const|type|interface)" src/renderer/src/stores/planPopupStore.ts` ≥ 3
- 6 个状态值：`grep -cE "'closed'|'generating'|'reviewing'|'modifying'|'executing'|'cap-reached'" src/renderer/src/stores/planPopupStore.ts` ≥ 6
- 20 turn cap：`grep -c "20" src/renderer/src/stores/planPopupStore.ts` ≥ 2（state init 上限 + startModify 检查）
- execute 不带 planOnly：`grep -E "sendMessage\\(.*'立即执行'.*undefined|sendMessage\\(.*'立即执行'\\)" src/renderer/src/stores/planPopupStore.ts` ≥ 1
- startModify 带 planOnly=true：`grep -E "planOnly:\\s*true" src/renderer/src/stores/planPopupStore.ts` ≥ 1
- cancelCurrent 调 stopChat：`grep -c "stopChat" src/renderer/src/stores/planPopupStore.ts` ≥ 1
- 测试 6 个 case：`grep -c "  it(" src/renderer/src/stores/planPopupStore.test.ts` ≥ 6
- `npx vitest run src/renderer/src/stores/planPopupStore.test.ts` exit 0
- `npx tsc --noEmit` 0 errors
</acceptance_criteria>
<decision_refs>C3-01, C3-03, C3-04, C3-05, PITFALL P7 (20-turn cap), PITFALL P11 (orphan runs)</decision_refs>
</task>

<task type="auto">
<name>Task 2: PlanPopup Radix Dialog 组件 + RTL 测试 + mount + dispatcher PlanMode 接入</name>
<read_first>
- src/renderer/src/components/ui/dialog.tsx（Radix Dialog 包装）
- src/renderer/src/components/ui/button.tsx（variants: default/secondary/ghost/destructive）
- src/renderer/src/components/ui/skeleton.tsx
- src/renderer/src/components/ChatArea/MarkdownRenderer.tsx（API：text/markdown prop）
- src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx（RTL 测试模板）
- src/renderer/src/stores/planPopupStore.ts（Task 1 产物）
- src/renderer/src/App.tsx（顶层 mount 位置）
- src/renderer/src/lib/commands/dispatcher.ts（PlanMode 现有 137-147 行）
- src/renderer/src/lib/commands/dispatcher.test.ts
- src/shared/types.ts（PlanMode.popupOpen 已在 P1 加入）
- .planning/phases/08.2-slash-command-execution-principle-optimization/08.2-UI-SPEC.md §"Surface 3" §"Copywriting Contract" §"Interaction Contract"
- .planning/phases/08.2-slash-command-execution-principle-optimization/08.2-PATTERNS.md §"PlanPopup.tsx"
</read_first>
<action>
1. **新建目录 + 组件 src/renderer/src/components/PlanPopup/PlanPopup.tsx**：
   - import Dialog / DialogContent / DialogHeader / DialogTitle / DialogDescription from `@/components/ui/dialog`
   - import Button from `@/components/ui/button`、Skeleton from `@/components/ui/skeleton`
   - import MarkdownRenderer from `@/components/ChatArea/MarkdownRenderer`
   - import usePlanPopupStore from `@/stores/planPopupStore`
   - 函数组件 PlanPopup() 默认导出 + named export
   - selector 订阅：isOpen / status / planContent / iterationCount / description / close / startModify / execute（一字一选，避免重渲染）
   - 内置 useState：`const [modifyOpen, setModifyOpen] = useState(false)` + `const [modifyInput, setModifyInput] = useState('')`
   - 顶级 Dialog open={isOpen} onOpenChange={(o) => !o && close()}
   - DialogContent className 包含 max-w-2xl + max-h-[80vh] + 顶部 4px accent 边线（border-t-4 border-accent）+ data-testid="plan-popup"
   - DialogHeader：
     - DialogTitle: 字面量 "📋 计划：" + description.slice(0, 60) + (description.length > 60 ? '…' : '')
     - DialogDescription: 字面量 "第 " + iterationCount + " 次迭代 · " + iterationCount + "/20 轮"（font-mono 显示数字）
   - 内容区 div className="max-h-[60vh] overflow-y-auto p-4"：
     - status === 'generating' 时渲染 Skeleton h-32 w-full（"正在生成计划…" copy 用 sr-only）
     - 否则 MarkdownRenderer text={planContent}
   - 中部 banner（仅 iterationCount >= 1 显示）：阻塞文本 "💡 你已修改 {iterationCount} 次。点击「立即执行」会关闭弹窗并把\"立即执行\"作为新消息发送给 agent。"
   - 临界警告：
     - iterationCount === 18 || 19 → 黄字 "⚠️ 剩余 " + (20 - iterationCount) + " 次修改机会。"
     - iterationCount === 20 → 红字 "已用尽 20 次修改。请点击「立即执行」或关闭弹窗。"
   - inline modify 区（仅 modifyOpen === true 时显示）：textarea + 提交按钮；onKeyDown 内拦截 Enter（无 Shift）→ 调 startModify(modifyInput.trim())、setModifyInput('')、setModifyOpen(false)；Shift+Enter 换行（默认行为放行）
   - 底部 div className="flex justify-end gap-2 pt-md"：
     - Button variant="secondary" 文本「修改计划」disabled={status === 'cap-reached'} onClick={() => setModifyOpen(true)}
     - Button variant="default" 文本「立即执行」onClick={() => void execute()}（始终 enabled）

2. **新建 src/renderer/src/components/PlanPopup/PlanPopup.test.tsx**（参考 SlashCommandPopup.test.tsx mock 模式）：
   - mock `@/stores/planPopupStore`（vi.mock + setState 重置）
   - 6 个 RTL case：
     - "renders with data-testid 'plan-popup' when isOpen=true"
     - "shows Skeleton when status='generating'"
     - "renders plan content via MarkdownRenderer when status='reviewing'"
     - "renders 「修改计划」 + 「立即执行」 buttons"
     - "「立即执行」 onClick calls store.execute()"
     - "at iterationCount===20: 「修改计划」 is disabled, 「立即执行」 is enabled"
   - 用 @testing-library/react render + screen.getByTestId / getByText / fireEvent.click

3. **App.tsx 顶层 mount**：在 App.tsx 现有 router/layout outermost 区域追加 `<PlanPopup />` 一次（与其它 Provider 平级）。若 App.tsx 是多 layout，挂在能覆盖 ChatArea 的最近共同祖先即可。

4. **dispatcher.ts PlanMode 修改**：
   - resolve() 中 `if (match.name === 'plan')` 分支返回值追加 `popupOpen: true` 字段：`return { kind: 'PlanMode', command: match, args, popupOpen: true };`
   - dispatch() `case 'PlanMode'`：
     - 顶部 `const description = plan.args.trim() || '(无描述)'`
     - 若 plan.popupOpen !== false：`const { usePlanPopupStore } = await import('@/stores/planPopupStore')` 或顶部静态 import + 调 `usePlanPopupStore.getState().open(description)`
     - 若 plan.popupOpen === false：保留原 toast.info('[plan] 进入 plan 模式：...')（escape hatch 给测试用）
     - 不论哪条分支，下一句仍是 `await sendMessage(projectId, plan.args, { planOnly: true })`
   - 不要静态 import `usePlanPopupStore` 在文件顶部如果存在循环依赖；若 import path 干净则首选顶部静态 import

5. **dispatcher.test.ts 扩展**：
   - mock `@/stores/planPopupStore`：`vi.mock('@/stores/planPopupStore', () => ({ usePlanPopupStore: { getState: () => ({ open: vi.fn() }) } }))`（或具名 mock store.open）
   - 新增 case "PlanMode with popupOpen=true: calls usePlanPopupStore.open(description) + sendMessage(planOnly:true)"
   - 新增 case "PlanMode with popupOpen=false: falls back to toast + sendMessage(planOnly:true)"
   - 调整既有 Phase 7 PlanMode 测试断言以匹配新 dispatcher 输出（toast vs store.open 分支）
</action>
<verify>
<automated>npx vitest run src/renderer/src/components/PlanPopup/PlanPopup.test.tsx 2>&1 | tail -30</automated>
<automated>npx vitest run src/renderer/src/lib/commands/dispatcher.test.ts 2>&1 | tail -30</automated>
<automated>npx tsc --noEmit 2>&1 | tail -10</automated>
<automated>npx vitest run src/main/deepagent/llm-adapter.test.ts 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria>
- 文件存在：PlanPopup.tsx + PlanPopup.test.tsx
- 组件使用 Radix Dialog wrapper：`grep -c "from '@/components/ui/dialog'" src/renderer/src/components/PlanPopup/PlanPopup.tsx` ≥ 1
- 组件订阅 store：`grep -c "usePlanPopupStore(" src/renderer/src/components/PlanPopup/PlanPopup.tsx` ≥ 4（4+ selector subscriptions）
- 关键 copy 存在：`grep -cE "📋 计划|立即执行|修改计划|第.*次迭代|/20 轮" src/renderer/src/components/PlanPopup/PlanPopup.tsx` ≥ 4
- 20-turn 警告：`grep -cE "已用尽 20 次|剩余.*次修改" src/renderer/src/components/PlanPopup/PlanPopup.tsx` ≥ 2
- data-testid 存在：`grep -c 'data-testid="plan-popup"' src/renderer/src/components/PlanPopup/PlanPopup.tsx` ≥ 1
- App.tsx mount：`grep -c "PlanPopup" src/renderer/src/App.tsx` ≥ 2（import + render）
- dispatcher.ts PlanMode 调 store.open：`grep -c "usePlanPopupStore" src/renderer/src/lib/commands/dispatcher.ts` ≥ 1
- dispatcher.ts resolve 设 popupOpen: true：`grep -cE "popupOpen:\\s*true" src/renderer/src/lib/commands/dispatcher.ts` ≥ 1
- 测试 6 个 RTL case：`grep -c "  it(" src/renderer/src/components/PlanPopup/PlanPopup.test.tsx` ≥ 6
- dispatcher.test.ts 含 PlanMode popupOpen 两组：`grep -cE "popupOpen.*true|popupOpen.*false|usePlanPopupStore" src/renderer/src/lib/commands/dispatcher.test.ts` ≥ 3
- 所有 verify 命令 exit 0
- SLASH-REGRESSION green
</acceptance_criteria>
<decision_refs>C3-01, C3-03, C3-05, F-01 (/plan surface = accent), UI-SPEC §Surface 3</decision_refs>
</task>

<task type="auto">
<name>Task 3: SlashCommandPopup user-invocable 过滤 + planOnly tool gate 回归测试（pre-execution probe） + 「立即执行」集成测试</name>
<read_first>
- src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx（cmdk 渲染流程，过滤插入点）
- src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx（已有 mock + 测试格式）
- src/main/deepagent/runtime.ts（**只读不改**：Phase 7 D-13 计划实现 planOnly 工具拒绝点；Issue 3 probe 决定实际状态 — 严禁修改本文件）
- src/main/deepagent/runtime.test.ts（运行时测试模板 + mock pattern）
- src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx（既有 handleSend 集成测试）
- src/renderer/src/components/ChatArea/ChatArea.tsx:628-654（5-line slash sniff，本任务不改）
- src/main/deepagent/llm-adapter.test.ts（SLASH-REGRESSION 守门）
- .planning/phases/08.2-slash-command-execution-principle-optimization/08.2-CONTEXT.md §"D-09"
- .planning/phases/08.2-slash-command-execution-principle-optimization/08.2-VALIDATION.md §"T-08.2-T12, T-08.2-T13"
</read_first>
<action>
1. **SlashCommandPopup user-invocable 过滤（D-09）**：
   - 读 src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx 找到 cmdk render 之前 commands 数组的来源
   - 在 render 之前插入一个 useMemo 或纯过滤：`const visibleCommands = commands.filter(c => c.frontmatter?.userInvocable !== false)`
   - 后续的 cmdk loop 用 visibleCommands 代替原 commands
   - **保持**冲突 badge / conflict warning / source 排序逻辑不变（只过滤，不重排）
   - 在 SlashCommandPopup.test.tsx 增加 1 个 case："filters out commands with frontmatter.userInvocable === false"：mock commands 列表含 1 个 userInvocable=false + 1 个 userInvocable=true + 1 个无 frontmatter，断言 cmdk render 只显示 2 个（无 frontmatter 视为 true）

2. **planOnly tool gate 回归测试新建（C3-02 — Issue 3 修订：pre-execution probe）**：
   - **step 1（probe）**：先在 src/main/deepagent/runtime.ts 中用以下命令确认实现状态：
     ```bash
     grep -n "planOnly" src/main/deepagent/runtime.ts
     grep -nE "writeTools|filterTools|toolGate|setAllowedTools|isReadOnly" src/main/deepagent/runtime.ts
     ```
   - **probe 解读**：
     - 若 `planOnly` 出现 + 含写工具过滤逻辑（`writeTools.filter` 或 `toolGate(planOnly, ...)` 或类似）→ **enforced**（C3-02 已落地）
     - 若 `planOnly` 出现但**只是透传**（如 `if (overrides.planOnly) {...}` 但未过滤工具）→ **NOT enforced**
     - 若 `planOnly` 完全不出现 → **NOT enforced**
   - **probe 结果 1（enforced）**：写 src/main/deepagent/planOnly-tool-gate.test.ts 含 3 组 `it` case：
     - "planOnly=true: write tools (Edit, Write, Bash) are blocked / not in tool set"
     - "planOnly=true: read tools (Read, Grep, Glob, list_directory) are allowed / in tool set"
     - "planOnly=false (or undefined): all tools are allowed"
     - 用 vitest + runtime.ts 的导出 / 函数级 unit test（参考 runtime.test.ts mock 模式）
     - 若是工具池过滤：断言过滤后的工具数组不含写工具；若是运行时拒绝异常：断言调写工具时抛特定 error
   - **probe 结果 2（NOT enforced）**：把 3 组 `it` 改写为 `it.todo` + 在 SUMMARY 中标注「PLAN ONLY GATE GAP — Phase 7 runtime.ts 仅透传 planOnly flag，未实现写类工具拒绝；本测试为占位。需 v1.2+ 补运行时拒绝（建议修改点：runtime.ts 新增 `if (overrides.planOnly) toolPool = toolPool.filter(t => !WRITE_TOOLS.has(t.name))`）」；同时在 phase orchestrator 决策项加 follow-up
   - **不修改** runtime.ts 逻辑（即使 probe 显示 NOT enforced；本 plan 不在本 plan 内补实现；runtime.ts 修改是独立 plan 的事）

3. **「立即执行」集成测试（C3-03）**：
   - 在 ChatArea.handleSend.test.tsx 追加 case："/plan dispatch → PlanPopup.execute() → sends '立即执行' as user message WITHOUT planOnly flag"
   - mock 流程：
     - mock dispatch（或直接调 store.execute()）
     - 断言 sendMessage 被调一次，第 2 参数 === '立即执行'，第 3 参数（overrides）为 undefined 或不含 planOnly: true
   - 此 case 验证 C3-03 的关键契约

4. **SLASH-REGRESSION 守门**：每个步骤后跑 `npx vitest run src/main/deepagent/llm-adapter.test.ts` + `git diff --stat src/main/llm.ts src/main/deepagent/llm-adapter.ts`（必须为空）。
</action>
<verify>
<automated>npx vitest run src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx 2>&1 | tail -20</automated>
<automated>npx vitest run src/main/deepagent/planOnly-tool-gate.test.ts 2>&1 | tail -20</automated>
<automated>npx vitest run src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx 2>&1 | tail -20</automated>
<automated>npx vitest run src/main/deepagent/llm-adapter.test.ts 2>&1 | tail -10</automated>
<automated>npx tsc --noEmit 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria>
- **Issue 3+4 修复守门**（planOnly runtime probe）：
  - **Pre-execution probe**：`grep -n "planOnly" src/main/deepagent/runtime.ts` 返回 ≥ 1 个匹配；若完全无匹配 → probe 判定 NOT enforced，测试改 it.todo
  - 若 probe 显示 enforced：测试文件含 `Edit|Write|Bash` 拒 + `Read|Grep|Glob` 允 3 组 case（`grep -cE "Edit|Write|Bash" src/main/deepagent/planOnly-tool-gate.test.ts` ≥ 3 且 `grep -cE "Read|Grep|Glob" src/main/deepagent/planOnly-tool-gate.test.ts` ≥ 3）
  - 若 probe 显示 NOT enforced：测试文件用 `it.todo` 占位（`grep -c "it\\.todo" src/main/deepagent/planOnly-tool-gate.test.ts` ≥ 3）+ SUMMARY 必须含「PLAN ONLY GATE GAP」标题段 + 在 phase orchestrator follow-up 列表加注
- SlashCommandPopup.tsx 过滤：`grep -cE "userInvocable !== false|frontmatter\\?\\.userInvocable" src/renderer/src/components/SlashCommand/SlashCommandPopup.tsx` ≥ 1
- SlashCommandPopup.test.tsx 新增 user-invocable case：`grep -c "userInvocable" src/renderer/src/components/SlashCommand/SlashCommandPopup.test.tsx` ≥ 2
- planOnly-tool-gate.test.ts 存在 + ≥ 3 个 case（it 或 it.todo）：`grep -cE "  it\\(|  it\\.todo" src/main/deepagent/planOnly-tool-gate.test.ts` ≥ 3
- ChatArea.handleSend.test.tsx 新增 「立即执行」 case：`grep -c "立即执行" src/renderer/src/components/ChatArea/ChatArea.handleSend.test.tsx` ≥ 1
- 所有 verify 命令 exit 0
- SLASH-REGRESSION green
- `git diff --stat src/main/llm.ts src/main/deepagent/llm-adapter.ts` 为空
- **`git diff --stat src/main/deepagent/runtime.ts` 为空**（**Issue 4 修复**：runtime.ts 从 files_modified 移除；plan 在 runtime.ts 上只读；如 probe 需实现修复，单独开新 plan）
- 若 planOnly 拒绝行为未实现，SUMMARY 中必须有「PLAN ONLY GATE GAP」标题段
</acceptance_criteria>
<decision_refs>D-09 (user-invocable), C3-02, C3-03, Issue 3 (pre-execution probe), Issue 4 (runtime.ts removed from files_modified)</decision_refs>
</task>

</tasks>

<verification>
**Phase-level gate after P2 complete:**

1. `npm test` exit 0
2. SLASH-REGRESSION green: `npx vitest run src/main/deepagent/llm-adapter.test.ts`
3. 手工 e2e：
   - 启动 app
   - 输入 `/plan 重构 ChatArea，把消息渲染移出 React state`
   - 期望：从顶部滑下 Radix Dialog，里头出现 LLM 生成的 markdown 计划；右上 "第 0 次迭代 · 0/20 轮"
   - 点「修改计划」→ 弹出 inline 输入；输入「先列出影响范围」→ Enter
   - 期望：计数器变 "第 1 次迭代 · 1/20 轮"，内容区清空 + Skeleton 重新出现，LLM 重新输出新计划
   - 点「立即执行」→ 弹窗关闭，对话流出现一条 user message "立即执行"，agent 进入执行（可见工具调用）
4. user-invocable e2e：
   - 在 `.cdf/commands/test.md` 写 frontmatter `user-invocable: false` + `name: hidden-cmd`
   - 触发 `/` popup
   - 期望：hidden-cmd 不出现在列表
5. 20-turn cap：在弹窗里连点 18 次「修改计划」→ 看到剩余警告；到 20 次时「修改计划」按钮变灰
6. planOnly tool gate 探测（手工或测试）：
   - 启动 app + Anthropic 模型
   - 输入 `/plan 修改 src/main/llm.ts 删一行`
   - 期望：plan 弹窗出现 + LLM 试图调 edit/write 工具**失败**（如 probe enforced）或**成功**（如 probe NOT enforced；若如此，SUMMARY 标注 GAP）
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer popup → sendMessage IPC | 用户在 popup 内输入的 modify opinion 直接进 LLM user message（与正常 chat 等价信任级别） |
| Plan UI → store → planOnly flag → runtime | planOnly=true 的工具白名单决策在 runtime 层（可能未实现 — Issue 3 probe 决定）；UI 层不强行执行权限 |
| frontmatter.userInvocable / allowed-tools → UI / dispatcher | 用户写入 .md 文件可任意声明 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-T11 | Tampering / DoS | Plan modify loop | mitigate | 20-turn cap 在 planPopupStore.startModify 强制；cap-reached 状态显式 UI 反馈；execute 始终可用（escape hatch） (V11) |
| T-08.2-T12 | Elevation | planOnly tool gate（runtime） | mitigate | **Pre-execution probe**（Issue 3 修订）：探明 runtime 是否实现写工具拒绝；enforced → 3 组 `it` case 锁住行为；NOT enforced → `it.todo` + SUMMARY GAP 标注 + v1.2 follow-up；本 plan 不修改 runtime.ts（V4） |
| T-08.2-T13 | Information Disclosure | execute() 模拟 user message | accept | 「立即执行」字符串是固定 magic string；用户能在 UI 看到这条 user message，无误导风险 |
| T-08.2-T8 | Elevation | user-invocable: false 仅过滤 UI | accept | 设计如此（D-10）；用户可手敲 /hidden-cmd 仍触发；不视为隐私边界 |
| T-08.2-T9 | Elevation | allowed-tools 运行时白名单 | mitigate | P1 已在 dispatcher 把 frontmatter.allowedTools 写入 overrides；本 plan 增 ChatArea 集成测试验证 overrides 透传；runLLMChat 已是 pass-through（V4） |
| T-08.2-T11b | DoS | orphan LLM runs (P11 pitfall) | mitigate | startModify 先调 cancelCurrent → stopChat(currentRequestId)；若 currentRequestId 因 store-runtime contract 缺失，SUMMARY 标注 fault；execute() / close() 同样调 cancelCurrent |
| T-08.2-T10 | Tampering | patch-package regression | mitigate | 每任务 SLASH-REGRESSION + diff llm.ts/llm-adapter.ts/runtime.ts 守门（runtime.ts 已从 files_modified 移除 — Issue 4 修复） |
</threat_model>

<success_criteria>
- [ ] 全部 3 个 task 的 acceptance_criteria 满足
- [ ] `npm test` green
- [ ] `npx tsc --noEmit` 0 errors
- [ ] SLASH-REGRESSION green
- [ ] 手工 e2e：/plan 弹窗能开、能改、能执行；20-turn cap 触发；user-invocable 过滤生效
- [ ] git diff --stat 显示 llm.ts / llm-adapter.ts / runtime.ts 均未被改动（**Issue 4 修复确认**）
- [ ] SUMMARY 标注 planOnly 拒绝行为是否在 Phase 7 真正实现（probe 结果 + 若是 GAP，v1.2 follow-up）
- [ ] SUMMARY 标注 PLAN ONLY GATE GAP（如 probe 显示 NOT enforced）
</success_criteria>

<output>
完成后创建 `.planning/phases/08.2-slash-command-execution-principle-optimization/08.2-02-SUMMARY.md`。
SUMMARY 须含：planPopupStore 状态机图（mermaid 或 ASCII）、PlanPopup 截图占位（dev build）、planOnly 拒绝测试结果（含 GAP 标注 — Issue 3）、SLASH-REGRESSION 输出 tail。
</output>
