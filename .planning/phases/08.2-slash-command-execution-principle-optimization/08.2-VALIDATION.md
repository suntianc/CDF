---
phase: 08.2
slug: slash-command-execution-principle-optimization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-05
---

# Phase 08.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> 5 workstreams across 4 plans: P1 (.md body + frontmatter foundation) → P2 (/plan popup) → P3 (/goal judge) → P4 (/context modal).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `vitest@4.1.8` + `@testing-library/react@16.0.0` |
| **Config file** | `vitest.config.ts` (existing, jsdom + lobehub inline) |
| **Quick run command** | `npx vitest run <pattern> 2>&1 \| tail -30` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~10s renderer unit + ~30s chokidar/IPC integration |
| **Patch guard** | `src/main/deepagent/llm-adapter.test.ts` (SLASH-REGRESSION, Phase 7 D-16) — must stay green |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run <relevant-test-file>`
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite must be green + SLASH-REGRESSION green + manual /goal /context /plan checkpoints
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> No formal REQ-IDs in REQUIREMENTS.md for 08.2. Mapped to D-01..D-30 from CONTEXT.md.

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.2-01-01 | 01 | 1 | D-05 | T-08.2-T1 | `SlashCommand.bodyPath` typed in `src/shared/types.ts` | type-only | `npx tsc --noEmit` | ❌ Wave 0 | ⬜ pending |
| 08.2-01-02 | 01 | 1 | D-06 | T-08.2-T2 | `commands:readBody` validates path under `~/.cdf/commands/` OR `<projectPath>/.cdf/commands/` (V5.1.3 path traversal) | unit | `npx vitest run ipc-handlers.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-01-03 | 01 | 1 | D-01 | T-08.2-T3 | dispatcher reads .md body on Enter, not on registry build | unit | `npx vitest run dispatcher.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-01-04 | 01 | 1 | D-02 | T-08.2-T4 | `$ARGUMENTS` / `$0`/`$1`/`$2` / `$name` substitution with frontmatter `arguments: [env, flags]` declaration | unit | `npx vitest run argSubstitution.test.ts` | ❌ Wave 0 | ⬜ pending |
| 08.2-01-05 | 01 | 1 | D-03 | T-08.2-T5 | body replaces user message; command name does NOT appear in user message | integration | `npx vitest run dispatcher.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-01-06 | 01 | 1 | D-04 | T-08.2-T6 | chokidar unlink removes from registry (debounce coalesce) | integration | `npx vitest run chokidar-watcher.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-01-07 | 01 | 1 | D-08 | T-08.2-T7 | `yaml@2.9.0` parses frontmatter (4 fields + arguments) | unit | `npx vitest run project-commands.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-01-08 | 01 | 1 | D-09 | T-08.2-T8 | `disable-model-invocation: true` removes from LLM context; `user-invocable: false` filters popup; `allowed-tools` passed to `payload.overrides.allowedTools`; `when_to_use` appends to description | unit | `npx vitest run command-registry.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-01-09 | 01 | 1 | D-09 (allowed-tools) | T-08.2-T9 | D-09 allowed-tools runtime: NOT modify `runLLMChat` body; only extend `ChatRuntimeOverrides` type; verify via mock that `payload.overrides.allowedTools` is passed through | integration | `npx vitest run llm.test.ts` | ✅ (extend; **RISK**) | ⬜ pending |
| 08.2-01-10 | 01 | 1 | ALL | T-08.2-T10 | SLASH-REGRESSION patch-package test still passes (6-hunk patch-package) | regression | `npx vitest run src/main/deepagent/llm-adapter.test.ts` | ✅ | ⬜ pending |
| 08.2-02-01 | 02 | 1 | C3-05 | T-08.2-T11 | PlanPopup renders plan content + "修改 / 立即执行" buttons | unit + RTL | `npx vitest run PlanPopup.test.tsx` | ❌ Wave 0 | ⬜ pending |
| 08.2-02-02 | 02 | 1 | C3-02 | T-08.2-T12 | planOnly=true: write tools (Edit/Write/Bash 写操作) blocked, read tools (Read/Grep/Glob/list files) allowed | unit | `npx vitest run planOnly-tool-gate.test.ts` | ❌ Wave 0 | ⬜ pending |
| 08.2-02-03 | 02 | 1 | C3-03 | T-08.2-T13 | "立即执行" 模拟 user message → next sendMessage without planOnly flag | integration | `npx vitest run ChatArea.test.tsx` | ✅ (extend) | ⬜ pending |
| 08.2-02-04 | 02 | 1 | C3-04 | T-08.2-T14 | modify loop: 修改意见 → planOnly=true re-send; cap at 20 turns (P7 DoS mitigation) | integration | `npx vitest run planPopupStore.test.ts` | ❌ Wave 0 | ⬜ pending |
| 08.2-03-01 | 03 | 2 | C1-05 | T-08.2-T15 | `GoalLoop` dispatch kind recognized; `useGoalJudge` hook calls `runLLMChat` with judge prompt | unit | `npx vitest run dispatcher.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-03-02 | 03 | 2 | C1-05 | T-08.2-T16 | judge returns `{ satisfied: bool, reason: string }` JSON parse; failure logged + bubble shows "judge 失败" (don't trust LLM blindly — V7) | unit | `npx vitest run useGoalJudge.test.ts` | ❌ Wave 0 | ⬜ pending |
| 08.2-03-03 | 03 | 2 | C1-02 | T-08.2-T17 | judge reason loop: reason becomes next user message; first message includes goal; cap at 20 turns | integration | `npx vitest run useGoalJudge.test.ts` | ❌ Wave 0 | ⬜ pending |
| 08.2-03-04 | 03 | 2 | C1-04 | T-08.2-T18 | Goal status bubble: `◎ 目标：X 状态：工作中 耗时：1m 23s`; `✅ 达成：X` on done | unit + RTL | `npx vitest run MessageItem.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-03-05 | 03 | 2 | C1-03 | T-08.2-T19 | judge triggered only on main agent "做完了" signal, not per tool call | integration | `npx vitest run useGoalJudge.test.ts` | ❌ Wave 0 | ⬜ pending |
| 08.2-04-01 | 04 | 2 | C2-01 | T-08.2-T20 | context aggregator returns: model name, total (X/Y + %), category breakdown, Free space, Autocompact buffer (15% of context_limit, CDF 85% threshold), per-MCP-tool detail | integration | `npx vitest run context-aggregator.test.ts` | ✅ (extend) | ⬜ pending |
| 08.2-04-02 | 04 | 2 | C2-02 | T-08.2-T21 | ContextModal renders with progress bar + breakdown rows + MCP tool detail rows | unit + RTL | `npx vitest run ContextModal.test.tsx` | ❌ Wave 0 | ⬜ pending |
| 08.2-04-03 | 04 | 2 | C2-04 | T-08.2-T22 | 常驻按钮 📊 触发同一 modal (slash + button dual entry) | unit + RTL | `npx vitest run ChatArea.test.tsx` | ✅ (extend) | ⬜ pending |
| 08.2-04-04 | 04 | 2 | C2-03 | T-08.2-T23 | `/context` 不注入 LLM 上下文（LLM 不知道） — verify via mock that no system message is added | unit | `npx vitest run dispatcher.test.ts` | ✅ (extend) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test files that must exist before Plan 01 starts. Without these, the per-task tests above cannot run.

- [ ] `src/shared/types.ts` — extend `SlashCommand` with `bodyPath: string` + `frontmatter: ParsedFrontmatter`; extend `CommandDispatchAction` with `GoalLoop` kind; extend `PlanMode` with `popupOpen: true` flag
- [ ] `src/main/ipc-handlers.ts` — add `commands:readBody(bodyPath)` IPC handler with path-traversal guard
- [ ] `src/renderer/src/lib/commands/argSubstitution.test.ts` — D-02 unit cases ($ARGUMENTS / $0 / $name + arguments declaration)
- [ ] `src/renderer/src/lib/commands/argSubstitution.ts` — D-02 pure string-replace (no eval — V10)
- [ ] `src/renderer/src/stores/planPopupStore.ts` + `planPopupStore.test.ts` — C3-04 modify loop state
- [ ] `src/renderer/src/hooks/useGoalJudge.ts` + `useGoalJudge.test.ts` — C1-05 judge loop (mock runLLMChat; **do NOT touch llm.ts**)
- [ ] `src/renderer/src/components/PlanPopup/PlanPopup.tsx` + `PlanPopup.test.tsx` — C3-05 Radix Dialog
- [ ] `src/renderer/src/components/ContextModal/ContextModal.tsx` + `ContextModal.test.tsx` — C2-02 Radix Dialog
- [ ] `src/main/deepagent/context-aggregator.ts` + `context-aggregator.test.ts` — C2-01 aggregator
- [ ] `package.json` — add `yaml@2.9.0` (verified via `slopcheck install yaml --json` at plan time)

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| `/goal` judge 自然语言循环 (e2e) | C1-02 | judge LLM 行为需要真实跑,无法 mock 完整对话 | dev build → `/goal 修好 login 流程` → 看到 bubble 实时更新 → 看 LLM 真做完后 judge 判 yes |
| `/context` modal 实际渲染 + 数字真实 | C2-01 | context breakdown 数字需要真实 LLM session | dev build → 长对话到 50% tokens → `/context` → 看 Free space 数字真实 |
| `/plan` 弹窗 + modify loop 真实操作 | C3-04 | planOnly flag 需 LLM 真实跑 + 工具分级 UI 反馈 | dev build → `/plan 重构 X` → 看到 plan → 写修改意见 → 看 plan 更新 → 点"立即执行" |
| `disable-model-invocation` 真实 LLM 上下文过滤 | D-09 | LLM 上下文需要真实 LLM 调用 | dev build → 建一个 `disable-model-invocation: true` 的 skill → 问 LLM "你有哪些 skill" → LLM 不应提到该 skill |
| `allowed-tools` 真实工具拒绝 | D-09 | 工具拒绝需 LLM 真实跑 | dev build → 建一个 `allowed-tools: ['Read']` 的 skill → LLM 尝试 Edit 应被拒 |
| chokidar 跨平台行为 | D-04 | macOS fsevents / Windows ReadDirectoryChangesW 不可在 jsdom 模拟 | CI matrix run |

---

## Patch Guard Detail

The 6-hunk patch-package at `patches/@langchain+anthropic+1.4.0.patch` is **load-bearing** for v1.1 SLASH-REGRESSION. The following rules MUST hold for 08.2:

1. **NEVER modify `src/main/llm.ts:306-425`** (`runLLMChat` body — Hard Do Not Touch per CONTEXT.md)
2. **NEVER modify `src/main/deepagent/llm-adapter.ts`** (patch-package consumer)
3. The only LLM extension seam is `payload.overrides.planOnly` (existing) + `payload.overrides.allowedTools` (new, type-only extension of `ChatRuntimeOverrides`)
4. After every Plan 01 task touching LLM-adjacent code: `npx vitest run src/main/deepagent/llm-adapter.test.ts` must stay green
5. If SLASH-REGRESSION test fails: STOP, revert, escalate. The patch-package cannot be regenerated easily.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (4 new test files + 1 new component + 1 new hook + 1 new aggregator + 1 IPC handler)
- [ ] No watch-mode flags (`--watch` forbidden in CI)
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter
- [ ] SLASH-REGRESSION test green at every Plan 01 commit
- [ ] Path-traversal guard in `commands:readBody` verified
- [ ] Modify loop 20-turn cap verified
- [ ] Judge JSON parse failure → bubble error path verified

**Approval:** pending
