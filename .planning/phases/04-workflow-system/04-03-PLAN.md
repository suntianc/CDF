---
wave: 2
depends_on: [04-01]
files_modified:
  - src/renderer/src/components/WorkflowEditor/WorkflowList.tsx
  - src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx
  - src/renderer/src/components/WorkflowEditor/AgentNode.tsx
  - src/renderer/src/components/WorkflowEditor/StartNode.tsx
  - src/renderer/src/components/WorkflowEditor/EndNode.tsx
  - src/renderer/src/components/WorkflowEditor/NodeConfigDrawer.tsx
  - src/renderer/src/components/WorkflowEditor/ExecutionPanel.tsx
  - src/renderer/src/components/Sidebar/Sidebar.tsx
  - src/renderer/src/stores/projectStore.ts
  - src/renderer/src/App.tsx
  - package.json
autonomous: true
requirements: [WFLO-01, WFLO-02, WFLO-05, WFLO-07]
---

# Wave 2: 工作流编辑器 UI — ReactFlow 可视化编辑器

## Objective

构建工作流可视化编辑器前端：工作流列表视图、ReactFlow 画布编辑器、自定义节点（Agent/Start/End）、节点配置侧边抽屉、执行状态面板。与 Sidebar 和 App.tsx 集成，新增「工作流」入口。

---

## Task 3.1: 安装依赖 + 扩展视图路由 + 工作流列表

<read_first>
- package.json (当前依赖)
- src/renderer/src/App.tsx (activeView 条件渲染模式)
- src/renderer/src/components/Sidebar/Sidebar.tsx (菜单按钮和 onChangeView 调用)
- src/renderer/src/stores/projectStore.ts (activeView 类型定义)
- src/renderer/src/stores/workflowStore.ts (fetchWorkflows, deleteWorkflow)
- src/renderer/src/components/AgentLibrary/AgentLibrary.tsx (列表+卡片布局参考)
- .planning/phases/04-workflow-system/04-UI-SPEC.md (空状态文案、色彩规范)
- .planning/phases/04-workflow-system/04-CONTEXT.md D-01/D-04 (Sidebar 入口、列表→编辑器模式)
</read_first>

<action>
**Part A — 安装依赖**：执行 `npm install @xyflow/react vaul`

**Part B — 视图路由扩展**：
1. `src/renderer/src/stores/projectStore.ts`：扩展 activeView 类型为 `'chat' | 'settings' | 'agents' | 'plugins' | 'tools' | 'workflows'`
2. `src/renderer/src/components/Sidebar/Sidebar.tsx`：
   - 更新 onChangeView prop 类型包含 `'workflows'`
   - 在菜单中新增「工作流」按钮（agents 附近），使用 `GitBranch` 图标（lucide-react）
   - 折叠模式下也新增入口
3. `src/renderer/src/App.tsx`：
   - 导入 WorkflowList 和 WorkflowEditor
   - 添加 `{activeView === 'workflows' && <WorkflowList />}` 或条件渲染编辑器

**Part C — 工作流列表**：创建 `src/renderer/src/components/WorkflowEditor/WorkflowList.tsx`
1. 使用 useWorkflowStore 获取 workflows 列表
2. Props: `onSelectWorkflow(id)`, `onCreateWorkflow()`
3. 顶部：标题「工作流」+「新建工作流」按钮（accent `#7c3aed`）
4. 卡片列表：name, description, status, updated_at
5. 空状态：「无可用工作流」+ 引导文案
6. 卡片操作：点击进入编辑器，删除按钮+确认对话框
7. 首次渲染调用 fetchWorkflows
</action>

<acceptance_criteria>
- package.json 包含 `@xyflow/react` 和 `vaul`
- projectStore activeView 类型包含 `'workflows'`
- Sidebar 包含 `onChangeView('workflows')` 调用和「工作流」按钮
- App.tsx 包含 `{activeView === 'workflows'`
- WorkflowList.tsx 存在并导出 `WorkflowList`
- 包含「新建工作流」按钮和空状态文案
- 卡片显示 workflow name 和 description
</acceptance_criteria>

---

## Task 3.2: 创建自定义 ReactFlow 节点 + 节点配置抽屉

<read_first>
- src/shared/types.ts (WorkflowNodeType, WorkflowNode, Agent)
- src/renderer/src/stores/agentStore.ts (fetchAgents, agents 列表)
- .planning/phases/04-workflow-system/04-CONTEXT.md D-05/D-06/D-08 (节点类型、Agent 引用资产库)
- .planning/phases/04-workflow-system/04-UI-SPEC.md (色彩、间距、Drawer 规范)
</read_first>

<action>
**Part A — 自定义节点**：

1. `src/renderer/src/components/WorkflowEditor/StartNode.tsx`：
   - 圆角矩形，绿色边框 (`#22c55e`)，「开始」标签
   - 只有 source handle（底部），使用 `Handle, Position` from `@xyflow/react`

2. `src/renderer/src/components/WorkflowEditor/EndNode.tsx`：
   - 圆角矩形，红色边框 (`#ef4444`)，「结束」标签
   - 只有 target handle（顶部）

3. `src/renderer/src/components/WorkflowEditor/AgentNode.tsx`：
   - 矩形卡片，紫色选中边框 (`#7c3aed`)
   - 显示 Agent 名称（data.label）、描述（截断）
   - 运行状态指示：pending(灰) / running(蓝色脉冲) / completed(绿) / failed(红)
   - source + target handles
   - 点击触发 onNodeClick 打开配置抽屉

**Part B — 节点配置抽屉**：创建 `src/renderer/src/components/WorkflowEditor/NodeConfigDrawer.tsx`
1. 使用 vaul 的 Drawer 组件（右侧滑出，400px 宽）
2. Props: `isOpen, onClose, node, onUpdateNode`
3. 布局：节点名称输入框、描述文本框、Agent 选择下拉框（从 agentStore 加载，D-08 只读继承）、失败策略选择（重试/跳过/停止）、重试次数、保存按钮
4. Agent 选择后显示 LLM/MCP/Skills 摘要（只读，不允许覆盖）
</action>

<acceptance_criteria>
- StartNode.tsx 存在，有 source handle，绿色边框
- EndNode.tsx 存在，有 target handle，红色边框
- AgentNode.tsx 存在，有 source + target handles，显示 data.label
- AgentNode 有运行状态视觉指示
- NodeConfigDrawer.tsx 存在，使用 vaul Drawer
- 包含 Agent 选择下拉框（从 agentStore 加载）
- Agent 信息摘要为只读
- 包含失败策略选择
</acceptance_criteria>

---

## Task 3.3: 创建工作流编辑器主视图

<read_first>
- src/renderer/src/components/WorkflowEditor/AgentNode.tsx
- src/renderer/src/components/WorkflowEditor/StartNode.tsx
- src/renderer/src/components/WorkflowEditor/EndNode.tsx
- src/renderer/src/components/WorkflowEditor/NodeConfigDrawer.tsx
- src/renderer/src/stores/workflowStore.ts (saveWorkflow, runWorkflow, currentWorkflow)
- src/renderer/src/stores/projectStore.ts (currentProject)
- .planning/phases/04-workflow-system/04-CONTEXT.md D-01/D-02/D-03 (全屏画布、拖拽面板、抽屉配置)
- .planning/phases/04-workflow-system/04-UI-SPEC.md (色彩、间距)
</read_first>

<action>
创建 `src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx`：

1. 使用 `@xyflow/react`：`ReactFlow`, `Background`, `Controls`, `MiniMap`, `addEdge`, `useNodesState`, `useEdgesState`
2. 注册节点类型：`{ start: StartNode, agent: AgentNode, end: EndNode }`
3. 布局：
   - 顶部工具栏：返回列表按钮、工作流名称（可编辑）、保存按钮（accent）、运行按钮（accent）/ 停止按钮
   - 左侧节点面板（可折叠）：拖拽 start/end/agent 到画布
   - 中央：ReactFlow 画布
   - 右侧：NodeConfigDrawer（点击节点时显示）
4. 功能：
   - 拖拽创建节点、连接 handle 创建边、Delete 键删除
   - 保存：收集 nodes/edges/viewport，调用 saveWorkflow
   - 运行：先保存后 runWorkflow(workflowId, projectId, 'editor')，显示 ExecutionPanel
   - 加载：currentWorkflow 不为 null 时从 graph_data 初始化
   - 新建：空画布 + 默认 start + end 节点
</action>

<acceptance_criteria>
- WorkflowEditor.tsx 存在，使用 `@xyflow/react` 的 ReactFlow
- 注册 start, agent, end 三种自定义节点类型
- 包含顶部工具栏（保存、运行/停止按钮）
- 包含左侧面板（可拖拽节点）
- 点击节点打开 NodeConfigDrawer
- 保存按钮调用 saveWorkflow
- 运行按钮先保存后执行
- 画布支持拖拽创建节点和连接边
</acceptance_criteria>

---

## Task 3.4: 创建执行状态面板 + 集成到编辑器

<read_first>
- src/renderer/src/stores/workflowStore.ts (subscribeToExecution, currentExecution, nodeRuns, stopWorkflow)
- src/shared/types.ts (WorkflowStreamEvent, WorkflowNodeRun, WorkflowExecution)
- src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx (运行按钮逻辑)
- .planning/phases/04-workflow-system/04-UI-SPEC.md (Semantic states: success/warning/info colors)
- .planning/phases/04-workflow-system/04-CONTEXT.md D-17 (IPC 事件流)
</read_first>

<action>
**Part A — 执行面板**：创建 `src/renderer/src/components/WorkflowEditor/ExecutionPanel.tsx`
1. Props: `executionId: string | null; onClose: () => void`
2. 订阅：调用 subscribeToExecution(executionId) 接收 WorkflowStreamEvent
3. 显示：执行状态（运行中蓝/完成绿/失败红/停止灰）、节点执行列表（名称+状态+耗时）、失败节点展开错误信息
4. 操作：停止按钮（运行中）、关闭按钮（完成后）
5. 事件驱动实时更新

**Part B — 集成到编辑器**：在 WorkflowEditor.tsx 中
1. 运行按钮逻辑：先 saveWorkflow → runWorkflow → 获取 executionId → 显示 ExecutionPanel
2. 运行中按钮变为「停止」，调用 stopWorkflow
3. ExecutionPanel 在画布右侧或底部显示，可关闭回到编辑模式
</action>

<acceptance_criteria>
- ExecutionPanel.tsx 存在，导出 `ExecutionPanel`
- 使用 subscribeToExecution 订阅事件流
- 显示执行状态和节点执行列表
- 失败节点显示错误信息
- 运行中显示停止按钮
- WorkflowEditor 中运行按钮先保存后执行
- 运行中按钮切换为停止状态
- ExecutionPanel 在执行开始后自动显示
</acceptance_criteria>

---

## Verification

```bash
# 文件存在性
ls src/renderer/src/components/WorkflowEditor/*.tsx

# App.tsx 集成
grep -c "workflows" src/renderer/src/App.tsx
grep -c "workflows" src/renderer/src/stores/projectStore.ts
grep -c "workflows" src/renderer/src/components/Sidebar/Sidebar.tsx

# ReactFlow 使用
grep -c "ReactFlow" src/renderer/src/components/WorkflowEditor/WorkflowEditor.tsx
```

Expected: 7 个组件文件存在，App.tsx 包含 workflows 视图路由，ReactFlow 集成正确。
